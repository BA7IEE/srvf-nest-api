# Dependencies
node_modules

# Build output
dist
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment files (only .env.example is committed)
.env
.env.local
.env.*.local

# Test / coverage
coverage
.nyc_output

# V2.x C-7.5 PR #7:LocalStorageProvider 默认根目录(STORAGE_LOCAL_ROOT default './tmp/storage')
tmp

# IDE / OS
.DS_Store
.idea
.vscode
*.swp
*.swo

# Claude Code local settings (per-machine; .claude/CLAUDE.md and .claude/settings.json are committed)
.claude/settings.local.json

# Claude Code worktrees (per-session; auto-created, not part of repo)
.claude/worktrees/
