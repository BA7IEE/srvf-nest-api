# 活动业务全流程改造 v1.1 —— 冻结开发合同

> 维护者 2026-08-03 下发(源包 `SRVF_活动业务文档_Markdown版_v1.1.zip`),同日入仓冻结。
> 本目录按 `docs/archive/**` 红区规则**只增不改**;实施状态与当前事实看
> [`docs/current-state.md`](../../../current-state.md) §2,不看本目录。

## 合同构成(四份共同生效,不可单独取用)

| 文件 | 角色 |
|---|---|
| [SRVF_活动业务全流程修正方案_正式版_v1.1.md](./SRVF_活动业务全流程修正方案_正式版_v1.1.md) | 业务合同(**业务合同 GO**;19 章;AC-001..072 + ADV-001..023) |
| [SRVF_活动业务全流程改造_详细开发文档_v1.1.md](./SRVF_活动业务全流程改造_详细开发文档_v1.1.md) | 开发合同(**有条件 GO:仓库预检 + 维护者授权后开工**;数据模型 / 状态机 / 锁序 / 批次 0-7) |
| [SRVF_活动业务规则_355项追踪矩阵_v1.1.md](./SRVF_活动业务规则_355项追踪矩阵_v1.1.md) | 355 项逐条追踪(状态=「合同已覆盖,代码待实现」) |
| [SRVF_活动业务文档_v1.1_修订说明.md](./SRVF_活动业务文档_v1.1_修订说明.md) | v1.0→v1.1 修订说明(12 项一级阻断的最终修正 + 开工口径) |

完整性:[SRVF_活动业务文档_v1.1_SHA256.txt](./SRVF_活动业务文档_v1.1_SHA256.txt) 覆盖上述四份,入仓时已原位 `shasum -a 256 -c` 全过。

## 非合同附件

- [SRVF_活动业务文档_系统性对抗性复核报告_v1.0.md](./SRVF_活动业务文档_系统性对抗性复核报告_v1.0.md) —— v1.1 的修订依据,**历史证据,不是合同**(不在 SHA256 清单内)。

## 权威顺序(合同 §0.1 自述,与仓库现行规则一致)

当前事实(`current-state` + 代码 + GitHub)> 本合同 > 旧业务文档与历史归档;冲突时暂停上报,不自行取舍。
代码参照基线:`0.66.0` 仓库快照(`47c4987514fef3772efb95a78adcd73dbd81c89c`)。

## 开工口径(修订说明 §10 要点)

第 0 批 = 仓库预检、授权、测试探针、合同入仓(本目录即该批「合同入仓」交付);
维护者授权后按第 1 批数据地基 → 第 2 批结算账本关账 → 第 3 批活动发布 → 第 4 批报名名额 →
第 5 批扫码 → 第 6 批工作人员工具 → 第 7 批规模上线顺序实施;
**禁止跳过第 1、2 批直接把二维码、批量代签或离线入口开放到真实环境**。

## 入仓时已知的与仓库现状的张力(留给实施期逐条裁决,不在本目录调和)

- 合同 §3 新增约 30 张表,全部落 D 档 schema lane;`current-state` §3「暂不启动清单」把新 schema / migration / Permission seed 列为**评审解锁制** ⇒ 每批仍需维护者单独拍板,合同 GO 不等于这条清单自动解锁。
- 合同要求删除 `directPublish` 成功路径、改 `activity-check-in-policy` 的 30 分钟口径、终审改走 `LedgerPostingBatch` —— 均为**既有 e2e 行为契约变更**,按 AGENTS §2「改既有断言 = 改行为契约 → 停下报告」,逐条走人话简报,不在实施 PR 内顺手改断言。
- 合同 §0.4 自述「不新增第 3 个 cron」与仓库「cron 终态恰好 2 个」一致;活动批处理走 PG durable job + 现有 worker 形态。
