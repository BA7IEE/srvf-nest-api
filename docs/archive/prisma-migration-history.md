# prisma migration 历史链(归档)

> 从 [`CODEMAP.md`](../../CODEMAP.md) 的 `migrations/` 单元格原样搬出(Harness 3.0 P4b,2026-07-28)。
>
> **搬出的理由**:这条链在 CODEMAP 里长到 11,111 字符 = 全文件 27%,而它没有退场机制 ——
> 每加一个 migration,上一条就永久钉在导航文档里。导航需要的是「有多少个 / 最新几个」,
> 那部分现由 `pnpm docs:codemap` 生成;每个 migration 具体做了什么,权威源是它自己的 SQL 与对应 PR。
>
> **本文件冻结,不再增长。** 2026-07-23 之后的 migration 说明只写在 PR body 与 CHANGELOG,不回流本文件。
> 归档层规则:内容按当时事实冻结,**不是当前事实**(AGENTS §0「不主动读」)。

---

## 原文(逐字)

65 个 migration(最新 `20260723225802_activity_responsibility_workflow_expand`:活动责任闭环 PR1 expand-only——Activity 新增 nullable 发起人和考勤完成声明及 workflowRevision=0；AttendanceSheet 新增 nullable 最近提交和退回字段并从既有 submitter/submittedAt 回填；净新发布审核与责任历史表，11 条 RESTRICT FK、3 条 partial unique 及责任/状态 CHECK；0 runtime/API/Permission/seed，未 deploy；前一为 `20260720193621_notification_prepared_template`:D-Outbox Wave2 G1b expand-only——`notification_outbox_intents.preparedTemplateId` 新增 nullable `TEXT`，为后续 runtime 在已确认 provider 尚未启动的终态 skip 场景恢复已预占微信订阅消息配额保留稳定模板标识；provider 结果未知不退款；零 default/index/constraint/backfill/delete，0 runtime 读写/0 API 与 contract 行为变化，未启用、未 deploy；前一为 `20260719220000_notification_publish_generation`:D-Outbox Wave2 G1 expand-only——`notifications.publishGeneration` 新增 `INTEGER NOT NULL DEFAULT 0` + 命名非负 CHECK；老行代次为 0，0 runtime 读写/0 API 与 contract 行为变化，未启用、未 deploy；前一为 `20260719160335_insurance_constraints`:D-INSURANCE v3 PR4 constraints-only——完整 preflight 非零 fail-fast、零修数/零删数；新增 2 MemberInsurance CHECK、Evidence 最终非空列 + 7 CHECK + 2 partial unique、member-match 23514 与 immutable 55000；无 route/DTO/permission/AuditEvent/BizCode/config/cron/runtime 变更；前一为 `20260719071116_insurance_expand`:D-INSURANCE v3 PR1 expand-only——`MemberInsurance` 新增 pending/v0/reviewer 字段并将所有 legacy(含软删)回填 pending/v0/null reviewer；新增兼容期全可空的双 source/双 owner Evidence 表与 RESTRICT FK；`TeamJoinCycle.requiresInsurance=false`；0 最终 CHECK/unique/trigger，0 runtime 行为；前一为 `20260718233000_attachment_storage_operations`:仅 Attachment key namespace 新增 `StorageObject`/`StorageObjectOperation` durable ledger;对象固定 provider locator、凭证仍取当前 settings;JIT→STRICT 由 dedicated worker 收敛(非 cron/queue);为旧 binary additive rollout 本期不加 `Attachment.key`→`StorageObject.key` FK,不宣称 repo-wide storage closure;见 [`runbook`](docs/ops/attachment-storage-consistency-rollout.md);前一为 2026-07-18 Membership 任期 CHECK 收口(存量 fail-fast、零修数);前一为 D-Outbox:纯 additive 新建空表 `notification_outbox_intents`,零回填/零 enum/零 drop;前一为 2026-07-18 D-RULE-1:同 `activityTypeCode × attendanceRoleCode` 未软删 ACTIVE pair partial unique；冲突预检失败且零自动清理，migration 仅 DROP/CREATE index，零 DML/回填；service pair 预查 + P2002→23002，calculator 漂移重复 pair fail-closed；前一为 2026-07-18 D-Throttle:净新空表 `throttler_buckets`〔`(throttlerName,key)` 普通唯一 + `retentionAt` 索引;6 个时间字段/数组均 `timestamptz(3)`;纯 CREATE TABLE/unique/index,零回填、零 enum、零 drop;goal 明确授权 `@@map` 物理名〕;前一为2026-07-17 identity session P0 PR1:`SmsPurpose` 仅 additive `IDENTITY_STEP_UP`，migration 仅 PostgreSQL enum `ADD VALUE`，0 table / column / index / backfill / seed、禁止 down migration；前一为 2026-07-16 活动岗位与时段 F1:净新空表 `activity_positions` + `ActivityRegistration.activityPositionId` nullable 列〔2 FK Restrict + 6 普通索引 + 手写 partial unique `(activityId,name) WHERE deletedAt IS NULL`;既有报名 active partial unique 逐字不动;无回填/无不可逆/无 enum/seed;goal 指定 `@@map`〕;前一为 2026-07-16 活动评价 F1:净新空表 `activity_feedbacks`〔2 FK Restrict + 5 单列索引 + 手写 partial unique `(activityId,memberId) WHERE deletedAt IS NULL`;无回填/无不可逆/无 enum/seed;goal 指定 `@@map`〕;前一为 2026-07-15 活动自助 GPS 签到 F1:净新空表 `activity_check_ins`〔3 FK Restrict + 6 单列索引 + 手写 partial unique `registrationId WHERE deletedAt IS NULL`;无回填/无不可逆/无 enum/seed;一次性 `@@map` 例外〕;前一为 2026-07-15 活动开始提醒 marker:Activity 加 nullable `startReminderSentAt`〔活动模块一致性收口 F12;纯 additive、无回填、无不可逆〕;前一为 v0.47.0 到期提醒:member_insurances / team_insurance_policies 各加 nullable `expireNotifiedAt`,纯 additive、无回填、无不可逆;前一为 2026-07-14 第七刀 settings 单例约束:四表先按 updatedAt/createdAt 最新优先安全去重并记录删除数,再加 constant unique `ON ((true))`;仅已有重复行时发生不可逆删除,执行前只读核对 + 实时授权;前一为 2026-05-02 init → 2026-07-13 招新证书闭环刀A:`recruitment_applications.certificateIssuanceInfo Json?` 发证机构/日期按类别暂存,纯 additive 可空、无回填无不可逆;前一为 2026-07-12 招新/入队十三项收口 PR-3:申请人证书审核态 + 入队轮配置预置列〔`recruitment_applications.certificateReviewStatus Json?` + `team_join_cycles.openOrganizationIds Json?`/`maxTargetOrgs Int?`;全 additive 可空,无回填无不可逆;刀H 代码于 PR-4 消费〕;前一为同 goal PR-1:同轮 active 报名 openid/phone 两条 partial unique;前一为 2026-07-11 十项收口一刀:「至多一个 open 轮」DB 兜底 + 建档搬运落点〔`recruitment_cycles_single_open_unique` / `team_join_cycles_single_open_unique` 两手写 partial unique(WHERE statusCode='open' AND deletedAt IS NULL;开轮 count-then-update 并发穿透收口,service P2002 转 28032/28231)+ `MemberProfile` +`detailedAddress`/`profileExtra`(promote 建档搬运落点,此前 promote 置空且无落点=真丢失);全 additive 无回填无不可逆〕;前一为同日招新可用性收口 F7:证书图上传与长期档案〔`recruitment_applications` +`certificateImages Json` 暂存位 + `Certificate` +`imageKeys Json`;promote 按类别建 pending Certificate 搬图;全 additive 可空无回填〕;前一为同日 F6:同轮防重 partial unique 排除集重建〔`<> 'rejected'` → `NOT IN ('rejected','withdrawn')`,自助撤销终态解锁同轮重报;纯索引重建零回填〕;前一为同日 F5:知情同意留痕 + 签名图〔recruitment_applications +privacyConsentAcceptedAt/+privacyConsentVersion/+signatureImageKey,MemberProfile +signatureImageKey;全 additive 可空,无回填无不可逆〕;前一为同日 F1:纯加空表 `recruitment_ocr_daily_counters`〔付费 OCR 按 IP × 北京自然日封顶持久计数;`(ip,dateKey)` 唯一 + `dateKey` 索引;无 FK 无回填无不可逆〕;前一为 2026-07-07 队员账号闭环 v2 schema 根改造:`User.memberId` 去全量 `@unique` 改手写 partial unique index `User_memberId_active_key` WHERE `deletedAt IS NULL`〔+ 普通 `@@index([memberId])` 兜底跨软删查询〕,同一 memberId 至多 1 条 live User 关联、软删后释放槽位;纯约束收窄,现有数据必满足更严全量唯一 ⇒ 零冲突零回填非破坏性;`Member.user User?` 反向关系随之改 `users User[]`(Prisma 一对一要求 FK 侧 `@unique`,partial unique 无法在 DSL 表达),消费方 `resource-resolver.service.ts`/`birthday-greeting.service.ts` 同步改 `where:{deletedAt:null}`+`take:1`;评审稿 [`member-account-loop-v2-review.md`](docs/archive/reviews/member-account-loop-v2-review.md);前一为 2026-07-03 冻结表 cleanup:DROP `MemberDepartment` + `user_roles` 两张冻结表〔档 D,不可逆;pre-production 窗口维护者拍板;两表均已零生产读写〔PR2/PR6 起〕,回填等值已用 count(...) 自证;无其它表 FK 指向这两表,纯删除表体〔含索引/FK/partial unique〕,无回填无新增,legacy department 3 端点 + user-roles 端点路由/DTO/码/行为逐字不变〕;前一为 2026-07-01 终态 scoped-authz PR7 职务→角色 policy:净新表 organization_position_role_policies〔+1 枚举 PolicyScopeMode{EXACT,TREE} + 3 索引 + 2 真 FK Restrict〔positionId→organization_positions/roleId→roles〕+ 普通唯一 (positionId,roleId);无 partial unique 无回填无不可逆;3 管理/监督角色 org-admin/group-manager/org-supervisor + 绑定 + 3 条默认映射〔仅正职 team-leader/dept-leader→org-admin@TREE、group-leader→group-manager@TREE;🔴 R5 副职零行,seed 运行时断言兜底〕由 seed 幂等 upsert 不落 migration;policy 表 + 3 新角色纯配置绝不进判权,消费方 PR8 AuthzService);前一为 2026-07-01 终态 scoped-authz PR6 RoleBinding:净新表 role_bindings〔+3 枚举 PrincipalType/BindingScopeType/BindingStatus + 6 索引 + 2 真 FK Restrict〔roleId→roles/scopeOrgId→Organization;principalId 多态无 FK〕+ 末尾手写 partial unique 全 8 scope 维度 WHERE deletedAt IS NULL AND status='ACTIVE' NULLS NOT DISTINCT〔PG16〕〕+ 回填每条 UserRole→RoleBinding(USER/GLOBAL/ACTIVE,复用 id),纯加 + 回填可逆(判权唯一读源重指向,UserRole 冻结零生产读写不删);前一为 2026-07-01 终态 scoped-authz PR5 分管:净新表 organization_supervision_assignments〔与职务正交;+2 枚举 SupervisionScopeMode{EXACT,TREE}/SupervisionStatus{ACTIVE,ENDED,REVOKED} + 4 索引 + 2 FK Restrict〔supervisorMemberId→Member/organizationId→Organization〕+ 末尾手写 partial unique (supervisorMemberId,organizationId) WHERE deletedAt IS NULL AND status='ACTIVE'〕,纯加空表无回填无不可逆(分管由 API 产生,不 seed);前一为 2026-07-01 终态 scoped-authz PR4 任职:净新表 organization_position_assignments〔+枚举 AssignmentStatus + 6 索引 + 3 FK Restrict + 末尾手写 partial unique (organizationId,positionId,memberId) WHERE deletedAt IS NULL AND status='ACTIVE'〕,纯加空表无回填无不可逆;前一为 2026-07-01 终态 scoped-authz PR3 职务定义:净新两空表 organization_positions + organization_position_rules〔+2 枚举 PositionCategory/PolicyStatus + 索引 + FK Restrict + 普通唯一〕,纯加空表无回填无不可逆(6 领导职务 + 30 默认规则由 seed 幂等 upsert);前一为 2026-07-01 终态 scoped-authz PR2 Membership:净新表 member_organization_memberships〔+2 枚举 MembershipType/MembershipStatus + 2 手写 partial unique〕 + 回填 active MemberDepartment→PRIMARY membership〔复用 id、startedAt=createdAt〕,纯加可逆;前一为 2026-07-01 终态 scoped-authz PR1 组织基座:organization_closure 闭包表〔WITH RECURSIVE 回填现有树〕 + Organization 两 additive 可空列 establishmentStatusCode?/groupFunctionCode?〔纯加无 enum 无回填无不可逆〕;再前为 2026-06-29 招新实名 OCR 鉴伪版充分利用:recruitment_applications +6 列〔ocrAddress/ocrNation/ocrAuthority/ocrValidDate + idCardCropImageKey/idCardPortraitImageKey;全可空 TEXT additive,无 enum 无不可逆无回填〕;再前为 2026-06-27 统一通知 S5 sms_settings.templateIdNotification 1 列 / S3 notifications.recipientMemberId 1 列 / S2 三表 / S1 两表)
