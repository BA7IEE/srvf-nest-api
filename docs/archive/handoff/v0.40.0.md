# SRVF API v0.40.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.39.0.md`](v0.39.0.md);逐笔详情见 CHANGELOG `## v0.40.0` 段 + `docs/current-state.md §2` 能力清单,本文件不重复。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件随 bump PR(#539)之后的 handoff PR 合入创建,**先于** tag / GitHub Release / current-state §1 回填(沿 [`process.md §5.1`](../../process.md) canonical 顺序 bump → handoff → tag → Release → current-state 回填);`v0.40.0` tag 将指向**本 PR 的 squash merge commit**(沿 §5.2 约定),GitHub Release(Latest)与 current-state §1 回填 + openapi.json 刷新随后依序完成。**handoff 是历史快照,tag / Release / 代码全不受其影响**。

---

## 1. TL;DR

- **v0.40.0 = 参与域生命周期收口 + 归属结束语义收敛 + 一键离队 + H5 手机通道发号**(七项现象一揽子)。立项 = goal「参与域生命周期收口 + 归属结束语义收敛 + 一键离队 + H5 手机通道发号(七项一揽子)」,维护者逐条代码核实全属实后全按推荐拍板;**全程 0 schema / 0 migration(migration 恒 40)是硬边界**。
- **5 笔 PR(#534–#538)**:
  - **T1 activity-registrations 收口刀**(#534,C 档):① approve 活动状态闸(`statusCode ∈ {cancelled,completed}` → 20124;reject/cancel 不拦,留清残留待审队列)② reopen 审批后悔药(状态机新边 `reject→pending`,新端点 `POST .../registrations/:id/reopen` + 新码 `activity-registration.reopen.record`;清空审核三字段;不发通知;顺解「被拒者占 partial unique 槽无法重报」死锁)③ cancel 考勤守卫(有未软删考勤记录 → 21033,不引 attendances service 防环,不回滚贡献值)。+1 权限码 / +2 BizCode / +1 端点。
  - **T2 activities 生命周期刀**(#535,C 档):③ 活动收口三件套(不引 cron)——(a) 报名 endAt 闸(`now>endAt` → 20125)(b) App 可报名池过滤(`endAt>=now`;detail 不动)(c) 手动完结端点(状态机新 action `complete: published→completed`,新端点 `POST .../activities/:id/complete` + 新码 `activity.complete.record`;audit 复用伞事件 `activity.publish`;attendances 首提直写 completed 零 diff,两通路并存)。+1 权限码 / +1 BizCode / +1 端点。
  - **T3 归属结束语义收敛 ENDED**(#536,B 档):⑥ 旧面 `member-departments` 的 `remove`/`set`换部门由**软删**收敛为 `status=ENDED + endedAt + endedByUserId`(对齐新面 end)。对外契约逐字不变(`primaryMembershipSelect` 不含 status/deletedAt;DELETE 后 GET 仍 NOT_FOUND);新面 `GET members/:id/memberships` 现可见 ENDED 历史行。audit characterization 翻面(仅 remove `after` 载荷)+ 白盒 DB 断言翻面(维护者拍板)。0 码 / 0 路由 / 0 BizCode。
  - **T4 一键离队编排**(#537,C 档):⑤ 新端点 `POST .../members/:id/offboard` + 新码 `member.offboard.record`。单事务四腿(直连 prisma 防环):member INACTIVE + END 全部 active 归属 + 停用 linked USER 账号并撤 refresh + 1 条伞 audit `member.offboard`。守卫复用 15001/15036/CANNOT_OPERATE_SELF;幂等;不级联任职/分管,残留数 advisory 回显。+1 权限码 / +1 AuditLogEvent / +1 端点。
  - **T5 H5 手机通道发号**(#538,C 档,招新高危区):④ `isPromotable` 登录通道由「有 openid」放宽为「有 openid 或 有已验证手机」——无 openid 有已验证手机的 H5 申请人建 SMS 登录通道 User(phone+phoneVerifiedAt,openid=null)。**通道分流以 `openid==null` 门控 → 微信路径 promote 逐字不变(行为锁)**。skip reasons 三变(`missing-openid` 停用 → `missing-login-channel`;+phone-already-bound;+duplicate-phone-in-batch)。0 码 / 0 路由 / 0 BizCode / 0 audit event。
- **⚠️ 行为变更 6 条**(前端/运维注):① 取消/完结活动不可再批报名(20124)② 已有考勤的报名不可取消(21033)③ 活动已结束不可报名(20125)+ App 池不再展示已结束活动 ④ 旧面解除/换部门改留 ENDED 痕(原软删)⑤ promote skip reason 字符串变化(`missing-openid` 停用 → `missing-login-channel`;+phone-already-bound/duplicate-phone-in-batch)⑥ 新增 reopen / complete / offboard 三端点。
- **footprint(累计,基于 v0.39.0 baseline)**:权限码 198 → **201**(+3:`activity-registration.reopen.record` / `activity.complete.record` / `member.offboard.record`,全绑 biz-admin)· biz-admin 74 → **77** · org-admin 57 → **60**(派生自动继承)· ops-admin **96** / member 9 **零变化** · `EXPECTED_ROUTES` 326 → **329**(+3 端点)· controller **66 零变** / 模块 **35 零变** · **migration 恒 40**(全程 0 schema)· 角色 **7 零变** · BizCode **+3**(20124/20125/21033)· AuditLogEvent **+1**(`member.offboard`)。**`src/modules/auth/**` 全程零 diff;判权核心(`RbacService.can()` / AuthzService 判权文件)零 diff;`prisma/schema.prisma` + `prisma/migrations/**` 零改动**。
- **收口链**:base v0.39.0(`2173a49c` / tag)→ #534(T1,`9a8ae1ce`)→ #535(T2,`b2f4fcd1`)→ #536(T3,`5e420208`)→ #537(T4,`e1807913`)→ #538(T5,`7dd9b3c7`)→ bump #539(`6597d8c3`)→ 本 handoff PR(= **tag 点**,squash commit 由 `v0.40.0` tag 指向)。
- `v0.40.0` tag + GitHub Release(Latest)+ current-state §1 回填 + openapi.json 刷新到 v0.40.0**将在本 PR 合并后依序完成**(process.md §5.1 stage 6-8),完成后不回改本文件。

---

## 2. 下一会话

- **无阻塞性技术债**。五刀均经 CI 全绿 + 计数亲核(`docs:rbacmap:check` 0 FAIL @ 201 / `docs:codemap:check` 0 FAIL @ migration 40);全程 0 schema / 0 migration / auth 零 diff / 判权核心零 diff 硬边界守住。
- **前端对接**:三新端点(reopen / complete / offboard)+ 三处行为变更文案见 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md)(§2.1 作战室 report 审批生命周期新规 + §2.4 一键离队)+ [`docs/handoff/miniapp.md`](../../handoff/miniapp.md)(App 池 endAt 过滤 + 报名 endAt/考勤守卫)。**promote skip reason 字符串变化**(`missing-openid` 停用)前端若硬编码须改。
- **上线部署须知**:① **无新 migration**(仍 40,0 schema);② reseed 权限(幂等,新增 3 码全绑 biz-admin,org-admin 派生继承);③ 一键离队会**同步停用队员的关联登录账号并撤 refresh**(该账号 access/refresh 立即 401),运维离队前须知晓;④ 参与域三处新守卫(approve 活动态 / 报名 endAt / cancel 考勤)均 409 冲突码,前端给对应文案。
- 本版**不**解锁新 cron(R-5 冻结:cron 仅生日批)、**不**改 `src/modules/auth/**` / 判权核心 / schema 任何文件、**不**做贡献值回滚 / 活动取消级联取消报名 / 离队级联撤任职分管 / reject→pass 直通(goal 明确禁区)。
- **序列外后续诉求触发再立项**:存量队员批量导入(NEXT_TASKS P1-15)· 报名前 openid 非会员推送路 · 全员短信批处理异步(P1-14)。

---

> 逐笔实现细节见 CHANGELOG `## v0.40.0` 段(五刀各一条,含行为锁自证 + 计数 + e2e 覆盖);能力清单见 `docs/current-state.md §2`;RBAC 权限地图见 `docs/ai-harness/RBAC_MAP.md`(201 码 / §2 端点→码 / §3 全集 / 五刀戳)。
