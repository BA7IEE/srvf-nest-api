# SRVF API v0.58.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> **阅读顺序(Harness 2.0)**:根 `AGENTS.md`(读取协议 §0)→ [`docs/current-state.md`](../../current-state.md) → [`docs/process.md §2/§3`](../../process.md) → 本文件;细则按 `AGENTS.md §6` 索引触碰才读。冲突处理沿 `AGENTS.md §0`,本文件不覆盖任何铁律。
> 本文件接续 [`v0.57.0.md`](v0.57.0.md);逐笔详情见 CHANGELOG `## v0.58.0` 段。
>
> **状态**:随 handoff PR 合入,先于 tag / GitHub Release / current-state 回填;`v0.58.0` tag 指向本 PR 的 squash commit(沿 `docs/process.md §5.1`)。

---

## 1. TL;DR

- **v0.58.0 = 第五轮全仓 review 修复闭环**。链路:三视角(第一性 × 对抗性 × 系统性)冻结报告 [`full-repo-fifth-review-v0.57.0.md`](../reviews/full-repo-fifth-review-v0.57.0.md)(#674;P0=0 / P1=0 / **P2=7 / P3=2** 全 CONFIRMED,含诚实负结果:证伪 v0.57 六修全部失败)→ **双 lane 并行修复**:#675(R5-01 取消通知收件集改 Activity 锁内确定,派发仍 commit 后;R5-07 handoff openapi `info.version` 刷至当前版)∥ #676(Harness 机器层六连:R5-02 counts 改 **TypeScript AST 真源计数** + 与 check-rbac-map 双口径交叉校验;R5-03 changelog-merge 拒收清单〔非 UTF-8 / 空 / 一二级 heading〕+ 先写后删失败不删;R5-04 e2e 库名加仓路径哈希永不回落主库;R5-05 恒读协议三文档对齐 AGENTS §0;R5-06 codemap+rbacmap 补挂 CI + CODEMAP LOC true-up;R5-08 lane 名必填 + bump 特征硬拒)→ 状态回填 #677;R5-09 登记 NEXT_TASKS 随下次参与域演进顺手。
- **行为变更**:①活动取消 × 并发新报名竞态下,被联动取消的新报名者现在也收到取消通知;②`pnpm changelog:merge` 对畸形 fragment 拒收;③linked worktree e2e 库名改 `app_test_<slug>_<hash6>`;④`agent:preflight` lane 模式必须带 lane 名且检测到 bump 特征硬拒。
- **footprint**:`src` 仅 activities 一处(收件集时序);**0 schema / 0 migration(54 恒)/ 0 权限码(206 恒)/ 0 BizCode(250 恒)/ 0 AuditLogEvent(113 恒)/ 0 新依赖**;新增两个 harness 自测脚本(`tsx scripts/harness-guards.selftest.ts` / `bash scripts/agent-preflight.selftest.sh`,改 scripts 前先跑)。

## 2. 验证快照

- 五 PR(#674-#678)+ bump #679 各自 required checks 全绿串行合入;双 lane 写集互斥核对通过;R5-07 与 lane F 独立上报互证(A5 跨模型/跨 lane 交叉验证实锤)。
- 四守护(readtax / counts / codemap / rbacmap)**现已全部挂 CI**;counts 为 AST 真源计数后九项现值不变。
- 本会话清理:6 个 lane worktree、14 个已合并分支(用户授权逐名 -D)、9 个派生测试库(dropdb)复核干净。

## 3. 下一步索引(不自动启动)

- P3-R5-09(报名状态机 fail-close + schema 注释五态)在 [`NEXT_TASKS`](../../ai-harness/NEXT_TASKS.md),随下次参与域演进顺手;
- `src/**` / `test/**` 注释内 harness-v1 节号引用动到再校准(`AGENTS.md §6` 重定向表)。
