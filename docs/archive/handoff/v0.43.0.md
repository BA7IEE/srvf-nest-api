# SRVF API v0.43.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.42.0.md`](v0.42.0.md);逐笔详情见 CHANGELOG `## v0.43.0` 段 + `docs/current-state.md §2` 能力清单,本文件不重复。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件随 bump PR #567 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。沿本次 goal 的明确拍板,`v0.43.0` tag 将指向 **bump squash `f9625708`**,不指向本 handoff 合并点;GitHub Release(Latest)与 current-state/OpenAPI 回填随后依序完成。

---

## 1. TL;DR

- **v0.43.0 = 招新证书闭环补强(刀A)+ team-join 小刀(刀B)**。范围 = 刀A #564 + 刀B #565 + landing #566 + bump #567 + 本 handoff PR。
- **⚠️ 前端须适配五条行为/契约变化**:证书上传新增必填 `issuingOrg`/`issuedAt`;approved 类别禁止申请人重传;红十字/BSAFE 标门槛须证书先 approved;微信查进度改活跃优先;一键入队复查开放部门清单。
- **刀A #564**:第 48 migration 增 `certificateIssuanceInfo Json?`;上传在行锁后重算图片/审核/发证三份 JSON;新增 28054/28055;promote 搬发证真值、存量回退占位并写招新审核 `verifyNote`;admin DTO 增证书摘要;微信/手机进度查询口径统一。
- **刀B #565**:App 改候选部门使用独立 audit `team-join-application.update-targets`;admin 一键入队在事务内复查 `cycle.openOrganizationIds`,非空清单外复用 28242,null/空仍代表全部 ACTIVE。
- **footprint(基于 v0.42.0)**:权限码 **205** / biz-admin **81** / org-admin **60** / ops-admin **96** / `EXPECTED_ROUTES` **336** / controller **66** / 模块 **35** / 角色 **7** 全不变;migration 47 → **48**;BizCode +2(28054/28055);AuditLogEvent 97 → **98**(`team-join-application.update-targets`)。
- **迁移快照**:第 48 migration `20260713120000_recruitment_certificate_issuance_info` 仅向 `recruitment_applications` ADD nullable JSONB,无回填/索引/enum/不可逆数据改写;干净临时库 48/48 重放 + seed 幂等二跑已绿。
- **验证快照**:刀A unit 76/2308、contract 606/2 snapshots、e2e 138/2782;刀B unit 76/2308、contract 606/2 snapshots、e2e 138/2783;两 feature PR 与 bump PR 的 required CI 全绿。
- **收口链**:#564 `dc9efaa5` → #565 `351e9720` → landing #566 `d5ae0ffc` → bump #567 `f9625708`(**tag 点**) → 本 handoff PR。
- `v0.43.0` tag + GitHub Release(Latest)+ current-state §1 回填 + handoff OpenAPI `info.version=0.43.0` 将在本 PR 合并后依序完成;完成后不回改本文件。

---

## 2. 下一会话开工提示

- 开工门禁沿 [`docs/current-state.md §5`](../../current-state.md) + [`process.md §2`](../../process.md);当前事实(版本/HEAD/open PR)读 current-state §1,不读本文件。
- **前端发布前置**:小程序/H5 证书上传表单必须提交发证机构与 date-only 发证日期;机构可提供「红十字会 / 深圳市急救中心 / 其他手填」快捷项。approved 类别换证须引导联系管理员先驳回。
- 管理后台读取报名 list/detail 时可消费新增 `certificates[]` 摘要;CSV 仍无该列,不要按摘要变化改 CSV。
- 招新/入队既有挂账与禁区仍以 current-state/NEXT_TASKS 为准;本 release 未启动 per-部门考核、机构字典、通知重试、自动推进或其余状态机改造。
