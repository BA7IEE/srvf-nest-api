# SRVF API v0.52.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.51.0.md`](v0.51.0.md);逐笔详情见 CHANGELOG `## v0.52.0` 段 + `docs/current-state.md §2`。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 能力终态 / 索引 / 操作提示;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR #627 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.52.0` tag 将指向本 handoff PR 的 squash merge commit。

---

## 1. TL;DR

- **v0.52.0 = 活动自助 GPS 签到(审计刀 6 第一件)**。交付链 = 四拍板(GPS / 仅 pass / 忘签退留空 / 宽进严出)→ goal 会话五 PR 全绿自合:T0 冻结 #622 `da1f42ea` → 证据表 #623 `ac8748cc` → App 自助面 #624 `723bed3d` → Admin 只读面 #625 `6649e5c3` → 收口 #626 `4f2651ef` → 主会话元核验四点全过 → bump #627 `32b6f4e3` → 本 handoff(**tag 点**)。冻结评审稿:[`docs/archive/reviews/activity-self-checkin-t0-review.md`](../reviews/activity-self-checkin-t0-review.md)。
- **纯 additive,0 行为变更**:既有报名/考勤/结算路径零 diff;打卡是**证据层**——append-only(不开放修改/删除端点,不写 AuditLog),结算仍唯一经既有 attendance-sheets 两级审批。
- **App 自助闭环(关闭审计 #3)**:`POST app/v1/my/activities/:activityId/check-in` / `POST .../check-out` / `GET .../check-in`。闸 = canUseApp + 本人**当前 pass 报名** + 活动状态(签到仅 published;签退允许 published|completed)+ 既有活动时间窗(复用 `ATTENDANCE_WINDOW_TOLERANCE_HOURS`,窗外硬拒);签退距签到至少 36s。签到/签退幂等(重复返回首次 winner,不覆盖快照);并发由 live `registrationId` partial unique + CAS 保证单行。
- **geofence(关闭审计 #18,经纬度字段首次消费)**:手写 Haversine(纯函数+单测)对活动坐标算距离;半径 env `ATTENDANCE_CHECKIN_RADIUS_METERS`(默认 500,`.env.example` 已登记)。**宽进严出**:活动无合法坐标 → 照常签、`geoVerified=false`;超半径 → 照常签、`outOfRange=true` 留痕给审批人;仅时间窗硬拒。
- **Admin 证据面**:`GET admin/v1/activities/:activityId/check-ins`(打卡列表,含距离/标记,保留取消/软删成员历史)与 `GET .../attendance-sheet-draft`(**只读**草稿:当前 pass 报名 × 打卡 → 预填 records + 每条 flags〔noCheckOut→endAt 预填 / outOfRange / unverified / absent=零打卡清单〕,admin 编辑后走**既有** `POST attendance-sheets`;e2e 锁定草稿产物可原样提交生成 pending Sheet)。两端点复用 `attendance.read.sheet` + activity ref,0 新码。
- **隐私**:请求/DB 的原始经纬度已加入 pino redact 清单;API 响应安全视图只含时间/距离/flags,**不回显坐标与 accuracy**;原始坐标只落证据表。
- **footprint(基于 v0.51.0)**:endpoint **345→350** / controller **69→71** / migration **51→52**(`activity_check_ins` 纯 additive 空表 + 手写 live registrationId partial unique)/ BizCode **238→240**(22077 家族两码;窗外/未发布等复用既有码)。权限码 **206** / AuditLogEvent **113** / cron **2** / 内置角色 **9** / module **35** 全不变,0 新依赖。

---

## 2. 验证快照

- **五 PR 全部三检查绿后串行合入**(分支保护强制);主会话元核验:①migration 恰好 +1 纯 additive 空表,schema diff 仅 1 model+3 反向关系;②路由恰好 350,权限/审计/cron/seed 零变,三 god-service/状态机/Policy/守卫零 diff;③签到闸 e2e 矩阵(非 pass 拒/窗外硬拒含 `endAt±36s` 边界/completed 仅可签退/幂等 winner/超距宽进 500m 边界两侧/8 路并发单行/坐标 DTO 边界/AuditLog 零写断言);④草稿全链 e2e(`draft.records 可原样 POST 既有 attendance-sheets 并生成 pending Sheet`;忘签退 endAt 预填;取消报名出局;零打卡 absent 清单)。
- **偏离记录(均已核可)**:partial unique 落 `registrationId`(非 goal 草案的 activityId×memberId)——语义更准(证据锚定单次报名),「取消旧报名→旧行出局→新 pass 另建一行」有显式 e2e;App 路径归入 `my/` 命名空间(GET/POST 同路径);BizCode 仅 +2(窗外/未发布复用既有码)。

---

## 3. 部署与前端联调

- **数据库**:`prisma migrate deploy` 有 1 条 pending(`20260715101605_add_activity_check_ins`,纯 additive 空表,无回填无不可逆);seed 无涉。
- **环境变量**:新增可选 `ATTENDANCE_CHECKIN_RADIUS_METERS`(默认 500,范围 50–10000);不设即默认。cron 零变化,运维零动作。
- **⚠️ 价值兑现前提**:geofence 只对**填了经纬度的活动**生效——运营创建活动时应尽量填坐标(admin-web 建表单地图选点是后续增强项);不填坐标的活动签到全部标「未校验」。
- **miniapp(核心适配方)**:签到按钮态由 `GET .../check-in`(200/22002)驱动;签到/签退需引导定位授权(经纬度必填,拒绝授权无法签到);错误码:22076(报名非 pass/软删)/22077 家族(无签到可退等)/复用窗外与未发布码;详见 [`docs/handoff/miniapp.md`](../../handoff/miniapp.md)。
- **admin-web**:活动详情可加「现场打卡」tab(check-ins 列表,outOfRange/unverified 标记醒目化)+「生成考勤草稿」按钮(draft → 可编辑表格 → 提交走既有考勤创建;absent 清单辅助缺勤判断);详见 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md)。
- 不回改本 handoff;发布后事实由 `docs/current-state.md §1/§2` 与 `docs/handoff/openapi.json` 承接。

---

## 4. 明确未做 / 后续接口

- 扫码签到(二期另议,拍板 P1 明确不做);自动补签退(拍板 P3 不做,不加 cron);打卡修改/删除端点(证据层 append-only);签到通知(范围外)。
- 刀 6 后续:候补递补 → 活动评价 → 岗位/时段(D7 排序),各自独立 goal;《志愿服务记录证明》导出可并行立项(聚合口径已由 v0.51.0 就绪)。
- 29 问台账剩余:#1 评价 / #2 岗位时段 / #5 候补 / #7 类型台账语义 / B9 幂等键 / B12 组织者字段 / B14 报名表单 schema。#3 与 #18 自本版关闭。
