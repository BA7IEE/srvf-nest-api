# SRVF API v0.26.1 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.26.0.md`](v0.26.0.md);v0.26.0 之后的工作是 **#399 全仓系统性 review 的 P2 六项修复**(review-then-fix,#400-#404,详见 §4),**纯 fix 收口、零 feature / 零 schema**。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.26.1` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7,维护者亦可手动)。

---

## 1. TL;DR

- **v0.26.1 = #399 全仓 review P2 六项修复 release**(review-then-fix;base = v0.26.0;**全 `### Fixed`、零 feature / 零 schema / 零 migration**)。冻结报告 [`full-repo-systematic-review-v0.26.0.md`](../reviews/full-repo-systematic-review-v0.26.0.md)(顶部「✅ 修复落地状态」)。
  - **F1**(安全·RBAC 提权,#400):`role-permission.assign` 加 SA-only 保留码分级闸——非 SUPER_ADMIN 不得把 6 条保留码(`user.update.role` / 4×`*-setting.reset.credentials` / `member.delete.record`)自授给任意角色 → 新 BizCode **30103**;6 码单一来源 + seed 漂移哨兵 + 4-lens 对抗 verify。
  - **F2**(安全·IDOR,#402,COS 接通前必修):attachment 模式 A `create()` 校验 `dto.key` 匹配 `attachments/<envPrefix>/` 派生格式正则(挡命名空间外任意对象签 URL)→ 新 BizCode **13014**;模式 B(HMAC token)本就安全。
  - **F3**(正确性,#404):emergency relation 字典校验抽 canonical 纯函数,**报名侧(提交即拒)+ promote(defense-in-depth)+ emergency-contacts(委托)三处共用** → 复用 **19010**;修掉「报名收非法 relation → 仅 promote 拒 → 永久卡 publicity」根因。
  - **F4**(正确性,#403):一级 `reject` 的 records 跟随软删(对称 `finalReject`)→ 释放 time-overlap 时间窗,解死锁;审计加 `beforeRecords`/`recordsCount`。
  - **F5 + F6**(依赖 CVE,#401):`pnpm.overrides` 钉 `multer ^2.2.0` + `fast-xml-parser ^4.5.5` + `request>form-data ^2.5.4` + `tencentcloud-…-common>form-data ^3.0.5`;4 目标路径 critical/high=0,audit 26→16。
- **全程 B/config 档,零 schema/migration/seed**;每项「代码确认 → 修 → 回归/边界测试 → 对抗式自检 → per-task PR + 元核验 → CI 绿合」。
- contract **212 路由零变化**(snapshot 353,仅 30103/13014/19010 错误码增量入既有端点 `@ApiBizErrorResponse`)/ migrations **21 零变化** / e2e **97 suites / 1960 tests**(+16:F1 +5 / F2 +8 / F4 +1 / F3 +2)/ unit **48 spec / 1568**(+2 spec:F1 reserved-codes / F2 attachment-key-format)/ 新 BizCode **30103 / 13014**(F3 复用 19010)/ 权限码 **146 零变化** / 业务模块 **27 零变化**
- bump PR 已合入:[#406](https://github.com/BA7IEE/srvf-nest-api/pull/406) / squash commit `b860dd05`
- **`v0.26.1` tag / GitHub Release 尚未创建**(latest 仍为 `v0.26.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)

下一会话:**无阻塞性技术债**。#399 P2 六项已清,底座结构性健康(0 P0/P1)。**残余 4 项 P3**(均无当前运行时危害,登记 [`NEXT_TASKS.md` P2-6](../../ai-harness/NEXT_TASKS.md)):F2 owner-绑定 key(COS 休眠接通前非紧急)· attachment.`*.other` 接 enforcement 时复核 F1 保留集 · dev-only CVE(`fast-uri`/@types-supertest)+ `cos>fast-xml-parser` <5.7.0 moderate(需 4→5 breaking)· CI `pnpm audit` gate(报告 F18)。报告 §3 的 **P3(13)** 其余(F10/F11/F12 等)未在本批,按报告 §7 后续另起 review-then-fix goal。三条运维接力仍悬(同 v0.26.0:实名核验 / 微信 / SMS,均「正确但休眠」)。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.26.1`** | PR #406 已 bump |
| Swagger `setVersion(...)` | **`0.26.1`** | PR #406 已 bump |
| `CHANGELOG.md` v0.26.1 段 | **已存在**(`## v0.26.1 - 2026-06-20`,**6 条目**:Fixed ×6〔F1 #400 / F2 #402 / F3 #404 / F4 #403 / F5 #401 / F6 #401〕)| PR #406 直接写入(**无 `## Unreleased` 可折叠**:六项 fix PR code-only 未带条目,维护者拍板「走 A 直接写 v0.26.1」;条目抽自 #400-#404 PR 描述 = 事实非编造) |
| 远端最新 git tag | **仍为 `v0.26.0`**(指向 `ec8b2e70`)| `v0.26.1` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.26.0`** | `v0.26.1` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`b860dd05`** | `chore(release): bump v0.26.1(…) (#406)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.26.0 → 0.26.1` = **patch**(**刻意偏离 process「0.x 默认 minor」**,维护者 2026-06-20 拍板)。

- ✅ **本版全为 `### Fixed`**(安全 F1·F2 / 依赖 CVE F5·F6 / 正确性 F3·F4),**零 feature / 零 `### Added`**。
- ✅ **零 schema / 零 migration / 零 seed**(migration 仍 21);新 BizCode 仅 30103 / 13014(F3 复用 19010),均挂既有端点错误响应、**无新路由**(contract 212 零变化)。
- ✅ **零破坏**:既有端点契约 + e2e 断言不受影响;F3/F4 是**预期行为修正**(reject 软删 records / submit 拒非法 relation),characterization 测试随之 true-up,非回归漂移。
- ✅ **依赖**:仅 `pnpm.overrides` 钉传递依赖补丁版(multer/fast-xml-parser/form-data),无新顶层依赖、无破坏性主版本跳跃(`fast-xml-parser` 留 4.x);build + provider 单测 + e2e 上传 smoke 全绿。

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.26.1 段](../../../CHANGELOG.md) 与各 PR + 冻结报告 [`full-repo-systematic-review-v0.26.0.md`](../reviews/full-repo-systematic-review-v0.26.0.md)。A 档 docs 收尾(#405)沿惯例不进 CHANGELOG。

### #399 全仓 review P2 修复 goal(review-then-fix;per-task PR + 元核验)

- **F1** [#400](https://github.com/BA7IEE/srvf-nest-api/pull/400)(B 档)RBAC 分级闸:`reserved-super-admin-permission-codes.ts` 6 码单一来源 + `assign()` `assertNoReservedCodesOrThrow` + 30103 + seed 漂移哨兵 + 4-lens 对抗 verify(bypassable=false)
- **F5+F6** [#401](https://github.com/BA7IEE/srvf-nest-api/pull/401)(B/config 档)依赖 CVE:`pnpm.overrides` ×4(multer ^2.2.0 / fast-xml-parser ^4.5.5 / request>form-data ^2.5.4 / tencentcloud-…-common>form-data ^3.0.5);4 目标路径 crit/high=0,audit 26→16;门禁 = audit 清零 + build + cos/SMS provider 单测 + e2e 上传 smoke
- **F2** [#402](https://github.com/BA7IEE/srvf-nest-api/pull/402)(B 档)attachment key 命名空间派生正则:`attachment-key-format.ts` `isDerivedAttachmentKey` + create() 校验(早于 tx)+ 13014;残余 owner-绑定留 P3
- **F4** [#403](https://github.com/BA7IEE/srvf-nest-api/pull/403)(B 档)attendance 一级 reject records 软删解 time-overlap 死锁(对称 finalReject)+ 审计 beforeRecords/recordsCount + 死锁修复 e2e
- **F3** [#404](https://github.com/BA7IEE/srvf-nest-api/pull/404)(B 档)emergency relation 字典校验 canonical 纯函数,报名侧主入口 + promote defense + emergency-contacts 委托三处共用(19010);报名侧根因(永久卡 publicity)经维护者复核补强
- **docs 收尾** [#405](https://github.com/BA7IEE/srvf-nest-api/pull/405)(A 档)#399 报告标已修 + current-state §4/§2 计数 true-up + NEXT_TASKS P2-6 残余 4 条

### v0.26.1 closeout

- bump [#406](https://github.com/BA7IEE/srvf-nest-api/pull/406) + 本 handoff PR(landing 阶段跳过——CHANGELOG 直接随 bump 写入〔走 A〕,跨文档事实由 #405 docs 收尾承接,bump 前零新增)

---

## 5. API Surface Index

**212 条 `EXPECTED_ROUTES` 零变化**(v0.26.0:212)——本版纯 fix,无新端点。F1/F2/F3 仅向既有端点 `@ApiBizErrorResponse` 增 30103 / 13014 / 19010 错误码(snapshot 内既有路由的错误响应增量,**路由数与前缀零变**)。落 **5 canonical 前缀**;`docs:rbacmap:check` 0 FAIL / 0 WARN(**146 码零变化** / 内置角色 ×3 / ops-admin 绑 63 / biz-admin 绑 57)。

---

## 6. Test / Contract Snapshot

> 数据沿 #400-#405 各 PR 本机 `agent:check:full` 等价链 + CI 记录(2026-06-20)+ 收口前主会话 `pnpm test` / `test:e2e` / `test:contract` 实跑亲核。

- ✅ E2E:**97 suites / 1960 tests**(v0.26.0:97/1944;**+16**:F1 +5〔role-perm 边界 4 + seed 哨兵 1〕/ F2 +8〔key 派生格式正例 + 6 反例 + 模式 B canary〕/ F4 +1〔reject 软删解死锁〕/ F3 +2〔报名侧拒 + promote defense〕;suite 数不变)
- ✅ unit:**48 specs / 1568 tests**(v0.26.0 handoff 记 46/1494;**前序基线实测 1539**〔旧记 1494 系既有 spec 增例未同步,current-state §2 已校正〕;本批 **+2 spec**:F1 `reserved-super-admin-permission-codes` 3 例 + F2 `attachment-key-format` 26 例 → 48/1568)
- ✅ contract:**353 passed**,**212 路由零变化**;snapshot 增量仅既有端点错误响应 + 2 个 attachment key example,排除 `info.version`;**零 L3**
- ✅ `docs:codemap:check` 0 FAIL(27 模块 / 21 migration 零变化)/ `docs:rbacmap:check` 0 FAIL 0 WARN(**146 码零变化**)
- ✅ migration:**21 个零变化**(本版零 schema);依赖侧 `pnpm.overrides` ×4(F5/F6)pnpm audit 26→16
- ✅ BizCode 新增 **30103**(`PERMISSION_RESERVED_SUPER_ADMIN_ONLY`)/ **13014**(`ATTACHMENT_KEY_INVALID`);F3 复用既有 **19010**

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md` §1(tag 后独立回填 PR)
- ❌ **不**做 #399 残余 4 项 P3(F2 owner-绑定 key / attachment.`*.other` 接 enforcement 复核保留集 / dev-only CVE + fast-xml-parser 5.x breaking / CI audit gate)— 登记 NEXT_TASKS P2-6,诉求/接线时处理
- ❌ **不**做报告 §3 的 P3(13)其余(F10 confirm owner / F11 容量竞态 / F12 即时清漏 / F13/F14 doc-drift / F15/F16 同批 openid / F17/F19 扩容文档)— 按报告 §7 分批后续另起 review-then-fix goal
- ❌ **不**碰真实腾讯云实名核验 / 微信 / SMS 凭证(三条运维接力,SOP 同 v0.26.0;均「正确但休眠」)
- ❌ **不**改源码业务逻辑 / schema / 测试 / 依赖顶层(本版仅 fix + overrides)

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.26.1 <handoff-pr-squash-commit>
git push origin v0.26.1
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.26.1 - 2026-06-20/{flag=1; next} /^## v0.26.0 - 2026-06-20/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.26.1`;target:`v0.26.1` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff;§2/§4 已随 #405 true-up,本批无新增)+ 地图快照戳(若落后)。

之后各线均不自动启动:① **#399 残余**(P2-6 四项 + 报告 §3 P3(13)其余,诉求/接线/分批触发);② **运维侧三条真实通道接力**(腾讯云实名核验 / 微信小程序 / SMS 送审;均「正确但休眠」);③ **招新 / 团队管理后续**(phase-3 之后,评审稿 §9 + NEXT_TASKS,诉求触发);④ **小程序前端**立项。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.26.0`(2026-06-20;指向 `ec8b2e70`) |
| Target | `v0.26.1`(pending) |
| Landing | 跳过(CHANGELOG 走 A 直接随 bump 写入;跨文档事实由 [#405](https://github.com/BA7IEE/srvf-nest-api/pull/405) docs 收尾承接,bump 前零新增) |
| Bump PR | [#406](https://github.com/BA7IEE/srvf-nest-api/pull/406) / `b860dd05` |
| Handoff PR | 本 PR(branch `claude/handoff-v0.26.1`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-20 |
