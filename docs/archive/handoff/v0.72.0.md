# SRVF API v0.72.0 Handoff Index

> 面向下一次 AI 开发会话。**本文件由 `pnpm release:prepare` 生成**;
> 它是 release 时刻的**快照**,合入后不回改 —— 发现过时请改 `docs/current-state.md`。
> 接续 [`v0.71.0.md`](v0.71.0.md);逐笔详情见 CHANGELOG `## v0.72.0` 段。

---

## 1. 版本与 footprint

- **v0.72.0**(Release 日期 **2026-08-31**)
- 生成时代码基线:`f508cb5fe27b2332ff29484531f5534716d3deec`
- 仓库 footprint(取自 `pnpm docs:counts` 守护计数,与 current-state 同源):

| 计数项 | 值 |
|---|---|
| 模块 | 43 |
| Controller | 108 |
| Endpoint | 570 |
| Migration | 100 |
| BizCode | 486 |
| 权限码 | 246 |
| AuditLogEvent | 156 |
| 内建角色 | 15 |
| Cron | 2 |

## 2. 本版交付(取自 CHANGELOG,逐笔详情见该段)

- 修复 Integration 控制面审计来源：统一使用全局规范化后的 request-id 和客户端 IP，拒绝控制器重新采纳原始代理头。

- 收口 Service Principal 角色资格不变量：角色权限整集替换与绑定创建/恢复共享 Role 锁，拒绝不合资格权限进入已绑定角色，并提供存量只读核验。

## 3. 状态口径

- 本版本是**代码与契约里程碑 Release**,不等于生产部署;部署边界见 `docs/current-state.md` §1 与 §4。
- tag 指向本 release PR 的 squash 合并提交(由 `pnpm release:finish` 打,并输出证据)。
