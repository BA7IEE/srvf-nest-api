# SRVF API v0.23.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.22.0.md`](v0.22.0.md);v0.22.0 之后的工作是**单一主线:微信小程序登录基础设施 goal(P1-8,T0-T4 全队列 #352-#356)**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.23.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7,维护者亦可手动)。

---

## 1. TL;DR

- **v0.23.0 = 微信小程序登录基础设施 release**(单一主线;**ARCHITECTURE §9「第一个小程序产品要接」升级路径正式解锁**,冻结评审稿 [`wechat-mini-login-review.md`](../reviews/wechat-mini-login-review.md);**"正确但休眠"**——DevStub 全链可测,真实可用待运维录入 AppID/AppSecret + 小程序前端):
  - **T1 schema**(#353):`User +openid String? @unique`(含软删占用)/ `SmsPurpose +WECHAT_BIND` / 新表 `wechat_settings` + enum `WechatProviderType`;migration **17 个**(16→17)
  - **T2 通道层**(#354):`src/modules/wechat/` 第 **23** 模块——`WECHAT_ENCRYPTION_KEY` AES-256-GCM(独立 key + 独立 salt,三把 key 密文互不可解)/ settings 三端点(reset 仅 SA)/ DevStub 确定性假 openid `dev-openid-<code>` / 真实 `code2session` **原生 fetch + 8s 超时,零新依赖**(session_key/unionid 解析即弃);权限码 117→**121**(ops-admin 58→**61**)
  - **T3 认证+绑定**(#355):`POST auth/v1/login-wechat` **第三个独立认证端点**(`createSession` 同构签发,`auth.service.ts` diff 仅 event union 类型行;未绑返 `bindingRequired:true`)+ `wechat-bind{,/send-code}`(**绑定锚点 = 手机短信**,七步校验顺序冻结,防枚举沿 login-sms 泛化 200 + 统一 24010)+ `GET/PUT app/v1/me/wechat`(openid 一律掩码)+ admin 清除(幂等,解绑唯一路径);BizCode **25xxx 段 4 码** / audit union 31→**35** / **第 8 throttler** `login-wechat`(IP 5/60)
  - **红区随队列改写**(goal 唯一授权,逐行 before/after 在各 PR):AGENTS §1(B 清单解锁记录)/ §8(微信端点行)/ §9(❌清单微信移除)+ baseline §1.1(25xxx 段加行)+ ARCHITECTURE §9(:171 标 ✅ 已解锁 + 实际落地形态注记;:181 未解锁清单移除微信);**密码登录契约零变化,auth 既有 e2e 断言零修改**
- **行为锁全程守住**:sms 模块零 diff / JwtPayload zero drift / 既有 7 throttler 配置零变化 / contract 仅新增(159→**168**)
- contract **168 路由** / migrations **17** / e2e **89 suites / 1818 tests** / unit **37 spec / 1324 tests** / 权限码 **121** / audit union **35**
- bump PR 已合入:[357](https://github.com/BA7IEE/srvf-nest-api/pull/357) / squash commit `195fe123`
- **`v0.23.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.22.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)

下一会话:**运维侧两条接力是真实可用的唯一闸门**——① 微信:注册小程序 → AppID/AppSecret → admin 录凭证(仅 SA)→ 真实验收(SOP [`ops/wechat-mini-production-rollout-checklist.md`](../../ops/wechat-mini-production-rollout-checklist.md));② SMS:腾讯云两模板送审(SOP [`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md));**招新需求会议(延期中)仍是业务主线入口**;小程序前端未启动。无阻塞性技术债。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.23.0`** | PR #357 已 bump |
| Swagger `setVersion(...)` | **`0.23.0`** | PR #357 已 bump |
| `CHANGELOG.md` v0.23.0 段 | **已存在**(`## v0.23.0 - 2026-06-12`,3 条目:#353/#354/#355)| PR #357 折叠 Unreleased 时填入(T0/T4 为 A 档沿惯例不登记) |
| 远端最新 git tag | **仍为 `v0.22.0`**(指向 `43630555`)| `v0.23.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.22.0`** | `v0.23.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`195fe123`** | `chore(release): bump v0.23.0(三文件…) (#357)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.22.0 → 0.23.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **纯新增功能**:+9 端点(contract 159→168,snapshot 经核验仅新增/纯移动零内容丢失)/ +1 migration(纯新增可空列 + 新表 + enum 新值,零回填零改写)/ +4 BizCode(新 25xxx 段)/ +4 权限码 / +4 audit 事件 / +1 throttler 实例
- ✅ **零破坏**:密码登录 / OTP 登录 / refresh / logout 契约零变化(auth 既有 e2e 断言零修改全绿);sms 模块文件零 diff;`JwtPayload` zero drift
- ✅ **零新依赖**(code2session 走 Node 原生 fetch);`WECHAT_ENCRYPTION_KEY` 为 production/smoke 新必填 env(fail-fast 保护,部署侧需注入——docker-smoke 已同步,deployment 见 §7 注)

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.23.0 段](../../../CHANGELOG.md) 与各 PR;A 档 docs PR 沿惯例不进 CHANGELOG。

### 微信小程序登录基础设施 goal(P1-8;T0-T4 全队列)

- **T0** [#352](https://github.com/BA7IEE/srvf-nest-api/pull/352)(A 档)评审稿冻结:[`wechat-mini-login-review.md`](../reviews/wechat-mini-login-review.md)(D-W1~6 拍板:锚点=手机短信 / 字段=openid only / 解绑=镜像手机 / 范围=完整 / §9 解锁;E-1~E-30 工程代决;五张清单 + 七步流程冻结 + 敏感字段三问)+ NEXT_TASKS P1-8 登记
- **T1** [#353](https://github.com/BA7IEE/srvf-nest-api/pull/353)(D 档)schema migration `20260612091522_add_wechat_mini_login_infra`(干净库 17/17 重放 + `migrate diff` 零漂移 + seed 幂等二跑)
- **T2** [#354](https://github.com/BA7IEE/srvf-nest-api/pull/354)(D 档)通道层 + 凭证设置(unit +23;e2e +`wechat-settings` 11 例;seed 亲核 121/61/4)
- **T3** [#355](https://github.com/BA7IEE/srvf-nest-api/pull/355)(C/D 档)认证 + 绑定端点(e2e +auth-wechat 13 例〔含第 8 实例真实 5/60 限流与物理隔离〕+app-me-wechat 7 例;AGENTS/baseline 红区随 PR)
- **T4** [#356](https://github.com/BA7IEE/srvf-nest-api/pull/356)(A 档)docs 收尾:ARCHITECTURE §9 标解锁 / current-state §1-§4 true-up / deployment 单实例注记 **6 处**(+wechat-settings 60s 缓存)·限流器 **8 个** / [`ops/wechat-mini-production-rollout-checklist.md`](../../ops/wechat-mini-production-rollout-checklist.md) 新建 + README §1 登记 / NEXT_TASKS P1-8 归档

### v0.23.0 closeout

- bump [#357](https://github.com/BA7IEE/srvf-nest-api/pull/357) + 本 handoff PR(landing 阶段探针满足跳过——跨文档事实已由 #356 T4 承接)

---

## 5. API Surface Index

**168 条 `EXPECTED_ROUTES`**(v0.22.0:159;**+9 全部微信**:wechat-settings 3〔system〕+ pre-auth 3〔auth〕+ me/wechat 2〔app〕+ admin 清除 1),全落 4 canonical 前缀;鉴权后缀(P2-2 惯例)168/168 全覆盖;`docs:rbacmap:check` 0 FAIL / 0 WARN(**121 码** / 内置角色 ×3 / ops-admin 绑 **61**)。

---

## 6. Test / Contract Snapshot

> 数据沿 #353-#357 本机 `agent:check:full` 等价链 + CI 记录(2026-06-12)。

- ✅ E2E:**89 suites / 1818 tests**(v0.22.0:86/1787;+wechat-settings 11 / auth-wechat 13 / app-me-wechat 7;**既有 auth 断言零修改**;seed 二档计数基准随授权 seed 变更同步 58→61,PR #354 显式登记)
- ✅ unit:**37 specs / 1324 tests**(v0.22.0:34/1301;+wechat-crypto 7 / dev-stub 2 / wechat provider mock-fetch 14)
- ✅ contract:**300 passed**,168 条路由;snapshot 仅新增 + 纯移动(`comm` 双向核验零内容丢失);**零 L3**(appSecretEncrypted / session_key / 完整 openid 在 snapshot 出现 0 次)
- ✅ `docs:codemap:check` 0 FAIL(1 WARN 为 god-service 体量观察,users 832L 本期新进,沿 current-state §4)/ `docs:rbacmap:check` 0 FAIL 0 WARN
- ✅ migration:**17 个**(+`20260612091522_add_wechat_mini_login_infra`);依赖零增减
- ✅ docker-smoke:`WECHAT_ENCRYPTION_KEY` 注入链(生成/掩码/GITHUB_ENV/docker run)通过(#354 起 CI)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md` §1(tag 后独立回填 PR)
- ❌ **不**碰微信真实凭证(注册小程序 / AppID·AppSecret 录入 / 真实微信验收 = 运维接力,SOP [`ops/wechat-mini-production-rollout-checklist.md`](../../ops/wechat-mini-production-rollout-checklist.md);**部署注**:升级到 v0.23.0 的 production/smoke 环境必须注入 `WECHAT_ENCRYPTION_KEY`,否则启动 fail-fast——这是预期保护)
- ❌ **不**碰腾讯云 SMS 送审 / 真实凭证(运维接力,[`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md))
- ❌ **不**做小程序前端 / unionid·session_key 存储 / 本人裸解绑 / openid 自动注册(评审稿 §12)
- ❌ **不**启动招新模块任何实装(**等需求会议**);**不**做 trust proxy / 连接池调优(**等部署拓扑确定**)
- ❌ **不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.23.0 <handoff-pr-squash-commit>
git push origin v0.23.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.23.0 - 2026-06-12/{flag=1; next} /^## v0.22.0 - 2026-06-12/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.23.0`;target:`v0.23.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零;§4 复核)+ 地图快照戳(若落后)。

之后三条线,均不自动启动:① **运维侧微信接力**(注册小程序 → 录凭证 → 真实验收;系统侧零代码等待)+ **SMS 送审接力**(两模板一批);② **招新需求会议**(延期中)→ 会后按 G1 文档(SRVF 仓)§四回填 8 问 → 字段冻结表 → D 档 schema 评审;③ **小程序前端**立项(微信登录后端已"正确但休眠"等待对接)。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.22.0`(2026-06-12;指向 `43630555`) |
| Target | `v0.23.0`(pending) |
| Landing | 跳过(探针满足:跨文档事实已由 [356](https://github.com/BA7IEE/srvf-nest-api/pull/356) T4 承接,bump 前零新增) |
| Bump PR | [357](https://github.com/BA7IEE/srvf-nest-api/pull/357) / `195fe123` |
| Handoff PR | 本 PR(branch `release-v0.23.0-handoff`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-12 |
