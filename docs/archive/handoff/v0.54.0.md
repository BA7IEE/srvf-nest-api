# SRVF API v0.54.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.53.0.md`](v0.53.0.md);逐笔详情见 CHANGELOG `## v0.54.0` 段 + `docs/current-state.md §2`。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 能力终态 / 索引 / 操作提示;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR #640 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.54.0` tag 将指向本 handoff PR 的 squash merge commit。

---

## 1. TL;DR

- **v0.54.0 = 活动评价(审计刀 6 第三件)**。交付链 = 四拍板(E1 真到场者才能评 / E2 1-5 星+可选文字 / E3 对管理员实名不对队员公开 / E4 completed 后 30 天可评可改)→ goal 五 PR 全绿自合:T0 冻结 #635 `b3b16425` → schema #636 `afae8acc` → App 自助面 #637 `2ed98af6` → Admin 面 #638 `75320987` → 收口 #639 `a5ede8a3` → 主会话四维元核验 + 对抗性复核(**0 存活 finding**)→ bump #640 `133a6fc3` → 本 handoff(**tag 点**)。冻结评审稿:[`docs/archive/reviews/activity-feedback-t0-review.md`](../reviews/activity-feedback-t0-review.md)。关闭审计项 **#1**。
- **纯 additive,0 既有行为变更**:三个 participation god-service 主文件、贡献值/考勤/候补/打卡/结算路径全部零 diff;`prisma/seed.ts` 与 `audit-logs/` 整目录字节级零 diff。评价**不进 AuditLog**、不影响任何结算。
- **第 36 个模块 `activity-feedbacks/`**:独立落点,不增厚既有 god-service;资格判定直连 prisma 读 `AttendanceRecord`(不 import AttendancesService,防环)。
- **4 个新端点(350→354)**:App `PUT/GET app/v1/my/activities/:activityId/feedback`(PUT = 窗口内 upsert;GET 返回本人那条 + `canSubmit` + `windowClosesAt` 供前端渲染按钮态);Admin `GET admin/v1/activities/:activityId/feedbacks`(实名列表)+ `GET .../feedback-summary`(count / avgRating / 1-5 星直方图 / 评价率)。判权:App = canUseApp + self-scope 无码;Admin = 复用 `attendance.read.sheet` + activity ref。**0 新权限码**。
- **资格 = approved-only**:仅当该 member 在该活动有 **approved sheet 内未软删的考勤记录**才可评(record 与 sheet 双 `deletedAt:null`);pass 报名没来 / 候补 / 无关队员 → `35032`。pending / rejected 考勤单里的记录**不算**资格。
- **窗口 = `endAt + N 天` 且 `statusCode='completed'`**(N = `ATTENDANCE_FEEDBACK_WINDOW_DAYS`,默认 30,范围 1–365)。**代决偏离(T0 §2.4 已记载)**:不新增 `Activity.completedAt` 列 —— completed 是人工动作、时点不稳定,endAt 是活动事实且已有索引;全链零处引用 completedAt。
- **可见性 E3**:App 面只能取本人那条,**任何 App 路径都拿不到他人评价或活动均分**——`participation-summary` 新增的 `feedback:{count,avgRating}` 挂在 **Admin** 侧 `ActivityParticipationSummaryDto`,App 的 participation-summary 走 attendances 模块另一份 DTO(该模块整包零 diff),均分不会溢出。
- **footprint(基于 v0.53.0)**:endpoint **350→354** / controller **71→73** / module **35→36** / migration **52→53**(`activity_feedbacks` 纯 additive 空表 + 手写 live partial unique)/ BizCode **240→244**(35002/35030/35031/35032)。权限码 **206** / AuditLogEvent **113** / cron **2** / 内置角色 **9** 全不变,0 新依赖。

---

## 2. 验证快照

- **五 PR 各自三检查全绿后串行合入**;主会话四维元核验(足迹与 schema / 资格与窗口与 upsert / 可见性与模块边界 / e2e 覆盖)+ 对抗性复核:11 条候选发现全部被推翻,**0 存活**。
- 关键亲核:`Activity` 未偷加 `completedAt` 或任何标量列(schema diff 仅 ActivityFeedback model + 3 处反向关系);migration SQL 纯 CREATE TABLE + 2 FK Restrict + 5 单列索引 + 末尾手写 partial unique,无回填/无不可逆/无 enum;`rating Int` 非 enum、无 `registrationId`;OpenAPI 结构化比对纯 additive(schemas 391→398 全为新增,既有仅 ActivityParticipationSummaryDto 加 feedback,0 required 收紧、0 属性删除)。
- **并发写**:PUT 走 live-`findFirst` → create/update;并发败者由手写 partial unique 兜到 P2002 并统一转 `35002`(数据恒只留一行,败者得明确错误码,重试即为更新)。
- e2e:`test/e2e/activity-feedbacks.e2e-spec.ts` 真实 DB 覆盖资格/窗口/统计/并发/no-audit;单测另有 fake-clock 覆盖窗口边界。

---

## 3. 部署与前端联调

- **数据库**:`prisma migrate deploy` 有 1 条 pending(`20260715193127_add_activity_feedbacks`,纯 additive 空表,无回填无不可逆);**seed 零涉**(本版无字典/权限/角色变更,但常规幂等跑一次无害)。
- **环境变量**:新增可选 `ATTENDANCE_FEEDBACK_WINDOW_DAYS`(默认 30,范围 1–365,越界或非整数**启动即报错退出**);不设即默认。cron 零变化。
- **miniapp**:活动详情(completed 且本人有考勤记录时)加「评价」入口 —— 先 `GET .../feedback` 拿 `canSubmit` 与 `windowClosesAt` 渲染按钮态与倒计时,已评则回显本人 rating/comment 供修改;`PUT` 提交(rating 1-5 必填、comment ≤500 可选)。错误码:`35032` 无到场记录(应在 UI 层就按 canSubmit 隐藏入口,避免撞码)、`35030` 活动未完结、`35031` 窗口已关、`35002` 并发重复(提示重试即可)。详见 [`docs/handoff/miniapp.md`](../../handoff/miniapp.md)。
- **admin-web**:活动详情加「评价」tab —— `feedbacks` 列表(实名 + 星级 + 文字 + 时间)、`feedback-summary`(均分/直方图/评价率)可做卡片;`participation-summary` 里也带 `feedback:{count,avgRating}` 可直接在概览位展示。**注意**:评价对队员不公开,admin 面的实名与内容不要回传给 App 端。详见 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md)。
- 不回改本 handoff;发布后事实由 `docs/current-state.md §1/§2` 与 `docs/handoff/openapi.json` 承接。

---

## 4. 明确未做 / 后续接口

- 不做:多维度评分(日后 additive 加列)、跨活动评价横扫端点、评价提醒通知、评价进 audit、评价影响贡献值、队员端互看评价与均分。
- **刀 6 只剩最后一件:岗位/时段**(审计 #2;体量最大 —— activity_positions 子表 + 每岗位独立容量/时段/要求 + 报名挂岗位,务必独立 goal)。
- 《志愿服务记录证明》导出可并行立项(聚合口径 v0.51.0 已就绪)。
- 29 问台账剩余:**#2 岗位时段 / #7 类型台账语义 / B9 幂等键 / B12 组织者字段 / B14 报名表单 schema**。#1 自本版关闭(#5 于 v0.53.0、#3/#18 于 v0.52.0、#8/#11/#22/#24 于 v0.51.0、其余 12 项于 v0.50.0)。
