# SRVF API v0.45.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.44.0.md`](v0.44.0.md);逐笔详情见 CHANGELOG `## v0.45.0` 段 + `docs/current-state.md §2`。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / finding 终态 / 索引 / 操作提示;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR #582 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.45.0` tag 将指向本 handoff PR 的 squash merge commit;GitHub Release(Latest)与 current-state 回填随后依序完成。

---

## 1. TL;DR

- **v0.45.0 = 系统性安全收口线**。承接 v0.44.0 全仓审计,维护者提出「别逐接口补刀、要把权限委派 / 状态迁移 / 并发计数 / 文件验证做成所有入口必经的统一底层规则」。15 项二次核实对着实际代码逐条确认,findings **1–7 全属实**,以「一个统一原语 + 全兄弟路径接线 + 漂移/并发测试」四刀收口;findings **8–15 接受登记**(记 NEXT_TASKS / 模块台账)。
- **实现链**:委派+受保护角色 #578 `cf9379bd` → 任期+最后管理员 #579 `d9d71cf8` → 状态迁移+证书信任 #580 `359c3e24` → 队员轴同类残留 #581 `99d3f22e` → bump #582 `69e16c53` → 本 handoff(**tag 点**)。四刀均主会话 commit-diff 元核验 PASS(每刀 4 核验点)。
- **#578 委派收口(findings 1/2/3)**:单一 `isControlPlanePermissionCode`(`rbac.*` ∪ `role-binding.*` ∪ 6 条 SA-only 保留码)+ 单一 `RoleDelegationPolicy`,统一覆盖 role-bindings create/preview/特权 update + user-roles assign/revoke + role-permissions 授码(非 SA 拒 `30102`/`30103`,SA 短路);7 内置角色禁删(`PROTECTED_ROLE_CODES` `30104` + seed 漂移哨兵)。
- **#579 任期+最后管理员(findings 4/5)**:共享任期谓词 `role-binding-validity`(`RbacService` 两读 + `AuthzService` 共用,过期/未来绑定三处一致失效);`LastAdminProtectionPolicy` 一把共享 advisory 锁(`users:last-super-admin` / 复用 `role-bindings:last-ops-admin`)串起 SUPER_ADMIN 与 ops-admin 全削权路径,最后持有人返 `30101`。
- **#580 状态迁移+证书信任(findings 6/7)**:单一公共 `claimAtStatus` CAS 原语接入 16 处状态写(activities / registrations / attendances / certificates / recruitment / team-join),各状态机 from→to 矩阵零改;证书核心字段编辑且旧态非 pending → 回退 pending 并清 `verifiedBy/verifiedAt/verifyNote`。
- **#581 队员轴残留(finding-4 同类)**:`MembersService` 三削权门(`updateAccountStatus` / `offboard` / `reopenAccount`)复用 #579 同一 `assertCanDeactivateOpsAdminUser` 与锁键。
- **⚠️ 行为变更三组**:① 委派收窄 / 内置角色禁删 / 任期失效 / 最后 ops-admin `30101`;② 并发状态写败者拒 `*_STATUS_INVALID`;③ 证书编辑回退 pending。详见 CHANGELOG `v0.45.0` 顶部。
- **footprint(基于 v0.44.0,亲核)**:权限码 **205** / biz-admin **81** / org-admin **60** / ops-admin **96** / `EXPECTED_ROUTES` **336** / controller **66** / 模块 **35** / migration **48** / 角色 **7** / AuditLogEvent **99** 全不变;**BizCode 231→232**(仅 `PROTECTED_ROLE_DELETE_FORBIDDEN=30104`);0 schema / 0 migration / 0 新端点 / 0 新依赖(`seed.ts` / 路由 / 审计事件跨四刀 diff 均空)。
- **验证快照**:四刀各 CI(Lint/Typecheck/E2E + Docker image build + Container boot/API smoke/graceful shutdown)全绿 + 主会话逐刀元核验 PASS;bump #582 CI 全绿。

---

## 2. findings 1–15 终态

| Finding | 终态 | 落点 |
|---|---|---|
| #1 角色委派可绕(漏 6 保留码 / 先绑后加权 / 旧 user-roles 路径) | 已修 | #578:单一 `isControlPlanePermissionCode` + `RoleDelegationPolicy`,非 SA 拒 `30102`/`30103` |
| #2 RoleBinding PATCH/preview 不重跑委派检查 | 已修 | #578:特权 update / preview 复用同闸 |
| #3 ops-admin 软删 ops-admin 角色本身 | 已修 | #578:`PROTECTED_ROLE_CODES` 7 内置禁删 `30104` + 漂移哨兵 |
| #4 最后管理员保护非事务 | 已修 | #579:`LastAdminProtectionPolicy` 一把共享 advisory 锁全路径 + #581 队员轴三门 |
| #5 全局 RBAC 忽略 startedAt/endedAt | 已修 | #579:共享任期谓词 rbac/authz 共用,过期/未来绑定三处一致失效 |
| #6 状态机 read-then-update 竞态 | 已修 | #580:`claimAtStatus` CAS 接入 16 处状态写,迁移矩阵零改 |
| #7 已核验证书改核心字段不回退信任 | 已修 | #580:非 pending 编辑核心字段回退 pending + 清核验三字段 |
| #8 短信码弱哈希(无 pepper) | 接受登记 | NEXT_TASKS:需 DB 泄露方生效,纵深防御 |
| #9 旧附件 create 绕过内容校验 | 接受登记 | NEXT_TASKS:privilege-gated;统一附件终验器另立项 |
| #10 招新身份证/签名/证书图只信客户端 MIME | 接受登记 | NEXT_TASKS:同附件终验器 |
| #11 附件 expireAt 不参与下载授权 | 接受登记 | NEXT_TASKS:字段目前装饰性 |
| #12 通知 commit 后崩溃丢失窗口 | 接受登记 | NEXT_TASKS:outbox 撞无 queue/cron 红线,需 D 档 waiver |
| #13 Storage/SMS 单例约束仅代码层 | 接受登记 | NEXT_TASKS:partial-unique 另立项 |
| #14 附件 / 旧 role-binding 列表全量后内存分页 | 接受登记 | NEXT_TASKS:现规模有界 |
| #15 用户状态/软删 + settings 凭证重置缺持久审计 | 接受登记 | NEXT_TASKS:控制面审计补全另立项 |

> findings 1–7(+ 元核验揪出的 finding-4 队员轴同类残留)全修;8–15 接受登记,均记 NEXT_TASKS / 模块台账,除 #12(outbox)撞无 queue/cron 红线外无一撞红线。

---

## 3. 下一会话开工提示

- 开工门禁沿 [`docs/current-state.md §5`](../../current-state.md) + [`process.md §2`](../../process.md);当前事实(版本 / HEAD / open PR)读 current-state §1,不读本文件。
- 前端 / 运营先看 CHANGELOG `v0.45.0` 顶部三组 ⚠️ 行为变更;接口路径与 DTO 字段集均未新增,但 **后台权限配置须适配**:非 SUPER_ADMIN 不能授控制面权限码 / 绑特权角色,7 内置角色不可删。
- **四个统一原语 = 后续同类收口的复用点**,新增同类入口必须走这些门、不再另起散补丁:`isControlPlanePermissionCode` + `RoleDelegationPolicy`(委派)、`role-binding-validity`(任期)+ `LastAdminProtectionPolicy`(末位管理员)、`claimAtStatus`(状态迁移 CAS)。
- 接受项 8–15 仅在触发条件出现后另立项:#12 outbox 需先解无 queue/cron 红线 waiver;#9/#10 附件统一内容终验器;#13 单例 partial-unique;#14 规模触顶;#15 控制面审计补全;#8 短信 HMAC+pepper。
- 不回改本 handoff;发布后事实由 current-state §1/§2/§4 承接。
- **不输出任何 secret** — 不打印 `.env` / bucket / APPID / SecretId / SecretKey / signed URL / JWT 内容,即使在调研 / 报告中。
