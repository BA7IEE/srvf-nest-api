# SRVF API v0.31.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.30.0.md`](v0.30.0.md);v0.30.0 之后是**招新→入队闭环优化 S1–S6 收口的 minor release**(GAP-006;7 切片 PR #439–#445)。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 索引 / 操作提示;不写长篇复盘(逐切片详情见 `docs/current-state.md §2` + CHANGELOG `## v0.31.0`)。
>
> **状态(post-release 补档)**:本文件在 **`v0.31.0` tag / GitHub Release / §1 回填之后**补写(与 current-state §1 回填同 PR);**handoff 是历史快照,tag / Release / 代码全不受其影响**(沿 [`process.md §5.2`](../../process.md))。

---

## 1. TL;DR

- **v0.31.0 = 招新→入队闭环优化 S1–S6(GAP-006)** — 招新报名→入队全链「可见 / 可控 / 可分流」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](../reviews/recruitment-phase4-loop-optimization-review.md)(T0 冻结 #438,12 项 Q-P4-* 已按推荐拍板)。7 切片 PR 累计一个 minor,全 additive / 向后兼容(无 breaking)。
- **7 切片(#439–#445)**:
  - **S1 进度模型 + 状态业务文案**(#439,A 档,纯读冷启动):公开本人查询出参 enrich 为进度模型(`RecruitmentApplicationProgressDto`);单一共享派生纯函数 `deriveRecruitmentStage`(`statusCode` + 门槛完成度 → 业务态 / 动作码 / 身份文案,供 S2 复用);`promoted` → 「已转志愿者 / 待入队」全程禁「已晋升」;展示文案落 `recruitment_stage` 字典。
  - **S2 招新工作台 stats**(#440,A 档,纯读聚合):`GET /api/admin/v1/recruitment/cycles/:id/stats`(新 QueryService `recruitment-stats.service.ts`;判权 R 模式 `rbac.can('recruitment-application.read.record')`)返五组(今日 / 待处理 / 门槛进度 / 综合评定 / 公示发号);复用 S1 派生 + `decidePromotionIssuance`(零第二套判定);有界 fetch + 派生,禁 N+1 / 禁跨轮全表扫。
  - **S3 RBAC 敏感字段分级**(#441,D-lite):「敏感查看」从 `read.record` 切出为新码 `recruitment-application.read.sensitive`;`read.record` 收窄(脱敏列表 / 详情 / 公示 / stats),明文证件号·手机 + 证件照 signed-URL 改判 `read.sensitive`(**响应字段集不变、仅 masking 随码**);新码经 `BIZ_ADMIN_PERMISSION_SEED` 默认补挂 biz-admin → 现有用户零行为变化,字段级分级仅对将来细分角色生效。
  - **S4a H5 + 报名前手机身份链**(#442,D 档含不可逆 enum):新表 `recruitment_identity_sessions`(`SmsPurpose.RECRUITMENT_BIND` 验码 → 一次性 `phoneVerificationToken` sha256 入库,`consumedAt` 原子单消费);`SmsCodeService.{issue,verifyAndConsume,assertValid}` 形参 `userId` 放宽 `string|null`(匿名报名);submit `wechatCode` 改可选 + 新增 `phoneVerificationToken`;**+5 公开端点**(发码 / 验码 / 手机查询② / 自助换微信 / 自助换手机);application +6 手机身份链可空列。
  - **S4b OCR 六分流 + 重拍计数**(#443,D 档 additive 列无 enum):`decideMainlandOcr` 拆为六分流纯函数 `recruitment-ocr-routing.ts`(matched → verified / 模糊 → retake 不落 / 不一致 → 三选一 / 防伪·上游首次 → retake·retry,**H5 会话连续 2 次**才落 high/system manual);application **+4 列**;submit 出参换 `RecruitmentSubmitResultDto`(**申请人侧绝不暴露 riskLevel / forgery**,文案恒中性);进度模型补 retake/confirm/manual_high 三态;S2 待人工三栏升真 `riskLevel`(去 `verifyOutcome` 代理)。
  - **S5 promote 志愿者化 + 入队门禁适配**(#444,D 档「最重一刀」,维护者拍板推翻 phase-3 E-J-6「双表示」):promote `member.create` 加 `gradeCode='volunteer'` + 同事务建 VOL 归口部门(`Organization.code='VOL'` 运行时解析守 ACTIVE,**≠ VOD**;缺失/非 ACTIVE → 建任何 member 前抛 `28044`);抽共享纯函数 `isUnenrolledVolunteer`(新口径 + legacy 双兼容)→ 两处入队门禁(自助发起 + 一键入队)同调;入队写「软删 VOL → 建目标 → `level-1`」守 `member_departments` 单部门 partial unique;历史 legacy 零迁移、不 backfill。**⚠️ ripple**:app `/me`(及回带 `Member.gradeCode` 的 app 出参)志愿者 `gradeCode` 由 `null` → `'volunteer'`(**miniapp 语义变**,已标 handoff)。
  - **S6 批量操作三端点**(#445,E 档,纯加端点):批量标门槛 `POST …/applications/batch-mark-threshold`(逐行复用单行 `markThreshold` = 逐行幂等 + 逐行容错,不整批回滚)/ 批量导出 `POST …/applications/export`(**持 `read.sensitive` → 明文列 / 仅 `read.record` → 脱敏列**,复用 S3 `toAdminDto`)/ 发号前预检 `GET …/cycles/:id/promote-precheck`(复用 `decidePromotionIssuance` **结构性保证「预检 = 实发」**);匹配纯函数 `recruitment-batch-matching.ts`(优先级 tempNo > 姓名+手机 > 手机;多命中 **ambiguous 绝不猜**);零 schema / 零新 RBAC 码 / 零 audit union 扩展。
- **footprint(累计,逐切片均 CI 实跑亲核)**:权限码 155 → **156**(S3 +`read.sensitive`)· BizCode +3(**28044** VOL 部门不可用 / **28050** token 失效 / **28051** 微信已绑他人)· audit union 61 → **63**(S4a `rebind-{wechat,phone}`)· migration 23 → **26**(S4a `add_sms_purpose_recruitment_bind` + `add_recruitment_identity_session` + S4b `add_recruitment_ocr_routing_fields`)· EXPECTED_ROUTES 234 → **243**(S2 +1 / S4a +5 / S6 +3)· 模块 **28** / controller **52** **零变**;新增逻辑全入模块内纯函数 / service(`recruitment-progress-presenter` / `recruitment-stats.service` / `recruitment-identity.service` / `recruitment-ocr-routing` / `recruitment-batch-matching` / `team-join.constants` 扩),**无新模块**。
- **收口链**:base v0.30.0(`f91a774a` / tag)→ 交接/评审 docs #435/#436/#437/#438(A 档,**不进 CHANGELOG**)→ S1 #439 → S2 #440 → S3 #441 → S4a #442 → S4b #443 → S5 #444 → S6 #445(`b5e4fc85`)→ bump #446(`e4c7e526` = **tag 点**)→ §1 回填 + 本 handoff(post-release docs PR)。
- `v0.31.0` tag(annotated,指 `e4c7e526`)+ GitHub Release(**Latest**)+ current-state §1 **均已完成**。

---

## 2. 下一会话

- **无阻塞性技术债**。S1–S6 各切片均经主会话元核验 + CI 全绿(per-slice true-up 见 `docs/current-state.md §2` 招新闭环优化 S1–S6 各条 + §2「测试与契约」末段);本 release 为**纯发版收口**(零业务代码 / schema / test 改动,仅 bump 三文件 + 本 docs 回填)。
- **S7 通知未做(阻塞 GAP-005)**:招新批量 / 各环节通知出口挂 §9 通知闭环,**待会员通知/公告模块(GAP-005,T0 冻结 #437)落地后随 S7 另出 goal**;本版 `## Unreleased` 留空。
- **前端对接**:招新→入队闭环新增能力见 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md)(工作台 stats / 人工队列三栏分流 / 批量操作 / RBAC 敏感分级)+ [`docs/handoff/miniapp.md`](../../handoff/miniapp.md)(进度模型 / H5 手机身份链 / OCR 六分流提交结果)。**改 auth / RBAC 契约须同步 `srvf-admin-web` 前端对接指南**(跨仓只写 docs)。
  - **对接 gotcha**:① app `/me` 志愿者 `gradeCode` 现返 `'volunteer'`(此前 `null`,S5 语义变);② 申请人侧 OCR 结果文案恒中性,**绝不暴露** `riskLevel` / forgery;③ 报名详情明文证件号/手机 + 证件照需持 `recruitment-application.read.sensitive`(仅 `read.record` 得脱敏);④ 批量标门槛「签到记录导入」= 前端解析签到表为匹配键数组,后端不碰文件。
- **运维侧**:三条运维通道仍休眠(实名 OCR / 微信 / SMS),沿 v0.29/30;招新失败者脱敏清理仍走手动 SQL + storage blob SOP(**不解锁 cron**)。
- 本版**不**接 `CertificatesService.isQualified` 自动核验证书 gate、**不**做历史 legacy 志愿者 backfill(留将来独立不可逆 D 档)、**不**解锁新 cron。
