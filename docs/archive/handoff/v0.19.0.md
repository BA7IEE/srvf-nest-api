# SRVF API v0.19.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.18.0.md`](v0.18.0.md);v0.18.0 之后的工作是**单条线:找回密码 SMS 验证码重置 T0-T3(#307-#310,含 D 档 enum migration + AGENTS §9 红区行)**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.19.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7,维护者亦可手动)。

---

## 1. TL;DR

- **v0.19.0 = 找回密码(SMS 验证码重置,pre-auth)的 release closeout**:**+2 公开端点**(`POST /api/auth/v1/password-reset/send-code` + `POST /api/auth/v1/password-reset`,endpoint 155→**157**)/ **+1 migration**(`SmsPurpose +PASSWORD_RESET` 单行,migrations 13→**14**)/ **联动撤销第 5 场景** `'self-password-reset'`(AGENTS §9 四→五场景红区行已更新)/ AuditLogEvent +1(`password.reset.by-sms`,union 共 30)/ **第 6 throttler 实例** `password-reset`(IP 3/60s)
- **防枚举为本功能安全核心**:四种无效号码场景 send-code 返回完全相同泛化 200(零留痕);reset 一切失败统一 24010;**零新增 BizCode / 零新增权限码**;全部 e2e 一致性断言锁定
- 冻结评审稿:[`docs/archive/reviews/password-reset-by-sms-review.md`](../reviews/password-reset-by-sms-review.md)(D 档拍板记录;实施零偏离,零人话简报)
- bump PR 已合入:[#311](https://github.com/BA7IEE/srvf-nest-api/pull/311) / squash commit `2c23c0b`
- **`v0.19.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.18.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)
- **行为锁全程守住**:auth 既有 e2e(login / refresh / logout / 改密)断言零修改全绿 / **`auth.service.ts` 与 `users.service.ts` 零 diff** / `AGENTS.md:242`(登录仅 username+password)原文不动 / P0-E refresh 冻结无触碰 / 「改密后旧 access 仍可用」反向锁定断言不破

下一会话:**SMS 真实通道仍未开通**——运维侧按 [`docs/ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) 接力(找回密码 DevStub 已全验,**运维侧零新增作业**);P1-7 剩 ② OTP 登录(需先解锁 AGENTS:242)③ 通知短信,挂 [`NEXT_TASKS`](../../ai-harness/NEXT_TASKS.md) 逐项单独立项;图形验证码重启条件成文于评审稿 §9。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.19.0`** | PR #311 已 bump |
| Swagger `setVersion(...)` | **`0.19.0`** | PR #311 已 bump |
| `CHANGELOG.md` v0.19.0 段 | **已存在**(`## v0.19.0 - 2026-06-11`,2 条目)| PR #311 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.18.0`**(指向 `8d9fd01`)| `v0.19.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.18.0`** | `v0.19.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`2c23c0b`** | `chore(release): bump v0.19.0(三文件) (#311)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.18.0 → 0.19.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **+2 endpoint**(找回密码两公开端点),全部新增,**零既有 endpoint / DTO 行为变更**;contract 155→157 路由,snapshot diff +365 行纯新增、零删除、零 L3 字样
- ✅ **+1 migration**(`20260611021208_add_sms_purpose_password_reset`:单行 `ALTER TYPE "SmsPurpose" ADD VALUE`;migrations 13→14;干净库 14/14 重放 + `migrate diff` 零差异 + seed 幂等二跑验证)
- ✅ **联动撤销第 5 场景** `'self-password-reset'`(同事务原子;`AGENTS.md §9` 四处「四场景」措辞 → 五场景,随 #309 逐行 before/after)
- ✅ **AuditLogEvent +1**(`password.reset.by-sms`:actor=本人,extra `refreshTokensRevoked` + phone 掩码 + codeId;union 共 30 项)
- ✅ **+1 throttler 实例**(`password-reset` IP 3/60s,第 6 个;`ThrottlerBizGuard` 同型扩展,既有五实例零行为变更)+ 新 env `PASSWORD_RESET_THROTTLE_*`(留空默认 3/60)
- ✅ **零新增 BizCode / 零新增权限码 / 零依赖变更**(复用 24010/10006/24120/24121/24030/24031/42900)
- ✅ `SmsCodeService` +`assertValid`(只验不消费;`verifyAndConsume` 行为零漂移,既有 unit 断言零改动全绿)+ 新装饰器 `ApiWrappedNullResponse()`

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.19.0 段](../../../CHANGELOG.md) 与各 PR;A 档 docs PR(#307/#310)沿惯例不进 CHANGELOG。

### ① 找回密码 SMS 验证码重置 T0-T3(#307-#310;冻结评审稿 + 三段实施)

- T0 [#307](https://github.com/BA7IEE/srvf-nest-api/pull/307)(A 档):评审稿冻结([`password-reset-by-sms-review.md`](../reviews/password-reset-by-sms-review.md):D-PR-1~4 拍板 + E-1~E-18 工程代决 + 防枚举设计 §4 + 图形码重启条件 §9 + 风险表 + 五张清单)+ NEXT_TASKS P1-7 ① 标立项
- T1 [#308](https://github.com/BA7IEE/srvf-nest-api/pull/308)(D 档):`SmsPurpose` 枚举 +`PASSWORD_RESET` 单行 migration;PR 描述贴 SQL + `srvf-prisma-change` 风险表 + 干净库重放记录;CODEMAP/prisma CLAUDE migration 计数 13→14 同 PR true-up(沿 #300 范式)
- T2 [#309](https://github.com/BA7IEE/srvf-nest-api/pull/309)(C/D 档):两端点(新文件 `auth/password-reset.service.ts`,校验顺序冻结 E-5:解析用户 → 码预检不消费 → 10006 不烧码 → 原子消费 → 事务三件套)+ 防枚举四场景泛化 + `@PasswordResetThrottle()` + AGENTS §9 红区行 + auth CLAUDE.md 校准 + e2e 新组 12 用例 + unit +6 + contract 同步
- T3 [#310](https://github.com/BA7IEE/srvf-nest-api/pull/310)(A 档):CHANGELOG Unreleased 两段 + current-state §1/§2 回填 + NEXT_TASKS P1-7 ① ✅ 归档 + 派生层四→五场景 true-up(security.md / ai-harness README / srvf-auth-security skill / CODEMAP auth 行 1072L)

### ② Release closeout(#311 / 本 PR)

- bump 三文件(#311);landing 阶段探针满足跳过(跨文档事实已由 #310 T3 收口)

---

## 5. API Surface Index

**157 条 `EXPECTED_ROUTES`** 全落 4 canonical 前缀(v0.18.0 的 155 + 找回密码 2:`POST /api/auth/v1/password-reset/send-code`、`POST /api/auth/v1/password-reset`,均 `@Public()`)。鉴权后缀(P2-2 惯例)157/157 全覆盖,检查项 G 锁一致性(`docs:rbacmap:check` 7 PASS)。

---

## 6. Test / Contract Snapshot

> 数据沿 #309 本地 `agent:check:full` + CI 记录(2026-06-11)。

- ✅ unit:**30 specs / 1257 tests**(v0.18.0:30/1251;+6 `SmsCodeService.assertValid` 分支,既有断言零改动)
- ✅ E2E:**76 suites / 1718 tests**(v0.18.0:75/1706;+`auth-password-reset` 12 用例:防枚举四场景一致性 / 全链后效 / 码错 5 次 / 过期 / 重用 / 10006 不烧码 / purpose 隔离 / 跨 purpose 间隔 / IP 限流);**auth 既有组原断言零改动全绿**
- ✅ contract:**289 passed**,157 条路由;snapshot diff 纯新增(2 path + 3 schema)
- ✅ `docs:codemap:check` 0 FAIL / `docs:rbacmap:check` 0 FAIL 0 WARN(7 检查项)
- ✅ migration:**14 个**;干净库 `prisma:deploy` 全量重放 + `migrate diff --exit-code` 零差异 + seed 幂等二跑通过

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md`(tag 后独立回填 PR)
- ❌ **不**开通真实 SMS 通道 / 录入真实凭证(运维侧按 [`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) 接力;找回密码侧**零新增运维作业**)
- ❌ **不**启动 OTP 登录(P1-7 ②,需先解锁 `AGENTS.md:242`)/ 通知短信(P1-7 ③)/ 图形验证码(重启条件见评审稿 §9)/ retention 清理(P2-6)/ Slow-3 / Slow-4
- ❌ **不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.19.0 <handoff-pr-squash-commit>
git push origin v0.19.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.19.0 - 2026-06-11/{flag=1; next} /^## v0.18.0 - 2026-06-11/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.19.0`;target:`v0.19.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零)+ 地图戳若有。

之后候选(沿 process §7.1,需用户立项 / 授权清单):**运维侧 SMS 真实通道接力**(系统侧无动作;开通后找回密码即可真实发送);NEXT_TASKS 余 P1-3(⏸ 等 Slow-3)/ P1-7 ②③(OTP 登录〔需 AGENTS:242 解锁评审〕/ 通知短信,逐项立项)/ P2-4(⏸)/ P2-6(retention,⏸);无阻塞性技术债。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.18.0`(2026-06-11;指向 `8d9fd01`) |
| Target | `v0.19.0`(pending) |
| Landing | 探针满足跳过(跨文档事实由 #310 T3 承接) |
| Bump PR | [#311](https://github.com/BA7IEE/srvf-nest-api/pull/311) / `2c23c0b` |
| Handoff PR | 本 PR(branch `release/v0.19.0-handoff`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-11 |
