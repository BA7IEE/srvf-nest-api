# SRVF API v0.56.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> **阅读顺序(Harness 2.0 起)**:根 `AGENTS.md`(读取协议 §0)→ [`docs/current-state.md`](../../current-state.md) → [`docs/process.md §2/§3`](../../process.md) → 本文件;细则按 `AGENTS.md §6` 索引触碰才读。冲突处理沿 `AGENTS.md §0` 唯一副本,本文件不覆盖任何铁律。
> 本文件接续 [`v0.55.0.md`](v0.55.0.md);逐笔详情见 CHANGELOG `## v0.56.0` 段。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 能力终态 / 索引;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.56.0` tag 将指向本 handoff PR 的 squash merge commit。

---

## 1. TL;DR

- **v0.56.0 = Harness 2.0:AI 协作底座全量重构**(#653–#660 八 PR;2026-07-16 立项四拍板 P1-P4 + 公理 A1-A5,T0 冻结稿 [`harness-2.0-t0-review.md`](../reviews/harness-2.0-t0-review.md);harness v1 全文冻结 [`archive/harness-v1/`](../harness-v1/))。
- **恒读层 −89%**:AGENTS 45,395 → 10,689 · current-state 89,108 → 3,123 · CLAUDE 3,521 → 1,037 字符,三文件 `docs:readtax:check` enforced 硬判;教学细则逐字搬家 `docs/reference/` 九篇(唯一机械改写 = 相对链接前缀);权威冲突表收敛为 `AGENTS.md §0` 全仓唯一副本;current-state 全指针化,§1 计数 9 项由 `docs:counts(:check)` 从真源生成守护。
- **lane 并行协议入法**(process §8):1 总控 + ≤3 执行 lane、写者唯一、goal 五要素(+写集声明)、migration token、串行集成 SOP、跨模型互查([`codex-review-sop.md`](../../ai-harness/codex-review-sop.md));`agent:preflight --lane`(global 逐字不变,E 档强制 global);`changelog.d/` fragment(本 release 收口即首用,`changelog:merge` 首跑真实归并);e2e 测试库按 worktree 派生(**双 worktree 并行实弹**:同 spec 同时各 23/23 绿,`app_test` / `app_test_harness_2_0` / `app_test_harness_drill` 三库并存零干扰)。
- skills 7 → **8**(新增 `srvf-lane-orchestrator` 总控行为清单;goal-author 升五要素;prisma-change 增 migration token 停条;其余引用重定向);治理期文档退场归档 `archive/plans/system-foundation-governance-period.md`。
- **footprint:`src/**` 与 `prisma/**` 零改动** —— endpoint **360** / controller **74** / migration **54** / BizCode **250** / 权限码 **206** / AuditLogEvent **113** / cron **2** / 模块 **36** / 内置角色 **9** 全部恒定;新增 scripts 3(counts / readtax / changelog-merge)+ preflight lane 模式 + test/setup 库派生 + CI docs 守护步骤 + PR 模板;0 新依赖。

## 2. 验证快照

- 八 PR 各自 required checks 全绿后串行合入;main push CI 连绿(PR3 出现一次 `admin-me` lastLoginAt 竞态 flake,rerun 即绿,已在 #655 评论留痕 = 发现②,待 B 档加固断言)。
- 守护四件套(`readtax` / `counts` / `codemap` / `rbacmap`)全绿;unit 102 suites / 2,597、全量 e2e 153 suites / 3,023 于系列中多轮通过;收口前审查:16 文件死链扫描 0 断链、`changelog:merge` 参数守卫加固(发现③)、lane-orchestrator 链接层级修正(发现④)。
- **行为契约零放宽**:D-1~D-9 / P0-E / 判权单轨 / 防枚举 / 红区清单 / 触发即停在 `AGENTS.md §2/§3` 逐项留存,细则原文在 `docs/reference/` 与 `archive/harness-v1/` 可整读回溯。

## 3. 下一步索引(不自动启动)

- 双 lane 正式试点(候选:第四轮全仓 review report-only lane × 一条 NEXT_TASKS feature lane;协议 process §8);
- 发现②:`admin-me.e2e-spec.ts` lastLoginAt 断言 B 档加固;
- `src/**` / `test/**` 注释内 harness-v1 节号引用动到再校准(`AGENTS.md §6` 重定向表可解析);
- 红区 / D 档 PR 建议投放 Codex 评审(SOP:`docs/ai-harness/codex-review-sop.md`)。
