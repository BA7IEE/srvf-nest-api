# SRVF API v0.50.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.49.0.md`](v0.49.0.md);逐笔详情见 CHANGELOG `## v0.50.0` 段 + `docs/current-state.md §2`。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 能力终态 / 索引 / 操作提示;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR #614 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.50.0` tag 将指向本 handoff PR 的 squash merge commit;GitHub Release(Latest)与 current-state 回填随后依序完成。

---

## 1. TL;DR

- **v0.50.0 = 活动模块一致性收口(审计刀 1–刀 4)**。交付链 = 29 问审计报告(2026-07-15,25 属实/4 修正 + B1–B14)→ 七项拍板 D1–D7 → goal 会话单 squash PR #613 `f73ac8e9`(实现 2 commits + 主会话 2 笔 docs true-up)→ bump #614 `93400f98` → 本 handoff(**tag 点**)。
- **⚠️ 行为变更恰好 12 条**(全对照表见 CHANGELOG `## v0.50.0`),最影响联调的四条:①活动 publish 请求体新增**必填** `requiresInsuranceConfirmed:true`;②考勤首提**不再**直写 completed,`POST :id/complete` 是唯一完结通路;③draft 活动的代报名/审批/交考勤全部被拒(参与写路径要求 published,考勤另允许 completed 补录);④考勤记录 checkIn/checkOut 必须落在活动时间窗 ± `ATTENDANCE_WINDOW_TOLERANCE_HOURS`(默认 2h)。
- **闸门收口单源**:新增 `ActivityParticipationPolicy`(纯决策,零 IO):canRegisterSelf / canRegisterByAdmin(不查公开性 = 非公开活动开放管理员定向代报名)/ canApprove(published + 未结束)/ canSubmitAttendance(published|completed)。App 可参加池只返回公开报名活动;报名新增性别闸(按 `MemberProfile.genderCode` 对 any/male/female)。
- **通知闭环**:通知类型码细分 activity-published / activity-changed / registration-result / attendance-result(activity-reminder 留给开始前提醒);公开活动发布广播、改期定向、退出通知 publishedBy、报名/考勤结果换新码;既有 09:00 expiry-reminder job 新增活动开始前 24h stage(`Activity.startReminderSentAt` 保证 at-most-once),**cron 仍恰好 2 个**。
- **读模型**:admin 活动列表/详情 + App 详情新增派生 `phase`(upcoming/ongoing/ended,读侧不入库);dashboard `activities` 新增 `pendingCompletion`(published 且已过 endAt);App 详情新增 `genderRequirementCode` / `requiresInsurance` / `passCount`。
- **footprint(基于 v0.49.0)**:migration **50→51**(唯一 additive nullable `Activity.startReminderSentAt`)/ BizCode **232→238**(+6)/ endpoint **338** / controller **67** / 权限码 **206** / AuditLogEvent **113** / module **35** 全部不变。0 新端点 / 0 新权限码 / 0 数据订正 / 0 新依赖。

---

## 2. 12 条行为变更与边界

- 完整对照表在 CHANGELOG `## v0.50.0`;白名单外零行为变更(主会话元核验逐条对过生产代码,见 PR #613 body「F1–F15 / 审计关闭证据」)。
- **刻意保留的旧语义**:reject / cancelAdmin / cancelMy 对已取消/已完结活动仍不拦(清理残留队列通道);App 活动 detail 口径不变(published 即可见,含已结束与非公开——仅「可参加池」过滤);贡献值/时长计算路径零改动;审计 event 名零改动(`attendance-sheet.submit` 的 extra.activityPushedToCompleted 保留恒 false)。
- **存量数据零订正**:时间窗外/registrationId 三元不一致的历史考勤行、已取消活动的历史 pass 报名全部原样保留;新写严格、读侧不追溯。
- 字典新增:`gender_requirement`(any/male/female,已入系统内置防误删守卫)+ `notification_type` 4 个新类型,均 seed 幂等 upsert。

---

## 3. 验证快照

- **#613(实现)**:CI Lint/Typecheck/E2E **14m21s** 全绿(true-up 后最终一轮);unit **89 suites / 2449 tests**;contract **610 tests / snapshots 2/2**;runner 本地全量 e2e **144 suites / 2922 tests**;考勤时间夹具修复 **6 specs / 60 处** timestamp(产线容差未为夹具放宽)。
- **主会话元核验四项全过**:①行为变更恰好 12 条逐条对生产代码,反向抽查零越界;②prisma diff = 1 migration(2 行 SQL)+ schema 1 行 + seed 2 段;③characterization 翻面仅落白名单对应处;④OpenAPI 快照恰好 2 簇契约变更、权限码 206 不变、BizCode +6 与新守卫一一对应、AGENTS.md 仅 cron waiver 2 行、受保护文档恰好 4 处。
- **migration 重放**:fresh `app_migration_check` 干净库完整重放 51 migrations + seed 两跑关键计数一致(#613 body「Migration」段);未触碰 `app`/生产库。
- **docs 核对**:`docs:rbacmap:check` 0 FAIL/0 WARN(206 permissions / 67 controllers / 338 routes);`docs:codemap:check` 0 FAIL(migration 计数 51 与 activities service LOC 已 true-up;剩 1 WARN 为既有 god-service 体量观察)。
- **#614 bump**:严格 3 文件白名单(package.json / apply-swagger / CHANGELOG 折叠)。

---

## 4. 部署与前端联调

- **数据库**:`prisma migrate deploy`(1 条 pending:`20260715013852_activity_start_reminder_marker`,单列 additive nullable,无回填无不可逆);随后**必须幂等运行一次 `pnpm prisma:seed`**(创建 `gender_requirement` 字典 + 4 个 notification_type 类型;缺 seed 则活动性别要求字段与新通知类型不可用)。
- **环境变量**:新增可选 `ATTENDANCE_WINDOW_TOLERANCE_HOURS`(默认 2,范围 0–168;`.env.example` 已登记);不设即默认。cron 无新增,运维零动作。
- **⚠️ admin-web 必须适配(不适配则发布功能全挂)**:①publish 调用必须带 body `{"requiresInsuranceConfirmed": true}`(缺失/false → 400);②draft 活动的代报名/审批/交考勤按钮会得到新 BizCode 拒绝(活动未发布),应在 UI 侧按状态隐藏;③capacity 缩容小于当前 pass 数被拒;④completed 活动不可再 cancel、终态仅可改 5 个展示字段;⑤活动列表/详情新增 `phase`、dashboard 新增 `activities.pendingCompletion` 可直接消费(「待完结」提醒位)。详见 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md)(#613 已同步)。
- **miniapp 适配(additive 为主)**:App 活动详情新增 `phase` / `genderRequirementCode` / `requiresInsurance` / `passCount`(报名前可展示门槛与余额);报名新增性别闸与未发布拒绝的新错误码;「可参加池」不再出现非公开活动;通知若按 typeCode 分组需纳入 4 个新类型。详见 [`docs/handoff/miniapp.md`](../../handoff/miniapp.md)。
- **通知模板**:新类型码当前仅站内渠道;微信订阅消息模板(`WechatSubscribeTemplate`)如需覆盖新类型码,运维在后台按类型码配置 templateId 后自然生效,不配置不发。
- 不回改本 handoff;发布后事实由 `docs/current-state.md §1/§2/§4` 与 `docs/handoff/openapi.json` 承接。

---

## 5. 明确未做 / 后续接口

- **刀 5(度量派生)未做**:no-show/缺席率/核对视图/活动级合计/个人累计时长/报表——拍板 D5(读侧派生、不复活 absent、不建表)已锁定,等下一个 goal。
- **刀 6(新能力)未做**:活动评价、候补递补、岗位/时段、自助签到(geofence)各自独立 goal,按 D7 排序:签到 → 候补 → 评价 → 岗位/时段。
- 29 问中 #1/#2/#3/#5/#7/#8/#11/#18/#22/#24/#28(记录与度量层)由刀 5/刀 6 承接;B4(改期保险复核)仅做提示型通知,快照语义未变;B9(考勤幂等键)/B12(组织者字段)/B14(报名表单 schema)随后续 goal 顺带评审。
- 未动:判权体系(0 新权限码)、Recruitment/team-join、贡献值计算与封顶、CSV 导出、审计事件族、App DTO 派生禁令等全部红线沿旧。
