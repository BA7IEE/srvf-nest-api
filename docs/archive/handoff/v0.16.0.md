# SRVF API v0.16.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.15.0.md`](v0.15.0.md);v0.15.0 之后的工作核心是 **🎉 Route B API surface 全量迁移(终态 4 canonical 前缀)+ docs 治理 G1/G2 + harness 基建与 characterization 全覆盖 + ai-harness 操作层 + P1-4 god-service 拆分系列(第一刀 + 收口)**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘;不逐 PR 长篇展开。
>
> **状态**:本文件在 **`v0.16.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 入口刷新在本 handoff PR 合入后由维护者按 §8 / §9 步骤手动执行。

---

## 1. TL;DR

- **v0.16.0 = Route B 全量迁移 + 治理/harness/characterization + P1-4 拆分系列的 release closeout**(0 schema migration / 0 新依赖 / 0 权限码变更)
- bump PR 已合入:[#283](https://github.com/BA7IEE/srvf-nest-api/pull/283) / squash commit `ebac748`
- 本 handoff PR 是**最小化版本快照**(单文件 docs-only)
- **`v0.16.0` tag 尚未创建**(latest tag 仍为 `v0.15.0`,指向 `089499d`)
- **`v0.16.0` GitHub Release 尚未创建**(latest 仍为 `v0.15.0`)
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)
- **API 终态已锁**:全仓路由仅落 `admin/v1` / `app/v1` / `auth/v1` / `system/v1` 4 前缀,零 `v2` / 零 legacy,由 contract 终态断言锁定

下一会话**不要**自动启动任何 PR;先读完本文件 §9 + `docs/process.md` + `docs/current-state.md`,再等用户拍板。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.16.0`** | PR #283 已 bump |
| Swagger `setVersion(...)` | **`0.16.0`** | PR #283 已 bump |
| `CHANGELOG.md` v0.16.0 段 | **已存在**(`## v0.16.0 - 2026-06-10`,13 条目)| PR #283 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.15.0`**(指向 `089499d`)| `v0.16.0` 待 handoff PR 合入后手动创建 |
| GitHub Release Latest | **仍为 `v0.15.0`**(2026-05-20T17:07:09Z)| `v0.16.0` Release 待 tag 后手动发布 |
| `docs/current-state.md` §1 | landing(#282)已刷至 `aca46fd` + 标记收口进行中 | tag / Release 后由独立 PR 终态回填 |
| main HEAD | **`ebac748`** | `chore(release): bump v0.16.0(三文件) (#283)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.15.0 → 0.16.0` = **minor**(沿 0.x 阶段全 minor 节奏;无 patch 先例)。

理由与一处诚实声明:

- ✅ **Route B 删除了全部 legacy 路径(142 个 operation)**——在"有外部消费者"场景这属 breaking;**经用户 2026-06-01 确认无生产消费者**并拍板豁免 deprecation 窗口直接 removal,0.x 阶段沿 minor 惯例(详 [`api-surface-migration-plan.md §6`](../../api-surface-migration-plan.md))
- ✅ 新前缀能力为**纯迁移**(行为 / DTO / Guard / RBAC / audit 零漂移,逐 Phase e2e + contract 锁定)
- ✅ **0 schema migration**(Prisma 表 / 字段 / migration 计 12 个,zero drift)
- ✅ **0 新运行时依赖**(`package.json` 仅 version 字段)
- ✅ **0 权限码变更**(76 码 / 绑定零变化,`docs:rbacmap:check` 0 FAIL)
- ✅ AttendancePresenter 抽离(#280)为 C 档内部重构,零行为漂移(characterization + snapshot 双锁)

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.16.0 段](../../../CHANGELOG.md) 与各 PR;post-v0.15.0 共 **118 commits**(#164-#281)+ 收口 3 PR(#282/#283/本 PR)。

### ① docs 治理 + Governance G1/G2 串(#164-#227 区间)

- Governance-1A 审计与文档权威源收口(#165 起)、docs 治理 PR-1~6(#204-#209:含 `ARCHITECTURE.md` 1547→294 行入口化)、`AGENTS.md` 压缩(#210)、G1 PR 串(#216-#223)、G2 worktree cleanup SOP(#224-#226)

### ② harness 基建 + characterization 全覆盖(#228-#256)

- 根 `CODEMAP.md` + 模块 CLAUDE.md 体系(#228-#231)、`agent:preflight`(#229)、5 个 `srvf-*` skill(#232/#235/#240/#244)、CODEMAP drift check(#238/#255)、**6 个 god/large-service service-level characterization spec**(#241/#243/#246/#247/#251/#253)、AGENTS §0 符号导航铁律(#256)

### ③ 🎉 Route B API surface 全量迁移(#257-#271)

- 立项冻结(#257/#258)→ Phase 1a/1b/1c alias 双挂 133 路由(#259-#261)→ Phase 2 OpenAPI deprecated(#262)→ Phase 4a-4e/4d2 删除全部 legacy(#263-#268)→ A 档收尾 true-up(#269-#271)。**终态:4 canonical 前缀,contract 280 passed + 终态断言锁定**

### ④ ai-harness 操作层 + P1 系列 + P1-4 拆分系列(#272-#281)

- ai-harness 9 文档 + 3 模板(#272)、CLAUDE.md 入口接线(#273)、`docs:rbacmap:check`(#274)、testing.md 死链修复(#275)、member-profiles dto 拆分(#276)、participation 终审方案 A 标注(#277)、**participation 6 纯组件 unit spec**(#278/#279)、**P1-4 第一刀 AttendancePresenter**(#280)、**P1-4 调研收口**(#281:3 个 ⚠G 模块判定已达 architecture-boundary 合理形态)

### ⑤ Release closeout(#282/#283/本 PR)

- landing 事实同步 + 地图 true-up(#282)、bump 三文件(#283)、本 handoff index

---

## 5. API Surface Index

15 个 `/api/app/v1/*` endpoint **与 v0.15.0 完全一致**(清单见 [`v0.15.0.md §5`](v0.15.0.md);Route B 删除的是其 legacy 重复路径,App surface 零变化)。Admin / System / Auth surface 全量见 contract `EXPECTED_ROUTES`(148 条,4 前缀)。

---

## 6. Test / Contract Snapshot

> 数据沿 PR #283 CI 记录(2026-06-10 全绿)。

- ✅ PR #283 CI 全绿(lint / typecheck / unit / e2e / contract / docker build / **docker smoke**)
- ✅ unit:**26 specs / 1185 tests**(自 v0.15.0 +6:6 纯组件 spec #278/#279/#280)
- ✅ E2E spec 总数 **72**(自 v0.15.0 的 63:Route B 净 +9 — 新增 app/横切 spec、删除 legacy spec 后)
- ✅ contract:**280 passed**,148 条 `EXPECTED_ROUTES` 全落 4 canonical 前缀(终态断言)
- ✅ `docs:codemap:check` / `docs:rbacmap:check` 双 0 FAIL

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 `v0.16.0` git tag / GitHub Release(维护者按 §8 手动)
- ❌ **不**刷新 `docs/current-state.md`(tag 后独立回填 PR)
- ❌ **不**启动 Slow-3 / Slow-4(业务方决议前提不变)
- ❌ **不**重开 P1-4 god-service 拆分(已收口;重开需 architecture-boundary §6 新触发)
- ❌ **不**改源码 / schema / 测试 / package / CHANGELOG / README / 六大红区文档

---

## 8. Manual Release Steps After This PR

> 本 handoff PR 合入后,由维护者手动执行以下 3 步。

### 8.1 创建并推送 tag

```bash
# <handoff-pr-squash-commit> = 本 handoff PR 合入 main 后的 squash commit hash
git tag v0.16.0 <handoff-pr-squash-commit>
git push origin v0.16.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.16.0 - 2026-06-10/{flag=1; next} /^## v0.15.0 - 2026-05-20/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.16.0`
- target:`v0.16.0` tag
- **mark as latest**
- notes:粘贴 §8.2 抽取的 CHANGELOG v0.16.0 段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(沿历次范式):

- **范围**:**仅改** `docs/current-state.md`
- **内容**:回填 §1 表(latest tag / latest release / main HEAD / 最新 handoff 指向本文件 / Unreleased 累计清零)
- **不**改其它任何文件

之后的候选方向(均需用户拍板,**不**自动启动):NEXT_TASKS 仅余 P1-3(Slow-4,挂起等业务方 Slow-3 决议)与 P2 低优项;无任何阻塞性技术债。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.15.0`(2026-05-20T17:07:09Z;指向 `089499d`) |
| Target | `v0.16.0`(pending) |
| Landing PR | [#282](https://github.com/BA7IEE/srvf-nest-api/pull/282) / `2fd738d` |
| Bump PR | [#283](https://github.com/BA7IEE/srvf-nest-api/pull/283) / `ebac748` |
| Handoff PR | 本 PR(branch `claude/friendly-visvesvaraya-ecb415`) |
| Tag | **pending**(本 handoff PR 合入后维护者执行 §8.1) |
| GitHub Release | **pending**(tag 创建后维护者执行 §8.3) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-10 |
