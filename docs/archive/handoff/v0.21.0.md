# SRVF API v0.21.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.20.0.md`](v0.20.0.md);v0.20.0 之后的工作是**单条线:B 队列一次收清 F1-F6(#322-#329,评审稿冻结 + storage 迁移 + retention SOP + OTP 登录 + 生日祝福 + landing)**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.21.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7,维护者亦可手动)。

---

## 1. TL;DR

- **v0.21.0 = B 队列一次收清的 release closeout**(维护者 2026-06-11 六项拍板,冻结评审稿 [`queue-b-otp-birthday-infra-review.md`](../reviews/queue-b-otp-birthday-infra-review.md)):
  - **OTP(验证码)登录**:`auth/v1` 两公开端点(`login-sms{,/send-code}`),密码登录的**并行方式**——**AGENTS:242 登录契约红区行解锁改写**(密码登录契约零变化);防枚举沿找回密码范式(泛化 200 零留痕 + 一切失败统一 24010,零新增 BizCode);会话签发经 `AuthService.createSession` 与密码登录**同构**(单一代码路径);第 7 throttler `login-sms` 5/60;audit `auth.login.sms`(union 31)
  - **生日祝福短信**:`@nestjs/schedule` 锁 6.1.3——**no-cron 铁律升级路径正式触发(解锁范围仅生日批)**;新 `src/modules/notifications/`(G-7 首个落地点,本仓唯一 `@Cron` 09:00 Asia/Shanghai);选取六条件仅 `User.phone`;幂等防重发(重启不重发);失败 FAILED 不重试不阻断;不进 audit;单实例部署前提成文;docker-smoke 启动锚行
  - **storage 迁移**:`src/common/storage/` → `src/modules/storage/` 纯搬迁零行为(snapshot 逐字节零 diff;P2-4 闭环)
  - **retention SOP**:[`ops/sms-data-retention-sop.md`](../../ops/sms-data-retention-sop.md)(验证码 90 天 / 流水 1 年;app_test 实测冻结;**不解锁 cron 清理**;P2-6 闭环)
- **+2 migration**(`SmsPurpose +LOGIN` 单行 / `SmsSettings +templateIdBirthday` 单列;migrations 14→**16**)/ **+1 依赖**(`@nestjs/schedule` 6.1.3)/ contract 157→**159**(零删除零 L3)/ **零新增 BizCode / 零新增权限码**
- **NEXT_TASKS P1-7 三项消费者整项闭环**(找回密码 / OTP / 生日祝福);P1 现无 open 项
- bump PR 已合入:[330](https://github.com/BA7IEE/srvf-nest-api/pull/330) / squash commit `723ab40`
- **`v0.21.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.20.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)

下一会话:**运维侧 SMS 真实通道接力是唯一人工作业**——按 [`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) **两模板一批送审**(验证码 + 生日祝福零变量模板);系统侧三消费者已全部就绪,通道开通即全部真实生效。无阻塞性技术债。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.21.0`** | PR #330 已 bump |
| Swagger `setVersion(...)` | **`0.21.0`** | PR #330 已 bump |
| `CHANGELOG.md` v0.21.0 段 | **已存在**(`## v0.21.0 - 2026-06-11`,5 条目)| PR #330 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.20.0`**(指向 `9116a67`)| `v0.21.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.20.0`** | `v0.21.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`723ab40`** | `chore(release): bump v0.21.0(三文件) (#330)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.20.0 → 0.21.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **+2 endpoint**(OTP 登录两公开端点;contract 157→**159**,snapshot 纯新增 + settings ±`templateIdBirthday` 共 +350 行,零删除、零 L3 字样)
- ✅ **+2 migration**(`20260611035400_add_sms_purpose_login` 单行 enum / `20260611060204_add_sms_settings_template_id_birthday` 单列;均干净库全量重放 + `migrate diff` 零差异 + seed 幂等二跑;migrations 14→**16**)
- ✅ **+1 依赖** `@nestjs/schedule` **6.1.3**(锁精确版本;no-cron 升级路径正式触发,解锁范围仅生日批)+ `ScheduleModule.forRoot()` 装配 + 新模块 `notifications/`(CODEMAP 19→**21**,含 F2 storage 迁入)
- ✅ **AGENTS §8 登录契约红区行解锁改写**(唯一红区例外,goal 拍板;before/after 见 #326)+ AuditLogEvent +1(`auth.login.sms`,union 共 **31**)+ 第 7 throttler 实例 `login-sms`
- ✅ **storage 模块归位**(纯搬迁零行为,snapshot 零 diff)+ retention 手动 SQL SOP(docs/ops,A 档)
- ✅ **零新增 BizCode / 零新增权限码 / 密码登录与 P0-E 行为冻结零触碰**(既有 e2e 断言零修改全绿)

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.21.0 段](../../../CHANGELOG.md) 与各 PR;A 档 docs PR(#322/#324/#329)沿惯例不进 CHANGELOG。

### ① B 队列 F1-F5(#322-#328;冻结评审稿 + 四项实施)

- F1 [#322](https://github.com/BA7IEE/srvf-nest-api/pull/322)(A 档):评审稿冻结([`queue-b-otp-birthday-infra-review.md`](../reviews/queue-b-otp-birthday-infra-review.md):六项拍板 D-QB-1~6 + 四节实施设计 + 风险表 10 项 + AGENTS:242 before/after 冻结 + no-cron 解锁记录 + 单实例边界)+ NEXT_TASKS 三项标进行中
- F2 [#323](https://github.com/BA7IEE/srvf-nest-api/pull/323)(D 档):storage 纯搬迁(20 文件 git mv + import 链 15 文件;snapshot 零 diff;P2-4 ✅)
- F3 [#324](https://github.com/BA7IEE/srvf-nest-api/pull/324)(A 档):retention 手动 SQL SOP(90d/1y;app_test 实测记录贴 PR;P2-6 ✅)
- F4 [#325](https://github.com/BA7IEE/srvf-nest-api/pull/325)(D 档)`SmsPurpose +LOGIN` → [#326](https://github.com/BA7IEE/srvf-nest-api/pull/326)(C/D 档)OTP 登录实施(两端点 + createSession 抽取 + AGENTS:242 改写 + e2e 8 例;contract 159)
- F5 [#327](https://github.com/BA7IEE/srvf-nest-api/pull/327)(D 档)`@nestjs/schedule` 6.1.3 + `templateIdBirthday` → [#328](https://github.com/BA7IEE/srvf-nest-api/pull/328)(C/D 档)生日祝福实施(ScheduleModule + notifications 模块 + 通道层扩展 + docker-smoke 锚行 + unit 7 例 + e2e 4 例)

### ② Release closeout(#329 / #330 / 本 PR)

- F6 landing [329](https://github.com/BA7IEE/srvf-nest-api/pull/329)(A 档):current-state §2/§3 + NEXT_TASKS P1-7 整项闭环 + RBAC_MAP 戳 + 送审清单两模板 + G-7 注记 + security/development/deployment true-up
- bump 三文件(#330)

---

## 5. API Surface Index

**159 条 `EXPECTED_ROUTES`** 全落 4 canonical 前缀(v0.20.0 的 157 + OTP 登录 2:`POST /api/auth/v1/login-sms/send-code`、`POST /api/auth/v1/login-sms`,均 `@Public()`)。鉴权后缀(P2-2 惯例)159/159 全覆盖,检查项 G 锁一致性(`docs:rbacmap:check` 0 FAIL / 0 WARN)。

---

## 6. Test / Contract Snapshot

> 数据沿 #326/#328 本机 `agent:check:full` + CI 记录(2026-06-11)。

- ✅ E2E:**86 suites / 1787 tests**(v0.20.0:84/1775;+`auth-login-sms` 8 例 + `notifications-birthday` 4 例〔直调 runOnce 范式〕;**auth 既有组与全仓断言零修改全绿**)
- ✅ unit:**31 specs / 1264 tests**(v0.20.0:30/1257;+`birthday-greeting.service.spec` 7 例)
- ✅ contract:**291 passed**,159 条路由;snapshot diff 纯新增(OTP 两路由 + settings ±templateIdBirthday)
- ✅ `docs:codemap:check` 0 FAIL(21/21 模块)/ `docs:rbacmap:check` 0 FAIL 0 WARN(7 检查项)
- ✅ migration:**16 个**;两个新 migration 均干净库全量重放 + `migrate diff --exit-code` 零差异 + seed 幂等二跑
- ✅ docker-smoke:新增「birthday cron registered」锚行步骤,#328 CI 实跑通过(ScheduleModule 生产镜像装配实证)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md` §1(tag 后独立回填 PR)
- ❌ **不**开通真实 SMS 通道 / 录入真实凭证(运维侧按 [`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) **两模板一批**接力;系统侧零等待)
- ❌ **不**启动:通知退订 / 群发 / 活动通知 / 农历生日 / 模板变量 / cron 数据清理 / 二要素登录 / 多实例分布式锁(评审稿 §9 本期不做全集)
- ❌ **不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.21.0 <handoff-pr-squash-commit>
git push origin v0.21.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.21.0 - 2026-06-11/{flag=1; next} /^## v0.20.0 - 2026-06-11/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.21.0`;target:`v0.21.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零)。

之后:**运维侧 SMS 真实通道接力为唯一人工作业**(两模板一批送审;系统侧零代码等待);NEXT_TASKS 现仅余 P2-3 / P2-5(均 ❌ 不做);无阻塞性技术债;新业务按需求调研 → per-feature goal 节奏立项。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.20.0`(2026-06-11;指向 `9116a67`) |
| Target | `v0.21.0`(pending) |
| Landing | [329](https://github.com/BA7IEE/srvf-nest-api/pull/329)(F6 跨文档终 true-up) |
| Bump PR | [330](https://github.com/BA7IEE/srvf-nest-api/pull/330) / `723ab40` |
| Handoff PR | 本 PR(branch `release/v0.21.0-handoff`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-11 |
