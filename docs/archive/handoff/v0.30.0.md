# SRVF API v0.30.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.29.0.md`](v0.29.0.md);v0.29.0 之后是**单一 goal 收口的 minor release**(队员/审批跨轴只读查询补全,#432)。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态(post-release 补档)**:本文件在 **`v0.30.0` tag / GitHub Release / §1 回填之后**补写(与 current-state §1 回填同 PR);**handoff 是历史快照,tag / Release / 代码全不受其影响**(沿 [`process.md §5.2`](../../process.md))。

---

## 1. TL;DR

- **v0.30.0 = 队员/审批「跨轴只读查询」补全**(单一 goal;支撑前端从资源后台转任务驱动后台)。后端报名/考勤本按所有权轴(活动/队员)**嵌套**查询,沿轴下钻已全有;本 release 只补**跨轴横扫**只读缺口,全落 `/api/admin/v1/*`(Route B §0),**既有嵌套路径端点零行为变更**。
- **5 个 admin 只读端点**(#432):
  - **Tier2 审批工作台(跨活动横扫)**:`GET /api/admin/v1/registrations` + `GET /api/admin/v1/attendance-sheets`(脱离 `:activityId` 路径段;分页 + 可选 `statusCode`;item 自带 `activityId`/`activityTitle`;后者为既有 `AttendanceSheetsResourceController` 加根 `@Get` 不新增 class)。
  - **Tier3 队员 360(沿队员轴下钻)**:`GET /api/admin/v1/members/:memberId/{registrations, attendance-records, contribution-summary}`。`attendance-records` **仅 approved sheet 内 records**;`contribution-summary` 返**生涯累计 capped 总分**;`MEMBER_NOT_FOUND` 守卫镜像 `admin-member-insurances`。
- **贡献值实时算不落库**:抽出 `team-join-progress.ts` 封顶核 `computeCappedContribution(client, memberId, cutoff: Date|null)`(`computeContribution` 委托之,team-join 各调用方签名/行为零变化);`cutoff=null`=生涯累计,approved sheet + 按 `checkInAt` 北京日封顶 `GLOBAL_DAILY_CONTRIBUTION_CAP=1.5` 再加总(**禁裸 SUM**——绕过封顶会算多)。
- **零侵入 footprint**:复用现成 read 码 **零新权限码**(155 不变)· **零 BizCode / 零 migration / 零 schema 列 / 零 enum / 零 audit / 零新模块 / 零新依赖**;controller 49→**52**;EXPECTED_ROUTES 229→**234**;OpenAPI snapshot **仅新增**(additions-only);新增出参 DTO 为独立 admin-surface class(不 extends/Pick/Omit,沿 §2.1/§0)。未拆 god-service(只加只读方法)。
- **反漂铁律生效**:同 PR 更新交接层 `docs/handoff/admin-web.md`(GAP-001/002 缺口 → 已发、§2.2/§2.3 ⛔→✅)+ 刷 `docs/handoff/openapi.json`。
- **收口链**:base v0.29.0(`4dcd9c48` / tag)→ 交接层 #431(A 档 docs,**不进 CHANGELOG**)→ 跨轴查询 #432(`6e26ea34`)→ bump #433(`f91a774a` = **tag 点**)→ §1 回填 + 本 handoff(post-release docs PR)。
- `v0.30.0` tag(annotated,指 `f91a774a`)+ GitHub Release(**Latest**)+ current-state §1 **均已完成**。

---

## 2. 下一会话

- **无阻塞性技术债**。**主会话元核验通过**(controller 层 5 端点声明 + 三零足迹〔migration/seed 码/schema diff 空〕+ `computeCappedContribution` 委托链 git 核证;unit 50/1655 + contract 389 + e2e +23 由 CI 全绿 + runner 亲核背书)。
- **跨轴只读端点现已可用**:前端可据 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md) §2.2/§2.3 接 **审批工作台**(跨活动待办)+ **队员 360**(报名/考勤履历 + 贡献值);活动作战室(Tier1)前端已落地(`cockpit.vue`)。
- **对接 gotcha**:跨轴列表过滤参数名 = **`statusCode`**(非草拟期 `status`);`contribution-summary` 已北京日封顶 1.5 返 capped,**前端直接展示别再加**;队员考勤记录仅 approved。
- **新增前后端交接层**(v0.30.0 周期内 #431 建):canonical = [`docs/handoff/`](../../handoff/)(能力图 + 缺口台账 + 反漂铁律);改 API surface 须**同 PR** 更新交接层 + `pnpm docs:handoff:openapi`(沿 `AGENTS.md` 权威源表)。
- 三条运维通道仍休眠(实名 OCR / 微信 / SMS),沿 v0.29.0。
