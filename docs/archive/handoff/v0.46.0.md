# SRVF API v0.46.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.45.0.md`](v0.45.0.md);逐笔详情见 CHANGELOG `## v0.46.0` 段 + `docs/current-state.md §2`。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / finding 终态 / 索引 / 操作提示;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR #588 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.46.0` tag 将指向本 handoff PR 的 squash merge commit;GitHub Release(Latest)与 current-state 回填随后依序完成。

---

## 1. TL;DR

- **v0.46.0 = 系统性安全收口第二批**。承接 v0.45.0(findings 1–7,四类底层规则单一入口),本版收口 findings **9/10/11 + 15 + 8c/13 + finding-15 的 members 轴残留**;#14 / #12 维持接受 / 待 waiver。
- **实现链**:文件验证 #585 `1513336a` → 控制面审计 #586 `edde3d42` → pepper/单例/members #587 `d93bfc12` → bump #588 `cbb0903f` → 本 handoff(**tag 点**)。三刀均主会话 commit-diff 元核验 PASS。
- **#585 文件验证(9/10/11)**:单一 `AttachmentContentValidator`(`validateFromObject` headObject+真实 size+blocklist+魔数 / `validateFromBuffer`)覆盖 6 文件入口(confirm-upload / legacy create / 招新三类图 / realname OCR);`expireAt` 在 `resolveAccessUrl` 单点生效 + public 列表过滤。复用既有签名表/blocklist,不新造第二份。
- **#586 控制面审计(15)**:users `updateRole`/`updateStatus`/`softDelete` + storage/sms/wechat/realname settings `updateSettings`/`resetCredentials` 共 11 处**在写行同事务内** `auditLogs.log`;凭证/密码/secret 明文与密文**永不入 audit**(e2e 双断言)。
- **#587 pepper/单例/members(8c/13 + 残留)**:短信码 `codeHash` 从裸 `sha256(code)` 升级为 `HMAC-SHA256(scrypt(SMS_ENCRYPTION_KEY, 独立 salt), phone:purpose:code)`,pepper 永不落库/日志/audit;**第 49 migration** 四 settings 表 constant `((true))` partial-unique 单例(dedup 保 `updatedAt DESC` 最新行 + RAISE NOTICE 记删除数 + 四表 LOCK/dedup/建索引同一事务),服务改 upsert、退役"取最早+WARN";members 轴 `updateAccountStatus` 新增同事务 `member.account.status-change` audit。
- **⚠️ 行为变更**:① 旧附件 `create` 现校验对象内容(不符返 `13016`);② 附件 `expireAt <= now` 不再签发/返回 `accessUrl`(public 列表过滤)。(#586/#587 多为内部/additive;短信码升级后**部署时未验旧码失效、需重发**。)
- **footprint(基于 v0.45.0,亲核)**:端点 **336** / 权限码 **205** / biz-admin **81** / org-admin **60** / ops-admin **96** / controller **66** / 模块 **35** / 角色 **7** / BizCode **232** 全不变;**migration 48→49**(`settings_singleton_constraints`);**AuditLogEvent 99→111**(+12:#586 +11、#587 +1);0 新端点 / 0 新权限码 / 0 新依赖。
- **验证快照**:三刀各 CI(Lint/Typecheck/E2E + Docker image build + smoke)全绿 + 主会话逐刀元核验 PASS(每刀 4 核验点);bump #588 CI 全绿。

---

## 2. findings 收口终态(承接 v0.45.0 handoff 的 1–7 已修)

| Finding | 终态 | 落点 |
|---|---|---|
| #9 旧附件 create 绕内容校验 | 已修 | #585 单一 `AttachmentContentValidator`(legacy create 补 headObject+魔数) |
| #10 招新图只信客户端 MIME | 已修 | #585 招新三类图 + realname OCR 走 `validateFromBuffer` |
| #11 附件 expireAt 不参与下载 | 已修 | #585 `resolveAccessUrl` 单点 + public 列表过滤 |
| #15 控制面缺持久审计 | 已修 | #586 11 处 in-tx audit(凭证不入 audit) |
| #8c 短信弱哈希(无 pepper) | 已修 | #587 `HMAC-SHA256(scrypt-pepper, phone:purpose:code)` |
| #13 settings 单例仅代码层 | 已修 | #587 第 49 migration 四表 partial-unique(dedup 保最新 + 全事务) |
| #15 members 轴残留 | 已修 | #587 `member.account.status-change` in-tx audit |
| #14 附件列表全量后内存分页 | 接受 | 权限内存过滤→DB 分页是有正确性风险重构,现规模有界(NEXT_TASKS) |
| #12 通知 commit 后丢失窗口 | 待 waiver | 撞无 queue/cron 红线,未获 waiver(NEXT_TASKS) |

---

## 3. 下一会话开工提示

- 开工门禁沿 [`docs/current-state.md §5`](../../current-state.md) + [`process.md §2`](../../process.md);当前事实(版本 / HEAD / open PR)读 current-state §1,不读本文件。
- 前端 / 运营:CHANGELOG `v0.46.0` 顶部 2 ⚠️(旧附件 create 内容校验 / expireAt 下载生效);**部署时**短信码升级后未验旧码失效,重发即可;确认 `SMS_ENCRYPTION_KEY` 已配(缺失则短信码哈希 fail-fast)。
- **可复用原语**(后续同类收口必经、勿再散补丁):`AttachmentContentValidator`(文件内容)、`RoleDelegationPolicy`+`isControlPlanePermissionCode`(委派)、`role-binding-validity`+`LastAdminProtectionPolicy`(任期/末位)、`claimAtStatus`(状态迁移 CAS)、控制面 in-tx audit 范式。
- 剩 **#14**(接受;规模/延迟触顶再单独设计 DB 分页下推)、**#12**(outbox;需先 waive「无 queue/cron」红线)。
- 不回改本 handoff;发布后事实由 current-state §1/§2/§4 承接。
- **不输出任何 secret** — 不打印 `.env` / bucket / APPID / SecretId / SecretKey / pepper / signed URL / JWT 内容,即使在调研 / 报告中。
