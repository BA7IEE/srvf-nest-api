# SRVF API v0.17.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.16.0.md`](v0.16.0.md);v0.16.0 之后的工作核心是 **AI Harness 手动点自动化治理(tag/Release AI 执行 + 授权清单内连续推进 + 人话简报)+ P2-2 Swagger 鉴权后缀文本化(148 endpoint)+ Route B 文档残留清理 + 收口台账 true-up**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.17.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7 新规,维护者亦可手动)。

---

## 1. TL;DR

- **v0.17.0 = harness 治理 + P2-2 Swagger 鉴权后缀 + Route B 文档残留清理的 release closeout**(0 schema migration / 0 新依赖 / 0 权限码变更 / 0 endpoint 变更;唯一 API 可见变化 = 148 个 `@ApiOperation` summary 鉴权后缀文案)
- bump PR 已合入:[#291](https://github.com/BA7IEE/srvf-nest-api/pull/291) / squash commit `bdc27e0`
- 本 handoff PR 是**最小化版本快照**(单文件 docs-only)
- **`v0.17.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.16.0`);本 handoff 合入后由 AI 执行 §8(本周期起沿 process §5.1 阶段 6/7 新规)
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)
- **流程新规已生效**(#286):release tag / GitHub Release 由 AI 以 gh 执行(维护者亦可手动);在用户已立项 / 已授权任务清单内可连续推进下一 PR,清单外仍等拍板;C 档范围已含于 goal → 免二次确认;人工确认请求需带「人话简报」三行

下一会话:沿 [`process.md §7`](../../process.md) 新规——**用户已授权清单内**可连续推进;清单外先读 `docs/current-state.md` + 等拍板。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.17.0`** | PR #291 已 bump |
| Swagger `setVersion(...)` | **`0.17.0`** | PR #291 已 bump |
| `CHANGELOG.md` v0.17.0 段 | **已存在**(`## v0.17.0 - 2026-06-10`,1 条目)| PR #291 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.16.0`**(指向 `e63b9c7`)| `v0.17.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.16.0`**(2026-06-10T07:29:23Z)| `v0.17.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`bdc27e0`** | `chore(release): bump v0.17.0(三文件) (#291)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.16.0 → 0.17.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **0 endpoint / DTO / 错误码 / 行为变更**;唯一 API 可见变化 = OpenAPI summary 文案(P2-2 鉴权后缀,148 处;snapshot diff 296 行全部 summary,逐行核验)
- ✅ **0 schema migration**(migration 仍 12 个,zero drift)
- ✅ **0 新运行时依赖**(`package.json` 仅 version 字段)
- ✅ **0 权限码变更**(76 码 / 绑定零变化;`docs:rbacmap:check` 7 检查项 0 FAIL)
- ✅ 流程规则修订(#286)为 docs-only 治理,不影响运行时

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.17.0 段](../../../CHANGELOG.md) 与各 PR;post-v0.16.0 = 回填 #285(v0.16.0 阶段 8 收尾)+ 本期 #286-#290 + 收口 PR。

### ① AI Harness 手动点自动化治理(#286,goal 拍板)

- G1 机械步骤全自动:process §5.1 阶段 6/7「维护者手动」→「AI 执行(gh),维护者亦可手动」;§7「不自动启动下一 PR」→「已授权清单内可连续推进」
- G2:C 档范围已含于用户任务说明 / goal → 免二次确认(AI 自行发起仍须拍板)
- G3:HUMAN_REVIEW_RULES §4 确认模板顶部增「人话简报」三行 + 推荐方案("按推荐"即生效)
- 同步:HUMAN_REVIEW_RULES / PROJECT_RULES / AI_HARNESS_DESIGN / TEST_MATRIX / CODEMAP / release-closeout skill / CHANGELOG 头部;AGENTS.md 零改动(主题盘点零命中)

### ② P2-2 Swagger 鉴权后缀文本化(#287 C 档预拍板 + #288 B 档配套)

- 148/148 endpoint `@ApiOperation` summary 统一后缀:`[rbac: <码>]`(81)/ `[roles: <角色>]`(44)/ `[public]`(6)/ `[auth]`(17);attachments 动态判权标 `attachment.<action>.*` 通配族
- `scripts/check-rbac-map.ts` 追加检查项 G(后缀 ↔ @Roles/@Public/seed 一致性;负向 3 连精确 FAIL),`docs:rbacmap:check` 现 7 检查项

### ③ 收口台账 true-up(#289)+ Route B 文档残留清理(#290)

- 地图四件套刷戳;NEXT_TASKS 终态标记(P2-3 ❌ / P2-4 ⏸ / P2-5 ❌);RBAC_MAP §2.3 计数修正;current-state §4 实测数字
- 4 文件 7 处「旧前缀当当前行为」修正(docker-smoke L110 / deployment ×2 / srvf-api-surface skill ×3 / auth CLAUDE.md);红区文档残留(AGENTS 19 行 / ARCHITECTURE 5 行 / baseline 2 行 / V2红线 1 行)留人话简报待授权

### ④ Release closeout(#291 / 本 PR)

- bump 三文件(#291)、本 handoff index;landing 阶段探针满足跳过(跨文档事实已由 #289/#290 收口)

---

## 5. API Surface Index

148 条 `EXPECTED_ROUTES` 全落 4 canonical 前缀,**与 v0.16.0 完全一致**(零增删);唯一变化 = 各 operation summary 追加鉴权后缀。15 个 `/api/app/v1/*` endpoint 清单见 [`v0.15.0.md §5`](v0.15.0.md)。

---

## 6. Test / Contract Snapshot

> 数据沿 PR #291 本地 + CI 记录(2026-06-10)。

- ✅ unit:**26 specs / 1185 tests**(与 v0.16.0 持平)
- ✅ E2E:**72 suites / 1664 tests**(与 v0.16.0 持平;P2-2 合入时本地全量绿)
- ✅ contract:**280 passed**,148 条路由;snapshot 35,777 行 / ~1,013 KB(P2-2 后实测)
- ✅ `docs:codemap:check` 0 FAIL / `docs:rbacmap:check` 0 FAIL(7 检查项,含新检查项 G)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md`(tag 后独立回填 PR)
- ❌ **不**修红区文档内的 Route B 路径残留(AGENTS.md 19 行等;人话简报待用户授权)
- ❌ **不**启动 Slow-3 / Slow-4 / P2-4 等挂起项;**不**重开 P1-4
- ❌ **不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7(2026-06-10 #286 新规);完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.17.0 <handoff-pr-squash-commit>
git push origin v0.17.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.17.0 - 2026-06-10/{flag=1; next} /^## v0.16.0 - 2026-06-10/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.17.0`;target:`v0.17.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零)+ §2/§4 若变 + 地图四件套快照戳。

之后候选(沿 process §7 新规,需用户立项 / 授权清单):NEXT_TASKS 余 P1-3(⏸ 等 Slow-3)与 P2-4(⏸ 等立项);红区文档 Route B 残留修订(待对人话简报拍板);无阻塞性技术债。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.16.0`(2026-06-10T07:29:23Z;指向 `e63b9c7`) |
| Target | `v0.17.0`(pending) |
| Landing | 探针满足跳过(跨文档事实由 #289/#290 承接) |
| Bump PR | [#291](https://github.com/BA7IEE/srvf-nest-api/pull/291) / `bdc27e0` |
| Handoff PR | 本 PR(branch `claude/exciting-ishizaka-v017-handoff`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-10 |
