# SRVF API v0.55.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.54.0.md`](v0.54.0.md);逐笔详情见 CHANGELOG `## v0.55.0` 段 + `docs/current-state.md §2`。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 能力终态 / 索引 / 操作提示;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR 之后的 handoff PR 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.55.0` tag 将指向本 handoff PR 的 squash merge commit。

---

## 1. TL;DR

- **v0.55.0 = 活动岗位与时段(审计刀 6 · 第四件/收官)**。交付链 = 四拍板(P1 一人一岗 / P2 岗位绑考勤角色 / P3 岗位带时段闸按岗位窗 / P4 有岗位时活动 capacity 只读派生)+ 方案 A 四修正(runner 拍板前触发「合同矛盾即停」,修正命名/端点枚举/读权限/度量范围后冻结 T0)→ goal 六 PR 全绿自合:T0 冻结 #643 → schema #644(migration 54)→ Admin CRUD #645 → 报名接线 #646 → 考勤/打卡接线 #647 → 收口 #648 → 主会话四点元核验(三过一缺口)→ **HTTP 层 e2e 补测 #649**(缺口闭合)→ bump #650 → 本 handoff(**tag 点**)。冻结评审稿:[`docs/archive/reviews/activity-positions-t0-review.md`](../reviews/activity-positions-t0-review.md)。关闭审计项 **#2**。
- **P1 基石:报名 partial unique 逐字未动**(一人一活动一条 active 报名;PR #648 以索引定义 SHA-256 对基线自证)。想换岗 = 取消再报,不开换岗端点。
- **岗位模型**:新表 `activity_positions`(name/attendanceRoleCode/capacity?/startAt?/endAt?/genderRequirementCode?/description/sortOrder;同活动岗位名 live partial unique;时段必须落活动窗内)+ `ActivityRegistration.activityPositionId String?`(可空 = 无岗位活动沿旧)。**全链命名 `activityPositionId` 全称**,与组织职务 `organization_positions` 零耦合零混用。
- **⚠️ 行为变更恰好 7 条,全部限定「有岗位」场景**(对照表见 CHANGELOG):①名额真相源转岗位(活动 capacity 不再判闸,读侧派生为各岗之和);②候补域收窄为岗位级(同岗 FIFO 递补,跨岗不互递);③打卡窗按岗位时段±容差;④考勤记录窗按岗位时段±容差;⑤考勤草稿 roleCode 由 'member' 改为岗位绑定角色(→ 贡献值规则零改动直接命中);⑥岗位性别闸叠加在活动性别闸之上;⑦有岗位活动的 capacity update 不再触发递补。**无岗位活动零行为变更**(逐字沿旧,回归 e2e 自证)。
- **有岗位活动的新语义**:报名必须选岗(缺 `activityPositionId` → 21035);岗位满员落 waitlisted(排位按岗位算);App 岗位列表给每岗 `remainingCapacity` 与 `canRegister`。
- **+6 端点(354→360)**:Admin `POST/GET/GET:id/PATCH/DELETE admin/v1/activities/:activityId/positions[...]`(写复用 `activity.update.record` + activity ref;读 login-only 沿活动读口径)+ App `GET app/v1/activities/:activityId/positions`。报名 body 与 Admin 报名列表的岗位字段均为 additive、不新增端点。
- **footprint(基于 v0.54.0)**:endpoint **354→360** / migration **53→54** / BizCode **244→250**(岗位六码:NOT_FOUND/重名/越窗/名额非法/禁删/必选岗)。**模块 36 恒(岗位归 activities 模块)** / 权限码 **206 恒** / AuditLogEvent **113 恒** / cron **2 恒** / 内置角色 **9 恒** / 0 新字典 / 0 新依赖。

---

## 2. 验证快照

- 六 PR 各自三检查全绿后串行合入;主会话元核验:①报名 partial unique 零触碰 + Activity 本体仅 +1 反向关系 + migration 纯 additive;②足迹全中 + 命名纪律 PASS(新代码零裸 `positionId`);③白名单外零行为变更(无岗位路径的 `activityPositionId` 恒 null 过滤选中同一行集、防重方法体逐字未改);④并发三件套 e2e(同岗并发 approve 不超额 / 同岗并发 cancel 不双递补 / 岗位名额并发调大只按锁后 delta)+ **变异验证**(把基线读挪到锁前 → "pending expected 2, received 4" 被抓住;v0.53 #631 教训的显式继承)。
- **元核验唯一缺口已闭合**:6 个新端点此前无 HTTP 层覆盖(e2e 经 prisma 直插/service 直调)——补测 #649 新增 `activity-positions-http.e2e-spec.ts`(6 it:401/30100/login-only 读/CRUD 全链/越窗/重名/禁删/删后 404/App 余量与 draft 防枚举/报名必选岗 21035 全走真实请求),本地 6/6 + CI 全绿。
- 岗位名额基线遵循「read-modify-write 基线必须取锁后重读」范式(`activities.service.ts` FOR UPDATE 后 `locked` 重读,含岗位集合)。

---

## 3. 部署与前端联调

- **数据库**:`prisma migrate deploy` 有 1 条 pending(`20260716045857_add_activity_positions`:新表 + 报名加可空列 + FK,纯 additive 无回填);seed 零涉(常规幂等跑一次无害)。
- **环境变量 / cron**:零新增,运维零动作。
- **⚠️ miniapp 必须适配(有岗位活动的报名流变了)**:①报名前先 `GET app/v1/activities/:id/positions` 渲染岗位选择(名称/时段/余量/canRegister);②有岗位活动报名必须带 `activityPositionId`,缺则 **21035**(无岗位活动照旧不带);③岗位满员报名会成功落 waitlisted(沿候补语义,提示排位);④打卡窗按本人岗位时段±容差(岗位窗外打卡被拒,提示语按岗位时间给)。详见 [`docs/handoff/miniapp.md`](../../handoff/miniapp.md)。
- **admin-web**:活动编辑页加岗位管理(CRUD;时段须落活动窗、同活动名不重、有活跃报名不可删);报名列表新增岗位列(`activityPosition{id,name}`);考勤草稿的 roleCode 已按岗位自动带出(审批人可改);有岗位活动的活动级 capacity 输入应置灰/提示「由岗位派生」。详见 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md)。
- 不回改本 handoff;发布后事实由 `docs/current-state.md §1/§2` 与 `docs/handoff/openapi.json` 承接。

---

## 4. 29 问审计收官台账(2026-07-15 立项 → 2026-07-16 六刀全落地)

六个版本收官:**v0.50.0**(刀 1-4 一致性收口,12 行为变更)→ **v0.51.0**(刀 5 度量与批量审批,+7 端点)→ **v0.52.0**(刀 6·GPS 签到,证据层)→ **v0.53.0**(刀 6·候补递补,状态机第 5 态)→ **v0.54.0**(刀 6·活动评价,第 36 模块)→ **v0.55.0**(刀 6·岗位/时段,本版收官)。

- 29 问关闭对照:#4/6/9/10/12–17/19–27/29 → v0.50;#8/11/22/24 → v0.51;#3/18 → v0.52;#5 → v0.53;#1 → v0.54;**#2 → v0.55(本版)**。B 系列:B1/B2/B5/B6/B7/B10 → v0.50;B8/B13 → v0.51。
- **剩余未做(挂账,非本审计范围内主件)**:#7 活动类型→时长台账语义(《志愿服务记录证明》导出的前置,可与证明导出一并立项);B9 考勤 submit 幂等键;B12 活动组织者/联系人字段;B14 报名表单 schema(registrationSchema 仍存而不用);岗位维度度量分组(本版明确不做,见 T0「明确未做」);扫码签到二通道;评价多维度。
- 后续推荐入口:①《志愿服务记录证明》导出(聚合口径 v0.51 就绪 + #7 台账语义一并);②第四轮全仓 review(六版连发后的全局体检)。

---

## 5. 明确未做 / 红线沿旧

- 不开换岗端点、不动报名 partial unique、不开 waitlisted→pass 直通、无手动递补端点;锁序恒 Activity→Registration。
- 岗位维度的 reconciliation/participation-summary/overview 分组本版不做(度量口径逐字不变)。
- `organization_positions`(组织职务)零改动;活动岗位全链 `activityPositionId` 命名边界写入 T0。
- 判权体系 0 新码;贡献值规则表与计算零改动(P2 绑定角色使其自然命中)。
