# SRVF API v0.57.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> **阅读顺序(Harness 2.0)**:根 `AGENTS.md`(读取协议 §0)→ [`docs/current-state.md`](../../current-state.md) → [`docs/process.md §2/§3`](../../process.md) → 本文件;细则按 `AGENTS.md §6` 索引触碰才读。冲突处理沿 `AGENTS.md §0`,本文件不覆盖任何铁律。
> 本文件接续 [`v0.56.0.md`](v0.56.0.md);逐笔详情见 CHANGELOG `## v0.57.0` 段。
>
> **状态**:随 handoff PR 合入,先于 tag / GitHub Release / current-state 回填;`v0.57.0` tag 指向本 PR 的 squash commit(沿 `docs/process.md §5.1`)。

---

## 1. TL;DR

- **v0.57.0 = 第四轮全仓 review 修复闭环 + e2e 竞态加固**。链路:report-only 冻结报告 [`full-repo-fourth-review-v0.56.0.md`](../reviews/full-repo-fourth-review-v0.56.0.md)(#665;P0=0 / P1=2 / P2=4,六条全 CONFIRMED)→ **双 lane 并行修复**(process §8 协议第二轮实战):#666(F4 dashboard 三源 scope + F5 评价率分母口径)∥ #667(F1 报名 Activity 聚合锁 / F2 `claimAtStatus` 收口取消×考勤竞态 / F3 改窗校验岗位窗 / F6 计数补 waitlisted)→ 状态回填 #668;另含 #664 admin-me lastLoginAt flake 有界轮询加固(发现②)。
- **行为变更**:①并发场景下"已有考勤记录的报名不可取消"在竞态中也成立(F2);②活动改窗若致任一 live 岗位越窗被拒(F3,复用既有越窗码);③副职只读角色的工作台待办数字改按三源可见范围(GLOBAL/SA 零变化,F4);④评价率恒 ≤100%(F5)。
- **契约面**:参与汇总 `registrationCounts` additive 新增 `waitlisted` 分项(五态分项和 = total,F6);handoff `openapi.json` + `admin-web.md` 已同 PR 刷新。
- **footprint**:`src` 改动集中 activities / activity-registrations / attendances / meta / activity-feedbacks 五模块;**0 schema / 0 migration(54 恒)/ 0 权限码(206 恒)/ 0 新 BizCode(250 恒)/ 0 AuditLogEvent(113 恒)/ 0 新依赖**;F1/F2 新增真并发 e2e(各注明"杀死哪种错误")。

## 2. 验证快照

- 四 PR(#664/#666/#667/#668)+ landing #669 + bump #670 各自 required checks 全绿后串行合入;双 lane 写集互斥核对通过(总控逐文件对写集声明);`openapi.json` 同文件先后落无缺失(waitlisted 在案,version 随 bump)。
- 守护四件套(readtax / counts / codemap / rbacmap)全绿;counts 九项恒定自证 0 schema / 0 权限码变化。

## 3. 下一步索引(不自动启动)

- lane 会话产物清理(4 个 worktree 与本地分支,待维护者一次性授权);
- `src/**` / `test/**` 注释内 harness-v1 节号引用动到再校准(`AGENTS.md §6` 重定向表)。
