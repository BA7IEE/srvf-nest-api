# SRVF API v0.44.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.43.0.md`](v0.43.0.md);逐笔详情见 CHANGELOG `## v0.44.0` 段 + `docs/current-state.md §2`,冻结设计依据见 [`security-concurrency-hardening-review-v0.44.0.md`](../reviews/security-concurrency-hardening-review-v0.44.0.md)。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / finding 终态 / 索引 / 操作提示;不覆盖当前事实源。
>
> **状态**:本文件随 bump PR #575 之后的 handoff PR #576 合入创建,先于 tag / GitHub Release / current-state §1 回填。按 `docs/process.md §5.1`,`v0.44.0` tag 将指向本 handoff PR 的 squash merge commit;GitHub Release(Latest)与 current-state/OpenAPI 回填随后依序完成。

---

## 1. TL;DR

- **v0.44.0 = 安全·并发·性能加固线**。2026-07-13 全仓审计 26 findings 终态 = **19 已修 / 6 接受登记 / 1 不成立**。
- **实现链**:冻结评审 #570 `3aa8e7c9` → P1 #571 `ca2d8722` → P2 #572 `0a42f961` → P3 #573 `81827b47` → landing #574 `f7dad0fe` → bump #575 `75fb3a4f` → 本 handoff #576(**tag 点**)。
- **P1 #571**:RoleBinding 高权分级/最后 ops-admin 保护/update audit;报名与考勤审批条件式原子竞争守卫;RBAC 角色/权限软删失效与 fail-closed 全清;CSV 公式注入转义;logout 未命中不写污染审计。
- **P2 #572**:SVG/HTML/XHTML 精确黑名单 + confirm ranged 魔数校验;两处 CSV async generator 真流式;考勤按 memberId advisory transaction lock 串行化。
- **P3 #573**:certificate 附件 owner 查询从 K 次 `findFirst` 收敛为一次 `findMany` + Map;接受项写入 NEXT_TASKS 与模块台账。
- **⚠️ 行为变更六条**:RoleBinding 新增 30102/30101 拒绝;审批竞争败者返 `*_STATUS_INVALID`;CSV 危险首字符前缀单引号;garbage logout 仍 200 但不写 audit;SVG/HTML/XHTML 恒拒且伪造图片/PDF 字节返 13016;同人重叠考勤并发仅一方成功。
- **footprint(基于 v0.43.0)**:权限码 **205** / biz-admin **81** / org-admin **60** / ops-admin **96** / `EXPECTED_ROUTES` **336** / controller **66** / 模块 **35** / migration **48** / 角色 **7** 全不变;BizCode 230→**231**(仅 13016);AuditLogEvent 98→**99**(仅 `role-binding.update`);MIME exact blocklist 8→**11**;0 新依赖。
- **验证快照**:main E 档 lint/typecheck/build + unit **80 suites/2347 tests** + contract **1/606/2 snapshots** + e2e **138/2792** 全绿;P2 CI 初次仅因 content e2e 夹具写零字节与新校验冲突,夹具 true-up 后重跑全绿;P3 CI 全绿;landing docs-only CI 全绿;bump CI 全绿(完整 CI 14m17s + Docker build 1m5s + Docker Smoke 1m23s)。

---

## 2. 26 findings 终态

| Finding | 终态 | 落点 |
|---|---|---|
| #1 RoleBinding 任意高权绑定 | 已修 | #571:移植角色分级,非 SUPER_ADMIN 绑定 ops-admin/高权角色返 30102 |
| #2 撤销最后 ops-admin | 已修 | #571:remove/update 同事务保护,返 30101 |
| #3 报名 approve/reject 并发覆盖 | 已修 | #571:期望状态条件式 updateMany,败者 STATUS_INVALID |
| #4 考勤一级审批并发覆盖 | 已修 | #571:同上 |
| #5 考勤终审并发覆盖 | 已修 | #571:同上 |
| #6 明细先软删后单据被覆盖 | 已修 | #571:单据守卫先胜出,败者在软删前 abort |
| #7 时间重叠先查后写竞态 | 已修 | #572:submit/edit 按 memberId advisory transaction lock |
| #8 无 DB 时间排他约束 | 接受登记 | #573/NEXT_TASKS:不引首个 `btree_gist` 扩展 |
| #9 garbage logout 污染 audit | 已修 | #571:token 未命中仍 200,不写 `auth.logout` |
| #10 attachment list 全量后分页 | 接受登记 | #573/NEXT_TASKS:现规模理论问题 |
| #11 certificate attachment N+1 | 已修 | #573:owner ids 一次 findMany + Map |
| #12 listByOwner 全量后分页 | 接受登记 | #573/NEXT_TASKS:单 owner 有界 |
| #13 CSV 全量入内存 | 已修 | #572:两路游标批次 async generator |
| #14 StreamableFile 包整 Buffer | 已修 | #572:`Readable.from(generator)`,BOM 首 chunk |
| #15 CSV 公式注入 | 已修 | #571:共享 CSV util 先中和危险首字符再 RFC4180 escape |
| #16 撤权后旧权限继续生效 | **不成立** | #570:常规撤权路径已同步清 cache;残余由 #17/#18/#19 精确覆盖 |
| #17 cache 持有人查询失败静默 | 已修 | #571:失败退化 `invalidateAll()` |
| #18 软删角色/权限不清持有人 cache | 已修 | #571:commit 后 `invalidateAllUsersWithRole()` |
| #19 多实例 cache 不跨节点失效 | 接受登记 | #573/NEXT_TASKS:当前单实例,扩容前另立项 |
| #20 业务成功后通知可能丢 | 接受登记 | #573/NEXT_TASKS:仅站内非权威提醒,不引 queue/cron |
| #21 commit 后崩溃丢通知 | 接受登记 | #573/NEXT_TASKS:同 #20 |
| #22 上传只信声明 MIME | 已修 | #572:confirm 时回读对象前缀 |
| #23 confirm 不验真实类型 | 已修 | #572:jpg/png/webp/gif/pdf 手写签名表 fail-close |
| #24 非图片伪装图片 | 已修 | #572:签名不符返 13016;SVG/HTML/XHTML 恒拒 |
| #25 RoleBinding PATCH 无 audit | 已修 | #571:+`role-binding.update` before/after |
| #26 高权绑定可无审计重激活 | 已修 | #571:update 复用高权/最后 ops-admin 守卫并留 audit |

---

## 3. 下一会话开工提示

- 开工门禁沿 [`docs/current-state.md §5`](../../current-state.md) + [`process.md §2`](../../process.md);当前事实(版本/HEAD/open PR)读 current-state §1,不读本文件。
- 前端/运营先看 CHANGELOG `v0.44.0` 顶部六条行为变化;接口路径与 DTO 字段集均未新增。
- 已知项只在触发条件出现后另立项:#8 托管库确认 + DB 排他约束评审;#10/#12 规模触顶;#19 横向扩容;#20/#21 可靠通知基础设施真实诉求。
- 不回改本 handoff;发布后事实由 current-state §1/§2/§4 + handoff OpenAPI 承接。
- **不输出任何 secret** — 不打印 `.env` / bucket / APPID / SecretId / SecretKey / signed URL / JWT 内容,即使在调研 / 报告中。
