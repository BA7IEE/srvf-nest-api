# SRVF API v0.39.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.38.0.md`](v0.38.0.md);逐笔详情见 CHANGELOG `## v0.39.0` 段 + `docs/current-state.md §2` 能力清单,本文件不重复。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件随 bump PR(#531)之后的 handoff PR 合入创建,**先于** tag / GitHub Release / current-state §1 回填(沿 [`process.md §5.1`](../../process.md) canonical 顺序 bump → handoff → tag → Release → current-state 回填);`v0.39.0` tag 将指向**本 PR 的 squash merge commit**(沿 §5.2 约定),GitHub Release(Latest)与 current-state §1 回填 + openapi.json 刷新随后依序完成。**handoff 是历史快照,tag / Release / 代码全不受其影响**。

---

## 1. TL;DR

- **v0.39.0 = 第三轮全仓 review(v0.38.0)findings 收口** —— review-then-fix(镜像 #484→#485–#491),把冻结报告 [`full-repo-first-principles-adversarial-review-v0.38.0.md`](../reviews/full-repo-first-principles-adversarial-review-v0.38.0.md)(report-only #526)的 findings 分四刀落地后发版。立项 = goal「第三轮 review findings 收口」。报告计数 **P0=0 / P1=1 / P2=1 / P3=5**;**无阻上线 P0**,P1 已上线前修。
- **4 笔 PR(#527–#530;T4 A 档不进 CHANGELOG)**:
  - **T1 队员轴账号护栏收口**(#527,C 档,F&A-1+A-4):`bindAccount` 只认领 `role=USER` 且 `status=ACTIVE` 悬空账号(新码 `MEMBER_ACCOUNT_TARGET_ROLE_NOT_ALLOWED` 15034 / `MEMBER_ACCOUNT_TARGET_NOT_ACTIVE` 15035);`updateAccountStatus`/`reopenAccount` 前置校验 linked user `role===USER`(`MEMBER_ACCOUNT_ROLE_NOT_MANAGEABLE` 15036,堵「先绑再提权」后路);true-up `:817-819` 假前提注释。堵住"把特权账号经队员轴挂到队员、再借旁轴停用/软删,绕过用户轴 `assertNotLastSuperAdmin` + `assertCanManageUser`"的越权面。刻意**不**照搬 user-axis 私有护栏进 members(跨模块复制否决)。+3 BizCode;0 权限码/端点/migration/audit event。
  - **T2 RBAC 授权配置写面接入审计**(#528,C 档,F&A-2):rbac-roles / role-permissions / permissions 三服务 8 写点在 `$transaction` 内**直写** auditLog(新增 [`permissions/config-audit.util.ts`](../../../src/modules/permissions/config-audit.util.ts) `writeConfigAudit`,镜像 `user-roles.service.ts:writeRoleBindingAudit` —— 避 `PermissionsModule↔AuditLogsModule` 模块环);删 `rbac-roles.service.ts:243` 假称有 `rbac.role.delete` 事件的僵尸注释;`docs/security.md` 收敛 **config-audit 权威规则**段(承接 v0.11.0 handoff 隐性 deferral 成文闭环)。+8 AuditLogEvent(`rbac-role.{create,update,delete}` / `role-permission.{grant,revoke}` / `permission.{create,update,delete}`);**对外契约零变更**(`AuditLogEvent` 不入 OpenAPI,contract snapshot 零 diff)。
  - **T3 member-profiles 敏感字段分级 + F-7 注释**(#529,**D 档** seed 变更,F&A-3):新增权限码 `member-profile.read.sensitive`(**绑 biz-admin**,镜像 `recruitment-application.read.sensitive`;`read.record` 语义收窄为脱敏);无该码者在 `findOne`/`create`/`update` 三出口看到掩码后的 `documentNumber`/`mobile`(`maskIdCard`/`maskPhone`),持码见明文。**掩码是值变换非 schema 变更**(DTO 字段名/类型不变)。org-admin 派生排除新码(逐码不变)。顺修 **F-7**:`seed.ts:3165` 等过时计数注释 true-up。
  - **T4 findings 收口台账翻账**(#530,A 档 docs-only,不进 CHANGELOG):`NEXT_TASKS` P1-19 ⏸→✅ 落地(批 1/2/3 引 PR;A-5/A-6 登记「接受/不做+理由」);冻结报告顶部加「✅ 修复落地状态」节;current-state §4 相关行合并为一条 ✅ 收口注记。
- **⚠️ 行为变更 4 条**(前端 / 运维注):① 队员轴 bind 目标收紧(非 USER / 非 ACTIVE → 拒);② 已绑账号提权后队员轴 status/reopen → 拒,走用户管理端点;③ member-profiles 管理详情 `documentNumber`/`mobile` 默认掩码,明文需 `member-profile.read.sensitive`(**srvf-admin-web 需适配**,handoff 已同步);④ RBAC 配置写面新增审计事件(纯增)。
- **footprint(累计,基于 v0.38.0 baseline)**:权限码 197 → **198**(+1:`member-profile.read.sensitive`)· biz-admin 73 → **74**(+1)· ops-admin **96** / org-admin **57** / member 9 **零变化**· `EXPECTED_ROUTES` **326 零变**· controller **66 零变** / 模块 **35 零变**(config-audit.util.ts 是既有 permissions 模块内 helper,非新模块)· migration **40 零变**(0 schema)· 角色 **7 零变**· BizCode **+3**(15034/15035/15036)· AuditLogEvent **+8**。**`src/modules/auth/**` 全程零 diff;判权核心(`rbac.service.ts` / authz 判权文件)零 diff;`prisma/schema.prisma` + `prisma/migrations/**` 零改动**。
- **收口链**:base v0.38.0(`607bd68d` / tag)→ #527(T1 护栏,`87ac4200`)→ #528(T2 config-audit,`11cdc013`)→ #529(T3 掩码分级,`ce018de0`)→ #530(T4 台账,`06a5f928`)→ bump #531(`1b7e954b`)→ 本 handoff PR(= **tag 点**,squash commit 由 `v0.39.0` tag 指向)。
- `v0.39.0` tag + GitHub Release(Latest)+ current-state §1 回填 + openapi.json 刷新到 v0.39.0**将在本 PR 合并后依序完成**(process.md §5.1 stage 6-8),完成后不回改本文件。

---

## 2. 下一会话

- **无阻塞性技术债**。四刀均经 CI 全绿 + 计数亲核(`docs:rbacmap:check` 0 FAIL @ 198 / `docs:codemap:check` 0 FAIL @ migration 40);报告 findings 全数收口(P1+P2+3×P3 ✅,A-5/A-6 接受不做),`NEXT_TASKS` P1-19 已翻 ✅。
- **接受不做(已登记 NEXT_TASKS P1-19,诉求触发再立项)**:**A-5** `bulkGrantAccounts` 逐行串行 bcrypt(已封顶 200 + ops-admin 高信任,轻度性能面非 DoS)· **A-6** authz `explain-batch`/`action-state` 每项重算 `collectGrants`(封顶 ≤200 + 诊断码 + caller 恒定,近低效非 DoS)。
- **前端对接**:member-profiles 敏感字段分级见 [`docs/handoff/admin-web.md`](../../handoff/admin-web.md)(§2.2 队员 360 「档案 profile 敏感字段分级」备注 + §3 权限码);**改 RBAC 契约须同步 `srvf-admin-web` 前端对接指南**(跨仓只写 docs)。本版给对应岗位角色补挂 `member-profile.read.sensitive` 即见明文,字段绑定无需改。
- **上线部署须知**:① **无新 migration**(仍 40,0 schema);② reseed 权限(幂等,新增 `member-profile.read.sensitive` 绑 biz-admin);③ RBAC 配置面(角色 / 权限点 / 角色权限映射)操作现进审计日志,运维排障可查 `audit_logs` 的 `rbac-role.*` / `role-permission.*` / `permission.*` 事件。
- 本版**不**解锁新 cron、**不**改 `src/modules/auth/**` / 判权核心 / schema 任何文件、**不**做 A-5/A-6 性能优化、**不**新增读 PII placeholder 审计(超出 finding 刻意不做)。
