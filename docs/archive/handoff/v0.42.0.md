# SRVF API v0.42.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.41.0.md`](v0.41.0.md);逐笔详情见 CHANGELOG `## v0.42.0` 段 + `docs/current-state.md §2` 能力清单,本文件不重复。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件随 bump PR(#561)之后的 handoff PR 合入创建,**先于** tag / GitHub Release / current-state §1 回填(沿 [`process.md §5.1`](../../process.md) canonical 顺序 bump → handoff → tag → Release → current-state 回填);`v0.42.0` tag 将指向**本 PR 的 squash merge commit**(沿 §5.2 约定),GitHub Release(Latest)与 current-state §1 回填 + openapi.json 刷新随后依序完成。**handoff 是历史快照,tag / Release / 代码全不受其影响**。

---

## 1. TL;DR

- **v0.42.0 = 招新/入队问题核查双收口(十项 + 十三项 + 评审微清理)**。范围 = 十项收口 #554 + 十三项四刀 #555–#558 + 五疵微清理 #559 + landing #560 + bump #561。
- **⚠️ 部署前小程序须完成签名图 + 验码流适配。** 本版有两条破坏性公开 submit 契约收紧:**`signatureImage` 必填 + `phoneVerificationToken` 必填**;旧小程序客户端未适配时提交一律 400。项目仍处于 pre-production、尚未部署,故本次发版本身无现网影响,但部署前必须先完成客户端联调与回归。
- **#554 十项收口一刀(七条行为/契约变化)**:公开 `stage` 隐去 `manual_high`;documentType 六值白名单;档案/紧急联系人敏感字段掩码扩容;招新/入队单 open 轮 partial unique + 专码;入队 gate 完成日禁未来;非大陆补录/单发年龄闸;新增公开公示端点。并完成 promote 搬运/即时清、retention true-up、身份会话顺手清、容量预检对齐、贡献达标提醒、profileExtra 上限。
- **#555–#558 十三项收口(八条行为/契约变化 + 正确性收口)**:进度联系信息按发号/终态门控;非大陆资料齐备可批量发号;admin 对外 `isForeigner` 改名;**signatureImage 必填**;approved 入队资格不随轮关闭失效;**phoneVerificationToken 对 H5/小程序统一必填**;新增证书审核闭环;入队轮新增开放部门与候选上限。正确性侧完成活跃行锚定、rebind 冲突闸、JSON 行锁、同轮 active openid/phone 唯一约束与 P2002 分流。
- **#559 五疵微清理**:退役状态防御分支 characterization;证书门槛反向映射单一真相源;移除未读 `isForeigner` 类型字段;证书 JSON 空对象统一 `Prisma.DbNull`;入队轮开放部门数组补 64 项上限。有效业务流零翻面。
- **footprint(基于 v0.41.0)**:权限码 203 → **205**;biz-admin 79 → **81**;org-admin **60** / ops-admin **96**;`EXPECTED_ROUTES` 334 → **336**;controller **66** / 模块 **35**;migration 44 → **47**;角色 **7**;BizCode +4(28032/28231/28243/28053);AuditLogEvent +1(`recruitment-application.certificate-review`)。`src/modules/auth/**`、`RbacService.can()`、Authz 核心零 diff。
- **迁移快照**:第 45 migration = 招新/入队单 open 轮 unique + profile carryover;第 46 migration = 招新 active openid/phone partial unique;第 47 migration = certificate review 状态 + 入队轮配置列。均已随 feature PR 合入;本 release 收口 PR 不新增 schema / migration。
- **收口链**:#554 `145ee19d` → #555 `fbc2991a` → #556 `6a14e6a5` → #557 `bb91ddf7` → #558 `65c14215` → #559 `d1422d20` → landing #560 `91fe384a` → bump #561 `d368c48d` → 本 handoff PR(= **tag 点**,squash commit 由 `v0.42.0` tag 指向)。
- `v0.42.0` tag + GitHub Release(Latest)+ current-state §1/§2/§4 回填 + openapi.json 刷新到 v0.42.0 **将在本 PR 合并后依序完成**(process.md §5.1 stage 6–8),完成后不回改本文件。

---

## 2. 下一会话开工提示

- 开工门禁沿 [`docs/current-state.md §5`](../../current-state.md) + [`process.md §2`](../../process.md);当前事实(版本/HEAD/open PR)读 current-state §1,不读本文件。
- **部署硬前置**:小程序 submit 必须同时适配签名图片上传与手机验证码→`phoneVerificationToken` 流程;完成前不要部署 v0.42.0。
- 招新/入队仍挂账:**P1-22 专业队 gate 配置化**、**P1-23 DB 历史列 `isForeigner` 改名**;均仅登记,未在本 release 自动启动,见 [`NEXT_TASKS.md`](../../ai-harness/NEXT_TASKS.md)。
- 真实腾讯云 OCR / 微信 / 短信与小程序前端联调仍沿现有 ops SOP 执行;本 release 未改生产凭证、Provider 或基础设施。
