# SRVF API v0.20.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.19.0.md`](v0.19.0.md);v0.19.0 之后的工作是**单条线:Slow-4 权限双轨收口 T0-T4(#314-#318,业务面 RBAC 接入)**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.20.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7,维护者亦可手动)。

---

## 1. TL;DR

- **v0.20.0 = Slow-4 权限双轨收口(业务面 RBAC 接入)的 release closeout**:**+36 条业务面权限码**(权限码全集 81→**117**)+ 新内置角色 **`biz-admin`(绑 35;`member.delete.record` 仅 SA)** + **业务面 7 模块 44 端点摘 `@Roles` 判权下沉 Service 层 `rbac.can()`**——**全仓活跃 `@Roles` 使用点 = 0**(RolesGuard 机制保留 Guard 链兜底)
- **零行为漂移为本期验收核心**:SA 全通 / 持 biz-admin 的 ADMIN 与迁移前一致(既有业务断言零修改全绿)/ 拒权码按既定语义 40300→30100(沿 P0-F #134 先例);7 个 `*-rbac-boundary` e2e spec(52 用例)锁定
- **零路由 / 零字段 / 零 L3 / 零 migration**:contract 仍 157 路由,snapshot diff 仅 summary 后缀(`[roles:]`→`[rbac:]` 42 处 + 2 处 `[auth]`)与 403 响应投影;schema 零触碰(纯 seed)
- seed 幂等不变式新增:「每个非软删 ADMIN 用户持有 biz-admin」(每次 seed 自动补挂 + 强校验)
- 冻结评审稿:[`docs/archive/reviews/slow4-rbac-business-face-review.md`](../reviews/slow4-rbac-business-face-review.md)(D 档拍板记录;48→44 端点亲核 true-up)
- bump PR 已合入:[#319](https://github.com/BA7IEE/srvf-nest-api/pull/319) / squash commit `f0f975e`
- **`v0.20.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.19.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)

下一会话:**B 队列 goal(2026-06-11 已拍板)接续推进**——评审稿冻结(storage 迁移 / retention SOP / OTP 登录〔AGENTS:242 解锁〕/ 生日祝福〔`@nestjs/schedule` 升级路径触发〕)→ F2-F5 实施 → v0.21.0 收口;SMS 真实通道仍由运维侧按 [`docs/ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) 接力。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.20.0`** | PR #319 已 bump |
| Swagger `setVersion(...)` | **`0.20.0`** | PR #319 已 bump |
| `CHANGELOG.md` v0.20.0 段 | **已存在**(`## v0.20.0 - 2026-06-11`,2 条目)| PR #319 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.19.0`**(指向 `2cc1850`)| `v0.20.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.19.0`** | `v0.20.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`f0f975e`** | `chore(release): bump v0.20.0(三文件) (#319)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.19.0 → 0.20.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **+36 权限码 seed + biz-admin 内置角色**(#315):纯 seed 无 schema,migrations 仍 **14**;ops-admin(58)/ member(9)绑定零变化;seed 幂等二跑 + 新 e2e `seed-biz-admin` 5 用例
- ✅ **44 端点判权下沉**(#316 member 族 21 端点 + #317 participation 23 端点):42 端点 service 第一条语句 `rbac.can('<code>')`(SUPER_ADMIN 短路,先判权后查资源),activities 列表/详情 2 端点无码化仅登录;入口仅 JwtAuthGuard
- ✅ **拒权语义统一**:40300→30100(`RBAC_FORBIDDEN`,沿 P0-F #134 先例;既有 spec 权限边界区块 36 处断言同步改码,属既定语义回归而非新行为)
- ✅ **contract 零路由变更**(157 不变):snapshot diff 仅 summary 后缀与 2 个无码化端点 403 响应投影移除(文档不撒谎);零 L3
- ✅ **AGENTS §8 新增「判权单轨现状」红区行**(随 #318,逐行 before/after;goal 授权)
- ✅ 零依赖变更 / 零 BizCode 新增 / 零 throttler 变更

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.20.0 段](../../../CHANGELOG.md) 与各 PR;A 档 docs PR(#314/#318)沿惯例不进 CHANGELOG。

### ① Slow-4 权限双轨收口 T0-T4(#314-#318;冻结评审稿 + 三段实施 + docs 收尾)

- T0 [#314](https://github.com/BA7IEE/srvf-nest-api/pull/314)(A 档):评审稿冻结([`slow4-rbac-business-face-review.md`](../reviews/slow4-rbac-business-face-review.md):44 端点逐行映射表〔48→44 亲核 true-up〕+ biz-admin 36 码绑 35 + 零漂移五类验收 + e2e 适配方案)+ NEXT_TASKS P1-3 标进行中
- T1 [#315](https://github.com/BA7IEE/srvf-nest-api/pull/315)(D 档):36 条业务面权限码 seed + biz-admin 绑 35(`member.delete.record` 仅 SA 沿 D1=A 镜像)+ ADMIN 全员幂等补挂强校验 + seed e2e 5 例 + RBAC_MAP 81→117 true-up
- T2 [#316](https://github.com/BA7IEE/srvf-nest-api/pull/316)(C/D 档):member 族 4 模块 21 端点摘 `@Roles` 下沉 `rbac.can()`(member 5 码 / member-profile 3 / emergency-contact 4 / certificate 6)+ summary `[rbac:]` 后缀 + biz-admin fixture + 4 权限边界 spec 32 例 + 既有 spec 拒权断言 40300→30100
- T3 [#317](https://github.com/BA7IEE/srvf-nest-api/pull/317)(C/D 档):participation 3 模块 23 端点(activity 5 码 / activity-registration 5 / attendance 8 + activities 列表详情无码化 `[auth]`)+ 3 权限边界 spec 20 例 + 13 既有 spec 补挂/改码/注入 rbac mock
- T4 [#318](https://github.com/BA7IEE/srvf-nest-api/pull/318)(A 档):AGENTS §8 判权单轨红区行 + current-state §1-§4 闭环 + NEXT_TASKS P1-3 ✅ 归档 + RBAC_MAP 大 true-up(G 表清零 / R 表 +7 行)+ participation §4 终审注记 + CHANGELOG Unreleased C/D 段

### ② Release closeout(#319 / 本 PR)

- bump 三文件(#319);landing 阶段探针满足跳过(跨文档事实已由 #318 T4 收口)

---

## 5. API Surface Index

**157 条 `EXPECTED_ROUTES`** 全落 4 canonical 前缀(对 v0.19.0 **零增减**)。鉴权后缀(P2-2 惯例)157/157 全覆盖;Slow-4 后 `[roles:]` 计 **0**(42 处改 `[rbac:]`、2 处改 `[auth]`),检查项 G 锁一致性(`docs:rbacmap:check` 0 FAIL / 0 WARN)。

---

## 6. Test / Contract Snapshot

> 数据沿 #317/#318 CI 记录与 current-state §2(2026-06-11;v0.20.0 对 #317 后零代码变更,数字直接沿用)。

- ✅ E2E:**84 suites / 1775 tests**(v0.19.0:76/1718;+7 `*-rbac-boundary` spec 52 用例 + seed-biz-admin 5 用例;既有业务断言零修改,拒权区块 36 处按语义改码)
- ✅ unit:**30 specs / 1257 tests**(与 v0.19.0 持平;13 既有 spec 注入 rbac mock,断言行为零漂移;2026-06-11 本地复跑亲核)
- ✅ contract:**157 条路由**(零增减);snapshot diff 仅 summary 后缀 + 403 响应投影
- ✅ `docs:codemap:check` 0 FAIL / `docs:rbacmap:check` 0 FAIL 0 WARN(117 码 seed↔代码双向对齐)
- ✅ migration:**14 个**(本期零新增);seed 幂等二跑通过(biz-admin 不变式强校验)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md`(tag 后独立回填 PR)
- ❌ **不**启动 B 队列后续阶段(评审稿冻结 / storage 迁移 / retention SOP / OTP 登录 / 生日祝福 → v0.20.0 收口完成后按 goal F1-F6 接续)
- ❌ **不**开通真实 SMS 通道 / 录入真实凭证(运维侧接力不变)
- ❌ **不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.20.0 <handoff-pr-squash-commit>
git push origin v0.20.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.20.0 - 2026-06-11/{flag=1; next} /^## v0.19.0 - 2026-06-11/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.20.0`;target:`v0.20.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零)+ 地图戳若有。

之后按 **B 队列 goal(2026-06-11 拍板,六项决议)** 接续:F1 评审稿冻结(`queue-b-otp-birthday-infra-review.md` 四节一文)→ F2 storage 迁移(P2-4 解挂)→ F3 retention SOP(P2-6 解挂)→ F4 OTP 登录(P1-7 ②,AGENTS:242 解锁)→ F5 生日祝福(P1-7 ③ 首批,`@nestjs/schedule` no-cron 升级路径触发)→ F6 v0.21.0 收口;运维侧 SMS 真实通道接力照旧(届时送审**两模板**:验证码 + 生日祝福)。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.19.0`(2026-06-11;指向 `2cc1850`) |
| Target | `v0.20.0`(pending) |
| Landing | 探针满足跳过(跨文档事实由 #318 T4 承接) |
| Bump PR | [#319](https://github.com/BA7IEE/srvf-nest-api/pull/319) / `f0f975e` |
| Handoff PR | 本 PR(branch `release/v0.20.0-handoff`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-11 |
