# SRVF API v0.41.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.40.0.md`](v0.40.0.md);逐笔详情见 CHANGELOG `## v0.41.0` 段 + `docs/current-state.md §2` 能力清单,本文件不重复。
>
> **本文件定位**:最小化 release handoff index,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件随 bump PR(#551)之后的 handoff PR 合入创建,**先于** tag / GitHub Release / current-state §1 回填(沿 [`process.md §5.1`](../../process.md) canonical 顺序 bump → handoff → tag → Release → current-state 回填);`v0.41.0` tag 将指向**本 PR 的 squash merge commit**(沿 §5.2 约定),GitHub Release(Latest)与 current-state §1 回填 + openapi.json 刷新随后依序完成。**handoff 是历史快照,tag / Release / 代码全不受其影响**。

---

## 1. TL;DR

- **v0.41.0 = 招新可用性收口:手工建档闭环 + 防重成本线 + 申请人自助包**。立项 = goal「招新可用性收口(手工建档闭环 + 防重成本线 + 申请人自助包)」——2026-07-11 主会话对招新环节 9 项问题全仓核实,维护者逐项拍板 R1–R7,goal 文本即拍板记录 + 执行授权;冻结评审稿 [`recruitment-usability-closeout-review.md`](../reviews/recruitment-usability-closeout-review.md)(F0,#542)。
- **8 笔 PR(#542–#549)+ landing #550**:
  - **F0 冻结评审稿**(#542,A 档):9 项核实(file:line)+ 拍板 R1–R7 + 切片表 + 工程决策 E-U-1~8;关键亲核:v0.40.0 T5 仅覆盖 promote 侧,F4 两项未被覆盖照做;skip reasons true-up 为 8 具名 + 1 兜底。
  - **F1 防重前移 + OCR 成本线**(#543,D 档,第 41 migration):submit 付费 OCR 前同轮活跃 openid/phone 去重(28004/28005);付费 OCR 按 IP × 北京自然日封顶(新表 `recruitment_ocr_daily_counters`,env `RECRUITMENT_OCR_DAILY_IP_LIMIT` 默认 30,超限 28060/HTTP 429);recognize 契约不加身份参数;排除集抽 `APP_INACTIVE_STATUS_CODES` 单一真相源。
  - **F2 admin 改资料**(#544,D 档,+1 码):`PATCH admin/v1/recruitment/applications/:id`(`recruitment-application.update.record` 绑 biz-admin);R1 白名单——非身份字段恒可改,身份字段仅 manual_review 或外籍(verified 大陆 → 28045);大陆 birthDate/genderCode 恒由证件号派生;改证件号镜像 submit 校验位/年龄/同轮去重;promoted/已脱敏行 28041;phone/openid 不在白名单(自助换绑通道);+1 AuditLogEvent(`recruitment-application.update`,身份字段仅掩码值)。
  - **F3 单人手动建档**(#545,D 档,+1 码):`POST .../applications/:id/promote-single`(`recruitment-application.promote.single` 绑 biz-admin);批量事务循环体抽取为 `buildOnePromotion` 内核共用(批量 try/catch 位置与整批回滚语义不变 = 行为锁);与批量共享原子号段(e2e 批量 26001 → 单发 26002 实证连续)+ 同一通知派发;放行外籍(缺 realName/birthDate/genderCode → 28047 先 F2 补录);锚点择优 E-U-4(openid 未占用→微信 / 缺·占用且 phone 未占用→手机 / 双缺双占→28046);仅 publicity 可建(promoted 重跑 28041 幂等零重复);audit extra additive viaPath/channel。
  - **F4 查询链修复**(#546,C 档,0 码 0 路由):① 闭轮 send-code/verify-code 放行 =「开放轮 或 手机命中未清除报名」,闭轮陌生手机防枚举泛化 200(沿 login-sms 范式)/verify 统一 24010,28030 从两端点摘除;② 发号后 query/query-by-phone fall-through:live User.openid / User.phone∪profile.mobile 锚 + Member ACTIVE 守卫 → promotedMemberId 定位真实报名行 → stage=volunteer 引导态(零新增 PII 留存/零合成 DTO)。
  - **F5 知情同意 + 签名图**(#547,D 档,第 42 migration):⚠️ submit 契约收紧 `privacyConsentAccepted=true` 必填(旧客户端 400)+ 可选 `privacyConsentVersion` + 可选签名图文件位 `signatureImage`(FileFields 双具名位);promote `privacyConsentSigned` 由硬编码 true 改搬真值(存量 → false)+ signedAt 搬真实时刻;签名图搬 `member_profiles.signatureImageKey` 长期留存(R5,不随脱敏清),报名行清空。
  - **F6 自助撤销**(#548,D 档,第 43 migration):`POST open/v1/recruitment/applications/withdraw`(双通道凭证;0 码);`withdrawn` 终态(非淘汰);终态 28052;partial unique 排除集重建 `NOT IN ('rejected','withdrawn')` → 撤销后同轮重报解锁;stage/stats/导出筛选 true-up(stats additive +`withdrawnCount`);字典 +`withdrawn`;+1 AuditLogEvent。
  - **F7 证书图上传与长期档案**(#549,D 档,第 44 migration):`POST open/v1/recruitment/applications/certificates`(双通道;category ∈ first_aid/bsafe;每类 ≤3 张重传整类覆盖)+ `GET admin/.../:id/certificate-image-urls`(复用 `read.sensitive`,0 码);promote 按类别自动建 **pending Certificate**(占位 issuingOrg/issuedAt 待核验)+ 图 key 搬 `Certificate.imageKeys`,报名行清空;走既有 certificates verify/reject 流;app `my/certificates` DTO v1 不动(P1-20 登记);+1 AuditLogEvent。
- **⚠️ 行为变更 6 条**(见 CHANGELOG `## v0.41.0` 置顶 blockquote):① submit 必填 consent(**旧客户端全量 400,前端须同步升级**)② 同轮同 openid/phone 二次提交改拒 + OCR IP 日封顶 ③ 闭轮发码语义变(28030 摘除)④ 发号后查询返 volunteer 引导态 ⑤ promote consent 搬真值(存量 → false)⑥ 新增 4+1 端点(改资料/单发/撤销/证书上传+取图)。
- **footprint(累计,基于 v0.40.0 baseline)**:权限码 201 → **203**(+`recruitment-application.update.record` / `+.promote.single`,全绑 biz-admin)· biz-admin 77 → **79** · org-admin **60**(`recruitment-` 前缀派生排除零波及)· ops-admin **96** / member 9 零变化 · `EXPECTED_ROUTES` 329 → **334**(+5)· controller **66** / 模块 **35** 零变 · **migration 40 → 44**(F1 计数表 / F5 consent+签名 / F6 索引重建 / F7 证书图列;全 additive,F6 为先删后建索引)· 角色 **7** 零变 · BizCode **+7**(28004/28005/28045/28046/28047/28052/28060)· AuditLogEvent **+3**(`recruitment-application.{update,withdraw,certificate-upload}`)· 字典 `recruitment_stage` 10 → **11**(+withdrawn)· env **+1**(`RECRUITMENT_OCR_DAILY_IP_LIMIT`,`.env.example` 已登记)。**`src/modules/auth/**` 全程零 diff;判权核心(`RbacService.can()` / AuthzService)零 diff;批量 promote 行为逐字不变(内核抽取共用)**。
- **收口链**:base v0.40.0(`29826466` 含 F0 #542)→ #543(F1,`6a6e4fb2`)→ #544(F2,`bd3c388f`)→ #545(F3,`9b11babc`)→ #546(F4,`adcacb1f`)→ #547(F5,`5d7f1186`)→ #548(F6,`c42e7832`)→ #549(F7,`a4e276d3`)→ landing #550 → bump #551 → 本 handoff PR(= **tag 点**,squash commit 由 `v0.41.0` tag 指向)。
- `v0.41.0` tag + GitHub Release(Latest)+ current-state §1 回填 + openapi.json 刷新到 v0.41.0 **将在本 PR 合并后依序完成**(process.md §5.1 stage 6-8),完成后不回改本文件。

---

## 2. 下一会话开工提示

- 开工门禁沿 [`docs/current-state.md §5`](../../current-state.md) + [`process.md §2`](../../process.md);当前事实(版本/HEAD/open PR)读 current-state §1,不读本文件。
- 招新域后续挂账:**P1-14**(报名前 openid 非会员推送路,通知域)、**P1-20**(app 侧证书图暴露给队员本人)、申诉模块(评审稿 E-U-8 禁区,未立项)——均诉求触发再立项,见 [`NEXT_TASKS.md`](../../ai-harness/NEXT_TASKS.md)。
- 运维注意:上线本版前小程序/H5 前端**必须**同步 submit consent 契约(行为变更①);招新活动期限流/OCR 封顶调参见 [`ops/realname-verification-rollout-checklist.md`](../../ops/realname-verification-rollout-checklist.md) §1.1。
