# SRVF API v0.18.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.17.0.md`](v0.17.0.md);v0.17.0 之后的工作是**两条线**:**AI Harness 瘦身 T1-T4(#295-#298,docs-only)+ 手机号验证码基础设施 T0-T4(#299-#303,含 D 档 schema/seed/依赖)**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.18.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7 新规,维护者亦可手动)。

---

## 1. TL;DR

- **v0.18.0 = SMS 基础设施(通道层 + 验证码服务 + 手机号绑定)+ AI Harness 瘦身的 release closeout**:**1 个新 migration**(三表 + User 2 字段)/ **1 个新运行时依赖**(`tencentcloud-sdk-nodejs-sms` 锁 4.1.240)/ **权限码 76→81** / **endpoint 148→155** / BizCode 新开 24xxx 段 / AuditLogEvent +3
- 冻结评审稿:[`docs/archive/reviews/sms-verification-infra-review.md`](../reviews/sms-verification-infra-review.md)(D 档拍板记录;实施零偏离,零人话简报)
- bump PR 已合入:[#304](https://github.com/BA7IEE/srvf-nest-api/pull/304) / squash commit `3d6d3cf`
- **`v0.18.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.17.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)
- **行为锁全程守住**:auth-* e2e 原断言零改动全绿 / JWT payload zero drift / `AGENTS.md:242`(登录仅 username+password)原文不动 / P0-E refresh 冻结无触碰

下一会话:**SMS 真实通道未开通**——运维侧按 [`docs/ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) 接力(签名/模板审核 → 凭证录入 → 真实发送验收);找回密码 / OTP 登录 / 通知三个消费者挂 [`NEXT_TASKS P1-7`](../../ai-harness/NEXT_TASKS.md),逐项单独立项。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.18.0`** | PR #304 已 bump |
| Swagger `setVersion(...)` | **`0.18.0`** | PR #304 已 bump |
| `CHANGELOG.md` v0.18.0 段 | **已存在**(`## v0.18.0 - 2026-06-11`,3 条目)| PR #304 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.17.0`**(指向 `2b57f8c`)| `v0.18.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.17.0`** | `v0.18.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`3d6d3cf`** | `chore(release): bump v0.18.0(三文件) (#304)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.17.0 → 0.18.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **+7 endpoint**(sms-settings ×3 / sms-send-logs / me/phone ×2 / admin 清号),全部新增,**零既有 endpoint/DTO 行为变更**;contract 148→155 路由,两次 snapshot diff 均纯新增零删除
- ✅ **+1 migration**(`20260610152152_add_sms_infra_user_phone`:三表三 enum + User 2 字段;migrations 12→13;干净库重放 + seed 幂等二跑验证)
- ✅ **+1 运行时依赖**(`tencentcloud-sdk-nodejs-sms@4.1.240` 精确锁版本,import 仅限单文件)
- ✅ **权限码 76→81**(+5;ops-admin 绑定 54→58;`sms-setting.reset.credentials` 不绑,镜像 storage D2=A)
- ✅ **BizCode 新开 24xxx 段 6 码**(baseline §1.1 段位表已加行,红区例外经 goal 唯一授权)+ **AuditLogEvent +3**(phone.bind.self / rebind.self / clear.by-admin,手机号一律掩码)
- ✅ **+2 throttler 实例**(`sms-send` 5/60、`sms-verify` 10/60;`ThrottlerBizGuard` 同型扩展,既有三实例零行为变更)
- ✅ **+1 独立 env**:`SMS_ENCRYPTION_KEY`(production/smoke 启动 fail-fast;docker-smoke workflow 同步)

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.18.0 段](../../../CHANGELOG.md) 与各 PR;A/B 档(#295-#298 等)沿惯例不进 CHANGELOG。

### ① AI Harness 瘦身 T1-T4(#295-#298,docs-only,goal 拍板)

- T1 #295:process §5.4 压缩为 8 条硬约束 + goal 协作模式 §7.1 + 人话简报模板迁 §4.1 + §3 组合命令补齐 + §6 派生文档两原则
- T2 #296:操作层 13 文件收口为 `docs/ai-harness/` 3 文件单页制(README / RBAC_MAP / NEXT_TASKS)
- T3 #297:current-state 瘦身至 76 行 + TASKS.md 墓碑化
- T4 #298:CLAUDE.md §1 表对齐新结构 + README 文档地图增 ai-harness 行(红区,维护者拍板)

### ② SMS 基础设施 T0-T4(#299-#303;冻结评审稿 + 四段实施)

- T0 #299:评审稿冻结([`sms-verification-infra-review.md`](../reviews/sms-verification-infra-review.md):D-SMS-1~12 拍板 + E-1~E-30 工程代决 + 风险表 + 五张清单 + AGENTS §2 providers/ 例外解锁 + 敏感字段三问)+ NEXT_TASKS 登记
- T1 #300(D 档):schema——User +`phone @unique`(含软删占用)+`phoneVerifiedAt`;`sms_settings` / `sms_verification_codes` / `sms_send_logs` 三表三 enum;migration SQL 全文贴 PR
- T2 #301(D 档):通道层——`src/modules/sms/` 15 文件(settings 三端点动词镜像 storage 现状 / send-logs 掩码列表 / DevStub+腾讯云双 Provider / 动态路由不静默 fallback / sms-crypto 独立 key+salt);权限码 seed 76→81;SDK 锁 4.1.240;SMS_ENCRYPTION_KEY fail-fast
- T3 #302(C/D 档):SmsCodeService(6 位 CSPRNG / TTL 5min / 单活码 / 错 5 次作废 / 成功即消费 / 明文三不 / 防刷三层〔60s + UTC+8 自然日 10 条 + IP 双 throttler〕)+ `me/phone` 发码/验绑换绑(沿 me/password 账号级豁免)+ admin 清号(幂等)+ 24xxx 六码 + 3 audit 事件 + e2e 三组 42 用例
- T4 #303:CHANGELOG Unreleased 三段 + current-state §2/§4 + CODEMAP sms 行 + governance G-7 行改"通道已就绪,通知用途未启动" + [`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) + NEXT_TASKS P1-6 归档

### ③ Release closeout(#304 / 本 PR)

- bump 三文件(#304);landing 阶段探针满足跳过(跨文档事实已由 #303 T4 收口)

---

## 5. API Surface Index

**155 条 `EXPECTED_ROUTES`** 全落 4 canonical 前缀(v0.17.0 的 148 + SMS 7:`GET/PATCH /api/system/v1/sms-settings`、`POST .../reset-credentials`、`GET /api/system/v1/sms-send-logs`、`POST /api/app/v1/me/phone/send-code`、`PUT /api/app/v1/me/phone`、`DELETE /api/admin/v1/users/:id/phone`);App surface 15→17。鉴权后缀(P2-2 惯例)155/155 全覆盖,检查项 G 锁一致性。

---

## 6. Test / Contract Snapshot

> 数据沿 #302/#303 本地 full + CI 记录(2026-06-10/11)。

- ✅ unit:**30 specs / 1251 tests**(v0.17.0:26/1185;+sms-crypto / dev-stub / tencent-sms〔mock SDK〕/ sms-code)
- ✅ E2E:**75 suites / 1706 tests**(v0.17.0:72/1664;+sms-settings 14 / app-me-phone-bind 19 / sms-throttle 9);**auth-* 全组原断言零改动全绿**
- ✅ contract:**287 passed**,155 条路由
- ✅ `docs:codemap:check` 0 FAIL / `docs:rbacmap:check` 0 FAIL 0 WARN(7 检查项)
- ✅ migration:**13 个**;干净库 `prisma:deploy` 重放 + seed 幂等二跑通过(81 权限码)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md`(tag 后独立回填 PR)
- ❌ **不**开通真实 SMS 通道 / 录入真实凭证(运维侧按 [`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md) 接力)
- ❌ **不**启动找回密码 / OTP 登录 / 通知用途(NEXT_TASKS P1-7 挂起;OTP 需先解锁 `AGENTS.md:242`)/ retention 清理(P2-6)/ Slow-3 / Slow-4
- ❌ **不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.18.0 <handoff-pr-squash-commit>
git push origin v0.18.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.18.0 - 2026-06-11/{flag=1; next} /^## v0.17.0 - 2026-06-10/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.18.0`;target:`v0.18.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零)+ 地图戳若有。

之后候选(沿 process §7.1,需用户立项 / 授权清单):**运维侧 SMS 真实通道接力**(系统侧无动作);NEXT_TASKS 余 P1-3(⏸ 等 Slow-3)/ P1-7(SMS 三消费者,逐项立项)/ P2-4(⏸)/ P2-6(retention,⏸);无阻塞性技术债。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.17.0`(2026-06-10;指向 `2b57f8c`) |
| Target | `v0.18.0`(pending) |
| Landing | 探针满足跳过(跨文档事实由 #303 T4 承接) |
| Bump PR | [#304](https://github.com/BA7IEE/srvf-nest-api/pull/304) / `3d6d3cf` |
| Handoff PR | 本 PR(branch `release/v0.18.0-handoff`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-11 |
