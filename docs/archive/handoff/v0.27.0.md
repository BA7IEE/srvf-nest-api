# SRVF API v0.27.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.26.1.md`](v0.26.1.md);v0.26.1 之后的工作是 **CMS 内容发布模块(第 28 模块)** —— goal「内容发布模块」T0-T5 全队列,**新增 feature(minor)**。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态(post-release 补档)**:本文件在 **`v0.27.0` tag / GitHub Release / §1 回填已完成之后**补写——v0.27.0 streamlined 收口当时跳过了 handoff,本档补齐近 4 版(v0.24/0.25/0.26/0.26.1)惯例缺口。**handoff 是历史快照,tag / Release / 代码全不受其影响**(沿 [`process.md §5.2`](../../process.md))。

---

## 1. TL;DR

- **v0.27.0 = CMS 内容发布模块(公告 / 公示 / 简报 / 推文,第 28 模块)release**(新增 feature;`### Added`)。goal「内容发布模块」T0-T5;冻结评审稿 [`content-module-review.md`](../reviews/content-module-review.md)。
- **正文 Markdown**(存原文,服务端不消毒,渲染安全交前端)· **5 档可见性**(public / member / formal_member / department / management,每篇选一;纯函数 [`content.visibility.ts`](../../../src/modules/content/content.visibility.ts))· 公示 = admin 手动发一篇内容 · **发布立即生效无 cron**。
- **附件复用既有 attachments(α 决议,推翻 T0 初版「storage key 不进 Attachment」)**:封面 / 正文图 / 文件统一 Attachment 行,owner = `content-image` / `content-file`;**写路径走 `AttachmentsService` RBAC**(`attachment.{upload,delete}.content-*` coarse,全绑 biz-admin),**读路径走可信只读** `listOwnerAttachmentsTrusted` / `resolveSignedUrlTrusted`(content 自签 + 仅过文章可见级后返)。
  - **演进 Slow-4 §6「biz-admin 不含任何 attachment.* 码」不变式 → 仅含 CMS content-* 4 码**(`seed-biz-admin.e2e` 第 ~229 行断言已 true-up)。**biz-admin 持 content-* attachment 码不是 bug。**
  - **范围例外(a):`content-image` / `content-file` 签名 URL 允许在 `open/v1`+`app/v1` 内容读取面返回(仅在过文章可见级后)** —— 「App 永不返回 signed URL」铁律的有意例外(content-* 是随文章展示素材,非 owner-scoped 敏感 PII);已注 [`attachments/CLAUDE.md`](../../../src/modules/attachments/CLAUDE.md) + current-state §2.1。其余 owner / L3 铁律不变。
- 正文图 `![](attachment:<id>)` **占位读时改写**(仅本文章 content-image id,外来 id 不泄露)· 封面**反范式双指针** `coverImageKey`/`coverAttachmentId`(列表缩略图直签免 N+1)· `viewCount` 累计 PV(详情打开原子自增,列表/预览/404 不计)· 搜索 **ILIKE 标题+正文** + 标签 **hasSome**(均 **AND 可见性、不旁路**)。
- `content/` 16 端点:admin `admin/v1/contents` ×12(CRUD + 状态机 publish/unpublish/archive + 附件 Mode B + 封面)/ open `open/v1/contents` ×2(`@Public` + 第 10 throttler `content-public`,published+public,detail 防枚举)/ app `app/v1/contents` ×2(canUseApp 准入 + 5 档可见性)。
- 收口链:base v0.26.1(`ee180f06` / #415)→ feature [#416](https://github.com/BA7IEE/srvf-nest-api/pull/416)(squash `e8f72b88`,含 post-review 元核验加固 `cab8bcdc`)→ bump [#417](https://github.com/BA7IEE/srvf-nest-api/pull/417)(`7b7118dc` = **tag 点**)→ §1 回填 [#418](https://github.com/BA7IEE/srvf-nest-api/pull/418)(`7c831fc9` = main HEAD)。
- `v0.27.0` tag(annotated,指 `7b7118dc`)+ GitHub Release(**Latest**)+ current-state §1 **均已完成**;本 handoff 为 post-release 补档。

下一会话:**无阻塞性技术债**。底座结构性健康。**v1 不做项**(评审稿 §10,见 §7)全部诉求触发再立项;唯一 review trivial nit(`visibleOrganizationIds` 无 `@ArrayMaxSize`,admin-only 输入,未改)登记备查。三条运维接力仍悬(同 v0.26.x:实名核验 / 微信 / SMS,均「正确但休眠」)。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.27.0`** | bump PR #417 |
| Swagger `setVersion(...)` | **`0.27.0`** | bump PR #417 |
| `CHANGELOG.md` v0.27.0 段 | **已存在**(`## v0.27.0 - 2026-06-21`;`### Added` 1 条目 = CMS 内容模块 T1-T5)| `## Unreleased` 折叠而来(bump PR 仅 3 文件,无夹带) |
| 远端最新 git tag | **`v0.27.0`**(annotated,指向 `7b7118dc`)| AI 创建(§8 已执行) |
| GitHub Release Latest | **`v0.27.0`** | notes 抽自 CHANGELOG v0.27.0 段;标 Latest |
| main HEAD | **`7c831fc9`** | `docs(current-state): §1 回填 v0.27.0 (#418)`(tag 点 `7b7118dc` 之后一commit)|
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.26.1 → 0.27.0` = **minor**(沿 process「0.x 默认 minor」)。

- ✅ **新增 feature**:第 28 模块 CMS 内容发布,`### Added` 一条目。
- ✅ **零 breaking**:既有端点契约 + e2e 断言不受影响;attachments 既有三 owner(member/certificate/activity)行为零变(回归锁定);新增均为加法(新表 / 新端点 / 新 owner 分支 / 新权限码绑 biz-admin)。
- ✅ **D 档 schema/migration/seed**:`contents` 新表 + 第 22 migration + content_type 字典 + 权限码 + attachment config 行(干净库重放 + seed 幂等二跑已验)。
- ✅ **一处不变式演进**(α,维护者拍板):Slow-4「biz-admin 不含 attachment.* 码」→ 含 CMS content-* 4 码;非漂移,见 §1 / 评审稿 §7。

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.27.0 段](../../../CHANGELOG.md) 与冻结评审稿 [`content-module-review.md`](../reviews/content-module-review.md)。

### goal「内容发布模块」T0-T5(单一 feature PR #416,squash `e8f72b88`)

- **T0** 评审稿冻结(含维护者增量包 A–E:正文图上传 / 附件复用 / 阅读量 / 搜索 / 标签;α 确认;两处冻结前修订:BizCode 基数 141→164 亲核、范围例外 a)。
- **T1** schema/migration/seed:`contents` 表 + migration `20260621090000_add_content_module`(第 22 个)+ content_type 字典 + content.* 5 + attachment.content-* 4 权限码(全绑 biz-admin)+ 2 条 AttachmentTypeConfig + attachments owner 分支 / 可信只读方法 / export。
- **T2** admin 面(`admin/v1/contents` ×12):CRUD + 状态机 + 附件 Mode B(委托 AttachmentsService)+ 封面 + audit(`content.{create,update,delete,publish}`)。
- **T3** open 公开面(`open/v1/contents` ×2):`@Public` + 第 10 throttler `content-public`(IP 60/60s);published+public;detail 防枚举;viewCount。
- **T4** app 会员面 + 5 档可见性(`app/v1/contents` ×2):canUseApp 准入;`content.visibility.ts`(21 unit);搜索 + 标签 AND 可见性;正文图读时改写。
- **T5** docs:current-state §2/§2.1/§3 · CHANGELOG · NEXT_TASKS · api-surface §0 · RBAC_MAP · CODEMAP · baseline §1.1 + AGENTS surface(红区 true-up)。
- **post-review 元核验加固** `cab8bcdc`(并入 #416):`listOwnerAttachmentsTrusted` 方法体开头加运行时护栏 —— 非 content-image/content-file owner 即 `throw`,防将来误用对 member/certificate 等 PII owner 无鉴权签出附件下载 URL;+3 unit。

### v0.27.0 closeout

- bump [#417](https://github.com/BA7IEE/srvf-nest-api/pull/417) / `7b7118dc`(仅 3 文件:`package.json` / `apply-swagger.ts` / `CHANGELOG.md` 折叠)
- §1 回填 [#418](https://github.com/BA7IEE/srvf-nest-api/pull/418) / `7c831fc9`
- 本 handoff PR(post-release 补档;tag 不回指,沿 §5.2「除非用户另行拍板」)

---

## 5. API Surface Index

**228 条 `EXPECTED_ROUTES`**(v0.26.1:212;**+16** = admin 12 + open 2 + app 2)。落 **5 canonical 前缀**;`open/v1` **第二用**(内容公开列表/详情,首用为招新一期 T3)。`docs:rbacmap:check` **0 FAIL / 0 WARN**(**155 码** = 146 + content.* 5 + attachment.content-* 4,全绑 biz-admin;内置角色 ×3;ops-admin 63 / member 9 零变化;controller 46→**49**)。app/open 读取面 self-scope / public **零权限码**。

---

## 6. Test / Contract Snapshot

> 数据沿收口前主会话 `pnpm test` / `test:e2e`(full)/ `test:contract` 本机实跑亲核 + PR #416/#417/#418 CI 记录(2026-06-21)。

- ✅ E2E:**100 suites / 2027 tests**(v0.26.1:97/1960;**+3 spec / +62**:content-admin 25 + content-public 19 + content-app 18,含 5 档可见 hit/miss「看不到不该看的」)
- ✅ unit:**49 specs / 1621 tests**(+content.visibility 21 + attachments.service 护栏 +3)
- ✅ contract:**379 passed**(212→228 路由,+16;snapshot 仅新增,排除 `info.version`;**零 L3**)
- ✅ `docs:codemap:check` 0 FAIL(模块 27→**28** / migration 21→**22**)/ `docs:rbacmap:check` 0 FAIL 0 WARN(**155 码**)
- ✅ migration:**22 个**(+`20260621090000_add_content_module`;干净库 deploy 22/22 重放 + seed 幂等二跑已验)
- ✅ BizCode:**164 → 169**(290xx 段 5:29001 NOT_FOUND / 29010 TYPE_INVALID / 29011 VISIBILITY_INVALID / 29012 VISIBLE_ORG_INVALID / 29030 INVALID_STATUS_TRANSITION;附件错误复用 13xxx)/ AuditLogEvent **57 → 61**(content.{create,update,delete,publish})

---

## 7. Out of Scope / v1 不做(评审稿 §10)

- ❌ content_reads 已读回执 / 回执表
- ❌ 评论 · 点赞
- ❌ 定时发布 / 新 cron(cron 解锁范围仍仅生日批)
- ❌ UV · 时间序列分析(v1 仅累计 PV)
- ❌ 部长角色 · 部门级内容权限细分(沿 Slow-3 挂起)
- ❌ 招新公示自动桥接(v1 公示 = admin 手动发内容)
- ❌ attachment Mime · SizeLimit override 行(v1 用 TypeConfig 默认值)
- ❌ 正文图 client 端 key 解析(v1 服务端读时改写)
- ❌ content 软删级联附件(物理删,孤儿 blob 走 retention SOP)
- ❌ 小程序前端

> 唯一 review trivial nit(未改):`CreateContentDto.visibleOrganizationIds` 无 `@ArrayMaxSize`(admin-only 输入,无 DoS 面;诉求出现再加)。

---

## 8. Release Steps(已执行)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7。**v0.27.0 为 streamlined 收口**(维护者拍板:bump → tag@bump 合并点 → Release → §1,无单独 handoff PR;本 handoff 为事后补档)。

- ✅ **tag**:`git tag -a v0.27.0 7b7118dc`(bump #417 squash 合并点)+ `git push origin v0.27.0`(annotated)
- ✅ **GitHub Release**:title「v0.27.0 — CMS 内容发布模块(第 28 模块)」;notes 抽自 CHANGELOG v0.27.0 段;**mark as latest**;`gh release list` 证据 = `v0.27.0 … Latest`
- ✅ **current-state §1 回填**:#418(版本四方一致 / HEAD / tag / Release / open PR 0 / Unreleased 折叠)
- ✅ **分支清理**:feature/bump/§1 三远端分支删;bump/§1 本地分支删(patch-equiv);**本会话 worktree `adoring-feynman-21eb7f` 待会话结束移除**(`git worktree remove` + `git branch -D claude/adoring-feynman-21eb7f`;已验 `e8f72b88 ≡ cab8bcdc` 完全合入)

---

## 9. 主会话元核验(v0.27.0 收口)

维护者五点核验**全过**:① 版本四方一致(package.json = Swagger = tag = Release = `0.27.0`)② CHANGELOG 折叠无夹带(bump PR 恰 3 文件)③ tag annotated 指 bump 合并点 `7b7118dc`(`git describe --exact-match` = v0.27.0)④ Release Latest(notes 抽自 CHANGELOG)⑤ §1 回填(#418)。

- **patch-equivalence 删支复核**:删 `release/v0.27.0` 时 `git diff main..branch` 报非空(假阳性 = main 经 #418 §1 前进污染);用正确判据 `git diff 7b7118dc 58af419f` = **空** 证 bump 分支内容 ≡ 其 squash,**零丢失**(教训:别越过 stop-gate,用 squash 提交为基准核验)。
- **全模块 review 干净**:可见性纯函数 / 签名 URL 可见级闸 / 状态机 / 附件集成 / biz-admin RBAC 演进 逐行核过;唯一 trivial nit(§7,admin-only,未改)。

---

## 10. Next PR After Release

各线均不自动启动:① **内容模块后续**(§7 v1 不做项,诉求触发再立项);② **运维侧三条真实通道接力**(腾讯云实名核验 / 微信小程序 / SMS 送审;均「正确但休眠」);③ **招新 / 团队管理后续**(评审稿 + NEXT_TASKS,诉求触发);④ **小程序前端**立项。

---

## 11. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.26.1`(2026-06-20;指向 `ee180f06` / #415)|
| Target | `v0.27.0`(已发布;tag `7b7118dc` annotated / Latest)|
| Feature PR | [#416](https://github.com/BA7IEE/srvf-nest-api/pull/416) / squash `e8f72b88`(含元核验加固 `cab8bcdc`)|
| Bump PR | [#417](https://github.com/BA7IEE/srvf-nest-api/pull/417) / `7b7118dc`(= tag 点)|
| §1 回填 PR | [#418](https://github.com/BA7IEE/srvf-nest-api/pull/418) / `7c831fc9`(= main HEAD)|
| Handoff PR | 本 PR(post-release 补档;branch `docs/handoff-v0.27.0`)|
| Tag / Release | **已完成**(streamlined 收口;tag 不因本补档回指)|
| Date | 2026-06-21 |
