# SRVF API v0.26.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.25.0.md`](v0.25.0.md);v0.25.0 之后的工作是**招新业务域第三段:招新三期(入队:志愿者 → 队员)**(#391-#395,详见 §4),承接招新一期(报名前段)+ 招新二期(招新后段),**招新业务域三段闭环**。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.26.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7,维护者亦可手动)。

---

## 1. TL;DR

- **v0.26.0 = 招新三期(入队)release**(招新业务域第三段;主线:**志愿者自助发起入队 → 10 项考核〔8 admin gate + 贡献值系统自动 + 综合评估〕→ 一键入队 = 设部门 + 级别 level-1**;**两层身份在入队这一刻完成转换**):
  - **入口态** = phase-2 promote 出的志愿者(`members` 行 status ACTIVE、**gradeCode=null 无级别、无 member_departments 无部门**、有 User 可登录小程序)。
  - **T1 schema**(#391,含 T0 评审稿冻结):**第 27 模块 `team-join/`** 两新表——`team_join_cycles`(入队轮,annual,至多一个 open)/ `team_join_applications`(入队申请,**有真实 `memberId` FK**,与招新表「无 Member FK」相反;`gateMarks Json` 8 通用 + 0~4 专业队 gate;`targetOrganizationIds` 候选数组;partial unique `(memberId,cycleId) WHERE deletedAt IS NULL AND statusCode<>'rejected'`)+ **第 21 migration** + seed(`member_grade` demo→**level-1~7**〔R13 code 稳定 / label 占位〕+ node_type 4 专业队 code)
  - **T2 admin 面**(#392):admin 9 端点——入队轮 CRUD + 标 gate(8 通用 admin + 4 条件性专业队;完成日→有效期〔**本轮按北京日历日** / 3年 / 2年 / 长期〕+ dept-assessment 可延长期;末次 8 通用全过 + **贡献值≥5 自动推进** `pending_evaluation`)+ 综合评估单一人工闸(approve 前重校)+ 贡献值只读汇总(approved sheet + `checkInAt < 入队年 3-31` cutoff,实时算不落库);BizCode **282xx**(28201/28202/28240)+ 权限码 +6;**含 2 元核验 bug 修复**(本轮按 `beijingDayNumber` + evaluate approve 前重跑门槛/贡献值)
  - **T3 app 自助面**(#393):`app/v1/me/team-join/*` 3 端点——发起申请 + 查进度 + 改候选;**AppIdentityResolver 准入(canUseApp=false→403)+ self-scope 锁 memberId 防 IDOR + 候选 org 存在+ACTIVE 校验 + 零权限码**;BizCode 28203/28210/28230
  - **T4 一键入队**(#394,最重一刀):`TeamJoinEnrollmentService` **单事务原子**设 `member_department` + `gradeCode='level-1'` → joined;**直连 prisma 不复用 service 防环**(沿 phase-2 promote);**原子 + 幂等(joined 离 approved 重跑 28240 + partial unique 兜底)+ 两层身份转换**;专业队需对应 `team-*` gate(28242)+ `evaluationExtendedUntil` 消费(cycle open 同轮认/closed 须延长期)+ 兜底重校验(28241);BizCode 28241/28242 + 权限码 +1(→**146**);**6-lens 对抗审查零确认问题**
- **两层身份铁律全程贯穿**:入队前 member **无部门、无级别**;入队(T4 一键入队)才赋部门 + 级别 level-1。调级 v1 走现有 `PATCH admin/v1/members/:id`(biz-admin),**不单独建 APD 角色**。
- contract **212 路由**(200→212,**+12 全部招新三期入队**:admin 8 + app 自助 3 + 一键入队 1)/ migrations **21**(+21 招新三期)/ e2e **97 suites / 1944 tests**(+team-join admin 27 / team-join-app 11)/ unit **46 spec / 1494**(零变化,phase-3 全 e2e)/ BizCode 新增 **282xx 8 码** / 权限码 **146**(+7)/ audit DB union **57**(+6)/ 业务模块 **27**(+team-join 27)/ throttler **9**(零变化,自助走鉴权面)
- bump PR 已合入:[396](https://github.com/BA7IEE/srvf-nest-api/pull/396) / squash commit `32a3b9c1`
- **`v0.26.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.25.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)

下一会话:**无阻塞性技术债**。招新业务域三段闭环;团队管理域「志愿者 → 队员」全链路即用——DevStub 通道(招新报名/微信)已验,真实凭证由运维侧录入即生效。**三条运维接力仍悬**(同 v0.25.0):① 真实腾讯云实名核验(SOP [`ops/realname-verification-rollout-checklist.md`](../../ops/realname-verification-rollout-checklist.md));② 微信小程序注册 → AppID/AppSecret(SOP [`ops/wechat-mini-production-rollout-checklist.md`](../../ops/wechat-mini-production-rollout-checklist.md));③ SMS 腾讯云两模板送审(SOP [`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md))。phase-3 之后(退队 / 晋升体系 / 多部门常态归属 / 级别历史 / 接 `CertificatesService.isQualified` 自动核验证书 gate / 入队部门级权限细分 / 贡献值年度窗口)挂评审稿 §9 + NEXT_TASKS,业务诉求触发再立项。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.26.0`** | PR #396 已 bump |
| Swagger `setVersion(...)` | **`0.26.0`** | PR #396 已 bump |
| `CHANGELOG.md` v0.26.0 段 | **已存在**(`## v0.26.0 - 2026-06-20`,**4 条目**:Added ×4〔招新三期入队 T1/T2/T3/T4 #391/#392/#393/#394〕)| PR #396 折叠 Unreleased 时填入(各 T0/T5 docs 与 §1 回填为 A 档沿惯例不登记) |
| 远端最新 git tag | **仍为 `v0.25.0`**(指向 `782f668e`)| `v0.26.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.25.0`** | `v0.26.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`32a3b9c1`** | `chore(release): bump v0.26.0(…) (#396)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.25.0 → 0.26.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **纯新增功能**:+12 端点(contract 200→212,snapshot 仅新增,均落既有 `admin/v1` / `app/v1` canonical 前缀,零新前缀)/ +1 migration(招新三期两新表 + partial unique,**纯建表无回填无改写**)/ +1 业务模块(26→**27**:team-join)/ 新 BizCode 段 **282xx 8 码** / 权限码 +7(139→146)/ audit DB +6(51→57)/ throttler 零增(自助走鉴权面)/ **零新必填 env**(贡献值直读 attendance / 拼音无关 / 无新通道)
- ✅ **零破坏**:既有模块(members / member-departments / certificates / 活动 / 考勤 / auth / wechat / sms / recruitment)端点契约与 e2e 断言**不受影响**(唯一共享改动 = `team-join-progress.ts` contribution/gate/presenter 抽离〔admin e2e 18 零漂移〕+ `reset-db` 纳入 team_join 两表 + seed-biz-admin 契约同步 +6 码;**全 phase-3 内 + 测试契约**);两层身份铁律:入队前无部门无级别、入队才赋,members 业务字段语义不变
- ✅ **零新依赖**(贡献值 `_sum` 走既有 Prisma aggregate;无新 npm / 无新通道)

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.26.0 段](../../../CHANGELOG.md) 与各 PR;A 档 docs PR 沿惯例不进 CHANGELOG。

### 招新三期(入队:志愿者 → 队员)goal(T0-T5 全队列;冻结评审稿 [`recruitment-phase3-review.md`](../reviews/recruitment-phase3-review.md))

- **T0+T1** [#391](https://github.com/BA7IEE/srvf-nest-api/pull/391)(D 档)评审稿冻结(§0.5 元核验:Q1 门槛清单以会议纪要纠正 + Q2 专业队条件性 + Q3 多部门候选 + Q4 贡献值口径 + Q5 level-1~7;3 wrinkle)+ schema 两表 + migration `20260619133232_add_team_join_phase3`(第 21 个;干净库 21/21 重放 + partial unique 落库亲核 + seed 幂等二跑)+ seed level-1~7 / node_type 4 专业队
- **T2** [#392](https://github.com/BA7IEE/srvf-nest-api/pull/392)(C/D 档)admin 面 8 端点(入队轮 CRUD + 标 gate + 综合评估 + 贡献值汇总)+ 状态机 + 权限码 +6 + BizCode 282xx 3 码 + audit +4 + CODEMAP 模块 26→27(含 2 元核验 bug 修复;e2e team-join +1 spec;contract 200→208)
- **T3** [#393](https://github.com/BA7IEE/srvf-nest-api/pull/393)(C/D 档)app 自助面 3 端点(发起/查进度/改候选;AppIdentityResolver 准入 + self-scope 防 IDOR + 候选 org 校验)+ BizCode 28203/28210/28230 + audit +1 + DRY 抽 `team-join-progress.ts`(e2e +1 spec 11 例;contract 208→211)
- **T4** [#394](https://github.com/BA7IEE/srvf-nest-api/pull/394)(D 档)一键入队 `TeamJoinEnrollmentService` 单事务设部门 + 级别 level-1(最重一刀;直连 prisma 防环;专业队 gate + evaluationExtendedUntil 消费 + 兜底重校验;原子/幂等/两层身份;权限码 +1→146;BizCode 28241/28242;audit +1;6-lens 对抗审查零确认问题;e2e team-join admin +9→27;contract 211→212)
- **T5** [#395](https://github.com/BA7IEE/srvf-nest-api/pull/395)(A 档)docs 收尾:current-state §1/§2/§2.1/§3 + CHANGELOG 登记 T1-T4 + RBAC_MAP(146/46)+ NEXT_TASKS(P1-13 转归档)+ CODEMAP

### v0.26.0 closeout

- bump [#396](https://github.com/BA7IEE/srvf-nest-api/pull/396) + 本 handoff PR(landing 阶段探针满足跳过——跨文档事实已由 #395 T5 docs 收尾承接,bump 前零新增)

---

## 5. API Surface Index

**212 条 `EXPECTED_ROUTES`**(v0.25.0:200;**+12 全部招新三期入队**:admin `admin/v1/team-join/cycles` 4 + `admin/v1/team-join/applications` 5〔含一键入队 join〕+ app `app/v1/me/team-join` 3),落 **5 canonical 前缀**(零新前缀,均 `admin/v1` / `app/v1`)。鉴权后缀(P2-2 惯例)212/212 全覆盖;`docs:rbacmap:check` 0 FAIL / 0 WARN(**146 码** / 内置角色 ×3 / ops-admin 绑 63 / biz-admin 绑 57);app 自助 `app/v1/me/team-join/*` self-scope `[auth]` 无 RBAC 码。

---

## 6. Test / Contract Snapshot

> 数据沿 #391-#395 各 PR 本机 `agent:check:full` 等价链 + CI 记录(2026-06-19 ~ 2026-06-20);test/spec 文件数为本机静态核验。

- ✅ E2E:**97 suites / 1944 tests**(v0.25.0:95/1906;+`team-join.e2e` 1 spec〔admin 27:轮次/标 gate+自动推进/贡献值/有效期/综合评估/状态机/一键入队 9〕+`team-join-app.e2e` 1 spec〔11:准入/发起/查进度/改候选+IDOR/防重〕;静态 `*.e2e-spec.ts` = **97 个**亲核;**既有模块断言零改动**)
- ✅ unit:**46 specs / 1494 tests**(v0.25.0:46/1494;**零变化**——phase-3 全 e2e,无新 unit spec)
- ✅ contract:**353 passed**,212 条路由;snapshot 仅新增 + 排除 `info.version`;**零 L3**(无 token / secret / 完整 signed URL / 身份证号 出现于 snapshot)
- ✅ `docs:codemap:check` 0 FAIL(27 模块 / 21 migration)/ `docs:rbacmap:check` 0 FAIL 0 WARN(**146 码**;招新三期 7 权限码全实装,孤码清零)
- ✅ migration:**21 个**(+`20260619133232_add_team_join_phase3`〔两表 + 手写 partial unique + 3 FK RESTRICT〕);依赖零增减
- ✅ BizCode 新增 **282xx 8**(28201/28202/28203/28210/28230/28240/28241/28242)/ audit DB union **57**(+6:`team-join-cycle.{create,update}` / `team-join-application.{submit,mark-gate,evaluate,join}`)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md` §1(tag 后独立回填 PR)
- ❌ **不**做 phase-3 之后(退队 / 晋升体系 / **多部门常态归属** / 级别历史版本化)— 评审稿 §9 + NEXT_TASKS,业务诉求触发再立项
- ❌ **不**接 `CertificatesService.isQualified` 自动核验证书 gate(v1 = admin 标记,W-J-2 留作未来:+cert_type seed + team-join↔cert 桥,单独立项)
- ❌ **不**做入队部门级权限细分(v1 = biz-admin 统一标 gate,沿 Slow-3 考勤终审同款挂起;「部门」职务无数据模型承载)
- ❌ **不**改贡献值「年度窗口」语义(v1 历史累计 ≥5 截至入队年 3-31;若业务改年度窗口 = 改 cutoff 下界,单独立项)
- ❌ **不**碰真实腾讯云实名核验 / 微信 / SMS 凭证(三条运维接力,SOP 同 v0.25.0 §7;均「正确但休眠」)
- ❌ **不**做 trust proxy / 连接池调优(**等部署拓扑确定**);**不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.26.0 <handoff-pr-squash-commit>
git push origin v0.26.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.26.0 - 2026-06-20/{flag=1; next} /^## v0.25.0 - 2026-06-19/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.26.0`;target:`v0.26.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零;§2/§4 若变随手 true-up)+ 地图快照戳(若落后)。

之后各线均不自动启动:① **运维侧三条真实通道接力**(腾讯云实名核验 / 微信小程序 / SMS 送审;均「正确但休眠」);② **招新 / 团队管理后续**(phase-3 之后:退队 / 晋升 / 多部门常态 / 级别历史 / 证书自动核验 gate / 部门级权限细分;评审稿 §9 + NEXT_TASKS,诉求触发);③ **小程序前端**立项(招新自助 + 入队自助 + 微信登录后端均已"正确但休眠"等待对接)。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.25.0`(2026-06-19;指向 `782f668e`) |
| Target | `v0.26.0`(pending) |
| Landing | 跳过(探针满足:跨文档事实已由 [395](https://github.com/BA7IEE/srvf-nest-api/pull/395) T5 docs 收尾承接,bump 前零新增) |
| Bump PR | [396](https://github.com/BA7IEE/srvf-nest-api/pull/396) / `32a3b9c1` |
| Handoff PR | 本 PR(branch `claude/handoff-v0.26.0`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-20 |
