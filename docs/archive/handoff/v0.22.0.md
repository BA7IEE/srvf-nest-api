# SRVF API v0.22.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`v0.21.0.md`](v0.21.0.md);v0.21.0 之后的工作是**三条线:对抗式 review 把关清理串(#332-#344 间的 A/B/D-comment-only 档)+ AI Harness 效率 goal(#333/#334)+ 会议延期窗口"无等待工作一次收清" goal(G1 招新地基文档〔SRVF 业务仓〕+ G2 #345 + G3 #346 + G4 本收口)**(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘。
>
> **状态**:本文件在 **`v0.22.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 回填在本 handoff PR 合入后由 AI 按 §8 执行(沿 [`process.md §5.1`](../../process.md) 阶段 6/7,维护者亦可手动)。

---

## 1. TL;DR

- **v0.22.0 = 基础设施硬化 + 缺陷收口 + 效率治理的 release closeout**(无业务功能;零新端点 / 零 migration / 零新依赖 / 零权限码):
  - **进程级崩溃可观测性兜底**(goal G2,#345):`src/bootstrap/apply-crash-handlers.ts` 仅 main.ts 注册——uncaughtException 记 fatal(pino 同步 flush)后 exit(1);unhandledRejection 记日志后 **re-throw 保持 Node 默认崩溃结局**;敏感信息沿 redact 清单;**不碰 SIGTERM/优雅关闭**(main.ts 注释已划界)
  - **外部 SDK 请求超时上限**(goal G3,#346):腾讯云 SMS `httpProfile.reqTimeout` **8s**(默认 60s)+ COS `Timeout` **8000ms**(默认无超时);错误语义零变化;**正确但当前休眠**(SMS/COS 真通道未接,配置就位由两 provider spec 构造断言锁定)
  - **纯日期时区缺陷收口**(把关 P2,#343):member-profiles / certificates 两处私有归一副本合并为 `src/common/datetime/date-only.util.ts`,按 UTC+8 日历日归一——带偏移 datetime 写入差一天缺陷修复;既有断言零修改
  - **CI docs-only 快速路径**(#334)+ **工具授权白名单扩充 + §7.1 CI 等待期惯例**(#333):纯 `.md` PR 跳过 DB/contract/e2e(~6min→≤2min);allow 11→32(机器校验零放宽)
  - **auth.service characterization 27 例**(#344,test-only 不进 CHANGELOG):防枚举四场景 / rotation+reuse / 幂等 logout 等 5 方法现状锁定,安全敏感服务零 unit 覆盖收口
- **招新技术地基对照文档**(goal G1,系统仓零代码):`SRVF/01-业务整理/招新模块_技术就绪度对照_v1.md`(业务资料仓)——批次5 Plan 依赖 11 项对照 v0.21.0 实装 / 报名表 30+ 字段逐项落点与缺口 / 纯技术开放问题 T1-T8 含倾向 / 会议 8 问阻塞映射
- contract **159 路由不变** / migrations **16 不变** / e2e **86 suites / 1787 tests** / unit **34 spec / 1301 tests**
- bump PR 已合入:[348](https://github.com/BA7IEE/srvf-nest-api/pull/348) / squash commit `866b39b4`
- **`v0.22.0` tag / GitHub Release 尚未创建**(latest 仍为 `v0.21.0`);本 handoff 合入后由 AI 执行 §8
- `docs/current-state.md` §1 回填在 tag / Release 完成后单独 PR(沿历次范式)

下一会话:**招新需求会议(延期中)是业务主线入口**——会后按 G1 文档 §四 回填 8 问答案 → 字段冻结表 → D 档 schema 评审;**运维侧 SMS 真实通道送审仍是唯一人工作业**(两模板一批,系统侧零等待);**trust proxy / 连接池等部署形态项**等部署拓扑确定后单独立项。无阻塞性技术债。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.22.0`** | PR #348 已 bump |
| Swagger `setVersion(...)` | **`0.22.0`** | PR #348 已 bump |
| `CHANGELOG.md` v0.22.0 段 | **已存在**(`## v0.22.0 - 2026-06-12`,5 条目)| PR #348 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.21.0`**(指向 `3a418c9`)| `v0.22.0` 待本 handoff PR 合入后由 AI 创建(§8.1) |
| GitHub Release Latest | **仍为 `v0.21.0`** | `v0.22.0` Release 待 tag 后由 AI 发布(§8.3) |
| main HEAD | **`866b39b4`** | `chore(release): bump v0.22.0(三文件…) (#348)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.21.0 → 0.22.0` = **minor**(沿 0.x 阶段全 minor 节奏)。

- ✅ **零新端点**(contract 维持 **159**;snapshot 排除 info.version,零 diff)/ **零 migration**(维持 16)/ **零新依赖** / **零新增 BizCode / 权限码**
- ✅ **运行时行为新增仅崩溃路径**:crash handlers 在崩溃前多两行结构化日志,崩溃结局(exit 1)与 Node 默认一致;SDK 超时为上限保护,真通道未接属休眠配置
- ✅ **缺陷修复**:纯日期 UTC+8 归一(#343,带偏移 datetime 差一天);既有 spec / e2e 断言零修改全绿
- ✅ **CI 行为变化仅 docs-only PR**(跳重活,check 名不变,required checks 兼容;代码 PR 全量链路一字不动)

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.22.0 段](../../../CHANGELOG.md) 与各 PR;A 档 docs PR 与 test-only / comment-only PR 沿惯例不进 CHANGELOG。

### ① v0.21.0 收尾 + 对抗式 review 把关清理串(#332 / #335-#342 / #343 / #344)

- [#332](https://github.com/BA7IEE/srvf-nest-api/pull/332)(A 档)v0.21.0 §1 回填;[#335](https://github.com/BA7IEE/srvf-nest-api/pull/335)/[#336](https://github.com/BA7IEE/srvf-nest-api/pull/336)(A/B 档)development true-up + settings.example 对齐
- [#337](https://github.com/BA7IEE/srvf-nest-api/pull/337)(A 档)process §5.4.2 **可选 --auto 合并形态**成文(main 分支保护 + required checks 兜底);[#338](https://github.com/BA7IEE/srvf-nest-api/pull/338)(A 档)review R4 跨 PR 文档漂移修正
- 把关清理批 T1-T4:[#339](https://github.com/BA7IEE/srvf-nest-api/pull/339)(A 档).env.example 限流 env 补全;[#340](https://github.com/BA7IEE/srvf-nest-api/pull/340)(A 档)AGENTS §1.B/C 软漂移 true-up(红区单点授权);[#341](https://github.com/BA7IEE/srvf-nest-api/pull/341)/[#342](https://github.com/BA7IEE/srvf-nest-api/pull/342)(B/D 档 comment-only)死码标注 + schema 行内注释
- [#343](https://github.com/BA7IEE/srvf-nest-api/pull/343)(B 档)**date-only UTC+8 归一修复**(把关 P2;唯一进 CHANGELOG 的把关项);[#344](https://github.com/BA7IEE/srvf-nest-api/pull/344)(B 档 test-only)auth.service characterization 27 例

### ② AI Harness 效率 goal(#333 / #334)

- [#333](https://github.com/BA7IEE/srvf-nest-api/pull/333)(B 档)工具白名单 allow 11→32 + process §7.1 CI 等待期惯例;[#334](https://github.com/BA7IEE/srvf-nest-api/pull/334)(D 档)ci.yml docs-only 快速路径

### ③ 会议延期窗口 goal「无等待工作一次收清」(G1-G4)

- **G1**(无 PR,产出在 SRVF 业务仓):`01-业务整理/招新模块_技术就绪度对照_v1.md`(只读分析;不设计 schema / 不写 API)
- **G2** [#345](https://github.com/BA7IEE/srvf-nest-api/pull/345)(D 档)进程崩溃兜底(unit +5 例);**G3** [#346](https://github.com/BA7IEE/srvf-nest-api/pull/346)(D 档)SDK 超时(两 spec 构造断言同步)
- **G4 closeout**:landing [#347](https://github.com/BA7IEE/srvf-nest-api/pull/347)(A 档:CODEMAP bootstrap 例外 + current-state §2 硬化行/测试计数 + RBAC_MAP 戳)+ bump [#348](https://github.com/BA7IEE/srvf-nest-api/pull/348) + 本 handoff PR

---

## 5. API Surface Index

**159 条 `EXPECTED_ROUTES` 与 v0.21.0 完全一致**,全落 4 canonical 前缀;鉴权后缀(P2-2 惯例)159/159 全覆盖;`docs:rbacmap:check` 0 FAIL / 0 WARN(117 码 / 内置角色 ×3 / 权限事实零变化)。

---

## 6. Test / Contract Snapshot

> 数据沿 #345/#346/#348 本机 `agent:check:full` + CI 记录(2026-06-12)。

- ✅ E2E:**86 suites / 1787 tests**(与 v0.21.0 持平;既有断言零修改)
- ✅ unit:**34 specs / 1301 tests**(v0.21.0:31/1264;+date-only 5〔#343〕+auth.service characterization 27〔#344〕+crash-handlers 5〔#345〕)
- ✅ contract:**291 passed**,159 条路由;snapshot 零 diff(本期零接口变更)
- ✅ `docs:codemap:check` 0 FAIL(1 WARN 为既有 god-service 体量观察,沿 current-state §4)/ `docs:rbacmap:check` 0 FAIL 0 WARN
- ✅ migration:**16 个不变**;依赖零增减
- ✅ docker-smoke:含「graceful shutdown」步骤通过——crash handlers 与优雅关闭并存实证(#345 CI)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 tag / Release(本 handoff 合入后按 §8 执行)
- ❌ **不**刷新 `docs/current-state.md` §1(tag 后独立回填 PR)
- ❌ **不**做 trust proxy / 连接池调优(**等部署拓扑确定**,goal 明确禁区)
- ❌ **不**启动招新模块任何实装 / schema / API(**等需求会议**;G1 文档只读分析)
- ❌ **不**碰腾讯云送审 / 真实凭证(运维侧接力,[`ops/sms-production-rollout-checklist.md`](../../ops/sms-production-rollout-checklist.md))
- ❌ **不**做超时重试 / 退避 / 可配置化;**不**做启动期(NestFactory.create 前)崩溃接管
- ❌ **不**改源码 / schema / 测试 / 依赖

---

## 8. Release Steps After This PR(AI 执行;维护者亦可手动)

> 沿 [`process.md §5.1`](../../process.md) 阶段 6/7;完成后输出 `gh release list` 证据。

### 8.1 创建并推送 tag

```bash
git tag v0.22.0 <handoff-pr-squash-commit>
git push origin v0.22.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.22.0 - 2026-06-12/{flag=1; next} /^## v0.21.0 - 2026-06-11/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.22.0`;target:`v0.22.0` tag;**mark as latest**;notes = §8.2 抽取段

---

## 9. Next PR After Release

release 完成后,**先**走入口回填 PR(A 档):`docs/current-state.md` §1(tag / release / HEAD / handoff / Unreleased 清零;§4 复核)。

之后三条线,均不自动启动:① **招新需求会议**(延期中)→ 会后按 G1 文档(SRVF 仓)§四回填 8 问 → 字段冻结表 → D 档 schema 评审;② **运维侧 SMS 送审接力**(两模板一批;系统侧零代码等待);③ **部署形态确定后**:trust proxy / 连接池 / 多实例前提复核单独立项。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.21.0`(2026-06-11;指向 `3a418c9`) |
| Target | `v0.22.0`(pending) |
| Landing | [347](https://github.com/BA7IEE/srvf-nest-api/pull/347)(跨文档 true-up) |
| Bump PR | [348](https://github.com/BA7IEE/srvf-nest-api/pull/348) / `866b39b4` |
| Handoff PR | 本 PR(branch `release/v0.22.0-handoff`) |
| Tag / Release | **pending**(本 PR 合入后 AI 执行 §8) |
| `current-state.md` 回填 PR | **pending**(release 完成后独立 PR) |
| Date | 2026-06-12 |
