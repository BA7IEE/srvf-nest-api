# SRVF API 当前状态入口

> **第一入口,当前事实唯一权威源**(权威分层见 process §6 / 根 CLAUDE.md §1)。历史档案在 `archive/`,逐版本变更在 [`CHANGELOG.md`](../CHANGELOG.md);release 合入后**必须**优先回填本文件。
> **铁律**:事实与蓝图冲突以本文件为准;遇冲突**不擅自调和、不擅自改文件**,先报告等拍板。

## 1. 当前版本状态

| 项 | 当前值 |
|---|---|
| 版本(三方一致) | **v0.41.0**(2026-07-11;package.json = Swagger = tag = Release 四方一致;tag `v0.41.0` 指向 handoff `5f2b78f4` 标 Latest;**招新可用性收口:手工建档闭环 + 防重成本线 + 申请人自助包**,F0 冻结评审稿 #542〔9 项核实 + 拍板 R1–R7,[`recruitment-usability-closeout-review.md`](archive/reviews/recruitment-usability-closeout-review.md)〕+ F1–F7 七刀 #543–#549 + landing #550:①**F1 防重前移 + OCR 成本线**(#543,第 41 migration)submit 付费 OCR 前同轮活跃 openid/phone 去重(28004/28005)+ 付费 OCR 按 IP 北京自然日封顶(`recruitment_ocr_daily_counters` 表,env `RECRUITMENT_OCR_DAILY_IP_LIMIT` 默认 30,超限 28060/429)②**F2 admin 改资料**(#544)`PATCH .../applications/:id` + 新码 `update.record`——R1 白名单,身份字段仅 manual_review/外籍(verified 大陆 28045),大陆 birth/gender 恒派生,改证件号镜像 submit 校验链 ③**F3 单人手动建档**(#545)`POST .../:id/promote-single` + 新码 `promote.single`——批量 skip 项收尾:`buildOnePromotion` 内核抽取共用 + 同原子号段,放行外籍(缺派生 28047 先 F2 补录),锚点择优 openid→phone(双缺双占 28046)④**F4 查询链修复**(#546)闭轮 send-code/verify-code 对「手机命中未清除报名」放行(防枚举泛化 200 沿 login-sms;28030 摘除)+ 发号后 query/query-by-phone 经账号锚 fall-through 返 stage=volunteer 引导态 ⑤**F5 知情同意 + 签名图**(#547,第 42 migration)submit 必填 `privacyConsentAccepted=true`(⚠️ 契约收紧)+ 可选签名图,promote consent 搬真值(存量→false)+ 签名图搬 `member_profiles.signatureImageKey` 长期留存 ⑥**F6 自助撤销**(#548,第 43 migration)`POST open/.../withdraw` 双通道凭证 + `withdrawn` 终态 + partial unique 排除集重建 → 同轮重报解锁 ⑦**F7 证书图**(#549,第 44 migration)公开上传(first_aid/bsafe 每类 ≤3 张重传覆盖)+ admin 取图(复用 read.sensitive)+ promote 自动建 pending Certificate 搬 `imageKeys`。**⚠️ 行为变更 6 条**(CHANGELOG v0.41.0 置顶):submit 必填 consent(旧客户端 400,前端须同步升级)/ 同轮同 openid·phone 重复提交改拒 + OCR IP 日封顶 / 闭轮发码语义变 / 发号后查询返引导态 / promote consent 搬真值 / 新增 5 端点(改资料·单发·撤销·证书上传+取图)。终值计数:权限码 201→**203**(+2 全绑 biz-admin)/ biz-admin 77→**79** / org-admin 60(前缀排除零波及)/ ops-admin 96 / `EXPECTED_ROUTES` 329→**334** / controller 66 / 模块 35 / **migration 40→44** / 角色 7;**+7 BizCode**(28004/28005/28045/28046/28047/28052/28060)/ **+3 AuditLogEvent**(`recruitment-application.{update,withdraw,certificate-upload}`)/ 字典 `recruitment_stage` +1(withdrawn)/ +1 env;`src/modules/auth/**` + 判权核心零 diff;批量 promote 行为逐字不变;要点见 CHANGELOG v0.41.0 段) |
| `main` HEAD | v0.41.0 后 Unreleased「招新/入队十三项收口」落地点(PR-1~PR-4 + 五疵微清理 PR;不 bump 不发版):刀A–H 完成、刀I 两条挂账,评审五疵已清;基线 `145ee19d` 之后累计 #554 + 十三项四刀 + 本微清理 |
| open PR / 工作树 / Unreleased | **本 PR 合入后 0** open PR / clean / `## Unreleased` 累计 **2** 条(十三项收口总账 + 五疵微清理)。终值:权限码 **205** / biz-admin **81** / org-admin 60 / ops-admin 96 / `EXPECTED_ROUTES` **336** / controller 66 / 模块 35 / migration **47** / 角色 7;新增 BizCode 28053 + audit `recruitment-application.certificate-review`;版本仍 v0.41.0 |
| 最新 handoff | [`archive/handoff/v0.41.0.md`](archive/handoff/v0.41.0.md)(随本次 release 收口创建,先于 tag / Release〔process.md §5.1 canonical 顺序〕;tag 指其合并点 `5f2b78f4`;不回改;接续 [`v0.40.0.md`](archive/handoff/v0.40.0.md)) |

## 2. 当前系统已具备能力

> 清单级;事实权威:字段 = [`schema.prisma`](../prisma/schema.prisma);接口 = `/api/docs` + [`EXPECTED_ROUTES`](../test/contract/openapi.contract-spec.ts) + snapshot;BizCode = `biz-code.constant.ts` + CHANGELOG。

- **v1 + V1.1 底座**:NestJS + Prisma + PostgreSQL + JWT + 三层 Role + 软删除 + 统一返回 + Swagger 100%;pino 日志 + 请求 ID + helmet + 限流 + 健康检查 + 优雅关闭 + CI
- **V2 模型全量**:dictionaries / organizations / members(`memberNo` 不复用)/ member_departments / member_profiles / emergency_contacts / certificates / activities / activity_registrations(+CSV)/ attendance_sheets+records(5 态含终审)/ contribution_rules(D14)/ audit_logs(A-1 不可改删)
- **token 体系**(v0.13/14,**行为冻结**):App 本人改密(不吊销 access,D-4)+ refresh / logout / logout-all(rotation always / family revoke / 90d absolute / 联动撤销 5 场景〔2026-06-11 +`self-password-reset`〕/ JWT 15m / payload 仅 `{sub,username}`)
- **RBAC 单轨(P0-F + Slow-4 收口)**:4 表 + `rbac.can()` + **121 码**(2026-06-11 Slow-4 +36;2026-06-12 微信 T2 +4)+ 内置角色 ops-admin(绑 61)/ member(绑 9)/ **biz-admin(绑 35,Slow-3 决议「ADMIN 内置角色边界 = 全量业务权限」承载;seed 幂等补挂每个非软删 ADMIN)**;**权限双轨收口完成**(2026-06-11 goal #314-#317,冻结评审稿 [`archive/reviews/slow4-rbac-business-face-review.md`](archive/reviews/slow4-rbac-business-face-review.md)):业务面 7 模块 44 端点摘 `@Roles`(**全仓活跃 `@Roles` = 0**,RolesGuard 机制保留),42 端点 Service 层判权 + activities 列表/详情 2 端点仅登录;`member.delete.record` 仅 SA(D1=A 镜像);零行为漂移由 7 个权限边界 spec(52 例)锁定
- **attachments + storage**:多态附件 + 配置三表(不合表不抽 facade)+ 业务面 `rbac.can()` 首批(20 码);Local / COS Provider + AES-256-GCM + fail-fast
- **SMS 基础设施**(2026-06-10,goal T0-T4;冻结评审稿 [`archive/reviews/sms-verification-infra-review.md`](archive/reviews/sms-verification-infra-review.md)):通道层(`sms/` 模块,DevStub/腾讯云双 Provider 动态路由 + settings/send-logs 4 端点 + `SMS_ENCRYPTION_KEY` AES-256-GCM)+ 验证码服务(6 位/5min/单活码/错 5 次作废/防刷三层/明文三不)+ 手机号绑定(`me/phone` 发码+验绑换绑 + admin 清号;phone 唯一含软删占用);purpose 三值 `PHONE_BIND` + `PASSWORD_RESET` + `LOGIN`(2026-06-11 B 队列 +LOGIN);BizCode 24xxx 段 6 码;权限码 76→**81**;AuditLogEvent +3(手机号一律掩码);**真实通道未开通**(运维接力 SOP:[`ops/sms-production-rollout-checklist.md`](ops/sms-production-rollout-checklist.md),**两模板一批送审:验证码 + 生日祝福**);消费者三项(找回密码 / OTP 登录 / 生日祝福)均已落地
- **找回密码(SMS 验证码重置,pre-auth)**(2026-06-11,goal T0-T3;冻结评审稿 [`archive/reviews/password-reset-by-sms-review.md`](archive/reviews/password-reset-by-sms-review.md)):`auth/v1` 两公开端点(`password-reset/send-code` + `password-reset`,`@PasswordResetThrottle()` 第 6 throttler 实例 IP 3/60s);**防枚举**(四种无效号码场景同泛化 200 零留痕 + reset 一切失败统一 24010 + 零新增 BizCode/权限码);10006 不烧码可同码重试;重置后效 = 同事务改密 + 联动撤销第 5 场景 `'self-password-reset'` + audit `password.reset.by-sms`(掩码);access 沿 D-4 不吊销;AuditLogEvent +1(`password.reset.by-sms`,union 共 30 项);**DevStub 已全验,真实短信仍只卡腾讯云审核**(运维接力同上行)
- **OTP(验证码)登录(pre-auth,密码登录的并行方式)**(2026-06-11,B 队列 goal F4 #325/#326;冻结评审稿 [`archive/reviews/queue-b-otp-birthday-infra-review.md`](archive/reviews/queue-b-otp-birthday-infra-review.md) §5):`auth/v1` 两公开端点(`login-sms/send-code` + `login-sms`,`@LoginSmsThrottle()` **第 7 throttler 实例** IP 5/60s);**防枚举沿找回密码范式**(四无效场景泛化 200 零留痕 + 登录一切失败统一 24010,不用 10004;零新增 BizCode/权限码);**会话签发与密码登录同构**(`AuthService.createSession` 单一代码路径:同 LoginResponseDto / 同 refresh family / lastLoginAt 同步);audit `auth.login.sms`(union 共 **31** 项,掩码);**AGENTS §8 登录契约行已解锁改写**(密码登录契约零变化,既有 e2e 断言零修改);不更新 phoneVerifiedAt / 不自动注册 / 无二要素
- **生日祝福短信(notifications 模块,G-7 首个落地点;本仓唯一定时任务)**(2026-06-11,B 队列 goal F5 #327/#328;评审稿 §6):`@nestjs/schedule` 锁 6.1.3 + `ScheduleModule.forRoot()`(**no-cron 升级路径已正式触发,解锁范围仅生日批**);每日 09:00 Asia/Shanghai 选取「`MemberProfile.birthDate` 月日=今天(UTC+8 日界)+ profile 未软删 + Member ACTIVE + User 绑 phone 且 ACTIVE」逐个单发(**仅 `User.phone`**;2/29 仅闰年发);**幂等防重发**(查 send_logs 当日同模板同号 SENT;重启不重发);失败记 FAILED 不重试不阻断;不进 audit_logs;`SmsSettings +templateIdBirthday`(零变量模板);零新端点/零权限码;**单实例部署前提**(多实例需先加锁;全部 7 处进程内耦合的横向扩容 checklist 见 [`deployment.md`](deployment.md) 顶部注记);docker-smoke 含启动锚行验证;**SMS 数据 retention 手动 SQL SOP** 同期收口([`ops/sms-data-retention-sop.md`](ops/sms-data-retention-sop.md):90 天/1 年,不解锁 cron 清理)
- **微信小程序登录基础设施(openid 绑定 + 登录;"正确但休眠")**(2026-06-12,goal P1-8 T0-T4 #352-#356;冻结评审稿 [`archive/reviews/wechat-mini-login-review.md`](archive/reviews/wechat-mini-login-review.md);**ARCHITECTURE §9「第一个小程序产品要接」升级路径正式解锁**):**通道层**(`wechat/` 第 23 模块:`wechat_settings` 单例表〔providerType `DEV_STUB|WECHAT`〕+ `WECHAT_ENCRYPTION_KEY` AES-256-GCM 独立 key·独立 salt + `system/v1/wechat-settings` 三端点〔reset 仅 SA〕+ 双 Provider——DevStub 确定性假 openid / 真实 `code2session` **原生 fetch 8s 零新依赖**,session_key 解析即弃);**认证三公开端点**(`auth/v1`:`login-wechat`〔已绑 `createSession` 同构签发 / 未绑 `bindingRequired:true`;第三个独立认证端点,密码登录契约零变化〕+ `wechat-bind{,/send-code}`〔绑定锚点 = 手机短信 `SmsPurpose.WECHAT_BIND`,七步校验顺序冻结,防枚举沿 login-sms 泛化 200 + 统一 24010〕,`@LoginWechatThrottle()` **第 8 throttler** 5/60);**authed**(`GET/PUT app/v1/me/wechat` 查询/换绑一体,openid 一律掩码回显,沿 me/phone 账号级豁免)+ admin 清除(幂等,解绑唯一路径);`User.openid` 唯一含软删占用;BizCode **25xxx 段 4 码**(25002/25010/25030/25031;25001/251xx 不开)+ 权限码 +4(117→**121**,ops-admin 58→**61**)+ AuditLogEvent +4(union 共 **35**,openid 一律掩码);**真实可用待二事**(运维注册小程序录 AppID/AppSecret 沿 [`ops/wechat-mini-production-rollout-checklist.md`](ops/wechat-mini-production-rollout-checklist.md);小程序前端未启动);DevStub 全链 e2e 已验(登录两路/绑定全链/防枚举一致性/限流/幂等)
- **保险模块(自购自助 + 队统一保单 + 活动报名门槛;顶层设计 §8 保险域首落地)**(2026-06-13,goal T0-T4 #363/#365/#366/#367;冻结评审稿 [`archive/reviews/insurance-module-review.md`](archive/reviews/insurance-module-review.md)):**三表**(`member_insurances`〔自购:保险公司/保单号/到期必填+起保可选,`coverageEnd` 是有效性唯一依据,自报即可 v1 无核验〕/ `team_insurance_policies`〔队统一保单一张=一条〕/ `team_insurance_coverages`〔保单×队员 join,partial unique (policyId,memberId) WHERE deletedAt IS NULL〕)+ `Activity.requiresInsurance @default(false)`(默认 false=迁移安全);**第 24 模块 `insurances/` 14 端点**:App 自助 `app/v1/me/insurances` ×4(**self-scope 锁 memberId 不接 RBAC**,防 IDOR 26001 防侧信道)/ 队保单 `admin/v1/team-insurance-policies` ×9(CRUD+覆盖名单单加/**全体在册一键加幂等仅 ACTIVE**/移除;保单软删不级联覆盖行)/ admin 查队员保险 ×1(`member-insurance.read.other`);**报名门槛**:`requiresInsurance=true` 时报名 create **双路径**(admin 代报名+自助)事务内校验「自购有效 **或** 队保单覆盖,任一即可」(北京日粒度含等号,快照不回溯),否则 `INSURANCE_REQUIRED=26030`;BizCode **26xxx 段 6 码**(261xx 不开)+ 权限码 +7 全绑 biz-admin(121→**128**,自助侧零码)+ AuditLogEvent +8(union 共 **43**)+ placeholder +1(29);baseline §1.1 红区加段(goal 授权);**v1 不做**(评审稿 §10):理赔/到期主动提醒/核验工作流/**保单图 attachments 接线**(ownerType 语义已预留,enum 分支+8 权限码单独立项)/App activities DTO 暴露 requiresInsurance;真实保险数据待业务侧录入
- **招新一期(招新前段:报名 + 实名核验 + 临时编号 + 通知;顶层设计 §招新业务域首落地)**(2026-06-18,goal T0-T4 #373/#374/#375/#376;冻结评审稿 [`archive/reviews/recruitment-phase1-review.md`](archive/reviews/recruitment-phase1-review.md)):**三表**(`recruitment_cycles`〔轮次,至多一个 open;`tempNoSeq` 行级原子发号〕/ `recruitment_applications`〔报名,**临时编号绑此表永不进 members**;FK 仅 → cycle;同轮身份证号 partial unique〕/ `realname_verification_settings`〔实名核验通道单例〕)+ **实名核验通道层第 25 模块 `realname/`**(镜像 wechat/sms:`system/v1/realname-settings` 三端点 + 双 Provider〔DevStub 校验位奇偶两路 + 真实腾讯云 fetch+TC3 签名 8s **休眠**〕+ `REALNAME_ENCRYPTION_KEY` 两段 AES-256-GCM;BizCode 270xx 2 码)+ **报名业务第 26 模块 `recruitment/`**(**`open/v1` 公开 surface 首用**:`@Public` multipart 报名提交〔证件照走 storage 短 TTL signed-URL,L3 不入日志〕+ 本人查询〔凭 wx.login code 换 openid〕,`@RecruitmentThrottle()` **第 9 throttler** IP 10/3600;admin 轮次 ×4 + 报名 ×4〔列表掩码 / 详情全显 / 取证件照 signed-URL / 人工 resolve〕);**校验顺序冻结**(付费实名核验 = 最后一道闸:open 轮 → 校验位 + 年龄 18-60 → code2session → 同轮身份证号去重 → 证件照落 storage → 建申请 + audit → 付费 verify → matched 发临时编号 `T{year}{seq}` / mismatch rejected;外籍走人工 `manual_review` → admin resolve);**两层身份铁律**(报名者**不**建 User / **不**进 members,phase-2 promote 才建 User+Member,出本期范围);BizCode **27xxx + 28xxx 段共 10 码**(270xx 2 + 280xx 8;271xx/281xx 不开,「实名核验不匹配」非 BizCode 是 verify 结果驱动状态机)+ 权限码 +8(128→**136**:realname-setting 3 + recruitment-cycle 3 + recruitment-application 2,后 5 全绑 biz-admin;reset.credentials 仅 SA)+ AuditLogEvent +5(union 共 **48**)+ placeholder +2(**31**);**身份证号 v1 明文入库**(同 `member_profiles.documentNumber` 口径,加密/哈希归既有 C-8 合规议题单独处理,见 §3);**真实腾讯云核验未开通**(运维接力 SOP [`ops/realname-verification-rollout-checklist.md`](ops/realname-verification-rollout-checklist.md));**失败者脱敏行级留存**(手动 SQL + storage blob 删除 SOP [`ops/recruitment-data-retention-sop.md`](ops/recruitment-data-retention-sop.md),**不解锁 cron**);DevStub 双通道全链 e2e 17 例已验(报名全链/校验失败/外籍人工/编号按序唯一/防重/轮次开关/容量满/付费前置免费校验/核验入审计/signed-URL/掩码/RBAC 边界/members 零增长);**一期 v1 不做**(phase-2 promote 已于 2026-06-19 落地,见下条):phase-3 / 理赔类 / 到期提醒 / 小程序前端。**⚠ 实名核验通道 2026-06-22 已 OCR 改造**(二要素真实性核验 → 腾讯云 OCR 证件识别;`mismatch→manual_review` 不再 `rejected`;详见下「招新实名环节 OCR 改造」条),**本条的 realname 双 Provider〔校验位奇偶〕/ 付费 verify / mismatch→rejected / 校验顺序描述为 phase-1 历史快照,以 OCR 改造条为当前事实**
- **招新二期(招新后段:门槛 → 综合评定 → 公示 → 一键发永久编号 = 建 User+Member;承接招新一期)**(2026-06-19,goal T0-T4 #383/#384/#385;冻结评审稿 [`archive/reviews/recruitment-phase2-review.md`](archive/reviews/recruitment-phase2-review.md)):入口态 `verified` → **状态机 +3 态**(`pending_evaluation`/`publicity`/`promoted`,沿 statusCode String **无 migration**)+ `eliminationStage` +2(evaluation/threshold-timeout);**schema 加列**(`recruitment_applications` +5〔`thresholdMarks Json`/`promotedMemberId`/`evaluatedByUserId`/At/Note〕+ `recruitment_cycles` +`memberNoSeq` + `MemberProfile` +`idCardImageKey` + **`email` 放宽可空**〔M-1〕;第 20 migration 纯加列无破坏)。**admin 面 +4 端点**:`PATCH .../applications/:id/thresholds`(标 5 门槛〔巡山×2/培训/红十字/BSAFE〕**轻量标记**,幂等,谁标/何时;**不打通活动/考勤/证书**只判完成与否;末次完成自动→待综合评定)/ `POST .../evaluate`(单一人工闸:通过→公示·不通过/门槛超期→淘汰)/ `GET .../cycles/:id/publicity-list`(公示名单 = 姓名 + 拟发编号,**拼音序零敏感**)/ `POST .../cycles/:id/promote`(**一键发号**单事务:按拼音序发 `{YY}{NNN}` + 建 User+Member+member_profiles+emergency_contacts)。**拼音 = Node 自带 full-ICU `Intl.Collator('zh-u-co-pinyin')` 零依赖**(不触 ARCHITECTURE §9);**永久编号 `{YY}{NNN}`**(年后两位 + 当年流水 `memberNoSeq` 行级原子;撞 999→28043 报错不扩位);**两层身份铁律**:promote 建的 Member **无部门、无级别**(phase-3 入队才有);**外籍 = admin 手动建档边界**(M-1:一键发号仅大陆可派生 birthDate+gender 项,外籍走完门槛/评定/公示仍 **skip+report 不进发号、需 admin 手动建档**,见 §3/§4);**原子 + 幂等 + 失败可恢复**(单事务全或无、号段连续、promoted 离开 publicity 重跑命中 0、整批回滚 seq 复位)。BizCode **28041-28043 3 码** + 权限码 +3 全绑 biz-admin(136→**139**)+ AuditLogEvent +3(union 48→**51**);**wrinkle① 证件照**留裸 key 进 `member_profiles.idCardImageKey`(不进 Attachment/不触 E-20);**wrinkle③ User** = openid 主 + 随机口令(密码登录天然关闭)+ username=memberNo(**零 auth 改**);**promote 即时清报名敏感**(PII 搬 member,留存 SOP 不再触 promoted 行)。e2e recruitment 17→**33**(+5 T2 门槛/评定/公示 + 5 T3 发号建 User+Member+迁移/幂等/外籍 skip/999 回滚);**全绿** + `rbacmap`/`codemap` 0 FAIL/0 WARN;**v1 不做**:phase-3(入队 10 项/部门/级别)/ 打通活动考勤证书给报名者 / 外籍一键发号 / 接真实腾讯云 / 新 cron
- **招新三期(入队:志愿者 → 队员;10 项考核 + 综合评估 → 设部门 + 级别 level-1;承接招新二期,招新业务域三段闭环)**(2026-06-19/20,goal T0-T5 #391/#392/#393/#394;冻结评审稿 [`archive/reviews/recruitment-phase3-review.md`](archive/reviews/recruitment-phase3-review.md)):入口态 = phase-2 promote 出的志愿者(member ACTIVE、**gradeCode=null 无级别 / 无 member_departments 无部门**、有 User 可登录)。**第 27 模块 `team-join/`** + **两新表**(`team_join_cycles` 入队轮〔annual,至多一个 open〕/ `team_join_applications` 入队申请〔**有真实 `memberId` FK**,与招新表「无 Member FK」相反;`gateMarks Json` 落 8 通用 + 0~4 专业队 gate;`targetOrganizationIds` 候选数组;partial unique `(memberId,cycleId) WHERE deletedAt IS NULL AND statusCode<>'rejected'`〕)+ 第 21 migration + seed(`member_grade` demo→**level-1~7**〔R13 code 稳定 / label 占位待运营〕+ node_type 4 专业队 code)。**状态机**(纯 String 无 migration):`joining`→`pending_evaluation`〔8 通用 gate 全过 + 贡献值≥5 自动推进〕→`approved`→`joined`/`rejected`。**admin 面 9 端点**:入队轮 CRUD 4 + 报名 list/detail + **标 gate**(8 通用 admin + 4 条件性专业队;通过/未通过 + 完成日→有效期〔本轮按**北京日历日** / first-aid 3年 / military 2年 / 长期〕+ dept-assessment 可延长期;幂等)+ **综合评估**(单一人工闸;approve 前重校门槛+贡献值)+ **一键入队**(`POST .../:id/join`:单事务原子设 `member_department` + `gradeCode='level-1'` → joined;**直连 prisma 不复用 service 防环**;专业队需对应 `team-*` gate;`evaluationExtendedUntil` 消费〔open 同轮认/closed 须延长期〕;原子/幂等/两层身份转换)。**app 自助面 3 端点**(`app/v1/me/team-join/*`):发起申请 + 选候选部门 + 查进度 + 改候选(AppIdentityResolver 准入 → 403;**self-scope 锁 memberId 防 IDOR**;候选 org 存在+ACTIVE 校验;零权限码)。**贡献值只读汇总**:`_sum contributionPoints`,approved sheet + `checkInAt < 入队年 3-31` cutoff,历史累计,Decimal 精度,实时算不落库。**wrinkle**:①专业队 = node_type code 约定识别(不改 Organization)/ ②证书类 gate v1 admin 标记(不接 `isQualified`)/ ③贡献值 approved-only 实时算。BizCode **282xx 8 码**(28201-28203/28210/28230/28240-28242)+ 权限码 +7 全绑 biz-admin(139→**146**;app 自助零码)+ AuditLogEvent +6(union 51→**57**)+ CODEMAP 模块 26→**27** / migration 20→**21**。e2e team-join admin 27 + app 11;**全绿** + `rbacmap`(146)/`codemap` 0 FAIL/0 WARN;**6-lens 对抗审查(一键入队)零确认问题**。**v1 不做**:phase 3 之后〔退队/晋升/多部门常态/级别历史〕/ 接 `CertificatesService.isQualified` 自动核验 / 部门级权限细分 / 真实腾讯云 / 新 cron / 贡献值年度窗口
- **招新实名环节 OCR 改造(二要素真实性核验 → 腾讯云 OCR 多证件识别;放弃联网验真)**(2026-06-22,goal「招新实名环节」T1+T2 #427;冻结评审稿 [`recruitment-realname-ocr-review.md`](archive/reviews/recruitment-realname-ocr-review.md);**当前事实,supersede 上「招新一期」条的 realname 二要素描述**):`realname/` 第 25 模块**语义换血**——Provider `verify(name,idCard)→{matched}` 改 `recognize(documentTypeCode,image)→{字段+防伪 warnings+清晰度}`,三 action 按证件类型分流(`RecognizeValidIDCardOCR` 身份证〔自带防伪〕/ `MLIDPassportOCR` 护照〔仅机读〕/ `MainlandPermitOCR` 回乡证〔仅来往内地〕)走 `ocr.tencentcloudapi.com`(service `ocr` / version `2018-11-19`),**复用现有 TC3 签名**(`buildSignedHeaders` 参数化 action,零新依赖)+ DevStub 改确定性 OCR 桩(证件照当 JSON 信封回显);**全仓无 `IdCardVerification` 调用路径**(grep 自证)。报名 submit **OCR 前置 + 单事务建终态**:`mainland_id` OCR **匹配+防伪无告警+清晰→自动 verified 发号**;**不匹配/防伪告警/不清晰/OCR 上游失败→manual_review(不再 rejected,「对不上转人工不误杀」)**;护照/回乡证→manual_review(识别端 OCR 回填 + 人工最终,提交端不重识别);台胞证/外国人永居/其余→manual_review 不 OCR。**退役 `pending_verification` 在途态 + FM-A 卡死类**(OCR 移到唯一事务之前,失败整体回滚无残留);`resolveManual` 只解 manual_review(人工最终权威,approve 含 OCR 不匹配的也可放行)。**新增公开识别端点** `POST open/v1/recruitment/applications/recognize`(`@Public` 第 9 throttler;无状态扫证件回填供确认;非 OCR 类型 `ocrSupported:false` / 不清晰 `clarityOk:false`;27030/27031 仅识别端浮现,提交端转人工不外抛)。**footprint 极小**:**零 schema migration**(`verifyOutcome` String 加细分值 forgery_warning/ocr_unclear/ocr_error/category_mismatch 零 migration)+ **零新 BizCode**(复用 27030/27031;OCR 失败/不清晰/不匹配非错误码)+ **零新权限码**(识别端 `@Public`)+ **AuditLogEvent union 零变**(`realname-verify` 语义重定)+ **零新依赖**;EXPECTED_ROUTES 228→**229**;`realname-settings` 三端点零行为漂移。**真实腾讯云 OCR 通道未开通**(运维接力 SOP [`ops/realname-verification-rollout-checklist.md`](ops/realname-verification-rollout-checklist.md) 已改 OCR 口径);DevStub 全链 e2e 已验(识别端点四路 + 提交端 verified/manual_review 翻转 + manual resolve + 防伪/不清晰);**收口并入下个 minor**
- **内容发布模块(CMS:公告/公示/简报/推文;第 28 模块,D 档功能串)**(2026-06-21,goal T0-T5;冻结评审稿 [`content-module-review.md`](archive/reviews/content-module-review.md)):**单表 `contents`**(正文 Markdown 原文 + 5 档可见 `visibilityCode` + `visibleOrganizationIds String[]` + `tags String[]` + `coverImageKey`/`coverAttachmentId` 反范式封面双指针 + `viewCount` 累计 PV + pinned + publishedAt + authorUserId〔无 FK〕)。**状态机** draft→published(置 publishedAt)→archived;unpublish published→draft(保留 publishedAt);archived 冻结(立即生效**无 cron**;非法跃迁 29030)。**`content/` 16 端点**:admin `admin/v1/contents` ×12(CRUD + publish/unpublish/archive + 附件 Mode B upload-url/confirm/删 + 封面设清)/ open `open/v1/contents` ×2(`@Public` + 第 10 throttler `content-public` 60/60s;仅 published+public;detail 防枚举 404;viewCount+1)/ app `app/v1/contents` ×2(canUseApp 准入;**5 档可见性纯函数过滤** public/member/formal_member/department/management;viewCount+1)。**附件复用 attachments**(α 决议):封面/正文图/文件统一 Attachment 行(ownerType `content-image`/`content-file`,经 AttachmentsService 写路径 RBAC);**读取面 content 自签 + 仅过文章可见级后返签名 URL**(范围例外 a,§5.7;**演进 Slow-4「biz-admin 不含 attachment.* 码」→ 仅含 CMS content-* 4 码**);正文图 `![](attachment:<id>)` 占位**读时改写**(仅本文章 content-image id,外来 id 不泄露)。**搜索**(keyword ILIKE 标题+正文)**+ 标签**(hasSome)读取面均 **AND 可见性不旁路**。权限码 146→**155**(content.* 5 + attachment.content-* 4 全绑 biz-admin;app/open 读零码)+ BizCode **290xx 段 5 码**(29001/29010/29011/29012/29030;附件错误复用 13xxx)+ AuditLogEvent +4(union 57→**61**:`content.{create,update,delete,publish}`〔publish 伞含 unpublish/archive〕)+ `content_type` 字典(announcement/publicity/briefing/post,label 占位)+ 2 条 AttachmentTypeConfig(content-image jpeg/png/webp 10MB / content-file pdf/docx/xlsx 20MB)。**v1 不做**(评审稿 §10):content_reads 已读回执 / 评论点赞 / 定时发布·cron / UV·时序分析 / 部长角色·部门级内容权限细分 / 招新公示自动桥接 / attachment Mime·SizeLimit override / 正文图 client 端 key 解析
- **字典内置 + 防误删守卫 + R13 收窄 + 组织树内置(v0.28.0,goal「字典内置」#421/#422 + 「组织树内置」#423/#424)**:**字典 seed 内置真实值**——国标参照(gender / blood_type / marital_status / political_status / document_type / education / ethnicity 56 民族 / emergency_relation)+ 队内真实值(member_grade 9 项〔volunteer + level-1~7 + reserve〕/ activity_type 二级树 **9 父 + 31 子**〔#422 微调终态〕)替换 demo 占位;**防误删守卫**(`dictionaries.service.ts` 常量 `SYSTEM_PROTECTED_DICT_TYPES` 21 类禁【类型】软删 + `ITEM_PROTECTED_DICT_TYPES` 16 类禁【项】软删;BizCode `DICT_TYPE_SYSTEM_PROTECTED`=12003 / `DICT_ITEM_SYSTEM_PROTECTED`=12015,与 IN_USE 引用检查并存);**R13(A-9 红线)收窄**为「仅禁成员 PII〔姓名/身份证/手机号〕+ 真实 memberNo,非敏感分类字典可内置 seed」(权威源 `V2红线与复活路径.md` A-9);**组织树内置**——`Organization` + `code String? @unique`(第 23 migration `add_organization_code`,仅加列)+ `seedOrganizations` 16 org(根 `深圳公益救援队`/SRVF/headquarters + 15 部门〔含 THQ 联合会〕扁平两层;node_type demo→8 真实分类,4 `professional-*` code 保留供 team-join 门槛),organizations API 暴露 `code`(可选入参 `^[A-Z0-9-]+$` + 响应字段;`ORGANIZATION_CODE_ALREADY_EXISTS`=11033,含软删占用 findUnique 预检查 + P2002 兜底)
- **活动闭环两处硬化(v0.28.0,goal #420)**:① **报名截止生效**——`registrationDeadline` 非 null 且 `now > deadline` 拒报名(自助 `createMy` / App `createMyForApp` / admin `create` 三路都拦,`approve` **不拦**〔截止前已报 pending 仍可批〕;`ACTIVITY_REGISTRATION_DEADLINE_PASSED`=20123,精确时刻不做北京日归一);② **贡献值全局每日封顶 1.5**——由「每条记录各自封顶」改为「一人单北京日历日总分封顶」,封顶移到 team-join `computeContribution` 汇总处(按 `checkInAt` 北京日分组,`GLOBAL_DAILY_CONTRIBUTION_CAP=1.5`),直接影响入队 ≥5 gate;`ContributionRule.dailyCap` 列标 **deprecated** 保留不删、calculator 不再读(零 schema / 零 migration)
- **队员/审批跨轴只读查询(支撑前端任务驱动后台;交接层 GAP-001 Tier2 / GAP-002 Tier3)**(2026-06-23,goal「队员/审批『跨轴只读查询』补全」;`## Unreleased`,未 bump):后端报名/考勤本按所有权轴(活动/队员)嵌套查询,沿轴下钻已全有;本 goal 补**跨轴横扫**只读缺口 **+5 admin 端点**(全 `/api/admin/v1/*`):**Tier2 审批工作台**——`GET /registrations` + `GET /attendance-sheets`(跨所有活动按可选 statusCode 横扫,脱离 `:activityId` 路径段;后者为既有 `AttendanceSheetsResourceController` 加根 @Get 不新增 class);**Tier3 队员 360**——`GET /members/:memberId/{registrations,attendance-records,contribution-summary}`(某队员报名履历 / 考勤记录〔仅 approved sheet,镜像 app /me Q-A14〕/ 贡献值**生涯累计 capped 总分**)。贡献值**实时算不落库**,复用 team-join `computeCappedContribution` 封顶核(approved sheet + 北京日封顶 1.5,生涯无 cutoff;**禁裸 SUM** 绕过封顶会算多);跨活动/跨人 item 自带 activity 上下文(activityId/title,Prisma 嵌套 select **无 N+1**);MEMBER_NOT_FOUND 守卫镜像 `admin-member-insurances`。**footprint 极小**:复用现成 read 码(`activity-registration.read.record` / `attendance.read.sheet`,**零新权限码** 155 不变)+ **零 BizCode / 零 migration / 零 schema 列 / 零 enum / 零 audit event(纯读)/ 零新模块 / 零新依赖**;controller 49→**52**(+3 新 class:`AdminRegistrationsController` / `AdminMemberRegistrationsController` / `AdminMemberAttendanceController`);**既有嵌套路径端点零行为变更**(只新增只读 surface,不动 god-service 业务逻辑、不 deprecate 旧端点);EXPECTED_ROUTES 229→**234**;同 PR 更新交接层 [`handoff/admin-web.md`](handoff/admin-web.md)(GAP-001/002 → 已发,§2.2/2.3 ⛔→✅,沿反漂铁律)
- **招新→入队闭环优化 S1(状态业务文案 + 新人进度模型;GAP-006 首切片,纯读冷启动)**(2026-06-24,goal「招新闭环优化 S1」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §4/§6;`## Unreleased`,未 bump):**公开本人查询**(`POST open/v1/recruitment/applications/query`)出参由 6 字段 `RecruitmentApplicationPublicDto` **enrich 为进度模型** `RecruitmentApplicationProgressDto`(`stage`/`stageText`/`statusText`/`nextAction`/`tempNo`/`memberNo`/`identityText`/`todoList[{code,name,done}]`/`meetingInfo`/`qqGroup`/`notice`;**submit 端点仍返旧 DTO**)。**单一共享 stage 派生纯函数** `deriveRecruitmentStage`([`recruitment-progress-presenter.ts`](../src/modules/recruitment/recruitment-progress-presenter.ts);`statusCode`+门槛完成度 → 业务态 `stage`/动作码/身份文案,**供 S2 工作台 stats 复用**)覆盖 7 现有数据可派生态(manual/threshold/threshold_done/evaluation/publicity/volunteer/rejected;`retake`/`confirm`/`manual_high` **预留枚举位不实现**待 S4 会话表/riskLevel);**`promoted`→`stage=volunteer` 文案「已转志愿者/待入队」,全程禁「已晋升」**(Q-P4-8;单测 + seed 双自证 0 occurrence)。门槛 `todoList` 由 `thresholdMarks` **真投影** 5 项(done 来自实际数据,非写死)。展示文案落 **`recruitment_stage` 字典**(seed +7 态 additive 幂等 + 防误删守卫 `SYSTEM`/`ITEM_PROTECTED_DICT_TYPES` 各 +1 类〔22/17〕;`stageText` 后台可维护,§4.1「后端不存展示文案明文」);**覆盖边界**:promote 即清 openid → 公开查询天然不可达 promoted 行,`memberNo` 恒 null(尾段经登录态 app 侧另见)。**footprint 极小**:**零 schema/零 migration/零 statusCode 流转或状态机改动(§14 行为锁 #6 纯展示派生)/零 BizCode/零权限码/零 audit/零依赖**;EXPECTED_ROUTES **234 不变**(仅 query 响应 schema 改 + 2 新 DTO);同 PR 更新交接层 [`handoff/miniapp.md`](handoff/miniapp.md)(能力图 + GAP-006 ledger)+ `handoff/openapi.json`(v0.30.0 true-up)。S2(工作台 stats)~S7(通知,阻塞 GAP-005)待后续切片另出 goal
- **招新→入队闭环优化 S2(招新工作台 stats;GAP-006 第二切片,纯读聚合)**(2026-06-24,goal「招新闭环优化 S2」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §7;`## Unreleased`,未 bump):新增**管理端聚合只读端点** `GET /api/admin/v1/recruitment/cycles/:id/stats`(`RecruitmentCyclesController` 加路由委托**新 QueryService** [`recruitment-stats.service.ts`](../src/modules/recruitment/recruitment-stats.service.ts);判权 R 模式 `rbac.can('recruitment-application.read.record')`,**零新 RBAC 码**),返评审稿 §7.1 **五组**:**今日数据**(`createdAt`/`verifiedAt`/`reviewedAt` 北京日界当日,沿 birthday/sms `startOfDayUtc8` 同口径)/ **待处理事项**(待人工 + `normal`/`high`/`system` 三栏 = `verifyOutcome` 代理〔system=`ocr_error` / high=`forgery_warning` / 余 normal〕,**Q-P4-3 精确 `riskLevel` 三栏待 S4**,出参 + 注释已标注 / 待综合评定 / 待发号)/ **门槛进度**(跟踪中人数 + 5 项完成分布,`thresholdMarks` 真投影)/ **综合评定**(待评定 / 已通过进公示 / 评定淘汰〔rejected+`eliminationStage=evaluation`〕)/ **公示发号**(公示中 / 可一键发号 / 需手动建档 / 已发号)。**零漂移口径**:各业务态计数**复用 S1 `deriveRecruitmentStage`**(单一 stage 派生,**不另立第二套判定**;评审稿 §7.1「待处理事项即各 stage 计数」),发号预判**复用 `decidePromotionIssuance`**(与 `publicityList` / 实际 `promote` 同序同判,三处计数结构性一致)。**聚合高效**:每轮 **1 次有界 `applications.findMany`(限定该轮 + 未软删)+ 1 次 `user.findMany`**(仅公示子集 openid 占用判定),**禁 N+1 / 禁跨轮全表扫描**(`verified`→`threshold`/`threshold_done` 拆分依赖 JSON 完成度、各门槛分布需折叠 `thresholdMarks`,非 Prisma count/groupBy 可表达,故有界 fetch+派生 = 评审稿明许「thresholdMarks 折叠限定在该轮」)。**footprint 极小**:**零 schema/零 migration/零写/零 `statusCode` 流转或状态机改动/零 BizCode/零新权限码(155 不变)/零 audit/零依赖**;EXPECTED_ROUTES 234→**235**(+1 路由 existence + 7 出参 DTO,snapshot 受控);controller **52 零变**(加路由不加 class)、新增 `recruitment-stats.service.ts` 为模块内 QueryService **非新模块**;同 PR 更新交接层 [`handoff/admin-web.md`](handoff/admin-web.md)(能力图 + GAP-003「招新进度」🟡 部分交付 + GAP-006 S2 进展)+ `handoff/openapi.json` true-up。S3~S7(S5 promote 志愿者化最重一刀;S7 通知阻塞 GAP-005)待后续切片另出 goal
- **招新→入队闭环优化 S3(RBAC 敏感字段分级;GAP-006 第三切片,判权细化零 schema)**(2026-06-24,goal「招新闭环优化 S3 — RBAC 敏感字段分级」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §11 / Q-P4-10;`## Unreleased`,未 bump):原 `recruitment-application.read.record` **一码看尽**脱敏列表 + **明文详情** + 证件照 signed-URL + 公示名单 + 工作台 stats(字段级无分级,合规风险)。本切片把「敏感查看」从 read.record 切出为**新码 `recruitment-application.read.sensitive`**:**`read.record` 语义收窄**(保留码、去明文)→ 脱敏列表 + **脱敏详情** + 公示名单 + 工作台 stats;**敏感路径改判 `read.sensitive`**——报名详情端点入口闸仍 `read.record`,持 `read.sensitive` → 明文证件号/手机、仅持 `read.record` → 脱敏详情(`detailForAdmin` 改 `toAdminDto(masked = !can(read.sensitive))`,**响应字段集不变、仅 masking 随码**,contract DTO snapshot 零变佐证);证件照 signed-URL 端点闸 `read.record`→`read.sensitive`。**迁移零行为回退**(评审稿 §11.2):新码经既有 `BIZ_ADMIN_PERMISSION_SEED` 过滤**默认补挂 biz-admin**(additive 幂等,seed 二跑无漂移),因业务面码现全绑 biz-admin 无其他角色,**本切片对现有用户零行为变化**(明文照旧),字段级分级仅对将来细分子角色生效。**footprint 极小**:**零 schema/零 migration/零 `statusCode` 流转或状态机改动/零 BizCode/零 audit event/零依赖/零新端点·controller**;权限码 155→**156**(`prisma/seed.ts` `RECRUITMENT_APPLICATION_PERMISSION_SEED` +1,全绑 biz-admin → 绑定 66→**67**;ops-admin 63 / member 9 零变化);EXPECTED_ROUTES **235 不变**(详情 + 证件照 2 端点仅 `@ApiOperation` summary 改判 rbac 后缀,paths snapshot 受控 / DTO snapshot 零变);`read.sensitive` 实装即用 **0 孤码**。判权矩阵 **unit**(详情明文/脱敏/30100 + 证件照 200/30100,mock `rbac.can` 按码返值)+ **e2e**(`read.sensitive` / 仅 `read.record` / 无码 三 profile 走真实 RBAC 非短路)**双自证**;同 PR 更新交接层 [`handoff/admin-web.md`](handoff/admin-web.md)(能力图 + GAP-006 S3 进展 + 敏感分级说明)+ `handoff/openapi.json`(2 summary patch)+ 前端对接指南(`srvf-admin-web` `docs/srvf-api-integration-guide.md`,跨仓只写 docs)。S4~S7(S5 promote 志愿者化最重一刀;S7 通知阻塞 GAP-005)待后续切片另出 goal
- **招新→入队闭环优化 S4a(H5 + 报名前手机身份会话表;GAP-006 第四切片前半刀,档 D 含不可逆 enum)**(2026-06-24,goal「招新闭环优化 S4a」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §3 / Q-P4-5/6;`## Unreleased`,未 bump):原 `wechatCode` 必填 + 无微信环境(线下扫码 H5)无法自助报名/查询 = 闭环缺口。本切片建**两套入口**:小程序链(`wechatCode`)+ **H5 链**(手机验码 → 短时一次性 `phoneVerificationToken` → 提交)。**新表 `recruitment_identity_sessions`**(报名前身份会话行;`SmsPurpose.RECRUITMENT_BIND` 验码成功落行 + 发 token〔sha256 入库,明文仅返一次〕+ `consumedAt` 原子一次性 + `phone+cycle`/`token`/`expiresAt` 索引;**预建 `ocrAttemptCount`/`lastOcrOutcome`/`requiresRetake` 供 S4b OCR 六分流重拍计数,本刀不写其逻辑**;会话行**不进 `recruitment_applications`、不占容量/去重/统计** = e2e 自证);`SmsCodeService.{issue,verifyAndConsume,assertValid}` 形参 `userId:string`→**`string|null`**(E-P4-4;匿名报名人传 null,既有调用传 string 不变,归属校验 `null===null` 天然放行,phone+purpose 为锚;105 例 SMS 消费方 e2e 零回归);submit DTO `wechatCode` 改可选 + 新增 `phoneVerificationToken`(H5 提交端事务内消费会话行 → 写 `phoneVerifiedAt/phoneVerificationMethod`,小程序链向后兼容不变);`application` 加 6 手机身份链可空列(`phoneVerifiedAt/phoneVerificationMethod/phoneChangedAt/phoneChangeReason/phoneBindingHistory/phoneRiskFlag`)。**+5 公开端点**(`open/v1/recruitment` surface):`identity/send-code` + `identity/verify-code`(发码/验码发 token)+ `applications/query-by-phone`(**查询②手机+验证码**,与微信 code 查询同进度模型/同派生口径)+ `applications/rebind-wechat`(自助换微信:当前手机验码校验本人 → 换 openid,防绑他人报名 28051)+ `applications/rebind-phone`(自助换手机:**双验**当前+新手机 → 换 phone + 换绑历史)。**行为锁全保**(评审稿 §14;e2e 自证):`verified` 唯一放行 + 单事务原子发临时号 + 容量 FM-C + 去重 idCardNumber + 敏感字段掩码三不 + SMS 既有调用向后兼容,**任一未破坏**。**D 档不可逆 migration**(`SmsPurpose ADD VALUE 'RECRUITMENT_BIND'` 独立 migration 镜像 `add_sms_purpose_login` 范式回避「同事务用新枚举值」)走 srvf-prisma-change:**干净库重放 25 migration 全绿 + `migrate diff` 零漂移〔SQL==schema〕 + seed 幂等二跑**自证。**footprint**:migration 23→**25**(enum 加值 + 新表/列)· BizCode 170→**172**(`RECRUITMENT_IDENTITY_SESSION_INVALID=28050` / `RECRUITMENT_WECHAT_ALREADY_BOUND=28051`,280xx 招新段)· audit union 61→**63**(`recruitment-application.rebind-{wechat,phone}`)· **权限码 156 零变**(公开自助无 RBAC 码)· 模块 28 / controller 52 **零变**(新增 `recruitment-identity.service.ts` 356L 为模块内 service 非新模块;5 端点入既有 `RecruitmentPublicController`)· **不碰 promote/team-join/入队门禁(S5)/批量(S6)/OCR 六分流(S4b)/状态机流转**;同 PR 更新交接层 [`handoff/miniapp.md`](handoff/miniapp.md)(H5 报名/手机查询/换绑 能力图 + GAP-006 S4a 进展)+ `handoff/openapi.json` true-up(+5 路由)。S4b(OCR 重拍计数,会话表预建列已就位)/ S5(promote 志愿者化)/ S6(批量)/ S7(通知阻塞 GAP-005)待后续切片另出 goal
- **招新→入队闭环优化 S4b(OCR 六分流 + 重拍计数;GAP-006 第四切片后半刀,档 D additive 列无 enum)**(2026-06-24,goal「招新闭环优化 S4b」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §2 / Q-P4-2/3/4;`## Unreleased`,未 bump):原 `submit` 大陆 OCR「**5 分支全塞 `manual_review`**」,admin 无法分流(模糊/篡改/不一致/上游失败混一桶)。本切片把 `decideMainlandOcr` 拆为**六分流纯函数** [`recruitment-ocr-routing.ts`](../src/modules/recruitment/recruitment-ocr-routing.ts)(`classifyOcrResult` 原始信号→outcome + `routeOcrOutcome` outcome+会话计数+三选一标记→落库/延迟决策;srvf-god-service-refactor「OCR 判定抽离别堆 god-service」):**matched**+清晰+无告警→`verified`+临时号(**唯一放行行为锁不变**)/ **ocr_unclear**(模糊)→`retake` **不落记录、永不进人工**(会话行 `requiresRetake`+计数++)/ **mismatch**→申请人**三选一**(①用 OCR 回填 ②改填写〔就地纠正重判**不进人工**〕③ `applicantConfirmedOcrWrong`→落**普通人工** `riskLevel=normal`/`manualReviewReason=ocr_mismatch_confirmed`)/ **forgery_warning**→首次 `retake`(重拍原件)、**H5 会话连续 2 次**(Q-P4-4)才落**高风险** `riskLevel=high`/`forgery_suspected`/ **ocr_error**(上游失败)→首次 `retry`、**连续 2 次**才落**系统异常** `riskLevel=system`/`system_ocr_error`/ **特殊证件·非 OCR**→`manual` `special_document`/`normal`。`application` **+4 列 additive 无 enum**(`manualReviewReason`/`riskLevel`/`lastOcrOutcome` String? + `applicantConfirmedOcrWrong` Boolean@default(false);既有 `verifyOutcome` 8 值复用不删不改语义);**重拍/上游计数落 S4a 会话表预建列**(`ocrAttemptCount`/`requiresRetake`/`lastOcrOutcome`;**延迟分流不消费 token** → 身份链保活可重试,submit 落库时 `lastOcrOutcome` 快照进 application);**无会话(小程序链)退化**为客户端计数 + IP 限流不服务端升级(Q-P4-1)。submit 出参 `RecruitmentApplicationPublicDto`→**`RecruitmentSubmitResultDto`**(`outcome` = submitted/retake/confirm/retry 判别;`confirm` 回带 `recognized` 供回填;**申请人侧绝不暴露 `riskLevel`/forgery**——高风险疑似造假不提示,文案恒中性,goal 三③隐私口径)。进度模型 `deriveRecruitmentStage` **实现预留三态** `retake`/`confirm`(会话态)/`manual_high`(`manual_review`+`riskLevel=high`,**申请人侧文案中性同 manual**),`recruitment_stage` 字典 **seed +3 态**(retake「待重拍」/confirm「待核对」/manual_high「待人工核验」,additive 幂等二跑无漂移,7→10 态)。**S2 工作台待人工三栏升真 `riskLevel`**(去 `verifyOutcome` 代理,单一口径);admin 报名列表 **+`riskLevel` 过滤**、admin DTO **+`riskLevel`/`manualReviewReason`**(人工队列三栏分流 + `manualReviewReason` 分组筛,§2.4)。**两处评审稿顺修**(冻结档 typo/一致性,非改设计):`verifyOutcome` 7→**8**(§0/§1.1/§1.2 误记)+ §2.1 `ocr_error` `riskLevel` `normal`→**`system`**(与 §2.4 系统异常栏一致)。**行为锁全保**(评审稿 §2.5/§14;unit+e2e 自证):`verified` 唯一放行 + 单事务原子发临时号 + 容量 FM-C + 去重 idCardNumber + 敏感掩码三不,**任一未破坏**。**footprint**:migration 25→**26**(`add_recruitment_ocr_routing_fields` 纯 ADD COLUMN,**无 enum / 无不可逆**;干净库重放 26 migration 全绿 + `migrate diff` 零漂移 + seed 幂等二跑自证)· **权限码 156 / BizCode 172 / audit union 63 / 模块 28 / controller 52 / EXPECTED_ROUTES 240 零变**(延迟分流走 200 outcome 非 BizCode;新增 `recruitment-ocr-routing.ts` 为模块内纯函数非新模块;submit 同路由换出参 schema + admin list +`riskLevel` query param,snapshot 受控)· 同 PR 更新交接层 [`handoff/miniapp.md`](handoff/miniapp.md)(六分流提交结果 + 三选一交互)+ [`handoff/admin-web.md`](handoff/admin-web.md)(人工队列三栏 + GAP-006 S4b)+ `handoff/openapi.json` true-up。S5(promote 志愿者化最重一刀)/ S6(批量)/ S7(通知阻塞 GAP-005)待后续切片另出 goal
- **招新→入队闭环优化 S5(promote 志愿者化 + 入队门禁适配;GAP-006 第五切片「最重一刀」,档 D 推翻 E-J-6,零 schema 列)**(2026-06-24,goal「招新闭环优化 S5」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §5 / Q-P4-7,**维护者明确拍板推翻 phase-3 冻结取舍 E-J-6「双表示」**;`## Unreleased`,未 bump):原 promote 建 `Member{gradeCode=null}` + 不建部门,靠「`null`+零部门」**隐性**表达「未入队志愿者」(前端/统计陷阱)。本刀**显式化**志愿者身份并同步入队链:**promote**([`recruitment-promotion.service.ts`](../src/modules/recruitment/recruitment-promotion.service.ts) §5.2a)`member.create` 加 `gradeCode='volunteer'` + **同事务建 VOL 归口部门**(`Organization.code='VOL'` 运行时解析守 ACTIVE,**≠ VOD 志愿者组织部**;缺失/非 ACTIVE → 建任何 member **前**抛 **`28044`** 清晰失败不留半成品);**抽共享纯函数 `isUnenrolledVolunteer`**([`team-join.constants.ts`](../src/modules/team-join/team-join.constants.ts) §5.2b;新口径 `volunteer`+仅一条 VOL active 部门 / legacy `null`+零部门**双兼容**)→ 两处入队门禁(**自助发起** `team-join-applications.app.service.ts` + **一键入队** `team-join-enrollment.service.ts`)同调零漂移;**入队写**(§5.2c 单事务)新志愿者**软删 VOL 部门 → create 目标 → `level-1`**、legacy 直建目标,守 `member_departments` 单部门 partial unique(任一时刻无两条 active);`join_source` 字典补 `recruitment`(seed additive 幂等二跑无漂移);**历史 legacy 零迁移、不做 backfill**(留将来独立不可逆 D 档)。**promote 行为锁全保**(单事务全或无 / 号段连续无空洞 / 幂等不双建 VOL / 失败可恢复;unit+e2e 自证)。**ripple 有界**:app `/me`(及回带 `Member.gradeCode` 的 app 出参)志愿者 `gradeCode` `null`→`'volunteer'`(**miniapp 语义变**,已标 [`handoff/miniapp.md`](handoff/miniapp.md))/ 字典防误删守卫(`member.count` by gradeCode)→ `volunteer` 项随落库变「在用受保护」(正确后果)/ member 列表 gradeCode 过滤更准 / **不破 S1 `identityText`(键于 statusCode)+ S2 stats**。**footprint**:**0 `schema.prisma` 列 / 0 migration**(数据触点仅 seed join_source additive + 运行时建 VOL `memberDepartment` 行)· BizCode **+1**(`RECRUITMENT_VOLUNTEER_ORG_UNAVAILABLE=28044`,promote 端 `@ApiBizErrorResponse` 补登 → OpenAPI snapshot **仅** promote 409 enum +28044)· **权限码 156 / controller 52 / 模块 28 / audit event 零变**;新增 `team-join.constants.spec.ts`(双口径 8 例)+ promote/team-join/team-join-app e2e 扩(promote→`volunteer`+VOL / 两处门禁双兼容 / 入队软删VOL+建目标恰 1 条 active / legacy `null` 双兼容 / 幂等不双建 VOL / VOL 缺失 28044 / 批量号段连续);`docs:rbacmap:check`(156)/ `docs:codemap:check` 0 FAIL · unit 1748/contract 411/相关 e2e 92 实跑绿。同 PR 更新交接层 [`handoff/admin-web.md`](handoff/admin-web.md)(业务主线 + GAP-006 S5)+ [`handoff/miniapp.md`](handoff/miniapp.md)(`/me` gradeCode 语义变)。S6(批量)/ S7(通知阻塞 GAP-005)待后续切片另出 goal
- **招新→入队闭环优化 S6(批量操作;GAP-006 第六切片,档 E/feature 纯加端点零 schema)**(2026-06-24,goal「招新闭环优化 S6」;立项源 = 冻结评审稿 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §8 / Q-P4-12;`## Unreleased`,未 bump):招新工作台缺批量入口(逐个标门槛 / 逐个看发号资格)。本刀加 **3 批量端点**,**零 schema / 零新 RBAC 码 / 零 audit union 扩展**(全复用既有口径,杜绝第二套):**①批量标门槛** `POST /api/admin/v1/recruitment/applications/batch-mark-threshold`(复用 `recruitment-application.mark.threshold`):入参 = 匹配键数组(临时编号 / 手机 / 姓名+手机;**「签到记录导入」= 前端解析签到表为本数组**,后端不碰文件,匹配收敛)+ thresholdCode + completed;**逐行复用单行 `markThreshold`**(各自独立事务 → **逐行幂等 + 逐行容错**:匹配不上/状态非法**不整批回滚**,返 per-row {marked/unmatched〔no-match/ambiguous/insufficient-key〕/failed〔errorCode〕} + 批次汇总;**自动推进 pending_evaluation 语义 + per-row `mark-threshold` DB 审计全由单行承载,零第二套**);批次汇总走 `logger.log`(沿 `promote` 批量范式)。匹配纯函数抽离 [`recruitment-batch-matching.ts`](../src/modules/recruitment/recruitment-batch-matching.ts)(优先级 tempNo>姓名+手机>手机;多命中 **ambiguous 绝不猜**;srvf-god-service-refactor 抽纯逻辑不堆 god-service)。**②批量导出 CSV** `POST …/applications/export`(入口闸 `read.record`;**持 `read.sensitive` → 明文列 / 仅 `read.record` → 脱敏列**,S3 §11 分级):按筛选(全部/待人工/已初审/门槛未完成/待评定/公示/发号/淘汰)导出;**脱敏单一真相源复用 S3 `toAdminDto`(masked=!read.sensitive),CSV 只投影已脱敏 DTO → 明文绝不在无 `read.sensitive` 时出列**;`StreamableFile`+BOM 沿 `activity-registrations` CSV 范式(不引新依赖);`export` placeholder 审计(复用 `read.other` pino + `operation` 区分,含 admin/范围/脱敏级)。**③一键发号前预检** `GET …/cycles/:id/promote-precheck`(复用 `recruitment-application.promote.member`,与实发同 audience):**纯读不写、不改 promote 结论**;**逐字复用 promote 事务前分区**(`loadBoundOpenids`+`comparePromotionOrder`+`decidePromotionIssuance`)→ **结构性保证「预检=实发」**;输出 per-row 可发/跳过 + §8.2 六类原因(foreign-manual-build/openid-already-bound/missing-openid/duplicate-openid-in-batch/missing-derived-field/incomplete-data)+ 重复 openid 高亮 + 缺手机/生日/性别 flag + 特殊证件 + 拟发编号(同公示推算)+ 汇总。**批量通知不做**(§8.1/§8.3 挂 §9 / GAP-005,随 S7)。**footprint**:**0 `schema.prisma` / 0 migration / 0 enum** · **0 新 RBAC 码**(复用 mark.threshold/read.record/read.sensitive/promote.member;rbacmap 156 不变)· **0 audit union 扩展**(per-row 复用 `mark-threshold` DB 事件 + export/precheck 复用 `read.other` pino)· EXPECTED_ROUTES 240→**243**(+3,snapshot 受控)· **BizCode / 模块 28 / controller 52 / 新依赖 零变**(新增 `recruitment-batch-matching.ts` 为模块内纯函数,3 端点挂既有 2 controller);新增 `recruitment-batch-matching.spec.ts` + promotion/applications 单测扩 + recruitment e2e 扩 8 例(批量 matched/unmatched/ambiguous/幂等/逐行容错/自动推进 · 导出脱敏 vs 明文矩阵 · **预检=实发同库一致性** · 预检六类原因/高亮/flag);`docs:rbacmap:check`(156)/ `docs:codemap:check` 0 FAIL · lint/typecheck/build/unit **1771**/contract **421**/recruitment e2e **59** 实跑绿。同 PR 更新交接层 [`handoff/admin-web.md`](handoff/admin-web.md)(批量能力 + GAP-006 S6)。S7(通知阻塞 GAP-005)待后续切片另出 goal
- **统一通知模块 S1 — 站内信渠道(GAP-005 首切片;第 28 模块 notifications 扩 controller;档 D)**(2026-06-25,goal「GAP-005 统一通知模块 S1」;立项源 = 冻结评审稿 [`unified-notification-dispatcher-review.md`](archive/reviews/unified-notification-dispatcher-review.md) §5/§9/§11 supersede [`member-notification-review.md`](archive/reviews/member-notification-review.md);`## Unreleased`,未 bump):运营缺「向队员主动推送通知/公告」(GAP-005;现 notifications 仅生日 cron)。本切片 = 统一通知中枢首切片 = **站内信渠道**,镜像 content「admin 撰写/发布 + 会员拉取 + 可见性分档」+ 轻量已读表。**2 表**:`Notification`(原 T0 列 title/body/notificationTypeCode/statusCode/visibilityCode/visibleOrganizationIds/readCount/pinned/publishedAt/authorUserId + **统一形状前向兼容列** `audienceType`〔broadcast〕/`sourceType`〔admin〕/`channels`〔["in-app"]〕,S1 仅用这些值)+ `NotificationRead`(notificationId×memberId append-once,**plain unique 无软删** = 与 TeamInsuranceCoverage 刻意差异);**不加 recipientMemberId**(S3 additive)。**第 27 migration**(纯 additive,无 enum/无回填;干净库重放 27/27 + seed 幂等二跑核过)。**admin 8 端点** `admin/v1/notifications`(CRUD + 状态机 publish/unpublish/archive,**R 模式 `rbac.can` 无 @RequirePermissions**,镜像 content;非法跃迁 **31030**;readCount 回显;audit `notification.{create,update,delete,publish}` 4 事件,publish 伞盖三跃迁)。**app 4 端点** `app/v1/notifications`(list/unread-count/detail/mark-read;canUseApp 准入〔403〕+ **可见性复用 `content.visibility` 4 档去 public 零第二套** + 防枚举 404;mark-read **幂等 upsert** + readCount **原子 +1 仅首插**〔二次 no-op 不重复增〕;unread-count = NOT EXISTS〔`reads.none`〕;**字面段 `unread-count` 路由先于 `:id`**;读者出参零敏感〔无 authorUserId/visibleOrganizationIds/statusCode/readCount〕+ 每项 `read` 已读标志)。**seed**:`notification_type` 字典 4 项(activity-reminder/recruitment/emergency/general)+ 5 RBAC 码 `notification.{read,create,update,delete,publish}.record` 全绑 biz-admin。**不引 cron/queue/事件总线/Effect 类**(站内 = pull 零发送);不碰 birthday cron/sms/wechat/content(`content.visibility` 只复用不改);状态机镜像 content 不另起。**统一形状不返工**:audienceType/sourceType/channels 本切片即加(S1 仅 broadcast/admin/in-app),后续 S2 微信 quota / S3 producer 定向(recipientMemberId)/ S5 短信兜底只 additive 加列加表。**footprint**:模块 **+0**(第 28 模块扩 controller)· migration 26→**27** · 权限码 156→**161**(biz-admin 67→**72**;ops-admin 63 / member 9 零变)· BizCode 310xx **+5**(31001/31010/31011/31012/31030)· audit union **+4** · 字典 +1(notification_type)· controller 52→**54** · EXPECTED_ROUTES 243→**255**(+12)· **cron/queue/事件总线 +0**;`docs:rbacmap:check`(161)/ `docs:codemap:check` 0 FAIL。同 PR 更新交接层 [`handoff/admin-web.md`](handoff/admin-web.md)(通知管理能力 + GAP-005 进行中)+ [`handoff/miniapp.md`](handoff/miniapp.md)(站内信 feed)。S2(微信 quota)/ S3(producer 定向 + Effect 正式化,含招新 S7)/ S4(活动·考勤触发)/ S5(短信兜底)待后续切片另出 goal
- **统一通知模块 S2 — 微信订阅 quota 渠道(GAP-005 第二切片;第 28 模块扩;档 D,含真实外部 API)**(2026-06-25,goal「GAP-005 统一通知模块 S2」;立项源 = 冻结评审稿 [`unified-notification-dispatcher-review.md`](archive/reviews/unified-notification-dispatcher-review.md) §3/§6.2/§7/§8/§9;`## Unreleased`,未 bump):站内 S1 之上加 **微信订阅消息渠道 + quota 机制**,触发 = admin 发布勾微信渠道的通知 → 对**可见且已订阅**的会员机会式推送。**3 表**:`NotificationDelivery`(通知×渠道×收件人投递态 pending/sent/failed/skipped + reasonCode/providerMsgId/errCode/attemptedAt;**仅推送渠道落,站内不 fan-out**;notificationId FK Restrict、memberId 松引用)+ `WechatSubscriptionQuota`(memberId×templateId→availableCount,`@@unique`;**+1 ack 封顶 D-N2 默认 5** / **-1 条件原子 `updateMany(availableCount>0)` count===1 才发**)+ `WechatSubscribeTemplate`(notificationTypeCode→templateId 运营可配 D-N3,templateId 默认 null)。**第 28 migration**(纯 additive,无 enum/无回填;干净库 28/28 重放 + seed 幂等二跑核过)。**微信 subscribe-send 能力**(additive 扩 `wechat/` Provider,登录 code2session 零改):`getAccessToken` 调 `/cgi-bin/stable_token` + 进程内缓存 ~7000s;`sendSubscribeMessage` POST `/cgi-bin/message/subscribe/send`,原生 fetch + `AbortSignal.timeout(8s)`,**token/secret/openid 禁入 URL/日志明文**(E-12 + maskOpenid,L3);WechatService 编排 token 失效(40001/42001)刷一次重试一次,失败归一不抛。**app 2 端点** `app/v1/notifications/subscriptions`(ack 上报授权 quota +1 封顶〔诚实非去重幂等〕/ status 查剩余配额;canUseApp 无码)+ **admin 2 端点** `admin/v1/notification-wechat-templates`(list 复用 read.record / upsert 新码 `notification.update.template`;独立 base path)。**微信派发分支**(`NotificationWechatDispatchService`,在 **publish DB 事务之外**同步调用,§6.2):候选 = 有 quota ∩ 可见(复用 `content.visibility` canSeeContent)− 已 sent;逐人 openid→原子扣 quota→发送→delivery;**43101 回补 quota** + need-resubscribe / 40003 invalid-openid / 47003 template-param;非订阅者不 fan-out。**字段映射内置代码**([`notification.wechat-data.ts`](../src/modules/notifications/notification.wechat-data.ts);thing ≤ 20 截断防 47003)。admin 通知 create/update 加 `channels` 勾选(站内恒发强制含 in-app)。**不引 cron/queue/事件总线/Effect 正式化**(同步发送,§8;派发器 Effect 正式化 = S3);**BizCode 零新增**(发送失败落 delivery errCode 非 API 异常)。**footprint**:模块 **+0** · migration 27→**28** · 权限码 161→**162**(biz-admin 72→**73**;ops-admin 63/member 9 零变)· controller 54→**55** · EXPECTED_ROUTES 255→**259**(+4)· **cron/queue/事件总线 +0**;`docs:rbacmap:check`(162)/ `docs:codemap:check` 0 FAIL。同 PR 更新 [`handoff/admin-web.md`](handoff/admin-web.md)(微信渠道勾选 + 模板配置)+ [`handoff/miniapp.md`](handoff/miniapp.md)(ack/status + 补授权交互)。**运维上线须**:配 `wechat_settings`(providerType=WECHAT + 凭证)+ 经 admin 端点填各类型真实 `templateId` + 按真实模板字段名核对内置字段映射。S3(producer 定向 + Effect 正式化,含招新 S7)/ S4(活动·考勤触发)/ S5(短信兜底)待后续切片另出 goal
- **统一通知模块 S3 — producer 接入 + 派发器 Effect 正式化 + 招新发号/入队定向通知(GAP-005 第三切片;第 28 模块扩;档 C/D)**(2026-06-25,goal「GAP-005 统一通知模块 S3」;立项源 = 冻结评审稿 [`unified-notification-dispatcher-review.md`](archive/reviews/unified-notification-dispatcher-review.md) §2.1/§2.2/§3.6/§6 + 招新 [`recruitment-phase4-loop-optimization-review.md`](archive/reviews/recruitment-phase4-loop-optimization-review.md) §9;`## Unreleased`,未 bump):招新闭环缺「发号/入队的系统自动通知」(招新 S7,阻塞于 GAP-005 落地)。本切片落 **producer → 通知派发器 Effect → 定向站内/微信**,接招新核心写路径**两后段触发**(申请人此时已是队员)。**recipientMemberId 1 列 additive**(`Notification` 加可空 TEXT + **FK→Member onDelete Restrict** + 复合索引 `[audienceType, recipientMemberId]`;广播为 null、定向挂单一收件人;**第 29 migration** 纯加列无破坏/无 enum/无回填,干净库重放 29/29 + `migrate diff` 零漂移〔SQL==schema〕 + seed 幂等二跑核过)。**`NotificationDispatcher` Effect 正式化**([`notification-dispatcher.ts`](../src/modules/notifications/notification-dispatcher.ts);= [`architecture-boundary.md §3.6`](architecture-boundary.md) 的**首个真实 Effect 类**,「真实副作用路径」= 微信外部 API 出现 → deferred→active):`dispatchTargeted(recipientMemberId, notificationTypeCode, title, body, channels)` → 建**已发布定向行**(audienceType=directed / sourceType=system / authorUserId=null / **跳过 draft 直 published**,不走 admin 状态机)→ 站内(该行)+ 微信(channels 含 wechat 时**复用 S2 单收件人发送** `dispatchDirected`,有 quota 才推、新志愿者常态无 quota → skipped no-quota / 无模板 → skipped no-template)。**§3.6 边界自证**:Effect **含** 派发/外部 API/payload 组装/delivery,**不含** 核心状态跃迁(留 producer service)/ 主 DB 事务所有权(定向行 create 是 commit 后独立小写,非 producer 事务)/ DTO 呈现;**外部 HTTP 一律在 producer 业务事务之外**(§6.2)。**feed 扩 where**([`notification-read.service.ts`](../src/modules/notifications/notification-read.service.ts) `buildFeedWhere`:**广播可见 ∪ 本人定向**;广播分支**按 `audienceType=broadcast` 收窄**复用的可见档 where〔杜绝定向行借 member 可见档泄漏给他人 = 越权〕、定向分支 `recipientMemberId=本人`+published;list/unread-count/detail/mark-read 全覆盖,**他人定向 → 31001 防枚举**)。**producer 接入两触发**(D-N5 同步直调,无事件总线):**发号** [`recruitment-promotion.service.ts`](../src/modules/recruitment/recruitment-promotion.service.ts) promote 事务 **commit 后**逐新建 member 派(站内+微信,payload memberNo + 入队入口)/ **入队结果** [`team-join-enrollment.service.ts`](../src/modules/team-join/team-join-enrollment.service.ts) join 事务 **commit 后**派(仅站内,payload 部门名 + 正式队员);**两处 dispatch 在事务外、`try-catch` 永不抛** —— 派发失败只记日志,**绝不破坏 promote 行为锁**(号段连续无空洞/全或无/幂等)**与入队行为锁**(单部门 partial unique/level-1/全或无)〔注入 dispatcher 抛错 e2e + 单测断言业务仍成功〕;**防环**:招新/team-join → notifications **单向** import,通知模块**绝不**回调 producer。**报名前 5 触发不做**(报名受理/转人工/门槛/评定/公示;申请人那时非队员,S1 站内/S2 memberId-quota 够不着 → 维持现状靠查询进度 pull;openid 非会员推送路另立项)。**不引 cron/queue/事件总线**(D-N5 直调);不碰 S1 状态机/S2 能力(只复用 + feed additive 扩 where);不碰 content/birthday cron/sms;招新 producer **零改既有业务行为**(只 commit 后多一个 dispatch 调用)。**footprint**:模块 **+0** · migration 28→**29** · **权限码 162 零变**(producer 内调,0 新 RBAC 码)· **BizCode 零新增** · **controller 55 零变**(派发器无端点;feed 扩复用既有 app 4 端点)· **EXPECTED_ROUTES 259 零变** · **cron/queue/事件总线 +0** · audit union 零变(系统定向不入 audit,沿 §13)· 新增 `notification-dispatcher.ts`(Effect)为模块内类非新模块;`docs:rbacmap:check`(162)/ `docs:codemap:check` 0 FAIL。**架构边界 doc 同步**([`architecture-boundary.md §3.6`](architecture-boundary.md) Effect 行 deferred→**active**,记 NotificationDispatcher 为首个落地 Effect)。同 PR 更新交接层 [`handoff/admin-web.md`](handoff/admin-web.md) + [`handoff/miniapp.md`](handoff/miniapp.md)(系统定向通知 + **招新 S7 部分交付〔发号/入队〕** + 报名前 5 触发维持 pull 说明 + GAP-005/GAP-006 进展)。S5(短信兜底)/ 报名前 openid 推送路 待后续切片另出 goal
- **统一通知模块 S4 — 活动/考勤 producer 定向触发(GAP-005 第四切片;第 28 模块扩;档 C,纯 producer 接入零 schema)**(2026-06-25,goal「GAP-005 统一通知模块 S4」;立项源 = 冻结评审稿 [`unified-notification-dispatcher-review.md`](archive/reviews/unified-notification-dispatcher-review.md) §6/§11;`## Unreleased`,未 bump):活动闭环缺「考勤/报名/取消的系统自动通知」。本切片镜像 S3 招新发号/入队接入模式,把 **三处队员事件**接入 S3 已就绪的 `NotificationDispatcher.dispatchTargeted`,全部 **commit 后、事务外、`try-catch` 永不抛**,收件人均为队员、渠道仅站内(`activity-reminder` 类型,微信 opt-in 延后)。**① 报名审批结果** [`activity-registrations.service.ts`](../src/modules/activity-registrations/activity-registrations.service.ts) `approve`/`reject` commit 后 → 派给报名本人(payload 活动名 + 通过/驳回 + reviewNote 理由若有)。**② 活动取消** [`activities.service.ts`](../src/modules/activities/activities.service.ts) `cancel` commit 后 → **遍历该活动仍在册报名者**(`pending`+`pass`,registration→member 解析 + memberId 去重;reject/cancelled 不扰)逐人派(payload 活动名 + 取消原因);整体 try-catch + 单人各自吞,**某人失败不阻断其余**。**③ 考勤结果/贡献值** [`attendances.service.ts`](../src/modules/attendances/attendances.service.ts) `finalApprove` commit 后 → sheet 内逐 record 本人(payload 活动名 + 本次贡献值)。**行为锁零影响**:三 producer 各把通知要素带出 `$transaction` 再派 → 取消状态机 / 报名审批状态机(含 capacity `FOR UPDATE`)/ 考勤 finalApprove + **贡献值生效 + `attendance.recorded` 事件**既有行为**零改零破**(**注入 dispatcher 抛错 e2e + 单测断言三处业务仍成功** = 硬证事务外 + 吞)。**防环单向**:三模块 `imports: [NotificationsModule]`,通知**绝不**回调三者;**无事件总线/cron/queue**(D-N5 直调);**活动发布不自动广播**(admin 手动走 S1)。**不碰** S1 状态机/S2 能力/S3 派发器内部(只调 `dispatchTargeted`)/content/birthday/sms;三 producer **零改既有业务行为**(只 commit 后多一个 dispatch)。**footprint**:模块 **+0** · **migration 29 零变(0 schema)** · **权限码 162 零变** · **BizCode 零新增** · **controller 55 零变 / EXPECTED_ROUTES 259 零变**(无新端点)· **cron/queue/事件总线 +0** · audit union 零变(系统定向不入 audit)· 复用既有 `notification_type` 字典 `activity-reminder`(notification.constants.ts +1 常量)+ S1 feed 读取面;三 producer service.spec 各扩 S4 单测 + 新增 e2e [`notifications-participation-producers`](../test/e2e/notifications-participation-producers.e2e-spec.ts)(三触发各 → 本人定向到达 + feed 仅本人〔他人 404〕+ **活动取消 → N 报名者各一条** + reject/cancelled 不收 + 注入失败断言三业务仍成功)。`docs:rbacmap:check`(162)/ `docs:codemap:check` 0 FAIL。同 PR 更新 [`handoff/admin-web.md`](handoff/admin-web.md) + [`handoff/miniapp.md`](handoff/miniapp.md)(系统定向通知 S4)。S5(短信兜底)/ 报名前 openid 推送路 待后续切片另出 goal
- **统一通知模块 S5 — 短信兜底渠道(GAP-005 末位切片;第 28 模块扩;档 D,含真实计费外发)**(2026-06-27,goal「GAP-005 统一通知模块 S5」;立项源 = 冻结评审稿 [`unified-notification-dispatcher-review.md`](archive/reviews/unified-notification-dispatcher-review.md) §4/§8.3/§9;`## Unreleased`,未 bump):紧急召集缺「站内/微信够不着时的短信兜底」。本切片落 **短信渠道 = admin 显式发起、计费确认必需的紧急召集兜底**,复用 sms 基建不 fork;**短信永不默认/不强制/不随 publish 自动发**(站内+微信优先),**无 cron/queue/事件总线**(同步发送 §8)。**复用 `SmsProviderRouter` additive `sendNotification`**(零变量镜像生日批;**不改 verifyCode/birthday 既有发送** = 行为锁):SmsProvider 接口 + DevStub + Tencent(`requireTencentContext('notification')` 取 `templateIdNotification`)+ Router 各加;新模板键 `SMS_TEMPLATE_KEY_NOTIFICATION='notification'`。**短信派发分支** [`notification-sms-dispatch.service.ts`](../src/modules/notifications/notification-sms-dispatch.service.ts)(**外发在任何 DB 事务之外** §6.2):逐**可见且有手机**收件人(broadcast 走 `content.visibility` `canSeeContent` 复用;仅 `User.phone` 对齐生日批拍板⑤)经 Router 单发 → `NotificationDelivery`(channel=sms sent/failed/skipped+reasonCode+errCode,**recipientRef=`maskPhone`**)+ `sms_send_logs` 流水;**防滥发继承**(逐人查 `sms_send_logs`):同日同模板幂等(镜像生日批,一日一 nudge)+ 同号日封顶 10(`SMS_PHONE_DAILY_LIMIT`)+ 间隔 60s(`SMS_SEND_MIN_INTERVAL_SECONDS`)+ re-trigger 去重(本通知已 sent → `already-sent`);**FAILED 逐人不阻断**(镜像生日批);通道中途不可用零成本中止。**admin 1 端点** `POST admin/v1/notifications/:id/send-sms`(R 模式新码 `notification.send.sms` 无 @RequirePermissions):**计费确认必需** `confirmed=true` 才真发(每收件人 1 条计费)、`confirmed=false` = **预览受众计数**(`recipientCount`「将向 N 人发短信」,零发送零计费零 delivery)、缺失 → 通用 400;前置闸须 **published 且 channels 含 `sms`**(否则 `31013` `NOTIFICATION_SMS_NOT_SENDABLE`),通道未配置 → 既有 `24030`;**审计**复用 `notification.publish` 伞事件 `operation='send-sms'` + 收件人计数(**无新增 audit 串**;§13.2 admin 入 audit / 逐条投递不入 audit;手机号 maskPhone,audit 仅计数)。`channels` 白名单放开 `sms`(可声明;短信不随 publish 自动发)。**L2**:手机号一律 maskPhone(响应/日志/审计零明文)。**第 30 migration**(`sms_settings.templateIdNotification` 1 列 additive,纯加列无破坏/无 enum/无回填;干净库重放 30/30 + seed 幂等)。**不碰** S1 状态机/S2 微信/S3·S4 派发逻辑(只加短信分支)/content/birthday cron/wechat。**footprint**:模块 **+0** · migration 29→**30** · 权限码 162→**163**(`notification.send.sms`,biz-admin 73→**74**;ops-admin 63/member 9 零变)· BizCode 310xx **+1**(31013)· controller **55 零变**(扩既有 NotificationAdminController)· EXPECTED_ROUTES 259→**260**(+1)· audit union **零变**(复用 publish 伞事件)· **cron/queue/事件总线 +0**;新增 `notification-sms-dispatch.service{.ts,.spec.ts}` + provider 单测扩 + e2e [`notifications-sms`](../test/e2e/notifications-sms.e2e-spec.ts);`docs:rbacmap:check`(163)/ `docs:codemap:check` 0 FAIL。**运维上线须**:配 `sms_settings`(providerType=TENCENT_SMS + 凭证 + signName/sdkAppId/region)+ 填真实 `templateIdNotification`(零变量"请打开 App 查看"模板,须先过审)。**真·全员短信批处理异步明确不做**(唯一可能触碰 R-5;受众规模致同步延迟超阈 → 挂 NEXT_TASKS 观察,不自建异步基建,变更须维护者拍)。**至此 GAP-005 统一通知模块 S1–S5 全切片落地**(招新 S7 通知阻塞解除)
- **进程硬化两件(2026-06-12,goal「会议延期窗口·无等待工作一次收清」G2/G3,#345/#346)**:① 崩溃路径可观测性兜底(`src/bootstrap/apply-crash-handlers.ts`,仅 main.ts 注册:uncaughtException 记 fatal 后 exit(1) / unhandledRejection 记日志后 re-throw 保持 Node 默认崩溃结局;经 pino 沿 redact 清单;**不碰 SIGTERM/优雅关闭**);② 外部 SDK 请求超时上限(腾讯云 SMS `reqTimeout` 8s + COS `Timeout` 8000ms,超时沿既有错误路径语义零变化;**正确但当前休眠**——SMS/COS 真通道均未接,配置就位由两 provider spec 构造断言锁定)
- **App API Phase 2**(15 端点):`me`×3 / profile×2 / password / activities×2 / `my/*`×7;DTO 隔离 `dto/app/` 禁派生;self-scope;**永不返回 L3**;准入双 ACTIVE(D-5)
- **Admin surface 本人身份**(2026-06-14,goal「Admin surface 本人身份端点」#372):`GET /api/admin/v1/me` 只读身份 bootstrap(`AdminMeController` 单一 `Admin - Me` tag + 独立 `AdminMeResponseDto` 字段集恰好 9 = User 本体身份;入口仅 `JwtAuthGuard` 不挂 `@Roles`,任意登录用户返本人;**不内联角色/权限**——权限仍走 `system/v1/rbac/me/permissions`;不返 member 业务字段 / raw permission code / L3;零 prisma/BizCode/audit)
- **Route B 终态**(2026-06-01):全仓仅 4 前缀,零 v2 / 零裸前缀 / 零 legacy,contract 断言锁定(§2.1)
- **P2-2 鉴权后缀**(v0.17.0 落地 148;现 **235** endpoint,2026-06-22 亲核 EXPECTED_ROUTES = 实际路由数;183 → 196〔招新一期 T3 +10〕 → 200〔招新二期 +4〕 → 212〔招新三期入队 +12:T2 admin 8 + T3 app 自助 3 + T4 一键入队 1〕 → **228**〔CMS 内容模块 +16:T2 admin 12 + T3 open 2 + T4 app 2〕 → **229**〔招新实名 OCR 改造 +1 识别端点 `open/v1/recruitment/applications/recognize`〕 → **234**〔队员/审批跨轴只读 +5:Tier2 跨活动横扫 registrations + attendance-sheets 2 / Tier3 队员 360 registrations + attendance-records + contribution-summary 3〕 → **235**〔招新闭环优化 S2 招新工作台 stats +1:`admin/v1/recruitment/cycles/{id}/stats`〕 → **240**〔招新闭环优化 S4a H5 手机身份链 +5:open/v1 身份链 `identity/send-code` + `identity/verify-code` 2 + `applications/query-by-phone` / `applications/rebind-wechat` / `applications/rebind-phone` 3〕)summary 带 `[rbac:]/[roles:]/[public]/[auth]`,检查项 G 锁一致性(Slow-4 后 `[roles:]` 计 0,形态保留供机制兜底)
- **测试与契约**:e2e **97 suites / 1960 tests** + unit **48 spec / 1568 tests**(**2026-06-20 招新三期入队 goal true-up 亲核**:e2e +2 spec/+38 例〔`team-join.e2e` admin 27〔轮次/标 gate+自动推进/贡献值/有效期/综合评估/状态机/一键入队 9 含两层身份+原子+幂等+专业队+评估过期〕 + `team-join-app.e2e` 11〔准入/发起/查进度/改候选+IDOR/防重〕;unit 零变化〔phase-3 全 e2e〕;contract 341→**353**〔+12 路由 existence;snapshot 仅新增〕;full e2e 97/1944 本地 OrbStack 实跑亲核〕。前序基线:2026-06-18 招新 goal true-up 亲核:e2e +recruitment 1 spec/17 例〔本 goal T3〕+ realname-settings 1 spec〔T2〕;unit +realname crypto/provider 等,主要随 BizCode 27xxx/28xxx + audit union +5 参数化展开;**2026-06-18 招新系统性审查 R1 再 +e2e recruitment 4 例〔17→21:FM-A 卡死态 resolve 恢复 ×2 / FM-C 人工发号容量 28031 / F-1 超限 413〕+ unit `recruitment-applications.service` 1 spec/4 例〔FM-B 孤儿 blob 补偿〕**;**2026-06-19 招新 FM-A 收紧再 +e2e recruitment 2 例〔21→23:核验在途行 resolve 28040 / mismatch 卡死行只能 reject;verifyOutcome 前置落库 + resolve 闸收紧,零 schema〕**;**2026-06-19 招新二期再 +e2e recruitment 10 例〔23→33:T2 门槛全链/幂等/评定两路/公示名单拼音序零敏感外籍 + T3 一键发号建 User+Member+迁移/幂等/外籍 skip+report/999 回滚/RBAC〕**;**2026-06-19 promote 超时硬化再 +e2e recruitment 1 例〔33→34:批量发号 N=25 号段连续无空洞〕+ unit +1 spec〔45→46 spec,1492→1494:bcrypt 移出事务结构断言〕**;2026-06-14 `admin/v1/me` +9 / 2026-06-13 保险 +31 为前序基线;**e2e 1906 / unit 1494 / contract 341 三计数 2026-06-19 v0.25.0 bump #388 CI 实跑亲核**)+ contract **212 路由**白名单(183 → 196 → 200 → **212**〔招新三期入队 +12:admin 8 / app 自助 3 / 一键入队 1〕,均仅新增 + open/v1 5 canonical 前缀)+ `team_join_cycles` / `team_join_applications` 两表纳入 reset-db(连同既有 realname/recruitment 表);CI 全链 + docker-smoke(含生日 cron 启动锚行 + `WECHAT_ENCRYPTION_KEY` / `REALNAME_ENCRYPTION_KEY` 注入);**2026-06-20 #399 review P2 六项修复 goal true-up 亲核**:e2e 1944→**1960**(+16:F1 +5〔role-perm 边界 4 + seed 漂移哨兵 1〕/ F2 +8〔key 派生格式正例 + 6 反例 + 模式 B canary〕/ F4 +1〔reject 软删解死锁〕/ F3 +2〔报名侧拒 + promote defense-in-depth〕);unit 本批 +2 spec(F1 `reserved-super-admin-permission-codes` 3 例 + F2 `attachment-key-format` 26 例)→ **48 spec / 1568**(亲核;前序 46 spec 实测 **1539** 例,本行上方旧记 1494 系既有 spec 增例未同步、本次以 `pnpm test` 实跑校正)+ contract **353 无新增路由**(F1 30103 / F2 13014 / F3 19010 仅 `@ApiBizErrorResponse` 错误码增量,snapshot 受控);full e2e 97/1960 · unit 48/1568 · contract 353 本地实跑亲核;`docs:rbacmap:check` / `docs:codemap:check` 0 FAIL;**2026-06-21 CMS 内容模块 goal T0-T5 true-up 亲核**:full e2e 97→**100** suites / **2027**(+content-admin 25 + content-public 19 + content-app 18 = +62;旧记 1960 以 full e2e 实跑校正)· unit 48→**49** spec / **1618**(+content.visibility 21;旧记 1568 系陈旧,以 `pnpm test` 实跑校正)· contract 353→**379**(+16 路由:admin 12 + open 2 + app 2,snapshot 仅新增)· 权限码 146→**155** · BizCode 164→**169**(290xx 段 5)· audit union 57→**61** · controller 46→**49** · 模块 27→**28** · migration 21→**22**;`docs:rbacmap:check`(155)**0 FAIL/0 WARN** · `docs:codemap:check` 0 FAIL;**2026-06-21 活动闭环两处硬化 goal true-up 亲核**(报名截止生效 + 贡献值全局每日封顶 1.5):full e2e 100 suites / 2027→**2033**(+6:报名截止 5〔admin 4:截止前可报 / 截止后代报名拒 20123 / null 不拦 / 已 pending 仍可 approve;App 自助截止拒 1〕+ 贡献值同日封顶 1〔team-join ⑥b〕;team-join / team-join-app / prefill / attendances 既有夹具按新语义**原位重写**不计净增)· unit 49 spec / 1618→**1626**(本 goal 净 0:calculator「每日上限」块 4→4 原位改写;旧记 1618 陈旧,以 `pnpm test` 实跑校正)· contract **379 无新增路由**(20123 仅 admin create + App create 两处 `@ApiBizErrorResponse` 错误码增量,snapshot 受控)· BizCode 169→**170**(20123 `ACTIVITY_REGISTRATION_DEADLINE_PASSED`,201xx activities 段,无新段)· 权限码 155 / audit union 61 / controller 49 / 模块 28 / migration 22 **零变**(B 档;`ContributionRule.dailyCap` 列保留标 deprecated 免 migration);full e2e 100/2033 · unit 49/1626 · contract 379 本地实跑亲核;`docs:rbacmap:check` 0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL;**2026-06-22 v0.28.0 收口 true-up**(#421 字典+守卫 / #422 activity_type 微调〔终态 9 父 31 子〕 / #423-#424 组织树+Organization.code,bump #425):full e2e 100/2033→**2052**(+10 dict 防误删守卫 e2e〔#421〕 + +9 org code e2e〔#423-#424:建带 code / 撞 code / 不传回归 null / 格式 400 / PATCH 改·设回自身·撞他 / 软删占位〕;#422 seed 净 0)· unit 49/1626→**1641**(+10 dict守卫〔#421〕 + +5 BizCode 11033 结构〔#424〕)· contract **379 无新增路由**(本批 **+4 BizCode** 20123/12003/12015/11033 + org code 字段均 `@ApiBizErrorResponse`/DTO snapshot 受控增量;`info.version` 不入 snapshot)· migration 22→**23**〔`add_organization_code` 仅加列〕· 权限码 155 / endpoint 228 / 模块 28 **零变**;bump 分支 full e2e 100/2052 · unit 49/1641 · contract 379 实跑亲核;`docs:rbacmap:check`(155)/`docs:codemap:check` 0 FAIL/0 WARN;**2026-06-22 招新实名 OCR 改造 T1+T2 true-up 亲核(#427)**:full e2e 100/2052→**2056**(recruitment 净 +4:② mismatch→manual_review 翻转 + ②b/②c 防伪/不清晰 + 识别端点 OCR①-④ + manual resolve Ⓜ/Ⓜb;退役 FM-A 卡死 4 例;realname-settings 11 零漂移)· unit 49/1641→**1648**(realname provider/service spec 改写参数化:dev-stub OCR 桩 + tencent 三 action mock + service recognize)· contract 379→**380**(+1 识别端点 existence;snapshot 受控:+`RecruitmentOcrRecognizeResponseDto` + recognize 路由,submit 错误集去 27030/27031〔转人工不外抛〕)· **权限码 155 / BizCode 170 / audit union 61 / migration 23 / 模块 28 零变**(零 schema/零新码/零新依赖)· endpoint 228→**229**;full e2e 100/2056 · unit 49/1648 · contract 380 本地实跑亲核;`docs:rbacmap:check`(155)0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL;**全仓无 `IdCardVerification` 调用路径**(grep 自证);**2026-06-23 队员/审批跨轴只读查询 goal true-up 亲核**:full e2e 100→**102** suites / 2056→**2079**(+2 spec:`admin-cross-axis-registrations` 11〔跨活动横扫 存在/statusCode/空集/RBAC + 队员履历 scope/过滤/空集/MEMBER_NOT_FOUND/RBAC〕 + `admin-cross-axis-attendances` 12〔跨活动单据 + 队员考勤记录 approved-only + 贡献值生涯累计封顶 capped<裸SUM + 各 MEMBER_NOT_FOUND/RBAC〕)· unit 49→**50** spec / 1648→**1655**(+`team-join-progress` 7:封顶核 cutoff 参数化 / 生涯累计 / 1.5 封顶 capped<裸SUM / null→0 / 委托回归)· contract 380→**389**(+5 路由 existence + 4 schema〔`Admin{Registration,AttendanceSheet}ListItemDto` / `AdminMemberAttendanceRecordDto` / `MemberContributionSummaryDto`〕;snapshot **仅新增**〔additions-only 已核:每删除行均重现为新增行=纯键重排〕)· EXPECTED_ROUTES 229→**234** · controller 49→**52** · **权限码 155 / BizCode / audit union / migration 23 / 模块 28 零变**(纯读复用 read 码,零新码/零 schema/零依赖)· full e2e 102/2079〔其中 `rbac-roles` 1 suite 全量 runInBand 偶发 `loginAs 404` 整 suite 红,**隔离 42/42 全绿** = 已知 flaky [`flaky-auth-jwt-guard-e2e`] 非回归,本 goal 未触 auth/rbac〕· unit 50/1655 · contract 389 本地实跑亲核;`docs:rbacmap:check`(155)0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL;**2026-06-24 招新闭环优化 S1 true-up 亲核**:full e2e 102/2079→**102/2080**(+1 recruitment ⑩b 进度模型 todoList 真投影;⑩ `statusCode`→`stage` 断言原位改不计净增)· unit 50→**51** spec / 1655→**1671**(+`recruitment-progress-presenter` 16:stage 派生全分支〔含 `promoted` 禁「已晋升」断言〕+ todoList 投影 + 组装回退)· contract 389→**391**(+2 schema existence `RecruitmentApplicationProgressDto`/`RecruitmentTodoItemDto`;snapshot 受控:query 200 响应 ref `…PublicDto`→`…ProgressDto` + 2 新 schema,EXPECTED_ROUTES **234 零变**)· **权限码 155 / BizCode / audit union / migration 23 / 模块 28 / endpoint 234 / controller 52 零变**(纯展示派生:零 schema/零码/零依赖;新增 `recruitment-progress-presenter.ts` 193L 为模块内 util 非新模块)· `recruitment_stage` 字典 **seed 二跑无漂移自证**(7 项 ACTIVE,0「已晋升」);full e2e 102/2080 · unit 51/1671 · contract 391 本地实跑亲核;`docs:rbacmap:check`(155)0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL(WARN = 既有 5 god-service LOC,本 goal 派生逻辑入新 presenter 未新增候选);**2026-06-24 招新闭环优化 S2 true-up 亲核**:full e2e 102/2080→**102/2083**(+3 recruitment ㉕㉖㉗:S2 工作台 stats 五组多态夹具计数〔今日/待处理/门槛/评定/发号〕 / 普通 USER 403 RBAC_FORBIDDEN / 轮次不存在 28001)· unit 51→**52** spec / 1671→**1681**(+`recruitment-stats.service` 10:五组聚合 + 今日北京日界〔含 UTC 日≠北京日两边界行〕 + openid 占用复用 `decidePromotionIssuance` + RBAC/404 守卫)· contract 391→**399**(+1 路由 existence `…/cycles/{id}/stats` + 7 schema existence〔`RecruitmentCycleStatsDto` + 嵌套 today/pending/threshold/thresholdItem/evaluation/issuance 6〕;snapshot 受控仅新增:+1 路由 operation + 7 DTO,EXPECTED_ROUTES 234→**235**)· **权限码 155 / BizCode / audit union / migration 23 / 模块 28 / controller 52 零变**(纯读聚合:复用 `recruitment-application.read.record` 零新码 / 零 schema / 零写 / 零依赖;新增 `recruitment-stats.service.ts` 为模块内 QueryService 非新模块、加路由不加 controller class)· full e2e 102/2083 · unit 52/1681 · contract 399 本地实跑亲核(`rbac-roles` flaky 本轮全绿);`docs:rbacmap:check`(155)0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL;**2026-06-24 招新闭环优化 S3 true-up 亲核**:full e2e 102/2083→**102/2086**(+3 recruitment ⑪b/⑪c/⑪d:`read.sensitive`+`read.record`→详情明文证件号+证件照 200 / 仅 `read.record`→脱敏详情+证件照 30100+列表 200 / 无码→详情+证件照 30100;**三 profile 走真实 RBAC 非 SA 短路**;seed-biz-admin 净 0〔67→68 码 / 66→67 绑定就地改 + byModule recruitment-application 5→6〕)· unit 52 spec / 1681→**1686**(+`recruitment-applications.service` S3 判权 5:详情明文/脱敏/30100 + 证件照 200/30100,mock `rbac.can` 按码返值)· contract **399 无新增路由**(详情+证件照 2 端点仅 `@ApiOperation` summary 改判 rbac 后缀,paths snapshot 受控 2 行 / **components.schemas DTO snapshot 零变** = 响应字段集不变佐证)· **权限码 155→156**(recruitment-application `read.sensitive` +1,全绑 biz-admin → 绑定 66→67;ops-admin 63 / member 9 零变)· **BizCode / audit union / migration 23 / 模块 28 / controller 52 / endpoint 235 零变**(零 schema/零状态机/零依赖/零新端点)· full e2e 102/2086 · unit 52/1686 · contract 399 本地实跑亲核(`rbac-roles` flaky 本轮全绿)· `docs:rbacmap:check`(156)0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL(WARN = 既有 5 god-service LOC,本切片判权细化未新增候选);**2026-06-24 招新闭环优化 S4a true-up 亲核**:full e2e 102/2086→**103/2102**(+1 spec `recruitment-identity.e2e` 16:发码/验码发 token/H5 报名全链 verified+phoneVerifiedAt+openid 空 / token 一次性 28050 / 缺凭证 40000 / 手机不一致 40000 / 伪造 token 28050 / 查询②手机+验证码 / 换微信+冲突 28051 / 换手机+换绑历史 / 新旧同 40000 / **会话行不进报名表 + 去重 28003** / 小程序链向后兼容)· unit 52/1686→**53/1706**(+`recruitment-identity.service` 10:token helpers 2 + `assertPhoneSessionValid` 6〔过期/已消费/轮次/手机不符/不存在/合法〕 + `consumePhoneSession` 2〔原子成功/竞态 28050〕;旧记 1686 陈旧,以 `pnpm test` 实跑校正)· contract 399→**411**(+5 路由 existence + 7 schema existence〔`Recruitment{SendCode,VerifyCode}{,Response}Dto` + `RecruitmentQueryByPhoneDto` + `RecruitmentRebind{Wechat,Phone}Dto`〕;snapshot **additions-only 已核**〔genuine 删除仅 submit summary + 错误描述 2 处加 28050;submit DTO 字段改在 multipart opaque payload JSON 串不入 OpenAPI named schema〕)· EXPECTED_ROUTES 235→**240** · **migration 23→25**(`SmsPurpose ADD VALUE 'RECRUITMENT_BIND'` 不可逆独立 migration + `recruitment_identity_session` 新表 / application +6 手机身份链列;**干净库重放 25 migration 全绿 + `migrate diff` 零漂移〔SQL==schema〕+ seed 幂等二跑**自证)· BizCode 170→**172**(28050 token 失效 / 28051 微信已绑他人,280xx 段)· audit union 61→**63**(`recruitment-application.rebind-{wechat,phone}`)· **权限码 156 / 模块 28 / controller 52 零变**(新增 `recruitment-identity.service.ts` 356L 模块内 service,5 端点入既有 `RecruitmentPublicController`;`SmsCodeService` 形参放宽 string\|null,105 例 SMS 消费方 e2e 零回归);full e2e 103/2102 · unit 53/1706 · contract 411 本地实跑亲核;`docs:rbacmap:check`(156)0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL(WARN = 既有 5 god-service LOC〔applications.service 871→914 仍既有候选,本刀新逻辑入新 service 未增候选〕);**2026-06-24 招新闭环优化 S4b true-up 亲核**:full e2e 103/2102→**103/2109**(+7:`recruitment-identity.e2e` 六分流 +6〔⑰模糊 retake 不落+会话计数 / ⑱上游连续 2 次 retry→system / ⑲防伪连续 2 次 retake→high+中性 / ⑳三选一①②confirm 不落→回填 verified / ㉑三选一③→普通人工 / ㉒进度 manual_high 申请人侧中性〕 + `recruitment.e2e` 净 +1〔②/②b/②c 由「直落 manual_review」改写为延迟 confirm/retake 同位 + ②-③ mismatch 确认③→普通人工 +1〕)· unit 53/1706→**54/1732**(+`recruitment-ocr-routing` 26〔classifyOcrResult 6 + routeOcrOutcome 六分流逐路 + escalation + 无会话退化 20〕;presenter +6〔manual_high/retake/confirm 三态 + 优先级〕;stats 三栏夹具 verifyOutcome→riskLevel 同位改写)· contract **411 路由零变**(submit DTO `RecruitmentApplicationPublicDto`→`RecruitmentSubmitResultDto` 改名换字段 + admin DTO +`riskLevel`/`manualReviewReason` + admin list +`riskLevel` query param,snapshot 受控;EXPECTED_ROUTES 240 零变)· **migration 25→26**(`add_recruitment_ocr_routing_fields` 纯 ADD COLUMN 无 enum 无不可逆;干净库重放 26 migration 全绿 + `migrate diff` 零漂移 + seed 幂等二跑 recruitment_stage 7→10 态)· **权限码 156 / BizCode 172 / audit union 63 / 模块 28 / controller 52 / endpoint 240 零变**;full e2e 103/2109 · unit 54/1732 · contract 411 本地实跑亲核(`rbac-roles` flaky 本轮全绿);`docs:rbacmap:check`(156)0 FAIL/0 WARN · `docs:codemap:check` 0 FAIL(WARN = 既有 5 god-service LOC,六分流判定入新纯函数 `recruitment-ocr-routing.ts` 未增候选)

- **队员账号闭环 v1+v2(完整生命周期,2026-07-07,goal「队员账号闭环 v1(MVP)」+「队员账号闭环 v2(完整生命周期)」;冻结评审稿 [`member-account-loop-v2-review.md`](archive/reviews/member-account-loop-v2-review.md))**:给已存在队员开通/管理"手机验证码登录"账号的完整生命周期。**根改造**:`User.memberId` 全量 `@unique` → partial unique(`WHERE deletedAt IS NULL`,第 40 migration),同一 memberId 至多 1 条 live User 关联,软删旧号即释放槽位;连带 `Member.user`→`users` 一对多关系改造。**五端点**:`POST :id/account`(开号,新码 `member.grant.account` 绑 ops-admin;建号镜像 `recruitment-promotion.service.ts` 先例——随机不可用密码 + `username=memberNo` + `role=USER`;软删旧号后可重新开号)/ `POST :id/account/bind`(认领既有悬空账号,新码 `member.bind.account`)/ `POST :id/account/unbind`(只断链,不停用不软删)/ `POST :id/account/reopen`(软删旧号+开新号单事务原子;`username` 结构性冲突〔仍全量 `@unique`〕由代际后缀化解决)/ `PATCH :id/account/status`(队员面启停,复用 `user.update.status`;禁自我操作 + 联动撤销 refresh token)/ `POST accounts/bulk-grant`(批量开号,镜像 announcement-import 逐行 skip-on-error,复用 `member.grant.account`)。队员面 `MemberResponseDto` additive 加 `hasAccount`/`accountStatus`/`userId`(仅计 live 绑定;+list `?hasAccount=` 过滤);用户面 `UserResponseDto` additive 加**可选** `memberId`/`member`(仅 admin list/detail,App 自助面零改动)。+2 BizCode(`MEMBER_ACCOUNT_TARGET_ALREADY_LINKED`/`MEMBER_HAS_NO_LINKED_USER`)+ 4 audit event(`member.account-{granted,bound,unbound,reopened}`)。`src/modules/auth/**` 全程零 diff。

## 2.1 当前 API surface 状态

5 前缀(边界规则见 [`api-surface-policy.md`](api-surface-policy.md),迁移过程见 `api-surface-migration-plan.md`):**App** `/api/app/v1/*`(移动端只能落此,DTO 独立 `dto/app/`)| **Admin** `/api/admin/v1/*` | **Auth** `/api/auth/v1/*` | **System** `/api/system/v1/*`(D-1)| **Open** `/api/open/v1/*`(无账号公开 surface,`@Public`;**2026-06-18 招新一期 T3 首用** = 招新报名提交/查询;**2026-06-21 CMS 第二用** = 内容公开列表/详情〔`open/v1/contents`〕;第 5 canonical 前缀,contract `CANONICAL_PREFIXES` 锁定)。
**铁律**:❌ 不再新增 Mixed Controller(存量仅 rbac `me/permissions` 方法级 + dictionaries 同 surface 双 class,冻结);❌ App 永不返回 L3 字段(`passwordHash` / `*token*` / `secret*` / 完整 signed URL)。**范围例外(a)**(2026-06-21 CMS):owner=`content-image`/`content-file` 的签名 URL 在调用者**过文章可见级后**可于 `open/v1`+`app/v1` 内容读取面返回(content-* 是随文章展示的素材,非 owner-scoped 敏感 PII;沿 [`attachments/CLAUDE.md`](../src/modules/attachments/CLAUDE.md) + [`content-module-review.md §5.7`](archive/reviews/content-module-review.md));其余 owner / L3 字段铁律不变。

## 3. 当前明确未做 / 暂不启动

> 这些事项**不**由 AI 自行启动,需要用户拍板。

- **Slow-3 主决议已拍板,Slow-4 已完成**(2026-06-11 goal「权限双轨收口」#314-#317;冻结评审稿 [`archive/reviews/slow4-rbac-business-face-review.md`](archive/reviews/slow4-rbac-business-face-review.md)):ADMIN 内置角色边界 = 全量业务权限,由 `biz-admin` 承载(绑 35;`member.delete.record` 仅 SA;attachment 存量 20 码不绑);业务面 7 模块 44 端点已全部接入 `rbac.can()`,全仓活跃 `@Roles` = 0。**仍挂起的 Slow-3 子议题**:考勤终审部门级细分(2026-06-10 方案 A 沿用:维持 ADMIN 级终审〔现 = 持 biz-admin 的 ADMIN 或 SA〕,`finalReviewerUserId` 仅审计记录;"部长"职务无数据模型承载;重开需单独立项,详 [`participation-bounded-context.md §4`](participation-bounded-context.md))— **不**自动启动
- **不**自动启动 Slow-5(B8 入队同意书正文 / Q8 退队清理 N 值)— 等业务方提供
- **不**自动启动 Slow-7(uploadToken 重放黑名单 / 失败回滚 Provider 文件 / test-connection / multipart / STS / 跨 Provider 迁移)— 等真实使用反馈
- **不**自动启动 L-3(Storage Settings 配置变更 audit_logs)— 等用户授权
- **不**自动启动 `events` / `event_participants` / `member_profiles 扩展敏感字段` 等延后模型(沿 [`docs/V2红线与复活路径.md §4.3`](V2红线与复活路径.md))
- **不**自动引入 LLM / vector / Redis / queue(沿 [ARCHITECTURE.md §9](../ARCHITECTURE.md) 升级路径);**cron 已按升级路径解锁,范围仅生日批**(2026-06-11 B 队列拍板④,冻结评审稿 [`archive/reviews/queue-b-otp-birthday-infra-review.md`](archive/reviews/queue-b-otp-birthday-infra-review.md) R-5:`@nestjs/schedule` 仅承载 notifications 生日 job 一个 `@Cron`;**新增任何定时任务 = 新 D 档评审**;数据清理不解锁,沿 retention 手动 SOP)
- **不**自动启动新 schema / migration / Permission seed / Role 扩展(A-3 / A-4 红线)
- **不**自动接入运维侧真实 COS(bucket / IAM / CORS / lifecycle / SSE-COS / 真实凭证录入)— 由队组织运维侧执行,系统侧 SOP 见 [`docs/ops/cos-production-rollout-checklist.md`](ops/cos-production-rollout-checklist.md)
- **不**自动接入运维侧真实微信小程序(注册小程序 / AppID·AppSecret 录入 / 真实微信验收)— 由维护者运维侧执行,系统侧 SOP 见 [`docs/ops/wechat-mini-production-rollout-checklist.md`](ops/wechat-mini-production-rollout-checklist.md);小程序前端未启动,招新自助走小程序 meeting-gated(均沿 wechat 评审稿 §12)
- **不**自动启动保险模块后续(理赔记录 / 到期主动提醒〔新定时任务 = 新 D 档评审,cron 解锁范围仍仅生日批〕/ 保险核验工作流 / **保单图 attachments 接线**〔`AttachmentOwnerType` enum 分支 + `attachment.*.member-insurance.{self,other}` 8 权限码 + 配置三表行〕/ App activities DTO 暴露 `requiresInsurance`)— 评审稿 [`insurance-module-review.md §10`](archive/reviews/insurance-module-review.md) + NEXT_TASKS P1-10,业务/前端真实诉求触发再立项
- **不**自动启动招新后续(**phase-2 promote 已于 2026-06-19 落地** + **phase-3 入队已于 2026-06-19/20 落地**〔志愿者→队员:10 项考核 + 综合评估 → 设部门 + 级别 level-1;见 §2 + 评审稿 [`recruitment-phase3-review.md`](archive/reviews/recruitment-phase3-review.md);招新业务域三段闭环〕;**未做**:phase 3 之后〔退队 / 晋升体系 / 多部门常态归属 / 级别历史版本化〕/ 接 `CertificatesService.isQualified` 自动核验证书 gate〔v1 admin 标记,W-J-2 留作未来〕/ 入队部门级权限细分〔v1 = biz-admin 统一标,沿 Slow-3 考勤终审同款挂起〕/ 打通现有活动·考勤·证书给招新报名者〔approach A 已定〕/ 通知模板内容细化 / 贡献值年度窗口语义〔v1 历史累计〕/ 到期提醒〔新定时任务 = 新 D 档评审,cron 解锁范围仍仅生日批〕)— 评审稿 + NEXT_TASKS 归档行,业务诉求触发再立项;**不**自动接入运维侧真实腾讯云 **OCR 证件识别**(`SecretId`/`SecretKey` 录入 / 真实验收;**2026-06-22 实名环节已 OCR 改造**,放弃联网验真,见 §2「招新实名环节 OCR 改造」)— 系统侧 SOP 见 [`ops/realname-verification-rollout-checklist.md`](ops/realname-verification-rollout-checklist.md)(已改 OCR 口径);**招新失败者脱敏清理 = 手动 SQL + storage blob 删除 SOP**([`ops/recruitment-data-retention-sop.md`](ops/recruitment-data-retention-sop.md):rejected 或轮次结束 30 天后清姓名/身份证号/证件照/手机/紧急联系人/精确生日/详细住址,保留轮次/年龄段/性别/城市到区/来源渠道/淘汰环节/是否外籍行级脱敏;**promoted 行敏感已在 promote 时即时清,SOP 不再触**;**不解锁 cron**)
- **招新身份证号 v1 明文入库**(维护者 2026-06-18 拍板留作审计痕迹):同 `member_profiles.documentNumber` 现状口径(schema `idCardNumber String?` 即明文);phase-2 promote 把 `idCardNumber` 搬入 `member_profiles.documentNumber`(同明文口径);「落库加密 / 哈希」归既有 **C-8 合规议题**单独处理,**不**在招新范围
- **招新二期外籍 promote = admin 手动建档(已知边界,维护者 2026-06-19 拍板)**:一键发号(`POST admin/v1/recruitment/cycles/:id/promote`)**仅处理大陆可派生 birthDate+genderCode 的报名**(`MemberProfile.birthDate`/`genderCode` NOT NULL 由身份证号派生)。外籍报名(护照 / 港澳台 / 外国人永居)走完门槛 / 综合评定 / 公示后**仍不进一键发号**——promote 把它们 **skip + 在发号结果与公示名单中显式 report**(`reason=foreign-manual-build` / `needsManualBuild=true`,**发号前 admin 即可见,不会漏发误判**),由 admin 走既有 `POST admin/v1/members` 手动建档(手填大陆派生不出的 birthDate / 性别 / email 等);外籍报名 promote 后**保持 `publicity` 态**(待手动建档,不自动清出),**非 bug**。`MemberProfile.email` 已放宽可空(M-1)承载大陆 promote 不采集 email 的情形,admin 手动建档仍走 DTO `@IsEmail` 必填
- **不**自动启动内容模块(CMS)后续(content_reads 已读回执 / 评论·点赞 / 定时发布〔新 cron = 新 D 档评审〕/ UV·时间序列分析 / 部长角色·部门级内容权限细分 / 招新公示自动桥接 / attachment Mime·SizeLimit override 行 / 正文图 client 端 key 解析)— 评审稿 [`content-module-review.md §10`](archive/reviews/content-module-review.md) + NEXT_TASKS,业务诉求触发再立项
- **不**自动回改历史 handoff(沿 [`docs/V2红线与复活路径.md §5.1`](V2红线与复活路径.md))
- **不**把历史评审稿([`docs/archive/batches/`](archive/batches/) / [`docs/archive/reviews/`](archive/reviews/))当作"当前事实"— 它们是各批次冻结时刻的决策依据
- **Route B 全量迁移已完成**(2026-06-01;取代原"Phase 1B 暂缓 / 方案 C")— 见 [`api-surface-migration-plan.md`](api-surface-migration-plan.md) + [`AGENTS.md §21 D-9`](../AGENTS.md);**Phase 0 映射已签字冻结**(2026-06-01;[§3](api-surface-migration-plan.md) 全 156 路由 `tag→surface` + 终态验收基线 + 8 个 legacy mobile-like 端点纳入 Phase 4 删除);**Phase 1 alias 已完成**(1a auth+health 7 + 1b system 56 + 1c admin 70 = 133 非-app 路由双挂,contract 423 + e2e 双路径绿,老路径零回归);**Phase 2 完成**(老前缀 OpenAPI 标 `deprecated`、新前缀 canonical);**Phase 3 deprecation 窗口豁免**(无生产消费者,用户 2026-06-01 确认 → 直接 Phase 4);**Phase 4 removal 完成**(4a auth+health / 4b system / 4c admin / 4d `/api/users/me*` / 4e attachments / 4d2 registrations-me + attendances-me-records legacy + 主 spec 队员流迁 `app/v1/my` + 移除 apply-swagger deprecation 后处理 + 终态 contract 断言;contract 280 + full e2e 72 suites/1664 绿);**🎉 Route B 终态达成:全仓 API 只剩 `admin/v1` + `app/v1` + `auth/v1` + `system/v1`,零 `v2` / 零裸 `auth`·`health`·`users` / 零 legacy**(终态由 contract"全部路由仅落 4 canonical 前缀"断言锁定);`/api/open/v1/*` 已于 **2026-06-18 招新一期 T3 首用**(4 → 5 canonical 前缀,见 §2.1 + [`api-surface-policy.md §0`](api-surface-policy.md))。**A 档收尾已完成**:src/ 注释 / 模块 CLAUDE.md / contract-spec header(#269)+ docs 活文档(cos runbook / current-state / api-surface-policy §0+§5/§6 / development / security / docker-smoke / participation / attachment-config)均已 true-up 到终态;历史 v2 设计档(`v2-api-contract.md` / `V2红线与复活路径.md`)按设计意图保留
- **P1-C(Mixed Controller 物理拆分)已被 Route B 全量迁移收口**:原 `controllers/*-legacy.controller.ts`(users / attachments / activity-registrations / attendances)已于 Phase 4 删除,队员自助流在 `/api/app/v1/me*` / `/api/app/v1/my/*`;god-service 现状见 §4 P2 行(P1-4 拆分系列已于 2026-06-10 调研收口)
- **不**自动拆分 `attendances.service.ts`(1428 行,2026-06-23 跨轴只读 +3 只读方法)/ `attachments.service.ts`(911 行)/ `activity-registrations.service.ts`(1020 行,2026-06-23 跨轴只读 +2 只读方法)等 god-service — **P1-4 拆分系列已于 2026-06-10 调研收口**(用户逐项拍板):三模块均已达 [`architecture-boundary.md`](architecture-boundary.md) 政策下的合理形态(详见 §4 P2 行),重新开拆需出现 §6 新触发条件并单独立项;**跨轴只读 goal 只准新增只读方法、不拆 god-service**(沿 api-surface-policy §8)
- **不**自动引入 repository / `*.repository.ts` 抽象层 — service 直连 Prisma 沿用
- **不**在无 contract 审批 / 单独立项的情况下改 controller path / 改 OpenAPI snapshot;Route B 全量迁移已于 2026-06-01 完成(终态 4 前缀),新 endpoint 一律落 `admin/v1` / `app/v1` / `auth/v1` / `system/v1`(沿 [`api-surface-policy.md §0`](api-surface-policy.md))

## 4. 当前最大风险 / 债务(仅 open 项)

| 等级 | 债务 | 处理建议 |
|---|---|---|
| P1 | 前端联调包剩运维侧 P0-H 演练 + P0-I 排错 SOP | 运维侧立项;系统侧无动作 |
| P2 | god-service 体量观察(review #484 G23 true-up,2026-07-03,`pnpm docs:codemap:check` 实时输出为准):**attendances 1428L / activity-registrations 1020L(2026-06-23 跨轴只读 goal 各 +只读方法;授权内增长「只准新增只读方法、不拆 god-service」,codemap god-service WARN)** / attachments 911L(体量 source-only 口径,排除 spec) / users 871L(2026-06-12 微信 T3 +me/wechat 三方法) / **activities 722L(新晋候选,此前未登记;PR12 跨轴只读同批 +只读方法致体量增长)** / recruitment-applications 719L(较此前登记 839L 下降——OCR 判定 / 身份链 / 批量匹配 / 进度展示 / 工作台统计等逻辑已陆续抽至独立 service 文件,仍 >700L 故留观察) | 重开需 architecture-boundary §6 新触发 + 单独立项 |
| P2 | service 单测占比 ~11.8%(26/221 实测) | 刻意策略(e2e 为主,见 §2 测试行) |
| P2 | Mixed Controller 存量 2 处(见 §2.1) | 冻结仅兼容;详 api-surface-policy §5.1 |
| P2 | contract snapshot ~1MB / 35,777 行 | 已接受;review 用 diff,勿整读 |
| P3 | SMS 两表 retention 依赖维护者手动执行(系统侧无自动清理,刻意) | 沿 [`ops/sms-data-retention-sop.md`](ops/sms-data-retention-sop.md)(2026-06-11 P2-6 收口:季度例行 + 报警线;不解锁 cron) |
| P3 | 保险到期无自动提醒(刻意:cron 解锁范围仅生日批;临近到期靠管理员人工查看 coverageEnd) | 提醒诉求出现单独立项(新定时任务 = 新 D 档评审,沿 R-5);字段已留,见 [`archive/reviews/insurance-module-review.md §10`](archive/reviews/insurance-module-review.md) |
| P3 | 招新失败者脱敏清理依赖维护者手动执行(系统侧无自动清理,刻意;含 storage blob 删除) | 沿 [`ops/recruitment-data-retention-sop.md`](ops/recruitment-data-retention-sop.md)(rejected / 轮次结束 30 天后清敏感字段 + 证件照 blob,保留行级脱敏统计;不解锁 cron) |
| P3 | 招新公开报名同轮去重前置于付费核验(配套①成本纪律),`RECRUITMENT_DUPLICATE_APPLICATION`(28003)可被限流内枚举「某身份证号本轮是否已报名」 | **v1 接受不改实现**(维护者 2026-06-18 拍板):免费去重必须前置以省付费核验成本(评审稿 §4 校验顺序冻结);泄露面窄=需已知目标**完整**身份证号 + 同一在开轮次,且受 `@RecruitmentThrottle` IP 10/3600 限速;留痕备案,真实风险出现再单独评估(如 200 泛化 / 核验后才去重);**2026-06-18 系统性审查 R1 复评维持接受**(报告 [`recruitment-phase1-systematic-review.md`](archive/reviews/recruitment-phase1-systematic-review.md);同期 FM-A 卡死恢复 / FM-B 孤儿图补偿 / FM-C 容量原子化 / F-1 上传大小闸已修,B 档,业务语义零变化;**2026-06-19 FM-A 二次收紧**:`verifyOutcome` 前置落库 + resolve 仅救核验已出结果的真卡死行〔在途行不可碰、mismatch 只能 reject〕,零 schema migration 仍 19,见报告 §R1b + CHANGELOG) |
| P3 | 招新二期外籍报名 promote 不进一键发号、需 admin 手动建档(M-1 已知边界,刻意) | **v1 接受**(维护者 2026-06-19 拍板):一键发号仅大陆可派生 birthDate+gender 项;外籍走完门槛/评定/公示后 promote **skip+report**(`reason=foreign-manual-build`,公示名单 `needsManualBuild=true` + 发号结果 skipped 列**发号前可见、不漏判**),admin 走既有 `POST admin/v1/members` 手填大陆派生不出字段建档;外籍报名 promote 后保持 `publicity` 待手动建档(见 §3 + [`recruitment-phase2-review.md §10`](archive/reviews/recruitment-phase2-review.md)) |
| P3 | #399 全仓 review **P2 六项已修(#400-#404)+ P3 9 项已修(#409-#413)**;**接受+登记 3(F7/F8/F13)+ 延后 5(F18 + F1/F2/F5/F6 残余),均无当前运行时危害** | P3 修:F12 即时清漏 / F15 批内 openid 去重 / F16 N+1 / F9 公示口径(#409)· F10 confirm owner(#410)· F11 容量 FOR UPDATE(#411)· F17+F19 扩容文档(#412)· F14 check 脚本(#413)。**接受+登记**(详 [`ai-harness/NEXT_TASKS.md`](ai-harness/NEXT_TASKS.md)):F7 付费核验 cost-DoS(真实核验接通前 DevStub 免费;与 28003 同源)· F8 promote 字典契约(只写固定 canonical 码无注入,seed/ops 不变量)· F13 `BIZ_ADMIN_DESCRIPTION` 计数 cosmetic(改 seed.ts=D 不值)。**延后**:F18 CI `pnpm audit`→G-12 · F2 owner-key(COS 休眠)· attachment.`*.other` 接 enforcement 复核 F1 保留集 · `fast-uri`/@types-supertest dev-only CVE · cos>`fast-xml-parser` <5.7.0(需 4→5 breaking 低危)。冻结报告 [`archive/reviews/full-repo-systematic-review-v0.26.0.md`](archive/reviews/full-repo-systematic-review-v0.26.0.md) 顶部 ✅ P2 + ✅ P3 处理状态 |
| ✅ | 第三轮 review(v0.38.0)findings **已全数收口**(review-then-fix,发版 v0.39.0):**P1 F&A-1+A-4** 队员轴 bind/status/reopen 护栏收口〔bind 拒非 USER/非 ACTIVE + status/reopen 校验 linked role===USER,+3 BizCode〕(#527)· **P2 F&A-2** RBAC 授权配置三服务 8 写点接入 audit + 删 `rbac-roles:243` 僵尸注释 + security.md config-audit 权威规则(+8 AuditLogEvent)(#528)· **P3 F&A-3** 档案面掩码分级〔新码 `member-profile.read.sensitive`,197→198 码 / biz-admin 73→74〕+ **F-7** seed 计数注释 true-up(#529);**A-5/A-6 接受不做**(封顶 200 + ops-admin 高信任,轻度性能面非 DoS,诉求触发再优化) | 冻结报告 [`archive/reviews/full-repo-first-principles-adversarial-review-v0.38.0.md`](archive/reviews/full-repo-first-principles-adversarial-review-v0.38.0.md) 顶部「✅ 修复落地状态」节;`NEXT_TASKS` P1-19 已翻 ✅ |

## 5. 新任务开工前必须检查

门禁见 [`process.md §2`](process.md),任一不满足不开新功能。速记:`pnpm agent:preflight` 全过(工作树 clean · 无 open PR · 未落后 origin/main 三条机械门禁硬判 exit 0;版本三方 / handoff / Unreleased 等仍只读打印) / handoff 与 §1 一致 / Unreleased 无残留 / D 档降速(process §4)/ 拍板未到不动代码 / fresh worktree 先 install + prisma:generate。

## 6. 文档阅读顺序

1. **必读三件套**:本文件 → [`ai-harness/README.md`](ai-harness/README.md)(速查 / 三档 / 分区)→ [`process.md §2/§3`](process.md)(门禁 + 五档)
2. [`AGENTS.md`](../AGENTS.md) **按任务主题选读**(节号见 ai-harness 单页)
3. **按需**:[baseline](srvf-foundation-baseline.md) / [V2 红线](V2红线与复活路径.md) / `ARCHITECTURE.md`(先读顶部说明)/ api-surface-policy
4. **仅在相关时**:运行 SOP(development / testing / deployment / security / ops)与边界图(participation-bounded-context / attachment-config-boundary / architecture-boundary),均在 docs/ 下;权限地图([RBAC_MAP](ai-harness/RBAC_MAP.md));`archive/`(只作证据)
