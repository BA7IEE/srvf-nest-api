# SRVF API v0.15.0 Handoff Index

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → [`docs/srvf-foundation-baseline.md`](../srvf-foundation-baseline.md) → [`docs/V2红线与复活路径.md`](../V2红线与复活路径.md) → 本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。
> 本文件接续 [`docs/handoff/v0.14.0.md`](v0.14.0.md);v0.14.0 之后的工作是 **P0-F 管理面 RBAC 收紧 + Phase 0/1 客户端边界评审 + Phase 1A Swagger Tag 重命名 + App API Phase 2(15 endpoint)** 的 release closeout(详见 §4)。
>
> **本文件定位**:**最小化 release handoff index**,只做版本快照 / 索引 / 操作提示;不写长篇复盘;不逐 PR 长篇展开。
>
> **状态**:本文件在 **`v0.15.0` tag / GitHub Release 创建之前**撰写;tag / Release / current-state 入口刷新在本 handoff PR 合入后由维护者按 §8 / §9 步骤手动执行。

---

## 1. TL;DR

- **v0.15.0 = P0-F + Phase 1A + App API Phase 2 的 release closeout**(0 schema migration / 0 新依赖 / 0 breaking change)
- bump PR 已合入:[#162](https://github.com/BA7IEE/srvf-nest-api/pull/162) / main HEAD `22396c8`
- 本 handoff PR 是**最小化版本快照**(单文件 docs-only;不改 `current-state.md` / `CHANGELOG.md` / `README.md` / 源码 / 测试 / package)
- **`v0.15.0` tag 尚未创建**(latest tag 仍为 `v0.14.0`)
- **`v0.15.0` GitHub Release 尚未创建**(latest release 仍为 `v0.14.0`)
- `docs/current-state.md` 入口刷新尚未执行(等 tag / release 完成后单独 PR;沿 v0.14.0 范式)
- release 完整闭合后**建议**进入 Governance-1 文档治理专项(本 PR **不**启动)
- Phase 1B path alias(`/api/auth/v1/*` + `/api/public/v1/*`)**仍未启动**,作为独立通道后续立项

下一会话**不要**自动启动任何 PR;先读完本文件 §10 + `docs/process.md` + `docs/current-state.md`,再等用户拍板。

---

## 2. Version Status

| 项 | 当前值 | 备注 |
|---|---|---|
| `package.json#version` | **`0.15.0`** | PR #162 已 bump |
| Swagger `setVersion(...)` | **`0.15.0`** | PR #162 已 bump |
| `CHANGELOG.md` v0.15.0 段 | **已存在**(`## v0.15.0 - 2026-05-20`)| PR #162 折叠 Unreleased 时填入 |
| 远端最新 git tag | **仍为 `v0.14.0`** | `v0.15.0` 待 handoff PR 合入后手动创建 |
| GitHub Release Latest | **仍为 `v0.14.0`**(2026-05-17T19:16:06Z)| `v0.15.0` Release 待 tag 后手动发布 |
| `docs/current-state.md` 入口 | **仍指向 v0.14.0** | release 完成后由独立 PR 回填(沿 v0.14.0 范式) |
| main HEAD | **`22396c8`** | `chore: bump version to 0.15.0 (#162)` |
| 工作树状态 | clean | — |

---

## 3. SemVer Decision

`0.14.0 → 0.15.0` = **minor**(沿 v0.6.0 → v0.14.0 全部 minor 节奏;0.x 阶段无 patch 先例)。

理由:

- ✅ 新增 **15 个 `/api/app/v1/*` endpoint**(向后兼容能力扩展;移动端新 surface)
- ✅ **P0-F RBAC 管理面收紧**(rbac / config / users / audit-logs 4 模块接入 `rbac.can()`;Guard `@Roles(...)` 仍保留为入口校验;**不破**既有调用方)
- ✅ **Phase 1A Swagger Tag 重命名**(`@ApiTags` 向 `surface-module` 分类体系收敛;**0 endpoint 变更 / 0 path 变更 / 0 DTO 变更**)
- ✅ 旧 `/api/v2/*` / Admin / Ops / Auth 路径**保持兼容**(Phase 1B path alias 未启动)
- ✅ **0 schema migration**(Prisma 表数 / 字段 / migration 全部 zero drift)
- ✅ **0 新依赖**(`package.json` 仅 bump version 字段)
- ✅ **0 breaking change**(既有接口字段 / 路径 / 主响应字段 zero drift)

---

## 4. Included Work(索引,不展开)

> 细节回到 [`CHANGELOG.md` v0.15.0 段](../../CHANGELOG.md) 与各 PR 评审稿。

### P0-F RBAC 管理面收紧(4 PR)

- **PR-1**(#132):RBAC 管理面 `rbac/*` 接入 `rbac.can()`
- **PR-2 / 2A / 2B**(#133 评审 / #134 / #135 ops-admin grant SOP / #136):config 管理面接入
- **PR-3**(#137 评审 / #138):users 管理面接入
- **PR-4**(#139 评审 / #140):audit-logs 管理面接入

### Boundary / Swagger(2 PR)

- **Phase 0/1 客户端边界评审**(#141):顶层规范 + 现状盘点 + 分阶段路线 + Phase 1 评审 + 4 份 App 边界配套评审
- **Phase 1A Swagger Tag 重命名**(#142):`@ApiTags` 向 `surface-module` 分类体系收敛;**0 endpoint 变更**

### App API Phase 2(9-PR 串 P2-0 ~ P2-8)

- **P2-0**(#143):Phase 2 总评审稿
- **P2-1**(#144):`/me` identity / account / capabilities
- **P2-2**(#145 评审 / #146):`/me/profile` read + update
- **P2-3**(#147 评审 / #148):`/me/password`
- **P2-4**(#149 + #152 lock 评审 / #153 + #154):activities available + detail
- **P2-5**(#150 + #151 index sync 评审 / #155 + #156):my registrations read + write
- **P2-6**(#157 评审 / #158):my attendance-records
- **P2-7**(#159 评审 / #160):my certificates
- **P2-8**(#161):docs closeout

### Release closeout(2 PR)

- **bump PR**(#162):`package.json` `0.15.0` + Swagger `setVersion('0.15.0')` + CHANGELOG Unreleased 折叠为 `## v0.15.0 - 2026-05-20`
- **handoff PR**(本 PR):本 handoff index 单文件

---

## 5. App API Endpoint Index

15 个 `/api/app/v1/*` endpoint(沿 [Phase 0.5 §10.2 D-4](../app-permission-boundary-review.md) `/me/*` 与 `/my/*` 物理分离;App API DTO 与 Admin 物理隔离;**永不返回 L3 字段**):

```text
GET   /api/app/v1/me
GET   /api/app/v1/me/account
GET   /api/app/v1/me/capabilities
GET   /api/app/v1/me/profile
PATCH /api/app/v1/me/profile
PUT   /api/app/v1/me/password
GET   /api/app/v1/activities/available
GET   /api/app/v1/activities/{id}
GET   /api/app/v1/my/registrations
GET   /api/app/v1/my/registrations/{id}
GET   /api/app/v1/my/activities
POST  /api/app/v1/my/registrations
PATCH /api/app/v1/my/registrations/{id}/cancel
GET   /api/app/v1/my/attendance-records
GET   /api/app/v1/my/certificates
```

App 准入语义(沿 [`docs/app-permission-boundary-review.md §10.2`](../app-permission-boundary-review.md) D-5):仅 `User.memberId != null && User.status=ACTIVE && User.deletedAt IS NULL && Member.status=ACTIVE` 的正式队员可用;候选 / 临时编号志愿者**本期不支持**;Admin 兼队员走 linked-member self perspective。

---

## 6. Test / Contract Snapshot

> 数据沿 PR #162 CI 记录;本 handoff PR docs-only,不重跑。

- ✅ PR #162 CI 全绿(lint / typecheck / unit / e2e / contract / docker build / docker smoke)
- ✅ E2E spec 总数 **63**(自 v0.14.0 的 55 spec 增 8)
  - Phase 2 新增 8 个 App API e2e spec:`app-me` / `app-me-password` / `app-activities-available` / `app-activities-detail` / `app-my-registrations-read` / `app-my-registrations-write` / `app-my-attendance-records` / `app-my-certificates`
- ✅ Contract OpenAPI snapshot 已覆盖 **15 个 `/api/app/v1/*` 端点**(沿 [`test/contract/openapi.contract-spec.ts`](../../test/contract/openapi.contract-spec.ts) `EXPECTED_ROUTES`)
- ✅ Docker image build pass
- ✅ Docker smoke pass(`JWT_EXPIRES_IN=15m` + `JWT_REFRESH_EXPIRES_IN=90d` 沿 v0.14.0)

---

## 7. Out of Scope(本 PR 不做)

- ❌ **不**创建 `v0.15.0` git tag
- ❌ **不**创建 `v0.15.0` GitHub Release
- ❌ **不**刷新 `docs/current-state.md` 入口(沿 v0.14.0 范式作为下一步独立 PR)
- ❌ **不**启动 Governance-1 文档治理专项
- ❌ **不**启动 Phase 1B(`/api/auth/v1/*` + `/api/public/v1/*` path alias)
- ❌ **不**改源码 / schema / 测试 / package / CHANGELOG / README / CLAUDE.md / AGENTS.md / ARCHITECTURE.md / .github/

---

## 8. Manual Release Steps After This PR

> 本 handoff PR 合入后,由维护者手动执行以下 3 步。

### 8.1 创建并推送 tag

```bash
# <handoff-pr-squash-commit> = handoff PR 合入 main 后的 squash commit hash
git tag v0.15.0 <handoff-pr-squash-commit>
git push origin v0.15.0
```

### 8.2 抽取 release notes

```bash
awk '/^## v0.15.0 - 2026-05-20/{flag=1; next} /^## v0.14.0 - 2026-05-18/{flag=0} flag{print}' CHANGELOG.md
```

### 8.3 发布 GitHub Release

- title:`v0.15.0`
- target:`v0.15.0` tag
- **mark as latest**
- notes:粘贴 §8.2 抽取的 CHANGELOG v0.15.0 段

---

## 9. Next PR After Release

release 完成后,**先**走入口刷新 PR(沿 v0.14.0 范式):

- **范围**:**仅改** `docs/current-state.md`
- **内容**:回填 §1 表(latest git tag / latest release / `main` HEAD / 最新 handoff / Unreleased 累计清零等)+ §2 当前能力段并入"v0.15.0 已发布"语义
- **不**改其它任何文件

入口刷新 PR 合入后,**建议**(由用户拍板)进入 **Governance-1A 只读审计**(docs/ 体系庞大债务的第一步;沿 [`docs/current-state.md §4`](../current-state.md) P1 债务条目)。

---

## 10. Handoff Metadata

| 项 | 值 |
|---|---|
| Base | `v0.14.0`(2026-05-17T19:16:06Z;指向 `72763f5`) |
| Target | `v0.15.0`(pending) |
| Bump PR | [#162](https://github.com/BA7IEE/srvf-nest-api/pull/162) / `22396c8` |
| Handoff PR | 本 PR(branch `claude/v0.15.0-handoff-min`) |
| Tag | **pending**(本 handoff PR 合入后维护者执行 §8.1) |
| GitHub Release | **pending**(tag 创建后维护者执行 §8.3) |
| `current-state.md` entry refresh PR | **pending**(release 完成后独立 PR) |
| Governance-1 启动 | **pending**(入口刷新 PR 合入后由用户拍板) |
| Phase 1B path alias | **pending**(独立通道,本期不立项) |
| Date | 2026-05-20 |
