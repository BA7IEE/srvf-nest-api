# SRVF API v0.4.0 阶段交接说明

> 面向下一次 AI 开发会话 / Claude Code 使用。
> 阅读顺序固定:`ARCHITECTURE.md` → `CLAUDE.md` → `AGENTS.md` → `docs/srvf-foundation-baseline.md`,然后本文件。冲突一律以 `ARCHITECTURE.md` 与 baseline 为准,本文件不覆盖任何铁律。

---

## 1. 当前仓库状态

- 默认分支:`main`,最新 commit `dd13291`(docs(handoff): add v0.4.0 stage handoff for next AI session)
- **v0.4.0 tag 与 main HEAD 错位**:
  - v0.4.0 tag 指向 `074a2c7`(PR #14 chore: bump version to 0.4.0),**不是** main HEAD
  - main HEAD `dd13291` = v0.4.0 + **2 个 docs-only commit**:
    - `de97337`(PR #15 docs(baseline): record batch 3 BizCode segment allocation;`docs/srvf-foundation-baseline.md §1.1` 段位收口)
    - `dd13291`(本 handoff 文档自身,release 后 docs-only handoff commit)
  - 两个 commit **均未单独 release**;代码 / schema / 测试 / 接口契约与 v0.4.0 完全一致
- 工作树 clean,**open PR**:0(下一次会话开工前请用 `gh pr list --state open` 自行复核;若非空,先与项目维护者确认)
- 派生信息:`u-nest-api-starter` v0.1.6,派生日期 2026-05-04(`u-nest-api-*` 命名是工程契约,**不改名**)

---

## 2. 版本 / tag / release

| 项 | 值 |
|---|---|
| `package.json#version` | `0.4.0` |
| Swagger `setVersion(...)` | `0.4.0`(`src/bootstrap/apply-swagger.ts:20`) |
| 最新 git tag | `v0.4.0`(指向 `074a2c7`,**不是** main HEAD) |
| GitHub Release | `v0.4.0 — V2 batch 3 activities, registrations, attendances`(2026-05-11T09:09:05Z;not draft / not prerelease) |
| v0.4.0 之后 main 上的 diff | 2 个 docs-only commit:`de97337`(PR #15 baseline §1.1 段位收口) + `dd13291`(本 handoff 文档);均无 release |

改版本时:`package.json#version` 与 Swagger `setVersion(...)` 两处同改;Release 标题与 tag 同步打;handoff 文档同步刷新本表。

---

## 3. 已完成模块

v1 模板基线(继续生效,**不动**):

- `auth`(`username` / `memberNo` 双入口,服务端语义扩展,LoginDto schema **零漂移**)
- `users`(本人 + 管理员 CRUD,自我保护,最后一个 SUPER_ADMIN 保护)
- `health`(`/api/health` / `/api/health/live` / `/api/health/ready`)

V2 第一阶段(srvf-foundation,v0.2.0 落地):

- `dictionaries`(dict_types + dict_items 双表 + 父子树)
- `organizations`(单根树 + last-root 保护 + nodeTypeCode 走字典)
- `members`(`memberNo` 全局唯一不复用 + `gradeCode` 字典校验)
- `member-departments`(一人一部门 + partial unique + PUT 幂等)

V2 批次 1(v0.3.0):

- `member-profiles`(1:1,5 字典字段)
- `emergency-contacts`(N:1,priority ASC 排序)

V2 批次 2(v0.3.0):

- `certificates`(N:1,4 态状态机 pending / verified / expired / rejected,verify / reject / qualification-flag 动作)

V2 批次 3A(v0.4.0):

- `activities`(4 态状态机 draft / published / cancelled / completed,USER 与 ADMIN 同路由 service 按 Role 过滤,经纬度 `Decimal(10,7)`)
- `activity-registrations`(USER 自助 vs ADMIN 代报名拆开 + 4 态闭集 pending / pass / reject / cancelled + capacity 仅统计 pass + partial unique 取消后允许重报 + CSV 名单导出 `StreamableFile`)

V2 批次 3B(v0.4.0):

- `attendances`(Sheet + Record 双 model,3 态闭集 pending / approved / rejected,APD 完整审核视图,后端事务内生成 `previousSnapshot` + `version+1` 完整快照,同 memberId 跨 Sheet/Activity 时间不重叠左闭右开,serviceHours 三档校验,approve-only 触发 `attendance.recorded`,/me 仅 approved Sheet 内 records)

骨架占位(**不落地**):

- `src/modules/ai/`:仅 `README.md` 占位
- `src/common/storage/`:仅 interface + types,**无任何 Provider 实装**

---

## 4. 当前接口总数

- **累计 84 接口**(进入 contract snapshot 保护范围)
  - v1 14
  - V2 first stage 29(dict 13 + org 7 + members 6 + member-dept 3)
  - 批次 1 7(profile 3 + ec 4)
  - 批次 2 8(certificates)
  - 批次 3A 17(activities 7 + registrations 10)
  - 批次 3B 9(attendances 8 管理端 + 1 队员端)
- v1 14 接口 schema + paths **严格 zero drift**(LoginDto / UserResponseDto 不漂移)
- V2 70 接口全部进入 OpenAPI 快照保护

---

## 5. 关键测试基线

> **数字来源**:CHANGELOG.md v0.4.0 段(对应 commit `de97337`,与 PR #12 / PR #15 评审时报告的本地 + CI 数字一致)。**本次 review 未实跑**;下一批次开工前建议先本地全量回归一次,确认无环境侧漂移。

| 命令 | 数量 | 备注 |
|---|---|---|
| `pnpm test`(unit) | **517** / 4 suites | 含 BizCode 元属性遍历自动覆盖 |
| `pnpm test:e2e` | **592** / 30 suites | v1 + 批次 1 + 批次 2 + 批次 3A + 批次 3B 零退化 |
| `pnpm test:contract` | **154** + 2 snapshots | 84 接口 contract zero drift |
| `pnpm lint` | 0 warnings | `--max-warnings 0` 严格模式 |
| `pnpm typecheck` | PASS | `src/` + `test/` 双 tsconfig |
| `pnpm build` | PASS | `dist/` 生成 |
| CI 3 jobs | 全绿 | Lint/Typecheck/E2E ~2m47s + Docker build ~56s + Container smoke ~1m42s |

任何 PR 出现 v1 14 接口契约漂移、批次 1 / 2 / 3 既有接口契约漂移、测试基线下降,**视为不可合并**。

---

## 6. 批次 3 核心业务链路

```
Activity(draft → published)
  → ActivityRegistration(USER 自助 / ADMIN 代报名)
    → pass / reject / cancelled(partial unique 取消后允许重报;capacity 仅统计 pass)
      → AttendanceSheet(pending,事务内 create Sheet + N Records)
        → review-detail(R25 Activity+Sheet+Records[含 Member])
        → edit(D38 后端事务内生成 previousSnapshot + version+1;旧 records 软删 + 新 records 创建)
        → approve(R31 contributionPoints 必填;同事务触发 attendance.recorded)
        → reject(reviewNote 必填)
          → /me/attendance-records(Q-A14 仅 approved Sheet 内 records)
```

核心不变式(代码层强约束,**改 V2 代码前必须保留**):

- 同 memberId × `[checkInAt, checkOutAt)` 左闭右开,跨 Sheet / 跨 Activity 全局不重叠
- `serviceHours` 未传自动 `(checkOut - checkIn) / 3600`;`<= 0` 拒(`@Min(0.01)` + service 双兜底);`> 跨度` 拒(`22071`);允许 `< 跨度`(吃饭休息不计入)
- `registrationId !== null` 时校验 `registration.activityId === sheet.activityId`(`22073`,mismatch 与 not found 同码,沿 §1.7 风格)
- `attendance.recorded` 业务事件 **仅 approve 触发**,rejected / submit / edit / delete 一律不触发
- CSV 导出走 `StreamableFile`,`ResponseInterceptor` 已通过 `instanceof` 自动跳过,**未改 interceptor**;XLSX 入参直接 400

---

## 7. 不做项分层(关键:严格区分)

> 本节是**下一会话最容易"顺手补"的雷区**,严格按四档区分,**禁止合并理解**。

### 7.1 永久不做(批次 3 决议表锁定,**不会复活**)

任何 PR / 评审稿不得新增以下能力。如要做,必须 `ARCHITECTURE.md §9` 升级路径单独立项,且**不在批次 4 范围内**:

- `GET /api/v2/users/me/service-hours` 服务时长汇总接口(Q-A5)
- `POST /attendance-sheets/:id/clone` rejected Sheet 复制接口(Q-A4;前端从 `review-detail` 取字段组装新 POST)
- `PATCH /activities/:id/complete` 接口形态(Q-A11 不实装;`completed` 状态走字典占位)
- XLSX 名单导出(Q-A6;`format=xlsx` 入参直接 400)
- 动态表单引擎(R19;`extras` / `previousSnapshot` / `registrationSchema` 仅 `@IsObject()` / Json passthrough)
- 独立 `AttendanceRecord` CRUD 路径(Q-A9;通过 Sheet `review-detail` 一次返回)
- BizCode `22050 ATTENDANCE_RECORD_NOT_FOUND` / `22042 VERSION_CONFLICT` **不开**
- PG `EXCLUDE` 时间不重叠约束(只在 service 层做)

### 7.2 本批次不做 / 留批次 4 决议(**不是永久不做**)

CHANGELOG v0.4.0 段明文标注"留批次 4 决议",**形态未拍板**,批次 4 立项时再讨论:

- **`contribution_points` 独立流水表 / cron-job**(D49 / R32):是否独立流水表 vs 折算视图、计算规则、是否引 cron,**全部留批次 4 决议**
- **`completed` 状态推动机制**(配合 §7.1 `Activity.complete` 接口不实装):接口形态永久不做,但**推动机制**(手动 vs cron vs 自动 vs 派生)留批次 4

### 7.3 V2 复活路径(延后,**未拍板**)

V2 调研期已识别但第一阶段未做,触发条件见 `ARCHITECTURE.md §9` / `docs/srvf-foundation-research.md §3`:

- `attachments`(附件元数据 + 上传 Provider:本地 / OSS / R2 选型)
- `audit_logs`(把现有 `auditPlaceholder` hook 落到真实持久化;读审计范围待评估)

启动条件:用户单独立项 + research / baseline / interview-brief / data-model-draft 四文档复盘。

### 7.4 events / event_participants 命名废弃(能力已拆分落地)

`docs/srvf-foundation-baseline.md §1.1` 明文锁定 BizCode 段位归属:

- **`events` / `event_participants` 不会以本名复活**;v0.2.0 时 `docs/v2-plan.md §7.1` 列出的"events / event_participants V2.x 复活路径"已被批次 2 / 批次 3 按业务语义拆分为以下模块落地:
  - `certificates` = `180xx` + `181xx`(批次 2 已实装,v0.3.0,2026-05-10)
  - `activities` = `200xx` + `201xx`(批次 3 已实装,v0.4.0,2026-05-11)
  - `activity_registrations` = `210xx` + `211xx`(批次 3 已实装,v0.4.0,2026-05-11)
  - `attendances` = `220xx` + `221xx`(批次 3 已实装,v0.4.0,2026-05-11)
- BizCode 段位维度**不存在废弃保留**——所有段位要么已实装,要么基线预留 / V2 候选预留 / 未规划预留;v0.2 baseline 修订时曾把 `180xx` 误标"`events` 废弃保留",已由 baseline v0.3(2026-05-11)更正为 `certificates` 已实装
- 未规划模块 BizCode 段位从 `230xx` 起

### 7.5 V1 / V1.1 阶段铁律继续生效

`CLAUDE.md §1` / `§17.3` 一切"不做"项继续生效:RBAC、Redis、队列、定时任务、refresh token、本人改密码接口、文件上传 Provider、LLM、pgvector、多租户、`*.entity.ts`、Prisma 全局软删中间件、`LocalStrategy` 等。

---

## 8. 本地遗留已清理

- 工作树 clean,无未提交改动
- v0.2.0 后续 housekeeping 已记录在 CHANGELOG(`auth-login.e2e-spec.ts` 间歇性 404 / `ORGANIZATION_ROOT_ALREADY_EXISTS` message 措辞优化),**非阻塞**,留后续 PR
- 批次 3 schema 一次入库(commit `31c8187`),3A / 3B PR 均未动 schema / migration / seed / reset-db / response interceptor / event-placeholder / audit-placeholder / package.json
- 3A + 3B PR 均**未引入新依赖**(CSV 手写 `escapeCsvField`;previousSnapshot Json passthrough)
- v0.4.0 release 后追加 2 个 docs-only commit(`de97337` PR #15 baseline §1.1 段位收口 + `dd13291` 本 handoff 文档),均无 release

---

## 9. 下一阶段候选(均**未拍板**,需用户决议)

> 以下任一启动前,必须按 `CLAUDE.md §18` / baseline §14 流程立项,不得直接进入开发。
> **不在本节出现** = `§7.1` 永久不做、`§7.4` 段位废弃保留,**不要**作为候选提出。

| 候选 | 形态 | 关键悬而未决 | 对应不做项分层 |
|---|---|---|---|
| **批次 4 贡献值核算** | `contribution_points` 流水表 + 推动机制 + `/me/service-hours` **不复活**(永久不做),如需汇总走"数据统计 / APP 数据"模块 | 是否独立流水表 vs 折算视图;计算规则形态;是否引 cron | §7.2 留批次 4 决议 |
| **批次 4 `completed` 推动机制** | 配合 `Activity.complete` 接口永久不做,只解决推动方式(手动 / cron / 自动 / 派生) | 与上一候选可能合并 | §7.2 留批次 4 决议 |
| **数据统计 / APP 数据模块** | 队员侧聚合视图(服务时长、参与活动数、证书等) | 模块边界、缓存策略、是否引入 Redis | 新模块(段位从 `230xx` 起) |
| **`attachments` 复活** | 附件元数据 + 上传 Provider 选型(本地 / OSS / R2) | 上传链路、安全策略、`storage` interface 何时实装 | §7.3 V2 复活路径 |
| **`audit_logs` 复活** | 把现有 `auditPlaceholder` hook 落到真实持久化 | 持久化形态(同库表 vs 单独存储)、读审计范围 | §7.3 V2 复活路径 |

**不建议**作为下一阶段做的事:RBAC、Redis、refresh token、微信小程序登录、多租户、LLM / pgvector(均触发 `ARCHITECTURE.md §9` 升级,需用户单独立项);**events / event_participants** 已被 `activities / activity_registrations` 替代,`180xx` 段位废弃保留,**不复活**。

---

## 10. 推荐下一步

按"用户决议 → 立项 → 评审 → 开发"四步走:

1. **由用户拍板下一批次主题**(批次 4 / attachments / audit_logs 三选一,优先级由用户业务节奏决定)
2. **复盘 V2 调研期已锁定四文档**,确认拟做主题在四文档中的约束与悬而未决问题:
   - `docs/srvf-foundation-baseline.md`(13 项 A 档基线规范)
   - `docs/srvf-foundation-research.md`(§3 / §4 / §6 边界)
   - `docs/srvf-foundation-data-model-draft.md`(数据模型草案)
   - **`docs/srvf-foundation-interview-brief.md`**(访谈纪要,业务原始诉求源)
3. **产出立项稿**(对应 D6 / D7 / D8 evaluation)+ 评审收口(对应 `srvf-foundation-baseline.md §14` 验收门槛)
4. **任务卡拆分 + 段位分配 + 段位锁定**(`230xx` 起未规划模块段位预留)
5. **开发阶段**:每批次按"schema → 业务模块 → docs + CHANGELOG"三 PR 拆分,沿用 v0.3.0 / v0.4.0 已验证的节奏

如果用户在下一会话直接给出主题,**不要**跳过第 2 步上手写代码;若四文档与新主题冲突,先停下来回到四文档讨论。

---

## 11. 下一会话启动提示词(直接复制给下一次 AI)

> 我是 SRVF API 项目维护者。当前版本 v0.4.0(2026-05-11 发布),累计 84 接口 contract zero drift,批次 3(activities + registrations + attendances)已落地。
>
> **仓库状态**:
> - main HEAD `dd13291`(本 handoff 文档自身),**比 v0.4.0 tag `074a2c7` 多 2 个 docs-only commit**:`de97337` PR #15 baseline §1.1 段位收口 + `dd13291` 本 handoff 文档;代码 / schema / 测试 / 接口契约与 v0.4.0 完全一致,均未单独 release
> - open PR:0(开工前请用 `gh pr list --state open` 自行复核;若非空,先与本人确认)
> - 工作树 clean
> - 测试基线(CHANGELOG v0.4.0 段;非实跑):unit 517 / e2e 592 / contract 154 + 2 snapshots / CI 3 jobs 全绿
> - v1 14 + V2 70 接口 schema + paths zero drift,任何漂移不可合并
>
> 改任何代码前,请严格按以下顺序读完:
> 1. `ARCHITECTURE.md`(v1 蓝图 + §9 升级路径 + §11 V1.1 加固 + §12 V2 调研)
> 2. `CLAUDE.md`(v1 §1-§16 + V1.1 §17 + V2 §18 三层铁律)
> 3. `AGENTS.md`(与 CLAUDE.md 同步)
> 4. `docs/srvf-foundation-baseline.md`(V2 派生项目 13 项 A 档基线规范,**含 §1.1 段位分配**)
> 5. `docs/srvf-foundation-research.md`(§3 / §4 / §6 边界)
> 6. `docs/srvf-foundation-interview-brief.md`(业务访谈纪要)
> 7. `docs/srvf-foundation-data-model-draft.md`(数据模型草案)
> 8. `docs/v2-plan.md` / `docs/v2-api-contract.md` / `docs/v2-data-model.md`(已落地的 V2 第一阶段)
> 9. `CHANGELOG.md` v0.4.0 段(批次 3 决议表 + 永久不做 vs 留批次 4 决议)
> 10. `docs/handoff/v0.4.0.md`(本交接说明,如已落档)
>
> **不做项 / 命名废弃分四档,严格区分,不要混淆**:
> - **永久不做**(不会复活):`/me/service-hours` / rejected Sheet clone / `Activity.complete` 接口形态 / XLSX / 动态表单引擎 / 独立 `AttendanceRecord` CRUD / BizCode `22042` `22050` / PG EXCLUDE 约束
> - **留批次 4 决议**(形态未拍板):`contribution_points` 独立流水表 / cron-job、`completed` 状态推动机制
> - **V2 复活路径**(延后,未拍板):`attachments` / `audit_logs`
> - **命名废弃**(能力已拆分落地,非"不做"):`events` / `event_participants` 不以原模块名复活,能力已拆分为 `certificates`(180xx,批次 2)/ `activities`(200xx,批次 3)/ `activity_registrations`(210xx,批次 3)/ `attendances`(220xx,批次 3);BizCode 段位维度**不存在废弃保留**,未规划模块从 `230xx` 起
>
> 下一阶段主题尚未拍板。我会告诉你下一批次做什么;在我明确告诉你之前,**不要**动 `prisma/schema.prisma`、不要新建 `src/modules/*`、不要安装新依赖、不要"顺手"实装 `audit_logs` / `attachments`、不要"顺手补" `contribution_points` 表、不要"顺手开" `Activity.complete` 接口。
>
> 候选主题(待用户拍板):
> - 批次 4 贡献值核算(`contribution_points` 流水表 + `completed` 推动机制)
> - 数据统计 / APP 数据模块(队员侧聚合视图;段位 `230xx` 起)
> - `attachments` 复活(附件元数据 + 上传 Provider 选型)
> - `audit_logs` 复活(`auditPlaceholder` 落到真实持久化)
>
> 请先告诉我:你读完上述文档后,对当前项目状态、批次 3 关键不变式(`memberId × [checkInAt, checkOutAt)` 时间不重叠 / `serviceHours` 三档校验 / `attendance.recorded` approve-only / CSV `StreamableFile` 跳过 interceptor)、以及"不做项分四档"的理解是否准确;然后等我拍板下一批次主题,**不要**主动开始写代码。
