## 档位
B(docs 重写 + 2 个脚本参数级调整;T0 §3 PR3,P4 授权绿即合)

## 写集声明
`docs/current-state.md` · `scripts/docs-counts.ts`(仅行标签)· `scripts/docs-readtax.ts`(仅 enforced 开关)· `changelog.d/`

## 做了什么
- current-state 全指针化:151 行 / 89,108 字符 → **3,085 字符(-96.5%)**;§1 计数块接线(`docs:counts:check` 已转严格校验并通过);§2 历史叙事删除,事实指向 CHANGELOG / handoff / live swagger / CODEMAP / RBAC_MAP;§3 暂不启动与 §4 债务**逐项保留**、仅压缩叙事(来龙去脉见 `archive/harness-v1/current-state.md` 快照)
- `docs:readtax:check` 对 current-state 翻 `enforced=true`(首个硬判文件,实测通过);counts 块行标签精简适配预算
- changelog.d fragment 登记(吃狗粮第二单)

## 本次未做
- 未动 AGENTS / CLAUDE / process 正文(PR4/PR5);未动 `src/**`、`prisma/**`、任何测试
- AGENTS / CLAUDE 的 readtax 仍 report-only(PR4 收口后翻)

## 验证
`docs:counts` 接线 → `docs:counts:check` 严格通过;`docs:readtax:check` 通过(current-state 3,085/4,500 enforced);lint ✓ typecheck ✓;全量检查留 CI。
