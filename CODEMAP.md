# CODEMAP — 源码模块导航

> 给 AI 的**源码地图**。文档地图请读 [`docs/README.md`](docs/README.md);当前事实读 [`docs/current-state.md`](docs/current-state.md);长期铁律读 [`AGENTS.md`](AGENTS.md)。
> 本文件**不**列 HTTP endpoint(那是 Swagger `/api/docs` 与 [`README.md`](README.md) 路由总览的事),**不**复制 `AGENTS.md` 规则。

**体量级别**:`S` <500 行 / `M` 500–1500 / `L` 1500–2500 / `⚠G` god-service(service 单文件 >700 行)

---

## src/modules/(36 个业务模块,平铺,**禁止嵌套 system/business/core 子目录**)

| 路径 | 体量 | 职责 | 主要风险 / 本地铁律 | 本地约束 |
|---|---|---|---|---|
| `activities/` | L (6970L) | 活动主资源,4 态状态机；同模块 ownership 含 `ActivityPosition`、Admin 5 路 CRUD + App 1 路岗位列表 | 主 service 1241L 偏厚；岗位 CRUD/扩容递补边界化在 `ActivityPositionsService`(500L)，capacity 基线锁 Activity 后重读；有岗位时读侧 capacity 派生、Activity capacity 不判闸/不递补；岗位写 audit 复用 `activity.publish`；候补线调用 `activity-waitlist-promotion.ts` 并显式传 `activityPositionId` | [`CLAUDE.md`](src/modules/activities/CLAUDE.md) · [`docs/participation-bounded-context.md`](docs/participation-bounded-context.md) |
| `activity-registrations/` | L (4677L) | 活动报名,5 态 + partial unique + 岗位级 FIFO 候补/自动递补 + CSV export + 跨轴只读 | service 1603L(沿 §4 体量观察)；既有 `(activityId,memberId)` active partial unique 逐字不动；三路 create 接 `activityPositionId`，容量/approve/cancel/promotion/排位按 `(activityId,activityPositionId)` 隔离，null 无岗位路径沿旧；Admin 列表 additive 岗位摘要；固定 Activity→Registration 锁序；8 处判权走 `authz.explain`，v0.49.0 起扁平列表按组织范围下推 | [`CLAUDE.md`](src/modules/activity-registrations/CLAUDE.md) · [`docs/api-surface-policy.md §5.1`](docs/api-surface-policy.md) |
| `activity-feedbacks/` | S (F4 闭环) | 活动评价；completed 活动按 endAt 窗口、approved-only 到场资格、App self-scope / Admin 实名聚合 | App PUT/GET 固定 3 读（PUT +1 写）；Admin list/summary 固定 3/4 读；QueryService 单次 aggregate 接入 activity participation-summary；live partial unique P2002→35002；F4 真实 DB E2E 锁资格/统计/并发/no-audit；禁止 import 三个 participation god-service，评价不进 audit/结算 | [`CLAUDE.md`](src/modules/activity-feedbacks/CLAUDE.md) · [`docs/participation-bounded-context.md`](docs/participation-bounded-context.md) |
| `ai/` | S (placeholder) | LLM / 向量占位 | **本期不实现**;README 占位 | [`docs/current-state.md §3`](docs/current-state.md) |
| `announcement-import/` | S | 公告导入两段式工具(终态 scoped-authz PR11,2026-07-02):`POST admin/v1/announcement-import/{preview,execute}` 2 路由 + R 模式 2 码 `announcement-import.{preview,execute}.record`;preview 零写入逐行诊断(ok/blocked/already-exists/needs-manual)/ execute 幂等落库(单行失败不影响其它行);组织行 nodeType 恒为 `group` | **只做锚定解析 + 编排,不含第二套校验**:任命 5 校验/分管校验/closure 维护/audit 全部只存在于被复用的 `OrganizationsService`/`PositionAssignmentsService`/`SupervisionAssignmentsService`;preview 零写入靠三者 `create()` 新增的 `{dryRun?}` 沙箱哨兵参数(校验+写入真实执行后提交前整体回滚),不是另起只读校验;双锚铁律(execute 无 memberNo 即拒,不按姓名猜) | [`CLAUDE.md`](src/modules/announcement-import/CLAUDE.md) · [`docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md`](docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md)(冻结评审稿 §8.4/§11 PR11) |
| `attachment-configs/` | L (2175L) | 附件配置三表(type / mime / size) | **不合表、不抽 facade、override-with-default** | [`docs/attachment-config-boundary.md`](docs/attachment-config-boundary.md) |
| `attachments/` | ⚠G (service 967L) | 多态附件主模块 + RBAC 业务面首批接入 | `AttachmentContentValidator` 统一 object/buffer 黑名单与魔数 fail-close;过期附件不签 URL、公开 content owner 过滤过期行;L3 字段隔离;certificate owner 批量 K→1;仅 Attachment key namespace 的上传/删除 durable intent 接入 `StorageObject`/`StorageObjectOperation` ledger，JIT 可读 available/legacy_unverified/provider_unknown、STRICT 仅可读 available，由 dedicated worker 收敛/恢复(非 cron/queue)，不代表 repo-wide storage closure | [`CLAUDE.md`](src/modules/attachments/CLAUDE.md) · [`docs/attachment-config-boundary.md`](docs/attachment-config-boundary.md) · [`runbook`](docs/ops/attachment-storage-consistency-rollout.md) |
| `attendances/` | ⚠G (service 1781L) | 考勤主表 5 态(含终审)+ contribution 计算 + 跨轴只读(2026-06-23)+ **活动 GPS 自助打卡 F2**(`AppActivityCheckInsController` 3 operation；`AppActivityCheckInsService` 为 create/CAS 事务 owner)+ **F3 Admin 只读面**(`AdminActivityCheckInsController` 2 GET；`AdminActivityCheckInsService` 以 `attendance.read.sheet` + activity ref 编排 authz；`ActivityCheckInQueryService` 提供证据分页与考勤草稿)；**活动岗位 F4** 把岗位 `startAt/endAt/attendanceRoleCode` 接入打卡、考勤 record 与草稿；**终审两方法 PR9(2026-07-02)起判权走 `authz.explain`(本仓首个 authz 消费者);其余 8 处调用位点(create/read×多/update/delete/approve/reject)PR12(2026-07-02)起也切 `authz.explain`**;F2/B2 新增 listAllSheetsForAdmin 的 q/activityQ/organizationId/includeDescendants/dateFrom/dateTo/expand 过滤 + expand 展开映射(+70L) | GPS 打卡以 `ActivityCheckInPolicy` + `ActivityCheckInFieldPolicy` + `ActivityCheckInPresenter` + 纯函数 Haversine 隔离状态/时间、safe allowlist、DTO 映射与 geofence；F3 Admin list/draft 各固定 4 次业务查询(authz 分开计)，全链零写 Sheet/Record；F4 在既有 Activity→Registration 锁后重读内选择岗位窗，考勤批量 registration IN 同步选择岗位窗，草稿岗位角色继续喂给既有 contribution 规则；无岗位回落活动窗 / `member`，不改 policy / 贡献算法 / 锁序。原始坐标只落 evidence 表，App/Admin 均 deny raw 坐标与 accuracy；既有 5 个考勤边界类(state-machine / audit-recorder / time-overlap-policy / contribution-calculator / presenter #280)不改。P1-4 拆分系列 2026-06-10 调研收口,⚠G 仅体量观察(沿 current-state §4);v0.49.0 起扁平 sheet 列表按 activity.organizationId 下推可见组织集、队员记录/贡献汇总对 member ref 点判;App 端点另见 `controllers/app-my-attendance-records.controller.ts` 与 `controllers/app-activity-check-ins.controller.ts`,GPS Admin 只读面见 `controllers/admin-activity-check-ins.controller.ts`,跨轴只读队员面在 `controllers/admin-member-attendance.controller.ts`;**PR9 真收紧:自审 22074(SA 亦拒)/ 一级同人默认 22075(env `ATTENDANCE_ALLOW_SAME_REVIEWER` 可配)/ scoped RoleBinding 通路;PR12 起 submit/list 带 `{type:'activity',id}` ref、findOne/reviewDetail/edit/softDelete/approve/reject 带 `{type:'attendance_sheet',id}` ref;其余 deny 30100 不变** | [`CLAUDE.md`](src/modules/attendances/CLAUDE.md) · [`docs/participation-bounded-context.md`](docs/participation-bounded-context.md) |
| `audit-logs/` | M (715L) | 写入即不可改不可删(A-1 红线);`AuditLogEvent` 122 项 | 业务写路径 + C-2 明确的管理端敏感读取均落库;普通读在查询后、CSV 在 generator 前、签名 URL 在 provider 前 fail-closed | [`auth-jwt-refresh`](docs/reference/auth-jwt-refresh.md)` |
| `auth/` | M (2061L) | 登录 / refresh / logout / logout-all / 找回密码 + OTP 登录 + 微信登录与绑定 + identity step-up(pre-auth 与 step-up service 平铺) | **不引入 `LocalStrategy`**;`createSession` 仍是三种登录唯一签发点；`IdentityStepUpService` 仅验证当前因子、HKDF 两域 proof 与成功 audit，UsersModule 单向 import 消费；pre-auth 微信真实 bind/rebind 事务内撤旧 refresh 后才建新 family | [`CLAUDE.md`](src/modules/auth/CLAUDE.md) · [`AGENTS.md §2` 永久铁律](AGENTS.md) · [`docs/security.md`](docs/security.md) |
| `authz/` | M (2201L;F3 前 1900L 级,原「S」系陈旧,随本批 true-up) | **统一判权大脑**(终态 scoped-authz PR8,2026-07-02):AuthzService(三源 grant 推导 + covers + ActionConstraint〔同人开关 PR9 起经 ctx 从 app.config env 注入〕)+ ResourceResolver(11 类归属解析,fail-close);**PR10(2026-07-02)+ explain 端点** = 模块第一个 controller(`AuthzController`,`POST admin/v1/authz/explain` 1 端点 + 1 码 `authz.explain.decision` 绑 ops-admin;**F3「C 组」(2026-07-04,路线图 §4 C2/C3 + D8)+ explain-batch(扩 `AuthzController`,≤200 批量壳同 11 值枚举)+ action-state/batch(第二个 controller `ActionStateController` + `ActionStateService` + `action-state-checks.ts` 注册表 12 项:allowed = authz ∧ 状态机只读,reason ∪ `state_forbidden`;三 StateMachine 以零依赖纯类入本模块 providers,不 import 业务 module 不成环);+2 码 `authz.{explain-batch,action-state}.decision` 绑 ops-admin;**薄编排 `AuthzExplainService`:调用者 rbac.can → 目标用户加载〔不存在/软删 10001〕→ 委托 AuthzService.explain 原样返;**deny 是 200 数据非错误**〔resource_not_found 亦 decision reason〕;reason 11 值枚举入 OpenAPI 契约锁;无 audit) | 🔴 **PR9(2026-07-02)起首个消费者 = attendances 终审两方法**(自审/同人约束现网生效);**PR12(2026-07-02)起 activities/activity-registrations/attendances 三模块(participation 首批)全量切 authz.can/explain**(GLOBAL 行为逐字不变,scoped 持有者树内获新点动作能力);members/certificates/content/notifications 等其余业务面判权仍走 rbac.can(逐面迁移留后续批);无 ref 判权逐字等价 rbac.can(等价矩阵 e2e 行为锁);R5 副职零推导 / BD-2 不 hardcode 部门(`bd2-department-literal-gate.spec` CI 闸);explain 是只读诊断出口,**不改判权语义** | [`CLAUDE.md`](src/modules/authz/CLAUDE.md) · [`docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md §5`](docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md) |
| `certificates/` | M (1410L) | 证书 N:1 + 4 态闭集 + verify/reject | service 634L (large-service watch);v0.49.0 起 admin 嵌套资源按 certificate/member ref 走 scoped authz；**不**属 participation 上下文(独立 member-qualifications) | [`docs/participation-bounded-context.md`](docs/participation-bounded-context.md)(明确排除) |
| `content/` | M (2115L,T4) | CMS 内容发布(第 28 模块):admin 面(T2)内容 CRUD + 状态机 draft/published/archived(立即生效无 cron)+ 封面双指针 + 附件经 `AttachmentsService`(content-image/content-file)+ 正文 `attachment:<id>` 占位读时改写 + viewCount(详情不增);open 面(T3,`open/v1/contents` @Public + 第 10 throttler content-public)+ app 面(T4,`app/v1/contents` canUseApp 准入 + 5 档可见性 public/member/formal_member/department/management)读取面 list/detail(详情不可见 → 404 防枚举 + viewCount 自增) | service 577L;判权 R 模式 `content.*.record` 5 码(admin);读取面 open=公开无码 / app=canUseApp 准入无码,可见性纯函数 `content.visibility.ts`(21 单测);附件写路径走 `AttachmentsService` rbac(`attachment.{upload,delete}.content-*`),content 读取面自签且仅过文章可见级后返(范围例外 a);读者出参零 authorUserId / 零 visibleOrganizationIds | [`docs/archive/reviews/content-module-review.md`](docs/archive/reviews/content-module-review.md)(冻结评审稿) |
| `contribution-rules/` | M (914L) | D14 预填规则；D-RULE-1 起未软删 ACTIVE 槽位按 `activityTypeCode × attendanceRoleCode` 唯一，threshold 仅为规则内档位 | create/激活事务内 pair 预查 + DB partial unique/P2002→23002；calculator 对漂移重复 pair fail-closed，绝不按排序取首条 | [`docs/participation-bounded-context.md`](docs/participation-bounded-context.md) |
| `dictionaries/` | M (1057L) | 字典双表 + 父子树 + 系统内置防误删守卫(W3) | service 521L(large-service watch);同 surface 双 controller(**非 surface Mixed**) | [`docs/api-surface-policy.md §5.1`](docs/api-surface-policy.md) |
| `emergency-contacts/` | S (571L) | N:1 紧急联系人 | 子资源 | — |
| `health/` | S (137L) | 健康检查(live / ready) | **模块结构例外**:只有 module + controller,**无 service** | [`naming-dto-validation`](docs/reference/naming-dto-validation.md)` |
| `insurances/` | M (1551L) | 自购保险(App self-scope)+ 队保单 + 覆盖名单 + 报名门槛查询 | 覆盖名单 partial unique 在 migration 直写;`InsuranceRequirementService` 是唯一跨模块出口(activity-registration 单向依赖) | [`docs/archive/reviews/insurance-module-review.md`](docs/archive/reviews/insurance-module-review.md) |
| `member-departments/` | L (2109L) | 一人一部门 partial unique(旧面)+ memberships 终态全归属面(新面);两面共写 `member_organization_memberships`;F4「D 组」(2026-07-04,路线图 §4)+`MembershipsAdminController` 扁平/组织轴增强面:分页总表(expand=member,organization,D6)/ GET :id detail(read.record 预埋孤码实装)/ conflicts 只读诊断(4 类体检)/ **transfer 唯一写端点**(单事务 end 旧 + create 新)+ 组织轴归属分页 / 队员下拉(复用 MembersService.options 同一份投影) | partial unique 在 schema 显式;5 处写点(memberships create/end/**transfer**,legacy set/remove)接 `AuditLogEvent`(`membership.set`/`membership.end`/`membership.transfer`,viaPath ∈ {membership, department, membership-transfer} 区分),PATCH 不审计;transfer 一次迁移一条留痕(end+create 两腿不再各写 set/end) | [`CLAUDE.md`](src/modules/member-departments/CLAUDE.md) |
| `member-profiles/` | M (1337L) | 1:1 子资源,含敏感字段;`documentNumber`/`mobile` 默认掩码,`member-profile.read.sensitive` 解明文(§F&A-3,镜像 recruitment) | **L3 字段不外暴**;白名单严格 | [`docs/security.md`](docs/security.md) |
| `members/` | ⚠G (service 1231L) | 队员主资源 + 关联账号闭环 | v0.49.0 起 list/options 按 active PRIMARY membership 下推可见组织集，detail/写按 member ref 点判；全局 `memberNo` 不复用；Member lifecycle 行锁线性化离队与账号/任职/分管/direct binding 恢复入口；offboard 同事务关闭全部当前授权来源；最后 ops-admin 保护、refresh 撤销与 audit 语义保留 | [`CLAUDE.md`](src/modules/members/CLAUDE.md) · [`docs/srvf-foundation-baseline.md`](docs/srvf-foundation-baseline.md) |
| `meta/` | S | F1「A 组」net-new(admin-api-fe-integration-roadmap.md §4 A7):跨资源批量 id→label 解析,`POST admin/v1/meta/resolve-labels`(1 路由 + 1 码 `meta.resolve.label` 绑 ops-admin) | **只读、无 audit、无 schema**;不注入其它业务模块 service(镜像 `authz/resource-resolver.service.ts` 自包含 switch-per-type 范式);两层权限——入口码〔能否用本工具〕+ per-type 各资源既有 `.read.*` 码〔能读哪些 type,D2〕;`activity` type 无码(镜像 list/findOne `[auth]` 现状,RBAC_MAP §2.4 BD-3 won't-do);无权 type / 不存在 id 静默省略(D5 防枚举) | [`docs/plans/admin-api-fe-integration-roadmap.md`](docs/archive/reviews/admin-api-fe-integration-roadmap.md) |
| `notifications/` | L | 两个 09:00 Asia/Shanghai cron(生日/到期)+站内/微信/SMS 统一通知；D-Outbox 后 cron 只 enqueue，notifications-owned marker/状态/audit 与 `notification_outbox_intents` 同事务，独立 worker 以 PostgreSQL `SKIP LOCKED` + lease/fencing 多实例消费 | 微信/admin SMS generation 各有单 active partial unique；known 文案 canonical 脱敏、worker raw strict；SMS SENT log+delivery 同短事务；provider 事务外 at-least-once；成功 30d/死信 90d 手动 retention；cron 恰好 2，0 Redis/queue | [`CLAUDE.md`](src/modules/notifications/CLAUDE.md) · [`docs/ops/notification-outbox-retention-sop.md`](docs/ops/notification-outbox-retention-sop.md) |
| `organizations/` | L (1556L;service 696L) | 组织树;F4「D 组」+`GET /tree-with-summary`(树 + 每节点 ACTIVE 归属计数,单 groupBy 禁 N+1,展示读非判权) | 树形结构 | [`CLAUDE.md`](src/modules/organizations/CLAUDE.md) |
| `permissions/` | L (2213L) | RBAC 4 表 + `RbacService.can()`；GLOBAL permission resolution 每请求直读 PostgreSQL | `rbac.*` 14 条权限点;**`rbac/me/permissions` 方法级 Mixed 暂不拆 (P1-A)** | [`CLAUDE.md`](src/modules/permissions/CLAUDE.md) · [`auth-jwt-refresh`](docs/reference/auth-jwt-refresh.md) / §13` · [`docs/api-surface-policy.md §5.1`](docs/api-surface-policy.md) |
| `positions/` | S | 职务定义 + 职务规则配置面(终态 scoped-authz PR3):`OrganizationPosition` + `OrganizationPositionRule` CRUD;seed 6 领导职务 + 30 默认规则(R4/R6/R8);两 controller(positions 5 + position-rules 4)+ R 模式 8 码 `position.*.definition` / `position-rule.*.record` | **纯配置定义,绝不进任何判权路径**(消费它的 policy=PR7 / assignment=PR4 / authz=PR8);删除守卫:职务被规则引用禁删(32003) | [`docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md`](docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md)(冻结评审稿 §3.2/§3.3/§7.2) |
| `position-assignments/` | M (service 539L) | 任职双轴管理面(终态 scoped-authz PR4):`OrganizationPositionAssignment` 双轴 CRUD + 撤销 + 历史链;单 `PositionAssignmentsController`(`@Controller('admin/v1')` 跨 org/member/flat 3 根,**8 路由** = 旧 5 + F5/E1〔2026-07-04,路线图 §4〕全局分页总表〔expand=member,position,organization,D6;缺省含 REVOKED 历史〕+ detail + `POST /preview`〔dry-run 任命校验逐项收集 violations、零写入、与 create 共用 policy〕)+ R 模式 4 码 `position-assignment.{read,create,revoke}.record` / `.read.history`;新任命执行 active 配置、`allowMultiple/maxCount` 严格上限、Position/Rule 兼任交集与当前有效 membership，`required/minCount` 保持 advisory;任命·撤销写 audit(`position_assignment`) | 任职数据由 AuthzService 消费；任命 policy 不参与 action 判权。写路径按 Member → Position → Rule 锁后重算；partial unique `(org,position,member) WHERE active` P2002→32021 | [`src/modules/position-assignments/CLAUDE.md`](src/modules/position-assignments/CLAUDE.md) · [`docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md`](docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md)(冻结评审稿 §3.4/§7.3) |
| `supervision-assignments/` | M (service 551L) | 分管管理面(终态 scoped-authz PR5):`OrganizationSupervisionAssignment`(与职务**正交**)CRUD + 分管范围/被谁分管查询;单 `SupervisionAssignmentsController`(`@Controller('admin/v1')` 跨 flat/member/org 3 根,**9 路由** = 旧 6〔bare 数组端点仅 ACTIVE,逐字不动〕+ F5/E2〔2026-07-04,D9 同型〕`GET /page` 分页兄弟路由〔缺省含 REVOKED 历史 + expand=supervisor,organization,D6〕+ detail + `POST /coverage-preview`〔dry-run 覆盖预演,closure 展开展示读零写入〕)+ R 模式 4 码 `supervision-assignment.{read,create,update,revoke}.record`(三读端点共用 read.record;F5 0 新码);建校验(supervisor/org 存在+active/防重/任期,**不校验持职务**);supervision-scope(TREE 经 closure 展开后代/EXACT 仅该节点)+ supervisors(直接 DIRECT + 祖先 TREE 继承 INHERITED);建·撤销写 audit(`supervision_assignment`) | **分管 = 数据 + 展示,绝不进任何判权路径**(判权=PR8;RoleBinding=PR6);scope/supervisors 读 closure 仅展示非 judge;partial unique `(supervisor,org) WHERE active` P2002→33002 | [`docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md`](docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md)(冻结评审稿 §3.5/§7.4) |
| `role-bindings/` | L (1657L;service 872L,沿 §4 体量观察非立项) | 带 scope 的角色绑定管理面(终态 scoped-authz PR6):`RoleBinding`(principal × role × scope × 任期)CRUD;单 `RoleBindingsController`(`@Controller('admin/v1')`,8 路由 = 旧 4〔列/建/改/软删〕+ F3/C1 新 4〔page/detail/preview/batch〕)+ R 模式 4 码 `role-binding.{read,create,update,delete}.record`;v0.44.0 补 create/batch 高权角色分级、GLOBAL ops-admin 最后一人保护、update before/after audit | **🔴 UserRole→`RoleBinding(USER,GLOBAL)` 无损升级 = 判权唯一读源;RbacService 只读 GLOBAL,scoped 各型入库即止、绝不进判权路径**(判权=PR8);partial unique 全 scope 维度 `WHERE active` **NULLS NOT DISTINCT** P2002→34002;USER 主体变更提交后各实例下一请求直读当前 DB 权限事实;旧 UserRole 表已 DROP(冻结表 cleanup,第 39 migration) | [`docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md`](docs/archive/reviews/org-position-scoped-authz-terminal-design-review.md)(冻结评审稿 §3.6/§7.5/§8.2) |
| `realname/` | M (1087L) | 实名核验通道层(realname-settings 三端点 / 双 Provider / verify 编排 + 原生 fetch TC3 签名 8s + 27xxx 映射边界;招新一期 T2) | secretId/secretKey 两段 AES-256-GCM 永不回显;settings 由第 49 migration constant unique 强制单例、并发首配 P2002 重试;真通道休眠;姓名/身份证号不入日志;production-like 禁 DEV_STUB | [`docs/archive/reviews/recruitment-phase1-review.md`](docs/archive/reviews/recruitment-phase1-review.md)(冻结评审稿) |
| `recruitment/` | L (源 5264L) | 招新前段(T3)+ 二期(后段)+ S1–S3 + **S4a(H5 + 手机身份链)**:公开报名(`open/v1` multipart 提交 + 查询 + **H5 发码/验码/手机查询②/自助换微信换手机**)+ admin 轮次/报名管理 + 临时编号原子发号 + 实名核验编排 + 进度模型/工作台 stats/RBAC 敏感分级;**两层身份铁律**:临时编号绑 application,**不**进 members | 姓名/身份证号/手机仅入库不回显明文;证件照走 storage 短 TTL signed-URL + L3 不入日志;主证件照/签名图/证书图/OCR 裁剪图与 OCR 转发统一复用 attachments `AttachmentContentValidator`,校验失败零 OCR/零 storage 写;S4a 报名前会话表 `recruitment_identity_sessions` 承载 token(sha256 入库 + consumedAt 一次性,**不进报名表/不占容量去重统计**);`recruitment-identity.service.ts`(818L)复用 SmsCodeService(RECRUITMENT_BIND);**god-service 拆分(2026-06-28,纯重构零行为变更):`recruitment-applications.service.ts` 1248L→646L(拆分时;当前 867L)(公开 submit/recognize/query + 人工 resolve〔共享发号 FM-C〕),抽出 `recruitment-applications.presenter.ts`(纯视图/脱敏/CSV)+ `recruitment-applications-query.service.ts`(admin 读面)+ `recruitment-application-review.service.ts`(标门槛/批量/评定);沿 architecture-boundary §3.1/§3.2/§4** | [`CLAUDE.md`](src/modules/recruitment/CLAUDE.md) · [`docs/archive/reviews/recruitment-phase1-review.md`](docs/archive/reviews/recruitment-phase1-review.md) · [`recruitment-phase4-loop-optimization-review.md`](docs/archive/reviews/recruitment-phase4-loop-optimization-review.md)(冻结评审稿) |
| `sms/` | M (1437L) | SMS 通道层(settings/send-logs/双 Provider/动态路由)+ 验证码签发/校验/防刷 | codeHash=`HMAC-SHA256(scrypt(SMS_ENCRYPTION_KEY,独立 salt),phone:purpose:code)`;pepper/明文码不入库·不入响应·不入 audit;settings constant unique + 并发 P2002 重试;production-like 禁 DEV_STUB | [`docs/archive/reviews/sms-verification-infra-review.md`](docs/archive/reviews/sms-verification-infra-review.md)(冻结评审稿) |
| `team-join/` | M (1756L,T2-T4) | 招新三期(入队:志愿者→队员;T2-T4 全):入队轮 CRUD + 标 gate(8 通用 + 4 条件性专业队;完成日→有效期〔本轮按北京日历日〕;dept-assessment 可延长期)+ 综合评估单一人工闸 + 贡献值只读汇总(approved sheet,checkInAt<入队年 3-31)+ app 自助面(发起/查进度/改候选,self-scope 防 IDOR)+ **一键入队**(`TeamJoinEnrollmentService` 单事务设部门 + level-1,直连 prisma 防环);状态机 joining→pending_evaluation→approved→joined/rejected;**两层身份**:入队才赋部门 + 级别 level-1 | gate 全过 + 贡献值≥5 自动推进;专业队 = node_type code 约定识别 + 入队强制对应 gate;贡献值直读 attendance_records 只读汇总 Decimal 精度;`team-join-progress.ts` contribution/gate/presenter admin·app·enrollment 共用零分叉;evaluationExtendedUntil 跨轮入队消费 | [`docs/archive/reviews/recruitment-phase3-review.md`](docs/archive/reviews/recruitment-phase3-review.md)(冻结评审稿) |
| `wechat/` | M (948L) | 微信小程序通道层(wechat-settings 三端点 / 双 Provider / code2session 原生 fetch 8s + 25xxx 映射边界) | appSecret AES-256-GCM 永不回显;settings constant unique + 并发 P2002 重试;session_key 不存储即弃;openid 掩码;wx code / 含 secret URL 不入日志;production-like 禁 DEV_STUB | [`docs/archive/reviews/wechat-mini-login-review.md`](docs/archive/reviews/wechat-mini-login-review.md)(冻结评审稿) |
| `storage/` | L (1720L) | LocalStorageProvider + CosStorageProvider + 动态 Router + AES-256-GCM + `StorageObject`/`StorageObjectOperation` durable ledger | storage_settings 由第 49 migration constant unique 强制单例,并发首配 P2002 后重跑事务;ledger 固定 provider locator、执行时凭证取当前 storage settings；dedicated worker(非 cron/queue)负责 JIT→STRICT 收敛及人工恢复，当前仅 Attachment namespace 接入；凭证/签名 URL 红线与 production fail-fast 不变 | [`CLAUDE.md`](src/modules/storage/CLAUDE.md) · [`docs/archive/reviews/queue-b-otp-birthday-infra-review.md`](docs/archive/reviews/queue-b-otp-birthday-infra-review.md) · [`runbook`](docs/ops/attachment-storage-consistency-rollout.md) |
| `users/` | L (2854L) | 用户 CRUD + `/me*` + 改密 + refresh 联动撤销 + step-up 保护的 me/phone 与 me/wechat 绑定换绑 + admin 清号/清微信 | service 1025L(身份换绑在 parameterized User `FOR UPDATE` 锁内复核 proof snapshot；真实变更同事务撤全部 active refresh + masked audit，同目标 no-op)；Mixed Controller P1-C step 1 已拆完；phone / openid 占用均含软删；last-SUPER_ADMIN 与 last-ops-admin 削权守卫统一走共享事务锁策略 | [`CLAUDE.md`](src/modules/users/CLAUDE.md) · [`auth-jwt-refresh`](docs/reference/auth-jwt-refresh.md)` · [`docs/security.md`](docs/security.md) |

> 已存在的 module/common-local CLAUDE.md 均应在本表行内引用,避免 AI 导航漂移(可由 `pnpm docs:codemap:check` 检出)。

---

## src/common/(基础设施,**禁止跨模块 utils grab-bag**)

| 路径 | 体量 | 职责 | 主要风险 |
|---|---|---|---|
| `audit/` | S (57L) | PII 掩码纯函数 | `audit-placeholder.ts` 已于 2026-07-19 C-2 退役;事件类型唯一归 `modules/audit-logs/` |
| `decorators/` | S (242L) | `@Roles` / `@CurrentUser` / `@PasswordChangeThrottle` 等 | 与 Guard 配套,不单独使用 |
| `dto/` | S (74L) | 跨模块共享 DTO(列表分页基类等) | **禁止**业务 DTO 入此目录 |
| `event/` | S (26L) | 应用事件基础 | — |
| `exceptions/` | M (947L) | BizCode 常量 + 业务异常类 | **`biz-code.constant.ts` 是 BizCode 唯一权威源**;新增段位前先查 [`auth-jwt-refresh`](docs/reference/auth-jwt-refresh.md)` |
| `filters/` | S (86L) | 全局异常过滤器 + 统一返回 | 错误响应 schema 不动 |
| `guards/` | S (181L) | `JwtAuthGuard` / `RolesGuard` | Guard `@Roles(...)` + Service `rbac.can()` 双轨 |
| `interceptors/` | S (40L) | 统一返回包装等 | — |
| `prisma/` | S (22L) | PrismaService 注入 | **不**使用全局软删中间件 / client extension |

---

## prisma/

| 路径 | 职责 | 主要风险 / 本地铁律 | 本地约束 |
|---|---|---|---|
| `schema.prisma` | **数据模型唯一权威源**(字段 / 类型 / 约束 / 索引) | 修改前必先说明影响面 | [`CLAUDE.md`](prisma/CLAUDE.md) |
| `migrations/` | 60 个 migration(最新 `20260718233000_attachment_storage_operations`:仅 Attachment key namespace 新增 `StorageObject`/`StorageObjectOperation` durable ledger;对象固定 provider locator、凭证仍取当前 settings;JIT→STRICT 由 dedicated worker 收敛(非 cron/queue);为旧 binary additive rollout 本期不加 `Attachment.key`→`StorageObject.key` FK,不宣称 repo-wide storage closure;见 [`runbook`](docs/ops/attachment-storage-consistency-rollout.md);前一为 2026-07-18 Membership 任期 CHECK 收口(存量 fail-fast、零修数);前一为 D-Outbox:纯 additive 新建空表 `notification_outbox_intents`,零回填/零 enum/零 drop;前一为 2026-07-18 D-RULE-1:同 `activityTypeCode × attendanceRoleCode` 未软删 ACTIVE pair partial unique；冲突预检失败且零自动清理，migration 仅 DROP/CREATE index，零 DML/回填；service pair 预查 + P2002→23002，calculator 漂移重复 pair fail-closed；前一为 2026-07-18 D-Throttle:净新空表 `throttler_buckets`〔`(throttlerName,key)` 普通唯一 + `retentionAt` 索引;6 个时间字段/数组均 `timestamptz(3)`;纯 CREATE TABLE/unique/index,零回填、零 enum、零 drop;goal 明确授权 `@@map` 物理名〕;前一为2026-07-17 identity session P0 PR1:`SmsPurpose` 仅 additive `IDENTITY_STEP_UP`，migration 仅 PostgreSQL enum `ADD VALUE`，0 table / column / index / backfill / seed、禁止 down migration；前一为 2026-07-16 活动岗位与时段 F1:净新空表 `activity_positions` + `ActivityRegistration.activityPositionId` nullable 列〔2 FK Restrict + 6 普通索引 + 手写 partial unique `(activityId,name) WHERE deletedAt IS NULL`;既有报名 active partial unique 逐字不动;无回填/无不可逆/无 enum/seed;goal 指定 `@@map`〕;前一为 2026-07-16 活动评价 F1:净新空表 `activity_feedbacks`〔2 FK Restrict + 5 单列索引 + 手写 partial unique `(activityId,memberId) WHERE deletedAt IS NULL`;无回填/无不可逆/无 enum/seed;goal 指定 `@@map`〕;前一为 2026-07-15 活动自助 GPS 签到 F1:净新空表 `activity_check_ins`〔3 FK Restrict + 6 单列索引 + 手写 partial unique `registrationId WHERE deletedAt IS NULL`;无回填/无不可逆/无 enum/seed;一次性 `@@map` 例外〕;前一为 2026-07-15 活动开始提醒 marker:Activity 加 nullable `startReminderSentAt`〔活动模块一致性收口 F12;纯 additive、无回填、无不可逆〕;前一为 v0.47.0 到期提醒:member_insurances / team_insurance_policies 各加 nullable `expireNotifiedAt`,纯 additive、无回填、无不可逆;前一为 2026-07-14 第七刀 settings 单例约束:四表先按 updatedAt/createdAt 最新优先安全去重并记录删除数,再加 constant unique `ON ((true))`;仅已有重复行时发生不可逆删除,执行前只读核对 + 实时授权;前一为 2026-05-02 init → 2026-07-13 招新证书闭环刀A:`recruitment_applications.certificateIssuanceInfo Json?` 发证机构/日期按类别暂存,纯 additive 可空、无回填无不可逆;前一为 2026-07-12 招新/入队十三项收口 PR-3:申请人证书审核态 + 入队轮配置预置列〔`recruitment_applications.certificateReviewStatus Json?` + `team_join_cycles.openOrganizationIds Json?`/`maxTargetOrgs Int?`;全 additive 可空,无回填无不可逆;刀H 代码于 PR-4 消费〕;前一为同 goal PR-1:同轮 active 报名 openid/phone 两条 partial unique;前一为 2026-07-11 十项收口一刀:「至多一个 open 轮」DB 兜底 + 建档搬运落点〔`recruitment_cycles_single_open_unique` / `team_join_cycles_single_open_unique` 两手写 partial unique(WHERE statusCode='open' AND deletedAt IS NULL;开轮 count-then-update 并发穿透收口,service P2002 转 28032/28231)+ `MemberProfile` +`detailedAddress`/`profileExtra`(promote 建档搬运落点,此前 promote 置空且无落点=真丢失);全 additive 无回填无不可逆〕;前一为同日招新可用性收口 F7:证书图上传与长期档案〔`recruitment_applications` +`certificateImages Json` 暂存位 + `Certificate` +`imageKeys Json`;promote 按类别建 pending Certificate 搬图;全 additive 可空无回填〕;前一为同日 F6:同轮防重 partial unique 排除集重建〔`<> 'rejected'` → `NOT IN ('rejected','withdrawn')`,自助撤销终态解锁同轮重报;纯索引重建零回填〕;前一为同日 F5:知情同意留痕 + 签名图〔recruitment_applications +privacyConsentAcceptedAt/+privacyConsentVersion/+signatureImageKey,MemberProfile +signatureImageKey;全 additive 可空,无回填无不可逆〕;前一为同日 F1:纯加空表 `recruitment_ocr_daily_counters`〔付费 OCR 按 IP × 北京自然日封顶持久计数;`(ip,dateKey)` 唯一 + `dateKey` 索引;无 FK 无回填无不可逆〕;前一为 2026-07-07 队员账号闭环 v2 schema 根改造:`User.memberId` 去全量 `@unique` 改手写 partial unique index `User_memberId_active_key` WHERE `deletedAt IS NULL`〔+ 普通 `@@index([memberId])` 兜底跨软删查询〕,同一 memberId 至多 1 条 live User 关联、软删后释放槽位;纯约束收窄,现有数据必满足更严全量唯一 ⇒ 零冲突零回填非破坏性;`Member.user User?` 反向关系随之改 `users User[]`(Prisma 一对一要求 FK 侧 `@unique`,partial unique 无法在 DSL 表达),消费方 `resource-resolver.service.ts`/`birthday-greeting.service.ts` 同步改 `where:{deletedAt:null}`+`take:1`;评审稿 [`member-account-loop-v2-review.md`](docs/archive/reviews/member-account-loop-v2-review.md);前一为 2026-07-03 冻结表 cleanup:DROP `MemberDepartment` + `user_roles` 两张冻结表〔档 D,不可逆;pre-production 窗口维护者拍板;两表均已零生产读写〔PR2/PR6 起〕,回填等值已用 count(...) 自证;无其它表 FK 指向这两表,纯删除表体〔含索引/FK/partial unique〕,无回填无新增,legacy department 3 端点 + user-roles 端点路由/DTO/码/行为逐字不变〕;前一为 2026-07-01 终态 scoped-authz PR7 职务→角色 policy:净新表 organization_position_role_policies〔+1 枚举 PolicyScopeMode{EXACT,TREE} + 3 索引 + 2 真 FK Restrict〔positionId→organization_positions/roleId→roles〕+ 普通唯一 (positionId,roleId);无 partial unique 无回填无不可逆;3 管理/监督角色 org-admin/group-manager/org-supervisor + 绑定 + 3 条默认映射〔仅正职 team-leader/dept-leader→org-admin@TREE、group-leader→group-manager@TREE;🔴 R5 副职零行,seed 运行时断言兜底〕由 seed 幂等 upsert 不落 migration;policy 表 + 3 新角色纯配置绝不进判权,消费方 PR8 AuthzService);前一为 2026-07-01 终态 scoped-authz PR6 RoleBinding:净新表 role_bindings〔+3 枚举 PrincipalType/BindingScopeType/BindingStatus + 6 索引 + 2 真 FK Restrict〔roleId→roles/scopeOrgId→Organization;principalId 多态无 FK〕+ 末尾手写 partial unique 全 8 scope 维度 WHERE deletedAt IS NULL AND status='ACTIVE' NULLS NOT DISTINCT〔PG16〕〕+ 回填每条 UserRole→RoleBinding(USER/GLOBAL/ACTIVE,复用 id),纯加 + 回填可逆(判权唯一读源重指向,UserRole 冻结零生产读写不删);前一为 2026-07-01 终态 scoped-authz PR5 分管:净新表 organization_supervision_assignments〔与职务正交;+2 枚举 SupervisionScopeMode{EXACT,TREE}/SupervisionStatus{ACTIVE,ENDED,REVOKED} + 4 索引 + 2 FK Restrict〔supervisorMemberId→Member/organizationId→Organization〕+ 末尾手写 partial unique (supervisorMemberId,organizationId) WHERE deletedAt IS NULL AND status='ACTIVE'〕,纯加空表无回填无不可逆(分管由 API 产生,不 seed);前一为 2026-07-01 终态 scoped-authz PR4 任职:净新表 organization_position_assignments〔+枚举 AssignmentStatus + 6 索引 + 3 FK Restrict + 末尾手写 partial unique (organizationId,positionId,memberId) WHERE deletedAt IS NULL AND status='ACTIVE'〕,纯加空表无回填无不可逆;前一为 2026-07-01 终态 scoped-authz PR3 职务定义:净新两空表 organization_positions + organization_position_rules〔+2 枚举 PositionCategory/PolicyStatus + 索引 + FK Restrict + 普通唯一〕,纯加空表无回填无不可逆(6 领导职务 + 30 默认规则由 seed 幂等 upsert);前一为 2026-07-01 终态 scoped-authz PR2 Membership:净新表 member_organization_memberships〔+2 枚举 MembershipType/MembershipStatus + 2 手写 partial unique〕 + 回填 active MemberDepartment→PRIMARY membership〔复用 id、startedAt=createdAt〕,纯加可逆;前一为 2026-07-01 终态 scoped-authz PR1 组织基座:organization_closure 闭包表〔WITH RECURSIVE 回填现有树〕 + Organization 两 additive 可空列 establishmentStatusCode?/groupFunctionCode?〔纯加无 enum 无回填无不可逆〕;再前为 2026-06-29 招新实名 OCR 鉴伪版充分利用:recruitment_applications +6 列〔ocrAddress/ocrNation/ocrAuthority/ocrValidDate + idCardCropImageKey/idCardPortraitImageKey;全可空 TEXT additive,无 enum 无不可逆无回填〕;再前为 2026-06-27 统一通知 S5 sms_settings.templateIdNotification 1 列 / S3 notifications.recipientMemberId 1 列 / S2 三表 / S1 两表) | **禁止** `prisma migrate dev` / `reset` / `db push` 自动跑 | [`CLAUDE.md`](prisma/CLAUDE.md) · [`AGENTS.md §3`](AGENTS.md) |
| `seed.ts` | 默认 super admin + bootstrap 授予(PR6 起写 global RoleBinding)+ 内置 **7** 角色(ops-admin/member/biz-admin + PR7 org-admin/group-manager/org-supervisor〔纯定义零持有,PR8 起职务/分管推导〕+ PR9 attendance-final-reviewer〔终审中枢,绑 3 既有码,零持有零 policy,经显式 RoleBinding 生效〕)+ 3 条职务→角色默认映射(仅正职;🔴 R5 副职零行 + 运行时断言) | 生产环境强校验启动(`SUPER_ADMIN_*` / `JWT_SECRET` / `APP_CORS_ORIGIN`) | [`docs/deployment.md`](docs/deployment.md) |

---

## src/bootstrap/(应用启动装配)

`apply-global-setup.ts` / `apply-swagger.ts` / `logger-options.ts` / `request-id.ts` / `throttle-options.ts` / `postgresql-throttler-storage.ts` — `main.ts` 与 `test/setup/test-app.ts` **共用**这些文件,避免双份装配漂移；限流 storage 使用 PostgreSQL 跨实例共享配额并在数据库异常时 fail-closed。改其中任何一个之前先确认两边都被覆盖。

例外:`apply-crash-handlers.ts`(2026-06-12 #345)**仅 `main.ts` 注册**、test 入口刻意不注册——进程崩溃路径可观测性兜底(uncaughtException / unhandledRejection),与优雅关闭(`enableShutdownHooks`)职责互不重叠,边界见该文件头注释。

---

## test/

| 路径 | 职责 |
|---|---|
| `contract/` | OpenAPI snapshot + `EXPECTED_ROUTES`(接口契约权威源) |
| `e2e/` | E2E spec(v0.15.0 时 78 spec) |
| `fixtures/` / `helpers/` / `setup/` | 测试工具 |
| `jest-unit.config.ts` / `jest-e2e.config.ts` / `jest-contract.config.ts` | 三套独立 jest 配置 |

---

**冲突处理**:本表与代码冲突 → 以 `src/**` 与 `docs/current-state.md` 为准,本文件需回头修正;**不得**用本表否决 [`AGENTS.md`](AGENTS.md) 与 [`docs/api-surface-policy.md`](docs/api-surface-policy.md)。
