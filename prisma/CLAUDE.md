# prisma — 本地铁律

> 全局规则读 [`/AGENTS.md`](../AGENTS.md);部署 / 迁移流程读 [`/docs/deployment.md`](../docs/deployment.md);开工门禁读 [`/docs/current-state.md §5`](../docs/current-state.md)。

## 本地事实

- `schema.prisma` 是**数据模型唯一权威源**(字段 / 类型 / 约束 / 索引);Swagger / DTO / docs 任何与之冲突,**以 schema.prisma 为准**。
- `migrations/` 累计 90 个 migration；最新 `20260820100000_member_identity_master_record` 是队员身份主档终态升级(issue #1048 T1)——**单事务 expand → backfill → contract 三段式**,不是朴素 `migrate diff` 产物:`Member` 先加四列可空(`realName` / `nickname` / `memberSinceDate` / `memberOriginCode`)→ 自连接 LEFT JOIN 回填(`realName` ← `MemberProfile.realName` 否则 `Member.displayName`;`memberSinceDate` ← `joinedDate` 否则 `createdAt` **按北京日历日**取午夜;`memberOriginCode` ← `joinSourceCode` 否则 `'manual'`)→ 三列收 NOT NULL → **DROP 四列**(`Member.displayName` + `MemberProfile` 的 `realName`/`joinedDate`/`joinSourceCode`)→ 建 `Member_realName_idx`。**不可逆**(DROP COLUMN),无 down migration,回滚 = 回到本 migration 之前的库快照。⚠️ 回填必须走 LEFT JOIN 而非 `FROM "MemberProfile"`:`memberProfile` 是**可选**关系,INNER 语义会整片漏掉管理员手工建的无档案队员,然后在 SET NOT NULL 处以 23502 炸掉;`nickname` 刻意不回填(旧模型无此事实,编造会造出一批假外号)。已在 scratch 库以「先跑完前 89 个 + 灌三种分支存量行 + 再跑本刀」验证**非空库**可用,并逐条断言回填取值与北京日界。前一 `20260817100000_activity_b7_member_audience_tags` 为 B7 新增 `ActivityPublishReview.audienceTagCodes` nullable JSONB，以及 `MemberAudienceTagAssignment` 的赋标/撤标历史表、两个 live 查询索引、live `(memberId,dictItemId)` partial unique 和两个 RESTRICT FK。既有 review 的 NULL 保持 legacy 广播；纯 additive，零回填、删除、既有行重解释或生产 deploy。前一 `20260816183000_activity_v11_batch6_staff_import_offline` 按维护者批准的第 6 批补充合同新增 `OfflinePackage` / `OfflinePackageParticipant` / `OfflinePunchReviewItem`，并为 `AttendancePunchEvent` 增加离线包、序号与双摘要四锚点。
- 前一 `20260813150000_activity_v11_batch4_allocation_candidate_position_anchor` 是 D87 schema-only guard：只接受 `ActivityAllocationCandidate` 空表，count-only 23514 在任何 DDL 前失败，零回填；空表才新增必填 `activityId`/`sessionId` 与可空 `waitlistPositionId`。Candidate→Batch 固定 `(allocationBatchId,activityId,sessionId)` 复合 FK，候补岗位固定 `(activityId,sessionId,waitlistPositionId)` 复合 FK；result/rank/position 为两值闭集，只有 waitlisted 同时要求 rank+position。旧全 batch waitlist rank partial unique 已换为 `(allocationBatchId,waitlistPositionId,waitlistRank) WHERE waitlistRank IS NOT NULL`，查询索引同序；lotteryOrder/tieBreakKey 不变。仍零 writer/runtime/endpoint/20147/容量或候补 caller，生产未 deploy。
- 前一 `20260813100000_activity_v11_batch4_allocation_command_replay_projection` 只在 AllocationBatch/Candidate/已分配 ParticipationRevision 均为空时，单事务落 D86：Batch 增 `voidReason`/`voidedAt` 的状态形状（原始 wire 长度上限，空白拒绝）；专用 `ActivityAllocationCommandReceipt` 只收 `prepare/commit/void` 的安全不可变回执，并以 `(allocationBatchId,activityId)` 复合 FK 同锚、每 batch/command 和 activity/command/operationKey 双唯一；`ActivityAllocationApplicationProjection` 每 candidate 一条，identity 固定 `id+activity+session+member`，activity-person reservation 固定 `id+member+activity+bucket`（同 member 跨 session 正确复用），session/position reservation 继续固定 `id+identity+bucket`，并以复合 FK 固定 batch/candidate/revision/position/三层 reservation-bucket；allocated 与 waitlisted/not_selected 的可表达形状分 CHECK，receipt/projection 均拒 UPDATE/DELETE。`responseReceipt` 仅是固定 v1 安全传输信封，不是真值、不得含 L3 或 seed；DB 仅验 JSON shape 与列值/`responseHash` 一致，**不重算密码学 hash**。后续 runtime 的唯一算法是 UTF-8 canonical payload serialization（字段顺序 `activityId`,`allocationBatchId`,`batchStatusCode`,`commandCode`,`responseSchemaVersion`，排除 `responseHash`）作 SHA-256；JSONB 对象本身不承诺键顺序。Batch 当前 committed、candidate/revision 内容、reservation 类型和 Identity live pointer 仍须后续 Activity 根锁事务复核；实际 writer/replay/20147/seed/容量与候补 caller 尚未实现。前一 D85 与 D84 合同仍如既有。
- 下方第 79 migration 起的长条目是历史交接说明，不代表当前 migration 累计数；当前计数以上方“累计 89”条为准。
- `migrations/` 累计 79 个 migration(最新 `20260807183000_activity_v11_batch4_form_closed_sets`:活动业务改造 v1.1 第 4 批 Form 前置微刀 D-FORM-0——`RegistrationFormField.visibilityCode` 三值 CHECK、`RegistrationUploadSession.statusCode` 三值 CHECK 与 `consumed`/`consumedAt` 双向 CHECK、`attachments(ownerId)` WHERE `ownerType='registration-upload-session'` partial unique；零数据修改/回填/删除/default/列变更/rename/runtime/endpoint/DTO/permission/seed/生产 deploy；前一 `20260807154000_activity_v11_batch4_capacity_reservation_member_activity_unique`:活动业务改造 v1.1 第 4 批前置微刀——`CapacityReservation` 新增 nullable `memberId` / `activityId` 与可选 RESTRICT FK；active `activity_person` 行由空值安全 CHECK 强制双锚点，partial unique 保证 `(memberId,activityId)` 至多一条；零 default / 回填 / DROP / RENAME / runtime / endpoint / DTO / permission / seed / 生产 deploy；前一 `20260806234105_activity_v11_batch3_slice2pre_rule_snapshot_template_nullable`:活动业务改造 v1.1 第 3 批②-pre 微刀——仅将 `ActivityRuleSnapshot.templateVersionId` 放开为可空，保留可选 RESTRICT FK；无模板活动可在审核通过时生成不可变规则快照；零 runtime / endpoint / DTO / permission / seed / 回填 / 删数 / DROP / RENAME / 新 cron / 生产 deploy；前一 `20260806212358_activity_v11_batch3_slice1p5_templates_rule_snapshots_idempotency`:活动业务改造 v1.1 **第 3 批①.5** expand-only —— 净新空表 `ActivityTemplate` / `ActivityRuleSnapshot`；模板按 `(code,version)` 版本化，`statusCode` 取值集未定义故只存 `String`、刻意零 CHECK(缺口 #13)；snapshot 按 `(activityId,workflowRevision)` 唯一、三个 RESTRICT FK、DB trigger 禁 `UPDATE/DELETE` 而不拦 `TRUNCATE`；兑现 `ActivityAllocationBatch.ruleSnapshotId` 可空 FK 债，并补 §10.3 发布提交／审核／取消／提前终止的可空 key/hash 落点和 key unique。**零 runtime / endpoint / DTO / permission / seed / 回填 / 删数 / DROP / RENAME / ALTER COLUMN / 不可逆 / 新 cron**；零 seed 行、零生产 deploy)。
- 前二(第 75 个 migration) `20260804100000_activity_v11_slice5_allocation_waitlist_reserved_quota`:活动业务改造 v1.1 **第 1 批第五刀** expand-only —— 净新 **4 张空表**(`ActivityPositionPreference` / `ActivityAllocationBatch` / `ActivityAllocationCandidate` / `ActivityReservedQuotaGroup`,全部 §3.11)+ 既有表加 **1 列可空**(**兑现第一刀欠账**:`ActivityParticipationRevision.allocationBatchId` → `ActivityAllocationBatch`,连列带 FK,并补 §11.3 逐字点名的 `(allocationBatchId,statusCode)` 索引)+ 末尾 **4 条 CHECK**(零 partial unique、零 trigger)。**零 runtime / 零 endpoint / 零 DTO / 零 permission / 零 seed / 零回填 / 零删数 / 零 DROP/RENAME / 零 ALTER COLUMN / 零不可逆 / 零新增 cron**(全仓 cron 终态仍恰 2);四张新表**零调用方是预期状态**(消费方在第 4 批)。⚠️ **本刀的存在本身是在补合同的洞**:§3.11 四表**没有被 §14「第 1 批」建议拆分的任何一条列入**,而 §14「第 4 批」明写要用 Allocation 与 Waitlist —— 合同**第四处内部矛盾**,维护者 2026-08-04 拍板单独第五刀补齐。🔴 **一条 CHECK 初版是"有条件地对"**:`activity_allocation_batch_committed_shape_check` 初版写成朴素 OR `"statusCode" <> 'committed' OR "committedAt" IS NOT NULL`,注释断言"两侧恒二值不可能塌成 NULL"—— 那句话依赖的是 **statusCode 的 NOT NULL,而那是别处的列声明,不是本式的结构性质**。scratch 库变异实测:`DROP NOT NULL` 后插 `statusCode=NULL, committedAt=NULL`,`NULL <> 'committed'` = NULL、`NULL OR FALSE` = NULL ⇒ **CHECK 判通过且该行真的入库**。改成**守卫前置** `"statusCode" IS NOT NULL AND (…OR…)` 后同一行被 23514 拒(AND 是 FALSE-主导,塌成 FALSE 不是 NULL)⇒ 结构免疫。这是第四刀那条教训的同型复发,本刀在合入前抓住。⚠️ **另一条诚实的负面结论**:`capacity IS NULL OR capacity >= 1` 的 `IS NULL` 守卫是**自证文档而非行为** —— 变异实测换成朴素 `capacity >= 1` 后,NULL 照样入库、0 照样被拒,两种写法在全部输入上判定相同;保留显式写法只为可读性 + 与既有两条姊妹约束同形。**两者的区别就是守卫是不是 `IS [NOT] NULL` 谓词本身**。🔴 **`ActivityAllocationCandidate` 刻意不装 append-only trigger**,理由不止先例:①合同说的是「结果 **committed 后**不可改」,没像 §3.23.8 那样点名 DB 角色层禁 UPDATE/DELETE(沿 §3.17 EvidenceSeal / §3.19 SettlementReviewAction 两条先例)②更硬的正面理由:这是**条件不可变不是 append-only**,preparing 期正要往候选行写评分/结果,无条件 trigger 会把合法写路径堵死 ——**装上就是错的** ③"按父批次 statusCode 判"的条件 trigger 是跨行判据,行级 trigger 读父批次在并发下骗人(与第四刀日合计求和 trigger 同型)。⚠️ **`ruleSnapshotId` 不建**:指向 §3.4 `ActivityRuleSnapshot`,那张表至今没建(§14 第 3 批才实现)⇒ 沿「跨切片外键列不提前占位」,由建它的那一刀连列带 FK 补齐(**本刀新欠下的唯一一笔账**)。合同**未给**的一律不发明:`ActivityAllocationCandidate.resultCode` 不落闭集、本表零 unique;`ActivityReservedQuotaGroup.scopeTypeCode`/`fallbackMode` 不落闭集(照搬 §3.10 容量桶的 scope 闭集看着自然,但那是**另一张表**的闭集);`preferenceOrder` 不落范围(§3.11 没给 0-based 还是 1-based,姊妹列 `ActivityParticipationRevision.waitlistRank` 第一刀同样没有)—— 全部用「任意取值必须放行 + 该列零 CHECK」钉成会变红的判据。⚠️ 与合同的偏离(PR body 逐条列):`committedAt` 改可空(§3.11 自己的 `preparing` 态否则写不进来,并配 shape CHECK 把"committed 却无提交时刻"重新关上);`requestHash` 可空 + `createdBy` 命名 `createdByUserId` 且可空(沿全仓既有范式);`candidateSnapshotHash` **保持 NOT NULL**(同一 bullet 里作者对 `positionId`/`randomCommitment` 显式标了 `?` ⇒ "未标 = 必填"是刻意的)。🔴 **DoD 5 的答复**:本表唯一的唯一索引是 `operationKey` 单列(NOT NULL),可空的 `positionId` **没有进任何唯一索引**(合同没要求 ⇒ 不发明)⇒ 本刀无处也不该加 `NULLS NOT DISTINCT`;已用结构断言钉住,日后有人补含 positionId 的唯一索引会变红,那时必须同时决定该子句。⚠️ **一个会让变异 A/B 假绿的仪器陷阱(本刀实测)**:e2e 模板库由 globalSetup 跑一次 `migrate deploy` 建成,**按 migration 名幂等** ⇒ 改 migration 文件内容后重跑 e2e,库里仍是旧定义,变异根本没落地、41 例照样全绿。必须先 `DROP DATABASE <模板库>` 再跑才是真读数;落地后实测**恰好 1 例红**且正是"守卫前置"那条结构断言(行为断言在 statusCode 仍 NOT NULL 时两种写法判定相同,所以**必须**有那条结构断言)。骨架由只读 `migrate diff` 生成后剥掉两条**与本刀无关的存量** RenameIndex 再手工追加约束。model 名 ≥ 23 字符 ⇒ 所有 `@@unique`/`@@index` 一律显式 `map:`。已在干净库 `migrate deploy` 重放全部 75 个 migration + seed 幂等二跑逐表行数 IDENTICAL;独立 SQL 夹具**先立正对照**(19 条合法行确实能进)再跑 19 条反对照全部被拒;配套 e2e 41 例;四张新表**零出向外键的表 = 空集**且实测被 `resetDb` 的 TRUNCATE CASCADE 清到 0 行。生产未 deploy。前一为 `20260804080000_activity_v11_slice4_settlement_ledger_correction_closure_job`:活动业务改造 v1.1 **第 1 批第四刀** expand-only —— 净新 **14 张空表**(`AttendanceSettlementRun` / `AttendanceSettlementVersion` / `SettlementReviewAction`〔§3.19〕/ `ParticipantSettlementResultRevision`〔§3.20〕/ `ParticipantSettlementDay`〔§3.21〕/ `LedgerPostingBatch`〔§3.22〕/ `ParticipationLedgerEntry` + `LedgerEntryReversalClaim`〔§3.23〕/ `MemberContributionDayState`〔§3.24〕/ `AttendanceCorrectionRequest` / `CorrectionApplication`〔§3.25〕/ `ActivitySettlementClosureRevision`〔§3.26〕/ `ActivityBatchJob` / `ActivityBatchJobItem`〔§3.27〕)+ 既有表加 **2 列均可空**(**兑现第三刀欠账**:`ParticipantServiceSegmentRevision.effectiveBatchId` → `LedgerPostingBatch`;`AttendancePunchEvent.importJobItemId` → `ActivityBatchJobItem`)+ 末尾 **37 条 CHECK + 4 条手写 partial unique + 1 组 append-only trigger**。**零 runtime / 零 endpoint / 零 DTO / 零 permission / 零 seed / 零回填 / 零删数 / 零 DROP/RENAME / 零不可逆 / 零新增 cron**(全仓 cron 终态仍恰 2);十四张新表**零调用方是预期状态**(消费方在第 2 批)。⚠️ **本刀语义像钱** —— committed 之后的 `ParticipationLedgerEntry` 就是队员贡献值真值。🔴 **`recognized = credited + cappedOut`(§3.23.6)是本仓迄今最高危的一条 CHECK:纯算术等式**。三列任一为 NULL ⇒ 等式求值成 NULL ⇒ **CHECK 判通过、静默失效**,且只在「恰好有 NULL」的行上失效,正对照全绿完全看不出来。落两道防线:①四个 delta 列全 NOT NULL;②CHECK 把三条 `IS NOT NULL` 守卫写在 **AND 链最前**(AND 是 FALSE-主导 ⇒ 塌成 FALSE 不是 NULL)。第二道**实测**:scratch 库上 `DROP NOT NULL` 后插 NULL 仍被 23514 拒;换成朴素式 `a = b + c` 后**同一行被静默放行并真的入库**(双向变异 A/B)。⇒ 以后凡写算术等式 CHECK,守卫必须前置,不能靠别处的 NOT NULL 声明兜底。🔴 **`ParticipationLedgerEntry` 由 DB trigger 强制 append-only**(§3.23.8),镜像 `trg_attendance_punch_event_10_append_only`,`ERRCODE='55000'`;本仓第 **3** 组同形状 trigger。四条判据实测:INSERT 放行 / UPDATE 拒 / DELETE 拒 / **TRUNCATE 放行且 trigger 存活**(第四条是 e2e 地基,本表靠引用 `Activity`/`Member` 被 CASCADE 带走)。**加列后重跑了第三刀打卡 trigger 的同一组四条判据**(`ADD COLUMN` 是 DDL,行级 trigger 不参与,但必须证明没被顺手弄坏)。⚠️ **「日合计必须 0..3」(§3.24)刻意零 DB 执行位** —— 跨行不变量(同 member 同 ledgerDate 多条分录求和),表级 CHECK 只能看单行;trigger 求和在并发下**骗人**(两事务互相看不见未提交行,双方都判没超)。连 `MemberContributionDayState.committedCreditedPoints`(物化日合计)的**单行 range CHECK 也没加** —— 加了会让人误以为日上限已有 DB 执行位。用两条会变红的 e2e 钉死「刻意」:①同人同日合计 6.0 的两条分录**必须放行** ②账本三表上不得有 append-only 之外的任何 trigger。执行位归第 2 批 service(既有 member advisory lock 内 `FOR UPDATE`)。🔴 **`ledgerDate` 三处同型 `@db.Date`**(§3.21 明写「必须唯一选型」),`information_schema` 实测三行全 `date`;混型会让 `(memberId, ledgerDate)` 唯一在跨表 join 时静默错位。列型对 `unnest($1::text[],$2::date[],…)` 友好(第 0 批实测 Prisma bind 上限 32767,逐行 VALUES 每人 4 参数在 8191 人处确定性失败)。🔴 **`attendance_correction_request_open_unique` 必须带 `NULLS NOT DISTINCT`**:键含可空 `participationIdentityId`(NULL = 活动级更正),漏写则同一活动可提任意多条并行活动级 open 更正而一条不被拦,**人员级照样被拦 ⇒ 只测人员级完全看不出来**(变异 A/B 实测)。⚠️ `ActivitySettlementClosureRevision.serviceHours/contributionPoints` 取 **numeric(12,2)** 而非本刀其它表的 (5,2)—— 它们是**整活动合计**,万人 × 24h 会撑爆 999.99,这是刻意异型不是漂移。⚠️ **`numeric(5,2)` 对多余小数是四舍五入不是报错** ⇒ §3.23.7「小数位」这一半由列类型归一承担、不是「拒绝」;要报错只能在 service/DTO(已诚实标注,不假装 DB 守住了)。⚠️ **Prisma 的 23502(NOT NULL)`meta.message` 只有 DETAIL 行,既无约束名也无列名** —— 前三刀 spec 注释把它和 CHECK/FK 归成一类是**误述**,本刀按那个说法写断言导致三条用例全红才发现;23502 只能断言 sqlState,「是哪一列」改用 `information_schema` 结构断言覆盖。⚠️ 与合同的偏离(PR body 逐条列):`ActivityBatchJobItem.resourceType/resourceId` 改可空(`import_preview` 此刻没有资源可指);`AttendanceSettlementVersion.returnFromStage` 取值集从同节 `first/final` 推导;§3.23.7 范围值全部从合同其它条款推导(时长 ±24 ← §3.21 北京日拆分;credited ±3 ← §3.24 日合计;recognized/cappedOut **不设**上界 —— 封顶前的值设了会误杀合法行)。合同**未给**的不发明:`ActivityBatchJobItem.statusCode` **不落闭集**(§3.27 只给了 Job 的七值),用「任意值必须放行 + 该表零 statusCode CHECK」把**合同缺口**钉成会变红的判据。§3.11 分配四表与 `allocationBatchId` 是**合同第四处内部矛盾**(§14 任何一刀都没列入,第 4 批却要用),维护者 2026-08-04 拍板**另走第五刀**,本刀不建不占位。骨架由只读 `migrate diff` 生成后剥掉两条**与本刀无关的存量** RenameIndex(在 main 上跑同一条 diff 得逐字相同两条)再手工追加约束与 trigger。本刀 model 名普遍 ≥ 24 字符 ⇒ 所有 `@@unique`/`@@index` 一律显式 `map:`,免 PG 63 字符静默截断。已在干净库 `migrate deploy` 重放全部 74 个 migration + seed 幂等二跑逐表行数 IDENTICAL;独立 SQL 夹具**先立正对照**(25 条合法行确实能进)再跑 62 条反对照全部被拒;配套 e2e 82 例。生产未 deploy。前一为 `20260804060000_activity_v11_slice3_punch_evidence`:活动业务改造 v1.1 **第 1 批第三刀** expand-only —— 净新 **5 张空表**(`AttendanceQrCredential`〔§3.15〕/ `AttendancePunchEvent`〔§3.16〕/ `ActivityEvidenceState` / `EvidenceSeal`〔§3.17〕/ `ParticipantServiceSegmentRevision`〔§3.18〕)+ 末尾 **21 条 CHECK + 3 条手写 partial unique + 1 组 append-only trigger**。**既有表本刀零加列**(前两刀各加过列,本刀一列没加);**零 runtime / 零 endpoint / 零 DTO / 零 permission / 零 seed / 零回填 / 零删数 / 零 DROP/RENAME / 零不可逆**;五张新表**零调用方是预期状态**(消费方在第 5 批)。⚠️ **`AttendancePunchEvent` 由 DB trigger 强制 append-only**(§3.16「不提供 update/delete endpoint」「生产业务角色不得 UPDATE/DELETE」),镜像 `trg_insurance_evidence_20_immutable` 的函数 + trigger 两段范式,`RAISE ... ERRCODE='55000'`。四条判据全部实测:INSERT 放行 / UPDATE 拒 / DELETE 拒 / **TRUNCATE 仍放行** —— 第四条是 e2e 地基(`test/setup/reset-db.ts` 靠 TRUNCATE 清库,而本表不在 TRUNCATE 列表里、靠引用 `Activity` 被 CASCADE 带走);行级 trigger 不响应 TRUNCATE,实测 7 行 → 0 行且 trigger 仍在。⚠️ **void/replace 形状拆成三条 CHECK 而非一条大 OR**,但**理由不是 NULL 坍塌** —— 本刀实测核对:朴素式 `(A AND B) OR (C AND D)` 的每个操作数都恒二值(判别列 `eventTypeCode` NOT NULL ⇒ `IN` 恒二值,`IS [NOT] NULL` 亦恒二值),**不可能塌成 NULL**;把它说成"OR 就会塌"是套用第一刀教训的误述。真正理由是①朴素单条 OR 会**静默误杀合法行**(`early_departure_close` 让两支路同时为假 ⇒ 整式 false,变异实测立刻 23514 拒掉一条合法行)②拆开后每侧有独立可断言的约束名。采用的 `CASE ... ELSE TRUE` 显式放行未点名的 eventType,不误杀。⚠️ 坐标成对用**计数式** `(CASE WHEN … THEN 1 ELSE 0 END + …) IN (0,2)`;**accuracy/distance 刻意不入成对判定**(设备可以给出坐标却给不出精度;distance 还需参照点,而"不要求定位"的场次没有参照点)—— 并进来会比合同更严、误杀合法行。⚠️ 两条 partial unique 的键列**全 NOT NULL**(`(sessionId,actionCode)` / `(participationIdentityId,segmentKey)`)⇒ 与第二刀 `activity_invitation_active_unique` 不同,**不需要** `NULLS NOT DISTINCT`;`attendance_punch_event_supersede_target_unique` 的键列可空故按仓内纪律带上该子句,但诚实说明:supersede shape CHECK 已强制该谓词命中的行必有非空键 ⇒ 该子句**无独立可观测行为**,不是已验证判据。⚠️ **时间重叠刻意不进 DB**(§3.18 明写「时间重叠校验在现有 member lock 内完成」)⇒ 零 exclusion constraint、零 btree_gist,已用 `pg_constraint contype='x'` + `pg_extension` 两条 e2e 断言钉死。⚠️ 与合同的偏离(均因合同自身要求而必需,已在 PR body 逐条列):`ParticipantServiceSegmentRevision` 的 `sourceCloseEventId`/`checkOutAt`/`serviceHours` 三列**改可空**(合同字段表未标 `?`,但 §4.5「无开放段＋check_in → open」定义了"已签到未闭合"的段,此刻三者都不存在,NOT NULL 会让该形态根本写不进来 —— 沿第二刀同一处置);`reason` 必填只落**能无歧义映射到编码**的三类(特殊闭合/作废/替代),合同的「人工」在 `sourceCode` 闭集里没有唯一对应(proxy? bulk? correction?)故不自行选定。合同**未给**的一律不发明:`EvidenceSeal` 的「一活动至多一个 active seal」**不建**(§3.17 没给,§11.3 只给 Closure 点了 partial unique active),并用一条"第二条 active 必须放行"的 e2e 把"刻意不建"钉成会变红的判据。⚠️ 三个跨切片外键列 `offlinePackageId`/`importJobItemId`/`effectiveBatchId` **不建**(目标表分别在第 6 批与第四刀);其中 `OfflinePackage` 是**合同第三处内部矛盾** —— 被修订说明 §5 列为核心新对象、被 §3.16 当外键列引用、被 §5.7 详述协议字段,但 §3 数据模型从头到尾**没有定义它**,故不从散文推导表结构。骨架由只读 `migrate diff` 生成后剥掉两条**与本刀无关的存量** RenameIndex(在 main 上跑同一条 diff 得到逐字相同两条 ⇒ 长索引名 63 字符截断口径的存量漂移)再手工追加约束。⚠️ `ParticipantServiceSegmentRevision` model 名 33 字符 ⇒ 复合索引默认名普遍越过 PG 63 字符上限,全部显式 `map:`(`AttendanceQrCredential` 的三列 unique 默认名 65 字符同理)。已在干净库 `migrate deploy` 重放全部 73 个 migration + seed 幂等二跑逐表行数 IDENTICAL;独立 SQL 夹具**先立正对照**(20 条合法行确实能进,含开放段三 NULL、终态不占槽、七种 sourceCode)再跑 37 条反对照全部被拒;配套 e2e 27 例,并用两次变异 A/B 证明判据绑对(卸 trigger ⇒ 恰好 2 例红;去 QR partial 谓词 ⇒ 恰好 3 例红;两个红集**互不重叠**)。生产未 deploy。前一为 `20260804040000_activity_v11_slice2_form_qualification_invitation`:活动业务改造 v1.1 **第 1 批第二刀** expand-only —— 净新 **10 张空表**(`ActivityRegistrationRevision`〔§3.7〕/ `RegistrationFormVersion` / `RegistrationFormField` / `RegistrationFormAnswer` / `RegistrationUploadSession`〔§3.12〕/ `ActivityQualificationRuleSet` / `ActivityQualificationRule` / `QualificationEvaluationSnapshot`〔§3.13〕/ `ActivityInvitation` / `ActivityVisitor`〔§3.14〕)+ `ActivityRegistration` 加 **4 列**(`currentRevision` NOT NULL DEFAULT 0 + `currentFormVersionId`/`statusSummaryCode`/`sourceCode` 可空 ⇒ 有存量行的库也安全)+ **兑现第一刀欠账** `ActivitySessionPosition.qualificationRuleSetId` 连列带 FK(第一刀按「跨切片外键列不提前占位」暂缓,目标表正是本刀建的)+ 末尾 **21 条 CHECK + 3 条手写 partial unique**。**零 runtime / 零 endpoint / 零 DTO / 零 permission / 零 seed / 零回填 / 零删数 / 零 DROP/RENAME / 零不可逆**;十张新表**零调用方是预期状态**(消费方在第 4 批)。⚠️ **`RegistrationFormAnswer` 的 exactly-one 用计数式**(`CASE WHEN … IS NOT NULL THEN 1 ELSE 0 END` 求和 `= 1`)而非 AND/OR 串:`IS NOT NULL` 是二值谓词 ⇒ 和恒为非 NULL 整数 ⇒ 整式**结构上不可能求值成 NULL**,天然免疫「表达式为 NULL ⇒ CHECK 判通过」那个第一刀踩过的坑;拒绝用例覆盖「零个非空」「两个非空」「五个全非空」三种。⚠️ **`activity_invitation_active_unique` 必须带 `NULLS NOT DISTINCT`**(PG15+;沿 `role_bindings_active_unique` 先例):键含**可空**的 `sessionId`(NULL = 活动级邀请),PG 默认 NULL 互不相等 ⇒ 不带该子句时同一人可被重复发出任意多张活动级邀请而一条都不被拦,**索引恰好在它最该生效的那类行上完全失效**;而场次级邀请因 `sessionId` 有值照样被拦,漏写在只测场次级的用例里**完全看不出来**。已跑变异 A/B:去掉该子句后两条重复行全部入库。⚠️ **`requestKey` 幂等唯一取单列而非 `(requestKey, requestHash)` 复合** —— 复合唯一恰好放行「同一 key 配不同 payload」,那正是幂等键唯一要拦的冲突;单列唯一严格蕴含复合唯一,故同时满足合同 §3.7 字面。⚠️ **`ActivityVisitor` 刻意零 Member 外键**(合同 §3.14:「与 Member、Participation、Ledger 无 relation;禁止通过访客创建贡献分」)—— `invitedByMemberId` 是裸留痕列;e2e 用「填不存在的 memberId 仍能入库」+ `information_schema` 直查外键目标集两条判据把「没有外键」钉成可执行判据,哪天有人顺手补 FK 会立刻变红。⚠️ 与合同的偏离(全部因合同自身要求而必需,已在 PR body 逐条列):`QualificationEvaluationSnapshot.identityId`/`registrationRevisionId` 改可空(§3.13 要求「**展示**、提交和审核三次评估分别留快照」,展示发生在报名之前,两个锚点此刻都不存在,NOT NULL 会让该形态根本写不进来);`ActivityRegistrationRevision.formVersionId` 改可空(§3.12 未要求每个活动都建 FormVersion);`schemaHash` 改可空(激活时才冻结)。合同**未给**闭集的三处(`RegistrationFormField.visibilityCode` / `RegistrationUploadSession.statusCode` / `ActivityQualificationRuleSet.statusCode`)与**未给**的 RuleSet unique 一律**不自行发明**(AGENTS §2)。骨架由只读 `migrate diff` 生成后剥掉两条**与本刀无关的存量** RenameIndex(在 main 上跑同一条 diff 得到逐字相同两条 ⇒ 长索引名 63 字符截断口径的存量漂移)再手工追加约束。已在干净库 `migrate deploy` 重放全部 72 个 migration + seed 幂等二跑逐表行数 IDENTICAL;独立 SQL 夹具**先立正对照**(合法行确实能进,含五种合法单值、单边区间、三级作用域、展示态双 NULL 快照)再跑 43 条反对照全部被拒;配套 e2e 46 例,并用三次变异 A/B 证明判据绑对(去 `NULLS NOT DISTINCT` / `= 1` 改 `<= 1` / 删 `maxLength >= 1`,三次红集各恰好 1 例且互不重叠)。⚠️ 一条**诚实的负面结论**:range/length 类 CHECK 的 `IS NULL OR` 守卫是**自证文档而非行为** —— SQL 的 AND 是 FALSE-主导,朴素写法在本刀所有用例上判定完全相同;第一刀真正的失效发生在 **OR** 支路(`false OR NULL = NULL` ⇒ 放行),本刀凡用 OR 的 CHECK 其操作数均为二值谓词,不可能塌成 NULL。生产未 deploy。前一为 `20260804020000_activity_v11_slice1_sessions_participation_capacity`:活动业务改造 v1.1 **第 1 批第一刀** expand-only —— 净新 6 张空表(`ActivitySession` / `ActivitySessionPosition` / `ActivityParticipationIdentity` / `ActivityParticipationRevision` / `ActivityCapacityBucket` / `CapacityReservation`)+ `Activity` 加 **12 列**(3 列 NOT NULL 带 default〔`archiveWaitingDays`=7 / `currentEvidenceRevision`=0 / `currentPopulationRevision`=0〕,9 列可空 ⇒ 有存量行的库也安全)+ 末尾 **29 条 CHECK + 5 条手写 partial unique**。**零 runtime / 零 endpoint / 零 DTO / 零 permission / 零 seed / 零回填 / 零删数 / 零 DROP / 零 RENAME / 零不可逆**;六张新表**零调用方是预期状态**(消费方在第 3–5 批)。⚠️ **一条 CHECK 初版是失效的**:`locationRequired = true AND radiusMeters BETWEEN 50 AND 10000` —— SQL 三值逻辑下 `NULL BETWEEN …` = NULL,而 CHECK 在表达式为 NULL 时**判通过**,于是「要求定位却没填半径」被静默放行;补 `radiusMeters IS NOT NULL` 后才真拒(**拒绝用例实测抓出,不是推理出来的**)。⚠️ `(activityId, sessionId, memberId)` 是**普通 unique、不带删除条件**(合同 §3.8 / P0-04:取消重报只追加修订,永不再建身份行)—— 改成 partial unique 即破坏该语义,已有 e2e 钉死(改 cancelled 后再插仍须 23505)。合同 §3.1 要求的「删 `attendanceDeclaredComplete*` 两列 + `completed` 移出状态闭集」**刻意不做**(`completed` 全仓 376 处引用,归 expand→migrate→**contract** 的 contract 阶段);跨切片外键列 `qualificationRuleSetId` / `allocationBatchId` **不提前占位**,由建目标表的那一刀连列带外键一起加。骨架由只读 `migrate diff` 生成后剥掉两条**与本刀无关的存量** RenameIndex(主会话在 main 上跑同一 diff 得到一模一样两条 ⇒ 长索引名 63 字符截断口径的存量漂移)再手工追加约束(沿证书 PR-4b / 企微 T1 处置)。已在干净库 `migrate deploy` 重放全部 71 个 migration + seed 幂等二跑逐表行数 IDENTICAL,约束逐条跑过双向阳性对照;主会话另用**独立 SQL 夹具**复核 11 类非法数据全部被拒且合法行可进;生产未 deploy。前一为 `20260803090000_add_user_wecom_identity_version`:企业微信 T3 账号接管面 B2(P1-27 第一刀)**expand-only** —— `User` 加**一列** `wecomIdentityVersion INTEGER NOT NULL DEFAULT 0`。零新表 / 零索引 / 零约束 / 零 enum / 零 FK / 零删数 / 零 DROP / 零不可逆;存量行一律回填 0。**为什么需要它**:`WECOM_BIND` step-up proof 把"当前 active 企业微信身份指纹"拌进 snapshot,而**无绑定态的指纹是字面 `null`** ⇒ `null → bind → admin clear → null` 走完之后指纹**回到起点**,无绑定态签发的旧 proof 在 300s TTL 内复活(修前一次性取证探针在真 HTTP 上实测 200,身份被绑回去)。这是 ABA:用"状态值相等"当判据,而状态值会回到起点。代际单调递增、不回绕,把"回到起点"从状态空间里删掉。**为什么不放 `wecom_identities` 上**:它恰恰要在"当前一条 identity 行都没有"时仍然递增。**为什么不用「快照改存最新一条 identity 历史」**(评审给的备选):那条不变量靠"历史行不物理删"这个外部前提撑着,仓里没有执行位守它;代际列是显式的。**回填 0 是正确起点**:代际只需"从此刻起严格单调",不必追溯历史绑定次数,旧 proof 最长 300 秒寿命,部署完成即自然清空。递增落点恰好三处:两条绑定事务(bind / rebind)+ 撤销原语 `users/wecom-identity-revoke.ts`(admin clear / 软删 / 队员账号重开三个落点共用);"什么都没变"的路径(同目标 no-op 换绑、幂等空转 clear)**不**递增。⚠️ 表名是 `User` 不是 `users` —— User model 没有 `@@map`,本仓多数表有,写错会在 `migrate deploy` 时 42P01 直接失败(本刀初版实测)。该列**不进**任何响应 / Audit / 日志,判据钉在 `wecom-schema.e2e-spec.ts`(白名单 + 列类型/可空/default 三项)与 `wecom-account-takeover.e2e-spec.ts`。回滚 = `DROP COLUMN`(旧 proof 会随之全部失效,等价于一次强制重新 step-up);生产未 deploy。前一为 `20260802100000_notification_outbox_wecom_active_slot`:企业微信接入 T5B **expand-only** —— 在 `notification_outbox_intents` 上**只加一条**手写 partial unique `notification_outbox_wecom_delivery_active_unique`,键 `("eventType","aggregateId","destinationRef")`、谓词 `eventType='notification.wecom-delivery' AND status IN ('pending','processing')`。零新表 / 零新列 / 零 enum / 零 default / 零回填 / 零删数 / 零 DROP / 零不可逆;建索引时 `notification.wecom-delivery` 此前从未写入过任何一行,命中集必然为空,不可能因存量冲突失败。**为什么必须独立而不复用微信那条**:既有 `notification_outbox_wechat_delivery_active_unique` 的谓词写死 `eventType='notification.wechat-delivery'`;若改成不含 eventType 谓词的通用索引,同一通知同一人的 wechat child 与 wecom child 会互占槽位 ⇒ 两个渠道无法同时投递(冻结稿 §10.3 明确点名)。至此本表共 **3 条**形状一致的 active-slot partial unique(wechat / admin-sms / wecom)。已在干净库(69 个 migration 全量 `migrate deploy`)重放通过 + seed 幂等两跑计数逐字相同,并跑过**双向阳性对照**:同 pair 第二条 active wecom child 被 23505 拒 / 同 pair 的 wechat child 放行(**本 migration 的存在理由**)/ terminal 后槽位释放 / 同通知不同 member 互不干扰。回滚 = `DROP INDEX`,但须先按 [`runbook`](../docs/ops/wecom-message-channel-rollout.md) 关 `messageEnabled` 并用新 worker 排空;生产未 deploy。前一为 `20260801093000_wecom_identity_foundation`:企业微信接入 T1 **expand-only** —— 净新 3 张空表 `WecomSettings` / `WecomIdentity` / `WecomAuthAttempt` + `User` 两条反向 relation(**零标量字段**,冻结稿 §5.4 / §0.3 硬禁区:不把企业微信 UserId 写进 `User.openid`)+ `SmsPurpose` 追加 `WECOM_BIND`(T3 才消费)。2 条 RESTRICT FK 均指向 `User`。末尾 **5 条手写约束**:`wecom_settings_singleton_unique`(`ON ((true))`,沿第 49 migration 四张 provider settings 表形状)、2 条 active partial unique(`(corpId,wecomUserId)` / `(corpId,userId)` WHERE status='active' —— partial 是关键,否则解绑后换人再绑同一企业微信号会被永久挡死)、`status` 闭集 CHECK、撤销形状 CHECK(`active ⇔ revokedAt IS NULL`)。**零 runtime 读写 / 零 endpoint / 零 permission / 零 audit / 零回填 / 零删数 / 零默认绑定**;settings 端点在 T2,OAuth 与绑定在 T3。骨架由只读 `migrate diff` 生成后剥掉两条无关 RenameIndex 再手工追加 5 条约束(沿证书 PR-4b 处置)。已在干净库 `migrate deploy` 重放 + seed 幂等二跑双绿,5 条约束逐条跑过**双向**阳性对照;生产未 deploy。⚠️ 实测发现 `status_check` 在 INSERT 路径上被 `revocation_shape_check` **完全覆盖**(任何非法 status 也让 shape 两分支皆假),故 e2e 对非法 status 只断言 23514 不断言命中哪条 —— 断言 status_check 会是假绿。前一为 `20260730090000_certificate_standard_library_contract`:证书标准库 PR-4b **contract** —— 与 PR-2 成对收口。`Certificate` **DROP 四列** `certTypeCode`/`certSubTypeCode`/`isInternal`/`imageKeys`(连带 DROP `Certificate_certTypeCode_idx`)+ 三列转 **NOT NULL** `standardId`/`recognitionPolicyId`/`sourceCode`(`recognitionIssuerId` 仍可空 —— FREE_TEXT 认定规则下本就没有 issuer 实体);`recruitment_applications` **DROP 三列** `certificateImages`/`certificateReviewStatus`/`certificateIssuanceInfo`。末尾 **1 条手写 CHECK** `certificate_source_claim_consistency_check`(sourceCode=RECRUITMENT→sourceClaimId 非空;ADMIN→为空;Prisma DSL 表达不了 CHECK)。**不可逆**(DROP COLUMN);前置探针在 head schema 库上八项全 0(Certificate 零行 / 招新三 JSON 零非空 / ownerType=certificate 的 attachment 零行 / 三个待收紧列零空值),空库切换、零回填。**部署前必须在真实库重跑同一组探针**,SQL 见 [`docs/ops/certificate-standard-library-go-live.md`](../docs/ops/certificate-standard-library-go-live.md);任一非 0 即停,不 drop、不猜 Standard、不批量回填。骨架由只读 `migrate diff` 生成后剥掉两条无关 RenameIndex 并手工追加 CHECK。已在全新 scratch 库 `migrate deploy` 重放通过,并对该 CHECK 跑双向阳性对照(ADMIN 无 claim 放行 / RECRUITMENT 无 claim 被拒);生产未 deploy。前一为 `20260730013702_certificate_standard_library_expand`:证书标准库 PR-2 **expand-only**——净新 4 张空表 `CertificateStandard` / `CertificateRecognitionPolicy` / `CertificateRecognitionIssuer` / `RecruitmentCertificateClaim` + 8 枚举〔Kind/Status/PolicyStatus/IssuerPolicy/ValidityMode/NumberMode/Source/ClaimStatus〕;`Certificate` 加 5 个 **nullable** 列 `standardId`/`recognitionPolicyId`/`recognitionIssuerId`/`sourceClaimId`(@unique)/`sourceCode` 与 3 索引;`recruitment_applications` 加反向关系(无新列)。FK 全 RESTRICT,含 **4 条复合 FK**(Certificate 与 Claim 各 2:`(policyId,standardId)`→Policy`(id,standardId)`、`(issuerId,policyId)`→Issuer`(id,policyId)`;列此刻全 NULL,PostgreSQL MATCH SIMPLE 任一列 NULL 即放行,故空表期不受影响 —— 维护者 2026-07-30 拍板把冻结稿原定 PR-4b 的这项提前,PR-4a 一开始写入就有 DB 兜底)。末尾 **2 条手写 partial unique**(`certificate_recognition_policy_one_active_per_standard` WHERE status='ACTIVE' AND deletedAt IS NULL;`certificate_recognition_issuer_active_name_unique` (policyId,normalizedName) WHERE deletedAt IS NULL)+ **4 条 Claim CHECK**(APPROVED 完整性 / PROMOTED 完整性 / 日期区间 / version≥0)。零回填、零 DROP、零 default 变更、零不可逆;新模型**零业务写入口**(PR-3 起才有 API)。生产未 deploy。已在干净库 `migrate deploy` 重放 + seed 幂等二跑双绿,2 条 partial unique / 4 条 CHECK / 复合 FK 全部跑过阳性对照(违例被拒、合法放行);前一为 `20260723225802_activity_responsibility_workflow_expand`:活动责任闭环 PR1 expand-only——Activity 新增 nullable 发起人和考勤完成声明及 workflowRevision=0；AttendanceSheet 新增 nullable 最近提交和退回字段并从既有 submitter/submittedAt 回填；净新发布审核与责任历史表，11 条 RESTRICT FK、3 条 partial unique 及责任/状态 CHECK；0 runtime/API/Permission/seed，未 deploy；前一为 `20260720193621_notification_prepared_template`:D-Outbox Wave2 G1b expand-only——`NotificationOutboxIntent.preparedTemplateId` 新增 nullable `String?`，数据库为 nullable `TEXT`；G2 runtime 已消费 `preparedTemplateId`，在已确认 provider 尚未启动的终态 skip 场景恢复已预占微信订阅消息配额并让重领固定使用持久模板；provider 结果未知不退款；该 migration 仍为零 default/index/constraint/backfill/delete，生产未 deploy；前一为 `20260719220000_notification_publish_generation`:D-Outbox Wave2 G1 expand-only——`Notification.publishGeneration` 新增 `Int @default(0)`，数据库为 `INTEGER NOT NULL DEFAULT 0` + 命名非负 CHECK；老行代次为 0；G2 runtime 已消费 `publishGeneration` 作为 admin WeChat root/child 与 admin SMS 的发布代次 fence；该 migration 生产未 deploy；前一为 `20260719160335_insurance_constraints`:D-INSURANCE v3 PR4 constraints-only（本 PR 仅代码交付，未 deploy）——事务内先以 ACCESS EXCLUSIVE 锁两张 DDL 表并完整扫描 MemberInsurance version/review snapshot 与 Evidence shape/kind/interval/review/duplicate owner/四组合 member mismatch，任一非零 fail、零修数/零删数；随后落 2 个 MemberInsurance CHECK、Evidence 最终必填列 + 7 CHECK + 2 partial unique、member-match 23514 与 immutable 55000 双 trigger；无 route/DTO/permission/AuditEvent/BizCode/config/cron/runtime service 变更；前一为 `20260719071116_insurance_expand`:D-INSURANCE v3 PR1 expand-only——`MemberInsurance` 新增 `reviewStatusCode=pending`/`version=0`/nullable reviewer，并将所有 legacy(含软删)统一回填 pending/v0/null reviewer；新增兼容期字段全可空的 `insurance_eligibility_evidences` 双 source/双 owner 骨架；本 PR 合计 6 条 RESTRICT FK（Evidence 5 + reviewer 1）；`TeamJoinCycle.requiresInsurance` 为 NOT NULL DEFAULT false；无最终 CHECK/unique/trigger、无 runtime 行为；前一为 `20260718233000_attachment_storage_operations`:仅 Attachment key namespace 新增 `StorageObject`/`StorageObjectOperation` durable ledger;对象固定 provider locator、凭证仍取当前 settings;JIT→STRICT 由 dedicated worker 收敛(非 cron/queue);为旧 binary additive rollout 本期不加 `Attachment.key`→`StorageObject.key` FK,不宣称 repo-wide storage closure;见 [`runbook`](../docs/ops/attachment-storage-consistency-rollout.md);前一为 2026-07-18 Membership 任期 CHECK 收口(存量 fail-fast、零修数);前一为 D-Outbox:纯 additive 新建空表 `notification_outbox_intents`,零回填/零 enum/零 drop;前一为 2026-07-18 D-RULE-1:同 `activityTypeCode × attendanceRoleCode` 未软删 ACTIVE pair partial unique；migration 显式事务内先取与写冲突的 `SHARE` 表锁，再查存量冲突并 `RAISE EXCEPTION`，随后仅 DROP 旧 threshold 索引 + CREATE pair 索引，锁覆盖扫描→索引替换，零行级 DML/回填/历史 AttendanceRecord 重算；service pair 预查 + P2002→23002，calculator 漂移重复 pair fail-closed；前一为 2026-07-18 D-Throttle:净新空表 `throttler_buckets`〔`(throttlerName,key)` 普通唯一 + `retentionAt` 索引;6 个时间字段/数组均 `timestamptz(3)`;纯 CREATE TABLE/unique/index,零回填、零 enum、零 drop;goal 明确授权 `@@map` 物理名〕;前一为2026-07-17 identity session P0 PR1:`SmsPurpose` 仅 additive `IDENTITY_STEP_UP`，migration 仅 PostgreSQL enum `ADD VALUE`，0 table / column / index / backfill / seed、禁止 down migration；前一为 2026-07-16 活动岗位与时段 F1:净新空表 `activity_positions` + `ActivityRegistration.activityPositionId` nullable 列〔2 FK Restrict + 6 普通索引 + 手写 partial unique `(activityId,name) WHERE deletedAt IS NULL`;既有报名 active partial unique 逐字不动;无回填、无不可逆、无 enum/seed;goal 指定 `@@map`〕;前一为 2026-07-16 活动评价 F1:净新空表 `activity_feedbacks`〔2 FK Restrict + 5 单列索引 + 手写 partial unique `(activityId,memberId) WHERE deletedAt IS NULL`;无回填、无不可逆、无 enum/seed;goal 指定 `@@map`〕;前一为 2026-07-15 活动自助 GPS 签到 F1:净新空表 `activity_check_ins`〔3 FK Restrict + 6 单列索引 + 手写 partial unique `registrationId WHERE deletedAt IS NULL`;无回填、无不可逆、无 enum/seed;一次性 `@@map` 例外〕;前一为 2026-07-15 活动开始提醒 marker:`Activity` 加 nullable `startReminderSentAt`〔活动模块一致性收口 F12;纯 additive、无回填、无不可逆〕;前一为 v0.47.0 到期提醒:`member_insurances` / `team_insurance_policies` 各加 nullable `expireNotifiedAt`,纯 additive、无回填、无不可逆;前一为 2026-07-14 第七刀 settings 单例约束:四张 provider settings 表先按 `updatedAt DESC, createdAt DESC` 去重保留最新行并用 `RAISE NOTICE` 记录删除数,再各加 constant unique `ON ((true))` 强制至多一行;不可逆删除仅在已有重复行时发生,执行前必须只读核对并获实时授权;前一为 2026-05-02 `init` → 2026-07-13 招新证书闭环刀A:`recruitment_applications.certificateIssuanceInfo Json?` 发证机构/日期按类别暂存,纯 additive 可空、无回填无不可逆;前一为 2026-07-12 招新/入队十三项收口 PR-3:申请人证书审核态 + 入队轮配置预置列〔`recruitment_applications.certificateReviewStatus Json?` + `team_join_cycles.openOrganizationIds Json?`/`maxTargetOrgs Int?`;全 additive 可空,无回填无不可逆;刀H 代码于 PR-4 消费〕;前一为同 goal PR-1:同轮 active 报名 openid/phone 两条 partial unique;前一为 2026-07-11 十项收口一刀:「至多一个 open 轮」DB 兜底 + 建档搬运落点〔`recruitment_cycles_single_open_unique` / `team_join_cycles_single_open_unique` 两条手写 partial unique(WHERE "statusCode"='open' AND "deletedAt" IS NULL;开轮 count-then-update 在 READ COMMITTED 下可并发穿透,DB 收口至多一行,service P2002 兜底转 28032/28231)+ `MemberProfile` +`detailedAddress TEXT`/`profileExtra JSONB`(promote 建档搬运落点,此前两字段 promote 时被置空且无落点=真丢失;该表无 @@map,SQL 字面沿 F5 `"MemberProfile"`);全 additive、无回填、无不可逆、无 enum;唯一前提:建索引时库内不得已存在双 open 轮(pre-production 认定不存在)〕;前一为同日招新可用性收口 F7:证书图上传与长期档案〔`recruitment_applications` +`certificateImages Json`(按类别分组 storage keys 暂存位)+ `Certificate` +`imageKeys Json`(promote 按类别建 pending 行搬入,blob 单一属主=certificate);全 additive 可空,无回填/无不可逆;评审稿 §2.9 R6〕;前一为同日 F6:同轮防重 partial unique `recruitment_applications_cycle_idcard_active_unique` 排除集重建〔`<> 'rejected'` → `NOT IN ('rejected','withdrawn')`;自助撤销 `statusCode='withdrawn'` 终态〔String 无 enum〕解锁同轮重报;纯索引先删后建,现存数据必然满足更宽松新谓词,零回填零冲突;评审稿 §3 R4〕;前一为同日 F5:知情同意留痕 + 签名图〔`recruitment_applications` +`privacyConsentAcceptedAt`/`privacyConsentVersion`/`signatureImageKey` + `MemberProfile` +`signatureImageKey`;全 additive 可空,无回填/无不可逆;promote 的 `privacyConsentSigned` 由硬编码 true 改搬真值 = ⚠️ 行为变更;评审稿 §2.8 R5〕;前一为同日 F1:纯加空表 `recruitment_ocr_daily_counters`〔付费 OCR 按 IP × 北京自然日封顶持久计数,recognize+submit 共享;`(ip,dateKey)` 唯一 + `dateKey` 索引;无 FK / 无回填 / 无不可逆;评审稿 `docs/archive/reviews/recruitment-usability-closeout-review.md` §2.5/E-U-1〕;前一为 2026-07-07 队员账号闭环 v2 schema 根改造:`User.memberId` 去全量 `@unique` 改手写 partial unique index `User_memberId_active_key` WHERE `"deletedAt" IS NULL`〔+ 普通 `@@index([memberId])` 兜底跨软删状态的既有查询〕——同一 memberId 至多 1 条"活跃"(未软删)User 关联,软删旧号后释放槽位供重新绑定/开号/重开;纯约束收窄,现有数据必然满足更严的全量唯一 ⇒ 天然满足更宽松的 partial 唯一,零冲突、零回填、非破坏性。连带改动:`Member.user User?` 反向关系改 `users User[]`(Prisma 一对一关系要求 FK 侧字段 schema 级 `@unique`,partial unique 无法在 DSL 表达,故 Prisma 类型层诚实反映"至多 1 条 live、可有多条历史软删"的真实基数),消费方 `authz/resource-resolver.service.ts` / `notifications/birthday-greeting.service.ts` 同步改为 `where: { deletedAt: null }, take: 1` 取用(行为等价,软删账号从不可能是 currentUser)。评审稿 [`docs/archive/reviews/member-account-loop-v2-review.md`](../docs/archive/reviews/member-account-loop-v2-review.md);前一为 2026-07-03 冻结表 cleanup:`DROP TABLE "MemberDepartment"` + `DROP TABLE "user_roles"`〔档 D,不可逆;pre-production 窗口维护者拍板执行,项目尚未上线、无生产数据〕。两表均已连续两个终态 scoped-authz PR 冻结、零生产读写:`MemberDepartment` 经 PR2(#466)方案 A' 全量重指向 `member_organization_memberships` 后零生产读写(9 个消费者已迁移,旧 3 个 department 端点行为逐字锁定不变);`user_roles` 经 PR6(#471)回填 `RoleBinding(principalType=USER, scopeType=GLOBAL)` 后零生产读写(判权唯一读源已重指向,`user-roles` 端点契约不变)。两表回填等值已在 PR2/PR6 用 `count(...)` 自证;第二轮全仓 review(#484)L1 深审亲核 `src/` 0 处生产读写(仅历史回填 SQL)。无其它表 FK 指向这两表,故直接 `DROP TABLE` 无需 `CASCADE`;纯删除、无回填、无新增;**对外行为逐字不变**(0 端点 / 0 DTO / 0 权限码变更)。全仓 prisma client 用点(`prisma.memberDepartment`/`prisma.userRole`)已清零;`test/setup/reset-db.ts` TRUNCATE 列表移除两表名;前一为 2026-07-01 终态 scoped-authz PR7 职务→角色 policy:净新表 `organization_position_role_policies`〔+1 枚举 `PolicyScopeMode`〔EXACT/TREE〕+ 3 索引 + 2 真 FK Restrict〔positionId→organization_positions/roleId→roles〕+ 普通唯一 `(positionId,roleId)`;无 partial unique、无回填、无不可逆〕;seed 幂等 upsert 3 管理/监督角色(`org-admin`/`group-manager`/`org-supervisor`,不落 migration)+ 3 条默认职务→角色映射(仅正职:team-leader/dept-leader→org-admin@TREE、group-leader→group-manager@TREE;**R5 副职零行**,seed 运行时断言兜底)。**本表 + 3 新角色纯配置,绝不被任何判权路径读**(消费方是 PR8 AuthzService;0 新端点/0 新权限码/0 新 controller);前一为 2026-07-01 终态 scoped-authz PR6 RoleBinding:净新表 `role_bindings`〔+3 枚举 `PrincipalType`〔USER/MEMBER/POSITION_ASSIGNMENT/SYSTEM〕/`BindingScopeType`〔GLOBAL/ORGANIZATION/ORGANIZATION_TREE/ACTIVITY/RESOURCE/SELF〕/`BindingStatus`〔ACTIVE/ENDED/SUSPENDED〕+ 6 索引 + 2 真 FK Restrict〔roleId→roles/scopeOrgId→Organization;**principalId 多态无 FK**〕+ 末尾 1 手写 partial unique:`role_bindings_active_unique` (principalType,principalId,roleId,scopeType,scopeOrgId,scopeActivityId,scopeResourceType,scopeResourceId) WHERE deletedAt IS NULL AND status='ACTIVE' **NULLS NOT DISTINCT**〔PG16;令 GLOBAL 的 NULL scope 列参与去重 = 保住 UserRole 旧 `@@unique(userId,roleId)` 并发去重行为锁,区别于 contribution_rules 有意不去重 NULL〕〕 + **回填**每条 `UserRole`→`RoleBinding`〔principalType=USER/principalId=userId/scopeType=GLOBAL/status=ACTIVE,复用 id、保 createdAt/createdBy、updatedAt=createdAt;自证 count(UserRole)==count(GLOBAL USER binding)〕,纯加 + 回填、可逆(**判权唯一读源全量重指向 `RoleBinding(USER,GLOBAL)`,UserRole 表冻结零生产读写不删**,cleanup PR 再 DROP);前一为 2026-07-01 终态 scoped-authz PR5 分管:净新表 `organization_supervision_assignments`〔与职务**正交**;+2 枚举 `SupervisionScopeMode`〔EXACT/TREE〕/`SupervisionStatus`〔ACTIVE/ENDED/REVOKED〕+ 4 索引 + 2 FK Restrict〔supervisorMemberId→Member/organizationId→Organization〕+ 末尾 1 手写 partial unique:`organization_supervision_assignments_active_unique` (supervisorMemberId,organizationId) WHERE deletedAt IS NULL AND status='ACTIVE'〕,纯加空表、无回填、无不可逆(分管由 API 产生,不 seed);前一为 2026-07-01 终态 scoped-authz PR4 任职:净新表 `organization_position_assignments`〔+枚举 `AssignmentStatus`〔ACTIVE/ENDED/REVOKED〕+ 6 索引 + 3 FK Restrict〔organizationId/positionId/memberId〕+ 末尾 1 手写 partial unique:`organization_position_assignments_active_unique` (organizationId,positionId,memberId) WHERE deletedAt IS NULL AND status='ACTIVE'〕,纯加空表、无回填、无不可逆(任职由 API 产生,不 seed);前一为 2026-07-01 终态 scoped-authz PR3 职务定义:净新两空表 `organization_positions` + `organization_position_rules`〔+2 枚举 `PositionCategory`/`PolicyStatus` + 索引 + FK Restrict + 普通唯一 `code` / `(nodeTypeCode,positionId)`;无 partial unique、无回填、无不可逆;6 领导职务 + 30 默认规则由 `seed.ts` 幂等 upsert 不落 migration〕;前一为 2026-07-01 终态 scoped-authz PR2 Membership:净新表 `member_organization_memberships`〔+2 枚举 `MembershipType`/`MembershipStatus` + 末尾 2 手写 partial unique:`primary_active_unique` (memberId) WHERE deletedAt IS NULL AND status='ACTIVE' AND membershipType='PRIMARY' / `active_unique` (memberId,organizationId,membershipType) WHERE deletedAt IS NULL AND status='ACTIVE'〕 + 回填每条 active `MemberDepartment`→PRIMARY membership〔复用 id、startedAt=createdAt、createdAt/updatedAt 原样〕,纯加、可逆、旧表冻结不删;前一为 2026-07-01 终态 scoped-authz PR1 组织基座:新表 `organization_closure` 闭包表〔`(ancestorId,descendantId,depth)`,末尾 `WITH RECURSIVE` 从现有 `parentId` 树一次性回填含 depth-0 自身行〕 + `Organization` 两 additive 可空列 `establishmentStatusCode?`/`groupFunctionCode?`〔纯加、无 default、无 enum、无回填、无不可逆〕;再前为 2026-06-29 招新实名 OCR 鉴伪版充分利用:`recruitment_applications` +6 列〔`ocrAddress`/`ocrNation`/`ocrAuthority`/`ocrValidDate` OCR 扩展字段顾问式存档 + `idCardCropImageKey`/`idCardPortraitImageKey` 主体框/头像裁剪图 key;全可空 TEXT additive,无 enum、无不可逆、无回填〕;再前为 2026-06-27 统一通知模块 S5 短信兜底 `sms_settings.templateIdNotification` 1 列 / S3 `notifications.recipientMemberId` 1 列 / S2 三表 / S1 两表)。
- D-Outbox 表索引事实:`eventKey` 普通唯一 + claim/lease/aggregate/terminal/`createdAt` 普通索引 + **微信小程序 / admin SMS / 企业微信各一条** active partial unique(三条形状一致,谓词按 `eventType` 分域 —— 合并成一条通用索引会让同一通知同一人的两个推送渠道互斥)。
- migration 命名格式:`YYYYMMDDHHMMSS_<可读描述_下划线分隔>`(例 `20260510193742_v2_batch3_activities_attendances`);**不**允许 `auto` / `tmp` / `wip` 命名。
- 生产环境只允许 `prisma migrate deploy` 跑**已审查**的 migration(沿 `AGENTS.md §3`)。

## 不要做(踩雷区)

- ❌ **不**自动执行 `prisma migrate dev` — 必须先**说明将生成 / 执行的 migration 内容**并等待用户确认。
- ❌ **不**自动执行 `prisma db push`(绕过 migration,不可回溯)。
- ❌ **不**自动执行 `prisma migrate reset`(销毁全部数据)。
- ❌ **不**手动改 `migrations/**/migration.sql` 已合入历史(沿 A-3 / A-4 红线)。
- ❌ **不**使用 Prisma **全局软删中间件 / client extension**(沿 `AGENTS.md §2` 永久铁律)。
- ❌ **不**在 schema 上加 `@map` / `@@map` 改库表名(命名约定锁定)。
- ❌ 修改 schema 前**必须先**说明:影响哪些 service / DTO / Swagger / OpenAPI snapshot / contract test;以及是否触发 D 档(沿 [`/docs/process.md §4`](../docs/process.md))。

## seed.ts

- 默认 super admin + bootstrap global USER RoleBinding；ops-admin seed 与运行时共用当前任期真值，完成后必须同时有当前有效与 `endedAt=null` 常驻 holder。已有 ACTIVE slot 不改写任期，future/expired/仅临时配置 fail-closed；同 env 连跑不改变 binding 任期。生产启动强校验 `SUPER_ADMIN_*` / `JWT_SECRET` / `APP_CORS_ORIGIN`,任一不满足直接抛错退出。
- v0.61.0 PR-11 contract 只改 RolePermission 映射、不删 Permission：`biz-admin/org-admin/group-manager`=68/47/20；通用角色仅定点清理活动责任旧动作，保留无关自定义映射；三个 reviewer 角色零 PositionRolePolicy。
- 活动业务改造 v1.1 第 2 批第 ⑧b 刀的 5 个结算 action 码只在 `ACTIVITY_RESPONSIBILITY_WORKFLOW_PERMISSION_SEED` 中 upsert，绝不并入 `BIZ_PERMISSION_SEED`：负责人=`generate/submit/close`，考勤协办=`generate/submit`，一审员=`first-review`，终审员=`final-review`。五码同时进入 biz-admin 的定点摘权集合，避免旧通用角色因 seed 重跑意外获得结算写／审核权；不创建任何 RoleBinding 或 PositionRolePolicy。
