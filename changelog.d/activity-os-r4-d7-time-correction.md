## D7-1 分类时长认定更正（实施中）

- 按 #1333 的 90 路径实施；仅支持既有事实下的完整认定快照更正。v1 行为保留，v2 严格校验基础版本、完整根集合及原因，禁止携带参与段变化。
- 第 123 条 additive migration 新增永久更正清单、冲回/补记分录和提交收据；不回填、不删除业务数据。根账不变，连续更正只冲回指定前驱的 credit。
- 更正收据、旧账本提交、请求/应用收尾及审计同事务；公共提交、转换及自动提交不可绕过更正收尾。当前权限在等待后及重放时重新核验。
- 增加指定版本的 App correction-ledger GET，沿现有 read 权限和根版本资格查询，不返回更正原因；无新增 HTTP 写接口、权限码或审计事件。
- 已授权旧迁移仅适配当前回放、历史升级恢复及必要夹具前置，不放宽行为断言。客户端和治理清单随契约生成。
- 本地定向验证完成，满额查询优化后3b已按c2f06bcf6e19重签，4b沿用871a4c9d426d；全量验收以PR CI冷跑为准。旧D6一项夹具清理超时后原样单独复跑通过，保留失败记录，不宣称单轮全绿。D7-2、整体复审、合并、生产操作及 Gate 均未做。
