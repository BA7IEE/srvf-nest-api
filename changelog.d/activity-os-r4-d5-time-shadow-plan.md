### Documentation

- CI 后更正：最终指纹补充后重新生成 CODEMAP 体量及 ROUTE_AUTHZ 输入摘要，接口和权限声明不变。#1329 首轮 CI 的 D5 满额测试整体 120 秒超时，CI 对账本身为 640 毫秒；维护者已批准将该测试整体时限对齐既有 D4 的 600 秒，业务 30 秒、总查询 120 及全部断言不变。w98 六项回归通过（78.365 秒）；2000 身份对账 221 毫秒、总查询 32 次含 1 次连接健康检查，业务与授权查询三档均 31 次。更新 PR 保持 Draft，不合并、不启用 Gate。

- D5 implementation：新增同一 submitted 版本的只读时长影子对账接口，精确十进制换算、完整身份分母、独立计算/认定差额、确定性输入指纹与标准内嵌分页；沿用当前显式权限及双次访问资格复核，不写正式账本或审计。
- 同步 OpenAPI、客户端类型、权限管辖面基线和人工永久留存 SOP；权限码仍为 265，接口 633→634。既有权限说明“读取冻结分类桶及证据”仍适用于派生对账报告，逐项复核后显式登记说明不变。验证进行中，不代表已合并、已上线或真实业务验收。

- 维护者已确认 D5 方案 A，并授权本轮四份文档提交、推送和创建计划 PR；不合并、不实施。

- 记录 D4/#1327 与草稿依赖/#1326 已合并；D4 合并后 main CI 及五组 E2E 已通过，保留既有间歇性超时尚未确认根因的边界。
- 合并起草 D5 时长影子对账评审与实施计划草案，明确同版本比较、精确单位、完整分母、不可比较项和不写正式账本边界；收敛单一路由、候选查询预算、留存边界及 34 路径实施候选（4 个红区），不构成实施授权。真实业务验收前仍须指定永久证据位置。
