### Added

- D6 分类时长账本实施：新增不可变清单及分录，与冻结分类修订和既有账本批次同链绑定；四类及零秒桶完整保留，汇总采用精确十进制字符串。
- Human App 新增明确分类修订的正式账本 GET，仅 committed 可见，沿既有显式读取权限和当前负责人/该版本审核资格。

### Changed

- 既有准备与提交链核对分类来源、完整集合及幂等内容；终审人的当前身份与权限在锁等待后再次核验。自动提交继续使用原任务和统一事务，不新增权限码、默认角色授权或审计事件。
- D7 前拒绝分类账本更正，legacy 更正和旧贡献账本读源不切换。新增20226–20230错误码。
- 按精确授权适配旧测试清理及当前迁移计数，保留历史升级和业务断言。隔离测试只临时处理两张新表的具名TRUNCATE触发器，生产不可变限制不变。
- 经维护者补充批准，状态检查器仅新增 `ParticipationTimeLedgerEntry.categoryCode` 的精确发现，补1个正例及4个负例；不泛化其他模型/字段、不升级governed。
- 依计划映射D6具名SQL约束至20226/20227/20228；仅精确约束及当前写入的唯一键字段，未知异常和死锁原样抛出，不改旧错误码或重试语义。

### Verification status

- 本地完整契约1063项及2个快照通过；D6两套新E2E共11项及原更正24项联合回归通过，包含审计失败回滚、锁后撤权、分块幂等、租约接管、自动提交失败重试、历史版本读取及分类更正三个入口拒绝。类型检查与定向lint通过。
- 新增迁移两项验证通过：122条冷回放checksum逐条一致，121→122非空升级保持七张旧表内容和前121条checksum不变，两张新表为空。
- 新增九项真实双链引用组合反例、两项持锁期间批次失败的晚到INSERT反例；当前D6三套24项合跑通过，原行为断言不变。
- 后续错误映射三套单测47项及真实FK回滚用例通过；旧链合跑79通过/7项夹具初始化超时，7项单独复核通过。该轮不记作一次全绿合跑，未提高超时，详见计划实施记录。
- 新增锁等待期间User/Member/Organization失效及同计数换入另一合法来源桶的四项反例，最终定向合跑通过。另记录一次前置D4 prepare的500，带脱敏诊断复核未复现，根因待查，不宣称全量稳定。
- 补齐分类账本访问单测14项，四套D6单测合计61项通过；历史版本资格、显式组织范围和再次调用重读当前身份均有独立断言。
- 新增真实1/100/2000身份数据库性能验收，保留原预算：1/100档提交查询预算失败，2000档在准备finalize事务过期，均未记为通过；补齐失败步骤脱敏耗时及遗漏查询的计量，详见计划记录。
- 保留满额夹具的独立回滚诊断中，真实finalize在tsx/Jest分别273/412毫秒到ready后回滚；它不替代完整满额链的失败验收，不据此宣称超时根因已解决。
- 经维护者批准，D1-3共享夹具初始化失败时关闭应用与数据库连接，保留原错误；真实应用两项失败注入验证通过且进程正常退出。成功路径、断言及超时不变，原性能失败仍待解决。
- 维护者后续另行批准测试夹具清理事务30秒预算；仅resetDb调整，业务事务仍5秒，原清理安全限制和断言不变。满额复测已通过初始化，ready更新业务超时仍未解决。
- 满额写入后的执行计划定位ready触发器耗时约8.98秒；D6 migration双向集合检查等价改为EXCEPT，保留数量、总额、锚点及约束。新SQL冷回放与旧数据升级通过，最终性能复验与签字尚待完成。
- EXCEPT版本满额链到达正式读取：收尾399毫秒、提交1558毫秒，缺分录直接ready数据库拒绝反例通过；1/100/2000档提交查询固定85总计/56新增，仍未通过原新增≤6预算，预算更正待维护者确认，不记作G11全绿。
- 维护者随后批准提交预算新增≤56并增加总业务查询≤85限制，权限及完整性复核不变；原失败记录保留，按新预算重新执行验收。
- 新预算合跑31通过/1项D4前置P2028失败；2000身份单独复核通过，提交2151毫秒、新增56/总85次，GET55毫秒/31次。三档通过证据分属两次运行，未宣称一次全绿或前置超时根因已解决。
- 补齐计划中的独立进程测试：有效任务租约期间第二worker不可重复领取，分块提交后SIGKILL及过期租约接管保持分录逐行不变；两项通过，未自动提交或产生日汇总。台账标为实施验证中，CODEMAP刷新通过。
- 这是未发布变更。本地并发/满额及D4/D5兼容联动已通过；最终签字与最终SHA CI仍须完成，不代表生产可用，不启用Gate，不删除业务数据。
- 整组复验另遇既有copyBuckets来源复制长查询，已记录只读定位及待审批的同链条件显式化方案；未修改写集外生产代码、未提高超时。刷新冻结台账读数及路由摘要，保留首次静态检查失败记录，继续复核。
- 维护者批准后，仅给copyBuckets旧桶JOIN补齐既有复合外键保证的活动和版本条件；满额单档通过，同一夹具新旧SELECT均40000行且双向差异0。全部断言和超时保持，仍需整组复验，不将落盘后快速计划冒充原慢查询的实际计划。
- 上述JOIN补齐在整组仍不足以稳定通过；再次获批后，copyBuckets改为语句内一次性建立桶ID映射再集合复制来源，保留原计数、事务、权限、断言和预算。只读满额候选对照40000行无差异；正式事务整组复验结果另记，不将候选对照登记为验收完成。
- D4/D5/D6七套正式联动回归65项全部通过（475.303秒），未取消查询、无临时EXPLAIN、未改预算；新G4暂停点显式保活IPC，仍真实SIGKILL并验证接管。该次为单次整组通过；旧失败记录保留，最终签字和CI未完成。
- 最终本地契约1063项/2快照、unit8594项（5 todo保留）、冷lint/typecheck/build通过；代码地图行数刷新后完整守护自测退出0，既有已知缺口不变。2026-09-15维护者确认D6的3b/4b重签，摘要逐项核对、签字门禁通过；进入已批准的Draft PR流程，不代表CI、合并或生产可用。
