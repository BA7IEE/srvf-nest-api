- 独立交付复验（2026-09-13）：草稿依赖已拆为Draft PR #1326，D4以其为base。D4全仓单测385组8473项通过（5既有todo），typecheck/build通过；w98冷回放121条迁移后，主链／并发／迁移24项及contract1060项／2快照通过。1952个代码、测试和治理文件与验证候选逐字一致；3b/4b/7c已按维护者确认重签。下方未完成措辞为较早阶段记录；当前仍待PR CI、可信审批和整体验收，不合并、不启用Gate、不操作生产、不删除业务数据。
- Activity OS R4 / D4 分类时长工作台实施中：增加不可变分类时长版本、人员分类桶、来源行与命令收据，增加 D3 显式草稿来源证明及第 121 条迁移；旧版本、旧命令及业务数据持续保留，不执行生产迁移或清理。
- 增加 8 个 Human App 查询／命令路由、2 枚零内建角色默认授予的权限、1 个审计事件；分类提交复用既有送审事务与审核访问面，历史 V1 hash 不变，分类版本使用绑定桶及来源的 V2 hash。自动基线未知与零值分开，毫秒以字符串精确返回，列表及审计不泄漏理由或来源敏感明细。
- 补齐 D4 单元、HTTP、并发、空库／非空升级及 SQL 防篡改验证；旧迁移测试仅在批准范围适配当前链计数与清理前置，保留历史升级和行为断言；同步批准的派生契约、客户端、权限及事实登记。
- 维护者已批准 D4 补充方案 A：含授权查询的总次数上限为读页120／prepare400／submit950；检查器仅新增 ActivitySettlementTimeRevision.kindCode 的 L1 配置字段发现及五个正反例，不扩展其他模型或放宽既有守护。普通成员 1／100／2000 人的实际次数保持40／334／805，并发与规模计数组6项通过。
- 当前仍未整体验收：完整有效源规模／角色矩阵、全量 E2E／CI、最终签字尚未完成。不启用 Gate，不包含前端、Ledger 入账、合并或部署。
- D4 分支验收修正：历史桶、来源和有草稿证明的认定详情首次判权定位目标版本，避免较新自提交版本误封有权历史；保留当前资格及返回前复核、自审拒绝和撤权拒绝。补齐3项历史 HTTP 回归及隔离应用四种 Gate 状态下的写命令重放／历史可读测试，不改现有路由、DTO、权限码或 Gate 实现。
- 第121条未合入SQL等价优化：来源计数一次聚合后按集合判断，桶总额以集合差对拍，保留空桶/NULL/金额/理由及全部同链约束，避免满额时重复扫描。真实2000人口、10000有效段、50000切片的准备/分页/正式提交首次通过，准备14507ms、总SQL340/41/811；冷库前置封印检查曾两次超过既有5秒事务预算，仍是待处理缺口，不以暖态成功宣称完整冷启动验收通过。
- 后续冷态修正：获准单方法优化封印事件引用查询；D4切片校验改用既有分配索引计算对应区间，避免冷统计下约8亿次关联过滤。重建w98后的首次满额链通过，prepare12177ms；未手动ANALYZE、未预热、未增超时。上一条冷态缺口为当时记录，不再代表本次探针结果；完整发布验收仍未结束。
