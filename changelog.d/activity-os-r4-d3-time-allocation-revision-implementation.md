- 新增 Activity OS R4/D3 的不可变参与时长认定层：冻结源服务段、发布快照、选择修订和政策版本，记录原始分类区间、证据与幂等收据，不改旧 `serviceHours`、结算或账本。
- 新增仅供内部 application command 使用的 `activity.time-allocation.recognize`；该权限只纳入 seed 闭包与自定义角色授予面，不对任何内建角色默认授予，且不新增 HTTP、客户端或 Gate。
- 增加隔离 migration／事务回归和附件删除保护；本分支尚未合并、部署或启用 Gate，生产与业务数据未操作。
- 按维护者补充授权将 recognitionModeCode 仅登记为 L1 inventory／not-derived 的不可变配置；补齐前后修订复合锚、真实锁等待撤权／重放／源段更正／政策选择变更／附件删除双向竞争和审计失败回滚探针，不改变状态机裁决规则。
