### Added

- Report-only Phase 2 data-ownership observations: cross-domain read tiers, raw-SQL physical-table visibility, observed subdomain writes, and an exact fact-read allowlist.
- Completed architecture-debt semantics for the original 125 records and registered 76 blame-backed historical findings without changing business source lines.
- Added the maintainer decision material for `allowedEdges` and `publicSurface`; both declarations remain pending and no enforcement mode changed.
