# Activity OS R5 / E2 第一层隔离转换能力

新增纯追加的转换收据模型与第 132 条 migration、固定夹具 dry-run／提交／重放路径、脱敏审计及隔离测试。旧规则来源查询由 `contribution-rules` 属主模块提供，活动模块不直接查旧规则；原结构断言未改。OpenAPI 生成文件仅随模块遍历顺序刷新，前后 JSON 语义相等（514 条路径、936 项 schema）；13 份前端 client 生成物仅刷新 `inputDigest`，接口、DTO、类型和调用签名未变。31 个真实旧活动类型仍全部 hold；未查询或转换真实旧规则，未改变考勤预填、正式贡献结算、权限码、seed 或 Gate。第 132 条 SQL 与审计 174 总计／169 活跃已获维护者 3b／4b 重签；`w98` 冷回放／非空升级及固定夹具转换各 2/2 通过，全量单测 425 套、9060 条通过（5 条历史 todo）。本条不表示 PR CI、合并、部署或生产切换已完成。

#1356 首轮 CI 的旧迁移回放历史锚点、C2 当前计数、旧 schema 测试清理与 E2 worker 前置已按原写集作定向兼容修复；w98 相关定向用例通过。旧 D6 夹具清理获准在同一受控事务内加入 E1 政策选择三表并使用 CASCADE，w98 原有 5 条测试全部通过。级联仅作用于隔离测试夹具；本条仍不宣称新 SHA 的 CI 全绿或可标记 Ready。
