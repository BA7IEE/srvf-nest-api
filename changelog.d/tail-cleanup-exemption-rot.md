### 修复

- **C1 豁免名单静默腐烂 + 台账两处回填**(尾巴清理刀)

  **① 豁免名单指着已删文件,不报错也没人发现。**
  `scripts/check-activity-workflow-gate.ts` 的 `C1_EXEMPT` 指向
  `src/common/activity-workflow/activity-workflow-gate.criteria.ts` ——
  该文件已于 #1165 删除,判据正文搬进 `scripts/`。
  `includes(r)` 从此**永远不匹配**:不生效、不报错、不改判定,**没有任何机器发现它烂了**。

  修的是**类**不是实例:
  - `C1_EXEMPT` 清空,并写明这是**结构性结论** —— `collectProdFiles()` 只走 `SRC`,
    判据自己住在 `scripts/`,永远不在扫描面内 ⇒ 豁免失去存在理由;
  - 新增自证 `exemptionRot()`,接进 `runCriteria()` 的 **C0 段(先自证再报数)**:
    豁免名单里任何一条指向不存在的路径 ⇒ 直接判红,并给出「删掉 / 改对路径」两条修法。

  **执行位与变异对拍**(经真执行位 `pnpm cutover:check`,非直跑该文件):

  | | 自证报数 |
  |---|---|
  | 修复后基线 | **0** |
  | 变异(往名单塞一条不存在的路径) | **3** |

  ⚠️ 取证过程中先用 `tsx` 直跑该脚本做对拍,**恒退 0** ——
  真因是该文件是**纯库、无 CLI 入口**,直跑什么也不做。
  换成真执行位 `cutover:check` 才拿到 0 → 3。
  *选对拍仪器前先确认它真的会执行被测代码。*

  **② 台账两处回填。** `NEXT_TASKS.md` P2-18 状态 `⏸ 挂起(#1181 在飞)` → `已收口(#1181)`
  (#1181 已于 2026-08-24 合入,该条自己写着「合入后由收口方改」)。
