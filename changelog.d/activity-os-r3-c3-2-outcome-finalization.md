### Added

- Activity OS C3-2：新增 App managed 完整成果确认、更正准备/取消与现行正式读面；confirm/correct 使用独立显式 Human 权限，不自动授给内建角色，无管理员角色直通。
- 第 117 条 additive migration 增加不可变确认命令收据与来源关联，约束唯一正式成果、提交期完整性和不可逆修订推进；旧成果、候选、值、证据及收据长期保留，零业务数据删除、零回填。

### Changed

- 更正使用新草稿和新正式修订，不覆盖旧值；准备/取消不替代旧正式成果。候选确认按持久输入复算并验证当前来源，不能把草稿自身的版本推进误判为来源变化。
- 提取既有候选纯复算供两入口复用；原 C2/C3-1 路径和结构保持不变。计划附件锁序经维护者确认更正：确认取得附件引用锁后校验存储状态，删除路径才继续取得存储对象锁；未修改 Storage 生产代码。
- 刷新生成契约、客户端及治理登记；旧迁移测试仅更新当前总数和已批准前置，不修改历史升级目标或放宽原行为断言。

### Validation boundary

- 当前仍是未提交分支实现。仅使用 app_test_w98 隔离测试；生产部署、Gate、合并和整体跨模型复审未执行。3b/4b 已按维护者确认重签，机器对拍通过。
