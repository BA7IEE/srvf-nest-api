### 新增

- C5 正式成果报告：单活动GET及最多20活动POST200查询，逐活动验证当前成果权限、身份、组织与责任，按稳定锁序整批返回。
- 完整校验历史正式事实后仅返回非敏感指标定义与值；无正式成果为null，保留false/0及规范小数字符串；不返回证据、身份或内部来源，不跨活动求和。
- 不新增schema/migration/权限码/审计事件，不改变结算、归档和Gate，不删除业务数据；前端发布、生产部署及整体复审未完成。
