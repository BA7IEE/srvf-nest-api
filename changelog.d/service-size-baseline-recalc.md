### Changed

- 重算尺寸棘轮基线(Phase 6-B 第三域七刀收官):条目 **27 → 22**。六个经拆分跌破 700 阈值的文件退出(`activities` / `activity-registrations` / `attachments` / `members` / `recruitment-applications` / `role-bindings` 的主 service);同期 `attendance-offline-package.service.ts`(1373,#1052 引入)新入册。转闸摩擦(SERVICE_SIZE_RATCHET §3 严口径)由 **93 降至 27**,低于判据线 30 —— 连同 PR#1054/#1056 达成的 EC-1,该文 §4 Exit Criteria 的两条 ❌ 全部清除。
